window.__mm2 = window.__mm2 || {};
window.__mm2.rag = (function(){
// Hospital assistant: intent detection → permission → retrieval (RAG / structured / hybrid) → grounded answer with sources. Synthetic corpus.
const CORPUS = [
  { doc: 'Inpatient Discharge SOP', v: '3.1', eff: '01 Jul 2026', owner: 'Operations', sec: '§4.1–4.6', text: 'Discharge runs in six steps: consultant records discharge intent in the EMR; nursing confirms pending investigations and pharmacy clearance; the insurance desk requests final approval or enhancement from the TPA with the provisional bill and discharge summary (target response 2 hours); billing finalises after the TPA response and patient liability is explained before payment; the consultant signs the discharge summary; the ward releases the patient and informs housekeeping. Target total turnaround is 3 hours from intent.', tags: ['discharge', 'turnaround', 'process', 'steps', 'housekeeping', 'summary'] },
  { doc: 'Inpatient Discharge SOP', v: '3.1', eff: '01 Jul 2026', owner: 'Operations', sec: '§5 Medication instructions', text: 'Take-home medication instructions are given in the patient\'s preferred language (Tamil or English), verified by the ward nurse, and include dose, timing, duration and warning signs. AI-generated drafts must be verified by a nurse before teaching.', tags: ['medication', 'instructions', 'tamil', 'discharge', 'nurse'] },
  { doc: 'Insurance Preauthorisation SOP', v: '2.4', eff: '15 Aug 2026', owner: 'Insurance Desk', sec: '§3 Required documents', text: 'A cashless preauthorisation requires the insurer preauth form, treating doctor\'s advice with diagnosis and planned procedure, itemised estimate, patient ID and policy copy, and relevant investigation reports. Enhancement requests additionally require the procedure report and the interim bill.', tags: ['preauth', 'insurance', 'documents', 'cashless', 'enhancement', 'required'] },
  { doc: 'Insurance Preauthorisation SOP', v: '2.4', eff: '15 Aug 2026', owner: 'Insurance Desk', sec: '§6 Turnaround & escalation', text: 'Initial preauth response target is 4 hours; enhancement and final approval target is 2 hours. Queries must be answered within 30 minutes. Cases pending beyond target are escalated to the Insurance Supervisor and, at 6 hours, to the Finance Head. Only an insurance executive submits to the insurer; agents prepare packets.', tags: ['preauth', 'turnaround', 'escalation', 'query', 'submit', 'insurance', 'pending', 'delay'] },
  { doc: 'Tariff Schedule FY26-27', v: '1.3', eff: '01 Apr 2026', owner: 'Finance', sec: '§2 Room rates', text: 'Room charges per night: General ward ₹4,500; Semi-private ₹7,500 (paediatric ward ₹7,500); Private ₹10,000; ICU/CTICU ₹18,000. Room rate includes nursing and standard monitoring; consumables and procedures are billed separately.', tags: ['tariff', 'room', 'rate', 'charge', 'ward', 'icu', 'price', 'cost'] },
  { doc: 'Tariff Schedule FY26-27', v: '1.3', eff: '01 Apr 2026', owner: 'Finance', sec: '§4 Implants & consumables', text: 'Drug-eluting stents are billed at the NPPA-capped price. Balloons, guidewires and sheaths are billed per unit used as recorded in the cath-lab consumable log. Any pre-dilatation balloon beyond the estimate must be explained to the patient in plain language before final billing.', tags: ['stent', 'implant', 'consumable', 'balloon', 'tariff', 'estimate', 'variance', 'bill'] },
  { doc: 'Estimate Counselling SOP', v: '2.0', eff: '01 Apr 2026', owner: 'Finance', sec: '§3 Variance', text: 'If the running bill exceeds the counselled estimate by more than 5% the patient or attendant is informed; above 10% a billing executive must counsel the family and record the reason. The estimate itself is not a fixed quote; it is a projection based on the planned procedure and length of stay.', tags: ['estimate', 'variance', 'bill', 'counselling', 'exceed', 'higher', 'more'] },
  { doc: 'Refund Policy', v: '1.2', eff: '01 Jun 2026', owner: 'Finance', sec: '§2', text: 'Refunds of advance or excess payments are processed within 7 working days to the original payment mode after final bill settlement. Refunds above ₹10,000 require Finance Head approval. Insurer-settled amounts are reconciled before any patient refund is released. Bills are never deleted; a void or refund creates an audited reversal.', tags: ['refund', 'advance', 'excess', 'void', 'reversal', 'payment', 'return'] },
  { doc: 'Visiting Hours & Attendant Policy', v: '2.0', eff: '01 Jun 2026', owner: 'Patient Experience', sec: '§1', text: 'General wards: visiting 11:00–13:00 and 17:00–19:00, two visitors at a time. ICU/CTICU: one attendant, 12:00–12:30 and 18:00–18:30. One attendant pass per inpatient is issued at admission; children under 12 are not permitted in ICU.', tags: ['visiting', 'hours', 'attendant', 'visitor', 'icu', 'pass', 'timing'] },
  { doc: 'Diagnostic Preparation Guide', v: '1.8', eff: '10 May 2026', owner: 'Diagnostics', sec: '§2 Fasting tests', text: 'Lipid profile and fasting glucose require 10–12 hours fasting; water is allowed. Ultrasound abdomen requires 6 hours fasting; pelvic ultrasound requires a full bladder. Contrast CT requires a recent creatinine and 4 hours fasting. Instructions are sent in Tamil and English the evening before.', tags: ['fasting', 'preparation', 'lipid', 'ultrasound', 'ct', 'test', 'before', 'prepare', 'diagnostic'] },
  { doc: 'HR Leave Policy', v: '5.0', eff: '01 Jan 2026', owner: 'HR', sec: '§3 Casual leave', text: 'Staff are entitled to 12 casual leave days per calendar year, applied at least one day in advance except in emergencies; a maximum of 3 consecutive casual leave days. Unused casual leave lapses on 31 December.', tags: ['leave', 'casual', 'hr', 'holiday', 'days', 'entitlement'] },
  { doc: 'HR Leave Policy', v: '5.0', eff: '01 Jan 2026', owner: 'HR', sec: '§7 Night shift', text: 'Night shift (20:00–08:00) attracts an allowance of ₹350 per shift for nursing and ₹250 for support staff; a maximum of 7 consecutive nights, followed by 2 rest days.', tags: ['night', 'shift', 'allowance', 'hr', 'roster'] },
  { doc: 'Emergency Codes & Procedures', v: '2.2', eff: '01 Mar 2026', owner: 'Emergency', sec: '§1', text: 'Code Blue: cardiac/respiratory arrest, dial 2222, team responds within 3 minutes. Code Red: fire. Code Pink: infant/child abduction. Code Orange: hazardous spill. This document is past its review date and is shown with a warning.', tags: ['code', 'blue', 'red', 'emergency', 'arrest', 'fire'], expired: true },
  { doc: 'NABH Patient Rights Charter', v: '1.0', eff: '01 Feb 2026', owner: 'Quality', sec: '§4', text: 'Patients have the right to an itemised bill, to know the identity of their treating doctor, to information in a language they understand, to privacy and confidentiality, and to raise a grievance which is acknowledged within 24 hours.', tags: ['rights', 'patient', 'itemised', 'grievance', 'complaint', 'privacy', 'language'] },
  { doc: 'Critical Value Policy', v: '1.4', eff: '15 Aug 2026', owner: 'Laboratory', sec: '§2', text: 'Critical laboratory values are phoned and pushed to the treating clinician within 5 minutes of verification. Acknowledgement is required within 15 minutes; otherwise the department head is notified. The AI platform notifies and times acknowledgement; it does not interpret values.', tags: ['critical', 'value', 'lab', 'acknowledge', 'escalation', 'potassium', 'troponin'] },
  { doc: 'Laboratory Sample Rejection SOP', v: '2.1', eff: '01 Jun 2026', owner: 'Laboratory', sec: '§3 Rejection & recollection', text: 'A sample is rejected when it is insufficient, hemolyzed, clotted, in the wrong container or specimen type, leaking, unlabelled, mislabeled, delayed in transport or contaminated. The technician records the reason against the accession number, the LIS notifies the ward and the ordering doctor, and a recollection request with a new barcode is generated. The original sample record is retained with full audit history; results are never attached to a rejected sample.', tags: ['sample', 'rejection', 'reject', 'hemolyzed', 'clotted', 'recollection', 'barcode', 'lab', 'specimen'] },
  { doc: 'Laboratory TAT & Critical Result Policy', v: '1.4', eff: '15 Aug 2026', owner: 'Laboratory', sec: '§4 Turnaround', text: 'Routine TAT targets: CBC 60 min, biochemistry 90 min, lipid 120 min, cultures 48 h. STAT samples are processed first with a 45-minute target. Results move Result Entered → Technical Validation → Pathologist Validation → Released; only pathologists release. Delayed tests appear on the lab dashboard and the exception queue.', tags: ['tat', 'turnaround', 'stat', 'validation', 'release', 'pathologist', 'delay', 'lab'] },
  { doc: 'Inventory & Stores SOP', v: '1.6', eff: '01 May 2026', owner: 'Stores', sec: '§2 Issue, transfer, adjustment', text: 'Stock is issued to departments against a requisition and deducted from the issuing store; transfers move stock between stores with both keepers acknowledging; adjustments require a reason and Store Manager approval and are posted only after approval. Expired batches are quarantined and cannot be issued or dispensed. Low-stock alerts fire when quantity falls below the minimum level and a reorder suggestion is created.', tags: ['stock', 'inventory', 'issue', 'transfer', 'adjustment', 'expired', 'low', 'reorder', 'store'] },
  { doc: 'Procurement Policy', v: '2.0', eff: '01 Apr 2026', owner: 'Procurement', sec: '§3 Approval & receipt', text: 'Purchase requisitions above ₹1,00,000 require Procurement Officer and Finance Manager approval; below that, the Procurement Officer alone. Purchase orders go to empanelled vendors with active contracts. Goods receipt includes quality check before stock is posted; invoices are matched to PO and GRN (three-way match) before payment.', tags: ['purchase', 'procurement', 'requisition', 'approval', 'po', 'grn', 'vendor', 'invoice', 'three-way'] },
  { doc: 'Insurance Claims Procedure', v: '1.3', eff: '01 Apr 2026', owner: 'Insurance Desk', sec: '§5 Claim lifecycle & rejection', text: 'After discharge the claim is submitted with the final bill, signed discharge summary and authorisation number. Insurers may raise a query (answer within 48 h), approve, partially approve or reject. Common rejection grounds: pre-existing disease waiting period, non-disclosure, room-rent cap exceeded, non-covered implant. Rejections can be appealed once within 30 days with supporting documents; patient responsibility is counselled before collection.', tags: ['claim', 'rejected', 'rejection', 'insurer', 'settlement', 'appeal', 'waiting', 'period', 'pre-existing', 'query'] },
  { doc: 'GST Configuration Note (demo)', v: '0.9', eff: '01 Apr 2026', owner: 'Finance', sec: '§1', text: 'Healthcare services by a clinical establishment are GST-exempt (SAC 9993). Medicines, implants and consumables billed separately attract GST at the configured rate (demo: 12%), canteen food 5%, wellness packages 18%. Rates are maintained centrally in Tax Configuration and shown per line on invoices as taxable amount, CGST, SGST, total tax and gross. DEMO CONFIGURATION — confirm with tax counsel.', tags: ['gst', 'tax', 'cgst', 'sgst', 'exempt', 'hsn', 'sac', 'invoice', 'rate'] },
  { doc: 'Canteen & Therapeutic Diet Policy', v: '1.1', eff: '01 Jul 2026', owner: 'Dietetics', sec: '§2', text: 'Every inpatient has a diet order (standard, cardiac, renal, diabetic, soft, liquid) set by the treating doctor or dietitian; nurses may request changes. Meals are scheduled at 08:00, 12:30 and 19:00, prepared in the kitchen, delivered to the bed and confirmed. Food cost and wastage are tracked daily; staff meals are deducted through payroll.', tags: ['diet', 'meal', 'canteen', 'food', 'kitchen', 'renal', 'cardiac', 'diabetic'] },
  { doc: 'Analyzer Interface Manual (Cobas c311)', v: '1.0', eff: '01 Feb 2026', owner: 'Laboratory', sec: '§6 Result matching', text: 'Results arrive as ASTM messages carrying accession number, test code, value and unit. The LIS accepts a result only when patient, accession, test and unit match; unknown accessions, duplicates and unit mismatches go to a quarantine queue for technician review. In this prototype the analyzer link is a clearly labelled simulator.', tags: ['analyzer', 'machine', 'astm', 'hl7', 'interface', 'matching', 'accession', 'quarantine', 'cobas'] },
  { doc: 'Medication Safety — High-alert drugs', v: '4.0', eff: '15 Aug 2026', owner: 'Nursing', sec: '§3', text: 'CONFLICT: v4.0 requires second-nurse double-check for IV high-alert medication only; v3.2 requires it for all high-alert medication. Withheld from answers until the Nursing Superintendent resolves.', tags: ['double-check', 'high-alert', 'medication', 'safety', 'nurse'], conflict: true },
];

const ROLE_SCOPE = { Patient: ['own'], Doctor: ['clinical', 'ops'], Nurse: ['clinical', 'ops'], 'Front Office': ['ops', 'demographic'], Billing: ['financial', 'ops'], Insurance: ['financial', 'ops'], Radiologist: ['clinical'], Laboratory: ['clinical'], Pharmacy: ['clinical'], 'Hospital Management': ['aggregate', 'ops', 'financial'], 'AI Administrator': ['aggregate', 'ops'], 'Governance Officer': ['aggregate', 'ops'] };
const has = (role, s) => (ROLE_SCOPE[role] || []).includes(s);
const tok = q => q.toLowerCase().replace(/[^a-z0-9₹\u0B80-\u0BFF\s-]/g, ' ').split(/\s+/).filter(w => w.length > 2);
const STOP = new Set(['the', 'what', 'how', 'does', 'for', 'and', 'are', 'can', 'with', 'this', 'that', 'show', 'tell', 'about', 'much', 'many', 'why', 'when', 'who', 'which', 'meridian', 'hospital', 'please', 'you', 'have', 'has', 'her', 'his', 'she']);

function retrieve(q, k = 3) {
  const words = tok(q).filter(w => !STOP.has(w)); const scored = CORPUS.map(c => { const hay = (c.text + ' ' + c.tags.join(' ') + ' ' + c.doc).toLowerCase(); let s = 0; words.forEach(w => { if (c.tags.some(t => t.startsWith(w.slice(0, 5)))) s += 3; if (hay.includes(w)) s += 1; }); return { c, s }; }).filter(x => x.s > 0).sort((a, b) => b.s - a.s); const top = scored.length ? scored[0].s : 0; return scored.filter(x => x.s >= Math.max(3, top * 0.5)).slice(0, k);
}
function detectIntent(q, ctxTopic) {
  const s = q.toLowerCase();
  if (/another|other patient|someone else|all patients' balance|everyone's/.test(s)) return 'unauthorized-probe';
  if (/\b(owe|balance|bill|outstanding|how much|pay(ment)?s? (history|status)|paid|due|reversed|refund(ed)? (my|status))\b/.test(s) && /\b(my|i |me|kavitha|patient|john|murugan|fathima|[A-Z][a-z]+ [A-Z])/.test(s + ' ')) return /why|explain|reversed|pending|not reached|hasn/.test(s) ? 'hybrid' : 'structured';
  if (/\b(status|blocked|ready|discharge (status|time)|when can|go home|where is|which patients|how many|list|show)\b/.test(s) && !/policy|sop|rule|hours|procedure|how do|what is the/.test(s)) return 'structured';
  if (ctxTopic && /\b(it|that|this|also|fee|setup|same|there)\b/.test(s) && s.split(' ').length < 8) return 'rag';
  return 'rag';
}
const inr = n => '₹' + Math.round(n).toLocaleString('en-IN');
const fmtSrc = c => ({ label: c.doc + ' ' + c.sec, doc: c.doc, sec: c.sec, v: c.v, eff: c.eff, owner: c.owner, conflict: !!c.conflict, expired: !!c.expired });

function answer(q, { role, api, history }) {
  const S = api.S; const s = q.toLowerCase(); const lastTopic = history.length ? history[history.length - 1].topic : null;
  const intent = detectIntent(q, lastTopic);
  const me = S.patients.find(p => p.id === 'MER-2026-008421'); // patient-role identity
  const namedRaw = (q.match(/\b(Kavitha|Murugan|Fathima|Rajesh|Lakshmi|Arun|Meenakshi|Joseph|Priyanka|Venkatesan|Divya|Mohammed)\b/i) || [])[0];
  const named = namedRaw ? S.patients.find(p => p.name.toLowerCase().startsWith(namedRaw.toLowerCase())) : null;
  const target = role === 'Patient' ? me : named || (/\bmy\b/.test(s) ? null : (lastTopic && lastTopic.patientId ? S.patients.find(p => p.id === lastTopic.patientId) : null));
  const R = { intent, text: '', bullets: [], table: null, sources: [], actions: [], status: 'answered', confidence: 0, topic: lastTopic, masked: false };
  const rag = () => { const hits = retrieve(q, 3).filter(h => !h.c.conflict && !h.c.expired); const conflict = retrieve(q, 3).find(h => h.c.conflict); const expired = retrieve(q, 3).find(h => h.c.expired); if (!hits.length) { R.status = conflict ? 'conflict' : 'no-answer'; R.text = conflict ? 'Two governed documents disagree on this topic (Medication Safety v3.2 vs v4.0). I am not allowed to choose between them; the Nursing Superintendent has been asked to resolve the conflict.' : "I couldn't find this in Hospital's governed knowledge base. I won't guess. I've logged a knowledge gap for the Knowledge Manager and can route you to the duty manager."; R.confidence = 0.31; if (expired) { R.text += ' The closest document (' + expired.c.doc + ') is past its review date and is withheld.'; R.sources = [fmtSrc(expired.c)]; } R.actions = [{ label: 'Route to a person', page: 'contact' }, { label: 'Knowledge base', page: 'knowledge' }]; return; } R.text = hits.map(h => h.c.text).slice(0, 2).join(' '); R.sources = hits.map(h => fmtSrc(h.c)); R.confidence = Math.min(0.97, 0.7 + hits[0].s * 0.04); R.topic = { doc: hits[0].c.doc, patientId: target ? target.id : lastTopic && lastTopic.patientId }; };
  // ERP structured intents (no patient needed)
  const erpK = api.erpKpis ? api.erpKpis() : null;
  if (erpK && /stock|inventory|paracetamol|low stock|out of stock|expir/.test(s) && !/policy|sop|process|how/.test(s)) { if (!(has(role, 'ops') || has(role, 'aggregate') || ['Pharmacy', 'Laboratory', 'Store Manager', 'Procurement Officer', 'Canteen Manager'].includes(role))) { R.status = 'denied'; R.text = 'Inventory data is outside the ' + role + ' scope.'; R.confidence = 1; return R; } const named2 = S.items.find(i => s.includes(i.name.split(' ')[0].toLowerCase())); const list = named2 ? [named2] : S.items.filter(i => /low|out|expir/.test(s) ? (/expir/.test(s) ? i.expiryState !== 'OK' : /Low|Out/.test(i.status)) : true).slice(0, 10); R.intent = 'structured'; R.text = named2 ? named2.name + ': ' + named2.qty + ' ' + named2.unit + ' in ' + named2.store + ' (min ' + named2.min + ', status ' + named2.status + ', batch ' + named2.batch + ' expiry ' + named2.expiry + ').' : list.length + ' item(s) match. ' + (list.some(i => /Out/.test(i.status)) ? 'Out-of-stock items block dispensing; requisitions can be raised from each item.' : ''); R.table = { cols: ['Item', 'Store', 'Qty', 'Min', 'Expiry', 'Status'], rows: list.map(i => [i.name, i.store, i.qty + ' ' + i.unit, i.min, i.expiry, i.status]) }; R.sources = [{ label: 'Inventory ledger (live)', doc: 'Structured data', sec: 'items', v: 'live', eff: 'now', owner: 'Stores' }]; R.confidence = 0.97; R.actions = [{ label: 'Inventory', page: 'inventory' }]; R.topic = { doc: 'inventory' }; return R; }
  if (erpK && /machine|analy[sz]er|offline/.test(s) && !/how|sop|manual|process/.test(s)) { R.intent = 'structured'; const off = S.analyzers.filter(a => a.status !== 'Online'); R.text = off.length + ' analyzer(s) are not online: ' + off.map(a => a.name + ' (' + a.status + ', ' + a.section + ')').join('; ') + '. Tests routed to backup or manual entry.'; R.table = { cols: ['Analyzer', 'Section', 'Status', 'Comm', 'Next maintenance'], rows: S.analyzers.map(a => [a.name, a.section, a.status, a.comm, a.nextMaint]) }; R.sources = [{ label: 'Lab equipment register (live)', doc: 'Structured data', sec: 'analyzers', v: 'live', eff: 'now', owner: 'Laboratory' }]; R.confidence = 0.97; R.actions = [{ label: 'Analyzers', page: 'lab-analyzers' }]; return R; }
  if (erpK && /(cbc|lab|test|sample).*(today|done|waiting|validation|urgent|stat|pending)|urgent lab|validation/.test(s)) { R.intent = 'structured'; const stat = S.samples.filter(x => x.priority === 'STAT' && /Validation|Result Entered/.test(x.status)); const cbc = S.samples.filter(x => x.test === 'CBC'); R.text = /cbc/.test(s) ? cbc.length + ' CBC samples today; ' + cbc.filter(x => x.status === 'Released').length + ' released.' : stat.length + ' STAT sample(s) are waiting for validation. TAT compliance ' + erpK.tatCompliance + '%.'; R.table = { cols: ['Accession', 'Patient', 'Test', 'Priority', 'Status'], rows: (/cbc/.test(s) ? cbc : stat).slice(0, 8).map(x => [x.accession, x.patient, x.test, x.priority, x.status]) }; R.sources = [{ label: 'LIS work queue (live)', doc: 'Structured data', sec: 'samples', v: 'live', eff: 'now', owner: 'Laboratory' }]; R.confidence = 0.96; R.actions = [{ label: 'Lab work queue', page: 'lab-queue' }]; return R; }
  if (erpK && /employee|absent|attendance|present|staff.*today/.test(s) && !/policy|leave policy/.test(s)) { if (!(has(role, 'aggregate') || role === 'HR Manager')) { R.status = 'denied'; R.text = 'HR attendance data is restricted to HR Manager and Hospital Management.'; R.confidence = 1; return R; } R.intent = 'structured'; R.text = erpK.absent + ' employees are absent today, ' + erpK.onLeave + ' on approved leave, ' + erpK.present + ' present of ' + erpK.employees + '. ' + erpK.pendingLeave + ' leave requests await HR approval.'; R.table = { cols: ['Employee', 'Unit', 'Today'], rows: S.employees.filter(e => e.today === 'Absent').slice(0, 8).map(e => [e.name, e.unit, e.today]) }; R.sources = [{ label: 'HR attendance (live)', doc: 'Structured data', sec: 'employees', v: 'live', eff: 'now', owner: 'HR' }]; R.confidence = 0.97; R.actions = [{ label: 'HR dashboard', page: 'hr-dashboard' }]; return R; }
  if (erpK && /meal|canteen|served|diet/.test(s) && !/policy|sop/.test(s)) { R.intent = 'structured'; R.text = erpK.mealsToday + ' meals scheduled today: ' + erpK.delivered + ' delivered, ' + erpK.pendingMeals + ' pending (' + erpK.patientMeals + ' patient, ' + erpK.staffMeals + ' staff).'; R.sources = [{ label: 'Canteen schedule (live)', doc: 'Structured data', sec: 'meals', v: 'live', eff: 'now', owner: 'Canteen' }]; R.confidence = 0.96; R.actions = [{ label: 'Canteen', page: 'canteen' }]; return R; }
  if (erpK && /revenue|collection|receivable|payable|outstanding total|tax collected/.test(s) && !/policy|how/.test(s)) { if (!(has(role, 'financial') || has(role, 'aggregate'))) { R.status = 'denied'; R.text = 'Financial figures are restricted to Finance, Billing, Insurance and Management roles.'; R.confidence = 1; return R; } R.intent = 'structured'; const rev = S.payments.filter(p => p.ts >= 0 && p.status === 'Success').reduce((a, p) => a + p.amount, 0); R.text = 'Today\'s collections ' + inr(rev) + '. Patient receivables ' + inr(erpK.receivablesPatient) + ', insurer receivables ' + inr(erpK.receivablesInsurer) + ', vendor payables ' + inr(erpK.payables) + ', tax collected (est.) ' + inr(erpK.taxCollected) + '.'; R.sources = [{ label: 'Finance ledgers (live)', doc: 'Structured data', sec: 'payments · bills · purchases', v: 'live', eff: 'now', owner: 'Finance' }]; R.confidence = 0.95; R.actions = [{ label: 'Finance dashboard', page: 'finance' }]; return R; }
  if (erpK && /claim.*(reject|why)|why.*claim/.test(s)) { if (!(has(role, 'financial') || has(role, 'aggregate'))) { R.status = 'denied'; R.text = 'Claim data is restricted to Insurance, Finance and Management roles.'; R.confidence = 1; return R; } R.intent = 'hybrid'; const rj = S.preauths.find(p => p.claimStatus === 'Rejected' || p.status === 'Rejected'); const hits = retrieve('claim rejected pre-existing waiting period appeal', 1); R.text = rj ? rj.patient + ' · ' + (rj.claimNo || rj.id) + ' · ' + rj.insurer + ' rejected: ' + (rj.rejectReason || 'insurer cited policy exclusion') + '. Claimed ' + (rj.claimed || rj.requested) + '; patient responsibility ' + rj.liability + '. ' + hits[0].c.text : 'No rejected claims in the current data.'; R.sources = [{ label: 'Claims ledger · ' + (rj ? rj.claimNo || rj.id : '') + ' (live)', doc: 'Structured data', sec: 'claims', v: 'live', eff: 'now', owner: 'Insurance Desk' }].concat(hits.map(h => fmtSrc(h.c))); R.confidence = 0.94; R.actions = rj ? [{ label: 'Claim', drawer: ['preauth', rj.id] }, { label: 'Claims', page: 'claims' }] : []; return R; }
  if (intent === 'unauthorized-probe' || (target && role === 'Patient' && target.id !== me.id) || (named && role === 'Patient' && named.id !== me.id)) { R.status = 'denied'; R.text = 'Access denied. Your role can only see records within your relationship and consent scope; this request was for another patient. The attempt is logged in the audit trail.'; R.confidence = 1; R.actions = [{ label: 'My record', page: 'portal' }]; api.mutate(() => {}, role, 'Assistant request DENIED · other patient', '—', 'Denied', q); return R; }
  if (intent === 'structured' || intent === 'hybrid') {
    if (!target && /\bmy\b/.test(s) && role !== 'Patient') { R.status = 'no-answer'; R.text = 'Staff accounts have no "my" patient record. Name the patient (for example "Kavitha Raman") and I will look up what your role may see.'; R.confidence = 0.5; return R; }
    if (!target) { const dis = S.discharges.filter(d => !d.completed); const blocked = dis.filter(d => api.dischargeState(d).k === 'blocked'); if (/discharge|blocked|ready|go home/.test(s) && has(role, 'ops') || has(role, 'aggregate')) { R.text = blocked.length + ' of ' + dis.length + ' active discharges are blocked.'; R.table = { cols: ['Patient', 'Blocker', 'Owner', 'Predicted ready'], rows: blocked.map(d => [d.patient, api.dischargeState(d).dep + ' — ' + d.deps[api.dischargeState(d).dep].note, d.owner, d.eta]) }; R.sources = [{ label: 'Discharge board (live)', doc: 'Structured data', sec: 'discharges', v: 'live', eff: 'now', owner: 'Operations' }]; R.confidence = 0.95; R.actions = [{ label: 'Discharge board', page: 'discharge' }]; R.topic = { doc: 'discharge' }; return R; } R.status = 'no-answer'; R.text = 'I need a patient name to answer that from verified records.'; R.confidence = 0.4; return R; }
    const p = target; const allowedFin = has(role, 'financial') || has(role, 'own') || has(role, 'aggregate'); const allowedClin = has(role, 'clinical') || has(role, 'own'); const allowedOps = has(role, 'ops') || has(role, 'own') || has(role, 'aggregate');
    const b = S.bills.find(x => x.patientId === p.id && x.admissionId !== '—') || S.bills.find(x => x.patientId === p.id); const pa = S.preauths.find(x => x.patientId === p.id); const d = S.discharges.find(x => x.patientId === p.id && !x.completed) || S.discharges.find(x => x.patientId === p.id); const pays = S.payments.filter(x => b && x.billId === b.id);
    R.topic = { doc: 'patient', patientId: p.id };
    if (/owe|balance|outstanding|bill|due|how much/.test(s)) { if (!allowedFin) { R.status = 'denied'; R.text = 'Your role (' + role + ') does not include financial data for ' + p.name + '. Billing or Insurance roles can answer this.'; R.confidence = 1; return R; } if (!b) { R.status = 'no-answer'; R.text = 'No bill exists for ' + p.name + ' in the verified billing data.'; R.confidence = 0.6; return R; } const due = Math.max(0, b.patientShare - (b.paid || 0)); R.text = p.name + ' · bill ' + b.id + ' (' + b.status + '). Running total ' + inr(b.actual) + ' against an estimate of ' + inr(b.estimate) + '. Insurer share ' + inr(b.insurance) + (pa ? ' (' + pa.insurer + ' · ' + pa.status + ')' : '') + '. Patient share ' + inr(b.patientShare) + ', paid ' + inr(b.paid || 0) + ', outstanding ' + inr(due) + '.'; R.table = { cols: ['Item', 'Amount'], rows: [['Estimate', inr(b.estimate)], ['Running bill', inr(b.actual)], ['Insurer approved', inr(b.insurance)], ['Patient share', inr(b.patientShare)], ['Paid', inr(b.paid || 0)], ['Outstanding', inr(due)]] }; R.sources = [{ label: 'Billing ledger · ' + b.id + ' (live)', doc: 'Structured data', sec: b.id, v: 'live', eff: 'now', owner: 'Finance' }]; R.confidence = 0.98; R.actions = [{ label: 'View bill', drawer: ['bill', b.id] }, { label: 'Patient 360', page: 'patient', id: p.id }]; if (intent === 'hybrid' || b.actual > b.estimate * 1.05) { const hits = retrieve('estimate variance bill higher consumable balloon', 2); R.text += ' Why it changed: ' + (b.lines ? 'a second pre-dilatation balloon (+₹15,950) and one extra ward night (+₹7,500).' : 'additional length of stay and consumables.') + ' ' + hits[0].c.text; R.sources.push(...hits.map(h => fmtSrc(h.c))); } return R; }
    if (/payment|paid|history|reversed|pending|reached/.test(s)) { if (!allowedFin) { R.status = 'denied'; R.text = 'Your role does not include payment data for ' + p.name + '.'; R.confidence = 1; return R; } R.text = pays.length ? pays.length + ' payment(s) on ' + (b ? b.id : '') + ' for ' + p.name + '.' : 'No payments recorded yet for ' + p.name + (b ? ' on ' + b.id + ' (' + b.status + ').' : '.'); R.table = pays.length ? { cols: ['Payment', 'Amount', 'Mode', 'Status', 'When'], rows: pays.map(x => [x.id, inr(x.amount), x.mode.replace(/\d{4,}/g, '••••'), x.status, api.fmt(x.ts)]) } : null; const rev = pays.find(x => /Refunded|Failed/.test(x.status)); if (intent === 'hybrid') { const hits = retrieve('refund reversal void payment', 2); R.text += rev ? ' ' + rev.id + ' shows ' + rev.status + '. ' + hits[0].c.text : (pa && /Submitted|Query/.test(pa.status) ? ' The insurer response is still pending (' + pa.status + '), so the final bill has not been released and no patient payment is due yet. ' + retrieve('enhancement turnaround escalation', 1)[0].c.text : ' Nothing is reversed or pending on this account.'); R.sources.push(...hits.map(h => fmtSrc(h.c))); } R.sources.unshift({ label: 'Payments ledger (live)', doc: 'Structured data', sec: 'payments', v: 'live', eff: 'now', owner: 'Finance' }); R.confidence = 0.96; R.actions = b ? [{ label: 'View bill', drawer: ['bill', b.id] }] : []; return R; }
    if (/discharge|go home|when can|ready|status|blocked/.test(s)) { if (!allowedOps) { R.status = 'denied'; R.text = 'Your role does not include workflow status for ' + p.name + '.'; R.confidence = 1; return R; } if (!d) { R.text = p.name + ' has no discharge workflow yet (' + p.status + ').'; R.confidence = 0.9; R.actions = [{ label: 'Patient 360', page: 'patient', id: p.id }]; return R; } const st = api.dischargeState(d); const open = Object.entries(d.deps).filter(([, x]) => x.status !== 'done'); R.text = d.completed ? p.name + ' was discharged at ' + api.fmt(d.dischargedAt) + '; follow-up booked 19 Sep 10:30.' : p.name + ' is ' + st.label.toLowerCase() + '. Predicted ready ' + d.eta + '. ' + (open.length ? 'Open: ' + open.map(([k, x]) => k + ' (' + x.status + (x.note ? ' — ' + x.note : '') + ')').join('; ') + '.' : 'All dependencies cleared.') + ' The doctor signs and Billing releases; I coordinate.'; R.sources = [{ label: 'Discharge board · ' + d.id + ' (live)', doc: 'Structured data', sec: d.id, v: 'live', eff: 'now', owner: 'Operations' }]; if (intent === 'hybrid' || st.dep === 'insurance') { const hits = retrieve('enhancement turnaround escalation submit', 1); R.text += ' ' + hits[0].c.text; R.sources.push(fmtSrc(hits[0].c)); } R.confidence = 0.97; R.actions = [{ label: 'Discharge case', page: 'discharge-case', id: d.id }]; if (role === 'Patient') R.actions = [{ label: 'Discharge progress', portalTab: 'Discharge' }]; return R; }
    if (/result|echo|report|lab|test|normal/.test(s)) { if (role === 'Patient') { R.text = 'Your reports (ECHO, CBC, lipid profile) are ready and Dr. Menon has reviewed them. I cannot interpret results; he will go through them with you, or I can request a call from the ward nurse.'; R.status = 'boundary'; R.confidence = 1; R.actions = [{ label: 'Request nurse call', notify: true }]; R.sources = [{ label: 'Critical Value Policy §2 · boundary', doc: 'Critical Value Policy', sec: '§2', v: '1.4', eff: '15 Aug 2026', owner: 'Laboratory' }]; return R; } if (!allowedClin) { R.status = 'denied'; R.text = 'Clinical results for ' + p.name + ' are masked for ' + role + '. Use break-glass on the patient record if this is an emergency.'; R.confidence = 1; R.masked = true; return R; } const labs = S.labs.filter(l => l.patientId === p.id); R.text = labs.length + ' result(s) for ' + p.name + '. I present values and flags only; interpretation stays with the clinician.'; R.table = { cols: ['Test', 'Analyte', 'Value', 'Flag', 'Status'], rows: labs.slice(0, 6).map(l => [l.test, l.analyte, l.value + ' ' + l.unit, l.flag, l.status]) }; R.sources = [{ label: 'LIS (live)', doc: 'Structured data', sec: 'labs', v: 'live', eff: 'now', owner: 'Laboratory' }]; R.confidence = 0.95; R.actions = [{ label: 'Patient 360', page: 'patient', id: p.id }]; return R; }
    rag(); return R;
  }
  rag(); if (R.status === 'answered' && role === 'Patient' && /leave|shift|allowance/.test(s)) { R.status = 'denied'; R.text = 'That is staff-only HR information.'; R.sources = []; R.actions = []; }
  return R;
}

return { CORPUS, retrieve, detectIntent, answer };
})();
window.__mm2.data = (function(){
// Hospital synthetic data foundation — DEMO ENVIRONMENT • SYNTHETIC DATA • NOT REAL PATIENT INFORMATION
// Single source of truth for every screen. Today = 12 Sep 2026, clock ≈ 11:20.
const TODAY = '12 Sep 2026';
const NOW = '11:20';

const doctors = [
  { id: 'DR-014', name: 'Dr. Arjun Menon', dept: 'Cardiology', role: 'Senior Consultant, Interventional Cardiology' },
  { id: 'DR-003', name: 'Dr. Priya Venkatesh', dept: 'Cardiac Surgery', role: 'Consultant Cardiothoracic Surgeon' },
  { id: 'DR-021', name: 'Dr. Suresh Babu', dept: 'Orthopaedics', role: 'Consultant Orthopaedic Surgeon' },
  { id: 'DR-009', name: 'Dr. Nithya Rajan', dept: 'General Surgery', role: 'Consultant General Surgeon' },
  { id: 'DR-017', name: 'Dr. Karthik Iyer', dept: 'Nephrology', role: 'Consultant Nephrologist' },
  { id: 'DR-026', name: 'Dr. Deepa Krishnan', dept: 'Oncology', role: 'Consultant Medical Oncologist' },
  { id: 'DR-011', name: 'Dr. Vikram Nair', dept: 'Neurology', role: 'Consultant Neurologist' },
  { id: 'DR-030', name: 'Dr. Anjali Rao', dept: 'Obstetrics', role: 'Consultant Obstetrician' },
  { id: 'DR-006', name: 'Dr. Ramesh Pillai', dept: 'Pulmonology', role: 'Consultant Pulmonologist' },
  { id: 'DR-019', name: 'Dr. Sangeetha Mohan', dept: 'Paediatrics', role: 'Consultant Paediatrician' },
  { id: 'DR-024', name: 'Dr. Balaji Srinivasan', dept: 'Gastroenterology', role: 'Consultant Gastroenterologist' },
  { id: 'DR-002', name: 'Dr. Hemalatha Devi', dept: 'Radiology', role: 'Head of Radiology' },
];

// Hero patients — every screen references these by uhid.
const patients = [
  { uhid: 'MER-2026-008421', name: 'Kavitha Raman', age: 48, sex: 'F', lang: 'Tamil', dept: 'Cardiology', doctor: 'Dr. Arjun Menon', insurer: 'Star Health', policy: 'SH-FHO-77219043', procedure: 'PTCA with single drug-eluting stent', admission: 'IP-2026-3187', bed: 'C-412 · Cardiac Ward', admitted: '09 Sep 2026', los: '3 days', phone: '+91 98•••• 4410', status: 'Discharge planning', dischargeStatus: 'Insurance Blocked', blocker: 'Star Health final enhancement query open', eta: '4:20 PM', owner: 'Insurance desk · R. Sundar', sla: '30 min', elapsed: '1 h 42 m', nextAction: 'Upload angioplasty report and resubmit enhancement', hero: true },
  { uhid: 'MER-2026-007733', name: 'Murugan Selvam', age: 62, sex: 'M', lang: 'Tamil', dept: 'Cardiac Surgery', doctor: 'Dr. Priya Venkatesh', insurer: 'ICICI Lombard', policy: 'IL-HA-5583201', procedure: 'CABG (triple vessel)', admission: 'IP-2026-3102', bed: 'CTICU-04', admitted: '04 Sep 2026', los: '8 days', status: 'Post-operative day 5', dischargeStatus: 'Doctor Approval', blocker: 'Awaiting surgeon sign-off on summary', eta: '6:00 PM', owner: 'Dr. Priya Venkatesh', sla: '2 h', elapsed: '38 m', nextAction: 'Surgeon reviews AI draft summary', hero: true },
  { uhid: 'MER-2026-009104', name: 'Fathima Begum', age: 71, sex: 'F', lang: 'Tamil', dept: 'Orthopaedics', doctor: 'Dr. Suresh Babu', insurer: 'HDFC Ergo', policy: 'HE-OPT-330912', procedure: 'Total hip replacement (left)', admission: 'IP-2026-3151', bed: 'O-207 · Ortho Ward', admitted: '07 Sep 2026', los: '5 days', status: 'Discharge planning', dischargeStatus: 'Pharmacy Blocked', blocker: 'Enoxaparin 40 mg out of stock at ward pharmacy', eta: '3:10 PM', owner: 'Pharmacy · S. Devi', sla: '45 min', elapsed: '52 m', nextAction: 'Transfer stock from central pharmacy', hero: true },
  { uhid: 'MER-2026-006512', name: 'Rajesh Kannan', age: 35, sex: 'M', lang: 'English', dept: 'General Surgery', doctor: 'Dr. Nithya Rajan', insurer: 'Self-pay', policy: '—', procedure: 'Laparoscopic appendicectomy', admission: 'IP-2026-3211', bed: 'S-114 · Surgical Ward', admitted: '10 Sep 2026', los: '2 days', status: 'Fit for discharge', dischargeStatus: 'Ready', blocker: '—', eta: '12:30 PM', owner: 'Ward nurse · Sheela J', sla: '—', elapsed: '—', nextAction: 'Final bill release by Billing', hero: true },
  { uhid: 'MER-2026-008877', name: 'Lakshmi Narayanan', age: 58, sex: 'F', lang: 'Telugu', dept: 'Nephrology', doctor: 'Dr. Karthik Iyer', insurer: 'Aditya Birla Health', policy: 'AB-ACT-9012331', procedure: 'AV fistula creation', admission: 'IP-2026-3170', bed: 'N-305 · Medical Ward', admitted: '08 Sep 2026', los: '4 days', status: 'Awaiting results', dischargeStatus: 'Lab Blocked', blocker: 'KFT result pending — sample in analyser queue #14', eta: '2:45 PM', owner: 'Laboratory · M. Ganesh', sla: '60 min', elapsed: '25 m', nextAction: 'Expedite analyser queue', hero: true },
  { uhid: 'MER-2026-009230', name: 'Arun Prakash', age: 29, sex: 'M', lang: 'Tamil', dept: 'Orthopaedics', doctor: 'Dr. Suresh Babu', insurer: 'Bajaj Allianz', policy: 'BA-HG-4471820', procedure: 'ACL reconstruction (right knee)', admission: 'IP-2026-3198', bed: 'O-212 · Ortho Ward', admitted: '10 Sep 2026', los: '2 days', status: 'Discharge planning', dischargeStatus: 'Billing Blocked', blocker: 'Implant invoice not posted to bill', eta: '3:40 PM', owner: 'Billing · K. Meena', sla: '30 min', elapsed: '1 h 05 m', nextAction: 'Post implant charge ₹48,500 and revalidate estimate', hero: true },
  { uhid: 'MER-2026-005098', name: 'Meenakshi Sundaram', age: 66, sex: 'F', lang: 'Tamil', dept: 'Oncology', doctor: 'Dr. Deepa Krishnan', insurer: 'Star Health', policy: 'SH-SCR-11820934', procedure: 'Chemotherapy cycle 4 (day-care to IP)', admission: 'IP-2026-3205', bed: 'ON-108 · Oncology Ward', admitted: '10 Sep 2026', los: '2 days', status: 'Stable', dischargeStatus: 'Doctor Approval', blocker: 'Consultant on rounds; signature pending', eta: '1:15 PM', owner: 'Dr. Deepa Krishnan', sla: '2 h', elapsed: '20 m', nextAction: 'Sign discharge summary v2', hero: true },
  { uhid: 'MER-2026-007415', name: 'Joseph Antony', age: 54, sex: 'M', lang: 'Malayalam', dept: 'Neurology', doctor: 'Dr. Vikram Nair', insurer: 'Niva Bupa', policy: 'NB-RA-2290187', procedure: 'Stroke work-up and rehabilitation', admission: 'IP-2026-3128', bed: 'NR-03 · Neuro Ward', admitted: '05 Sep 2026', los: '7 days', status: 'Signed off', dischargeStatus: 'Housekeeping Pending', blocker: 'Bed turnaround not started', eta: '12:50 PM', owner: 'Housekeeping · P. Velu', sla: '40 min', elapsed: '12 m', nextAction: 'Dispatch housekeeping team B', hero: true },
  { uhid: 'MER-2026-009511', name: 'Priyanka Das', age: 32, sex: 'F', lang: 'Hindi', dept: 'Obstetrics', doctor: 'Dr. Anjali Rao', insurer: 'Care Health', policy: 'CH-JOY-6631007', procedure: 'LSCS delivery', admission: 'IP-2026-3190', bed: 'M-402 · Maternity', admitted: '09 Sep 2026', los: '3 days', status: 'Signed off', dischargeStatus: 'Transport Pending', blocker: 'Wheelchair transport requested 11:05', eta: '11:45 AM', owner: 'Patient transport · A. Raj', sla: '20 min', elapsed: '15 m', nextAction: 'Assign porter from lobby pool', hero: true },
  { uhid: 'MER-2026-004120', name: 'Venkatesan P', age: 74, sex: 'M', lang: 'Tamil', dept: 'Pulmonology', doctor: 'Dr. Ramesh Pillai', insurer: 'Self-pay', policy: '—', procedure: 'COPD exacerbation — NIV support', admission: 'IP-2026-3010', bed: 'P-118 · Medical Ward', admitted: '29 Aug 2026', los: '14 days', status: 'Long stay', dischargeStatus: 'Doctor Approval', blocker: 'Home oxygen arrangement pending family decision', eta: 'Tomorrow', owner: 'Dr. Ramesh Pillai', sla: '—', elapsed: '2 d', nextAction: 'Family counselling at 2 PM', hero: true },
  { uhid: 'MER-2026-009602', name: 'Divya Shankar', age: 8, sex: 'F', lang: 'Tamil', dept: 'Paediatrics', doctor: 'Dr. Sangeetha Mohan', insurer: 'Self-pay', policy: '—', procedure: 'Acute gastroenteritis — IV rehydration', admission: 'IP-2026-3215', bed: 'PD-06 · Paediatric Ward', admitted: '10 Sep 2026', los: '2 days', status: 'Discharged 10:05', dischargeStatus: 'Completed', blocker: '—', eta: '—', owner: '—', sla: '—', elapsed: '—', nextAction: 'Billing complaint FB-2026-0471 open', hero: true },
  { uhid: 'MER-2026-008056', name: 'Mohammed Ismail', age: 45, sex: 'M', lang: 'Tamil', dept: 'Gastroenterology', doctor: 'Dr. Balaji Srinivasan', insurer: 'ManipalCigna', policy: 'MC-PP-8823014', procedure: 'Laparoscopic cholecystectomy', admission: 'IP-2026-3201', bed: 'S-109 · Surgical Ward', admitted: '10 Sep 2026', los: '2 days', status: 'Discharged 09:40', dischargeStatus: 'Completed', blocker: '—', eta: '—', owner: '—', sla: '—', elapsed: '—', nextAction: 'Follow-up 19 Sep booked', hero: true },
  { uhid: 'MER-2026-003877', name: 'Saraswathi K', age: 80, sex: 'F', lang: 'Tamil', dept: 'Geriatrics', doctor: 'Dr. Ramesh Pillai', insurer: 'Self-pay', policy: '—', procedure: 'Community-acquired pneumonia', admission: 'IP-2026-3160', bed: 'P-121 · Medical Ward', admitted: '07 Sep 2026', los: '5 days', status: 'Fit for discharge', dischargeStatus: 'Ready', blocker: '—', eta: '1:00 PM', owner: 'Ward nurse · Sheela J', sla: '—', elapsed: '—', nextAction: 'Final bill release', hero: false },
  { uhid: 'MER-2026-009344', name: 'Karthikeyan M', age: 41, sex: 'M', lang: 'Tamil', dept: 'Urology', doctor: 'Dr. Nithya Rajan', insurer: 'Star Health', policy: 'SH-FHO-77003121', procedure: 'URSL for ureteric calculus', admission: 'IP-2026-3208', bed: 'S-121 · Surgical Ward', admitted: '10 Sep 2026', los: '2 days', status: 'Awaiting results', dischargeStatus: 'Lab Blocked', blocker: 'Urine culture 48 h read due 13:00', eta: '2:00 PM', owner: 'Laboratory · M. Ganesh', sla: '—', elapsed: '—', nextAction: 'Culture read at 13:00', hero: false },
  { uhid: 'MER-2026-009488', name: 'Rebecca Thomas', age: 27, sex: 'F', lang: 'English', dept: 'ENT', doctor: 'Dr. Balaji Srinivasan', insurer: 'Niva Bupa', policy: 'NB-RA-2210993', procedure: 'Septoplasty', admission: 'IP-2026-3218', bed: 'S-104 · Surgical Ward', admitted: '11 Sep 2026', los: '1 day', status: 'Discharged 08:55', dischargeStatus: 'Completed', blocker: '—', eta: '—', owner: '—', sla: '—', elapsed: '—', nextAction: '—', hero: false },
];

const kavitha = patients[0];

const kavithaTimeline = [
  { d: '12 Sep', t: '09:02', e: 'Discharge planning started · Discharge Orchestration Agent activated', k: 'ai' },
  { d: '11 Sep', t: '16:40', e: 'Cardiology round — Dr. Arjun Menon: clinically stable, likely discharge 12 Sep', k: 'clinical' },
  { d: '10 Sep', t: '14:15', e: 'PTCA with single DES completed — Cath Lab 1', k: 'procedure' },
  { d: '10 Sep', t: '08:30', e: 'ECHO completed — EF 52%, report signed', k: 'diag' },
  { d: '10 Sep', t: '07:10', e: 'CBC, KFT, Lipid profile — sample collected', k: 'diag' },
  { d: '09 Sep', t: '11:20', e: 'Admitted to C-412 · Star Health preauth ₹1,95,000 approved', k: 'admin' },
  { d: '08 Sep', t: '10:30', e: 'OP consultation — Dr. Arjun Menon · admission advised', k: 'clinical' },
  { d: '07 Sep', t: '19:12', e: 'Appointment booked via web · confirmed on mobile · WhatsApp query answered', k: 'channel' },
];

const kavithaBill = {
  estimate: 245000, current: 268450, variance: 23450, variancePct: '+9.6%',
  varianceReason: 'Second balloon (pre-dilatation) and one extra day in Cardiac Ward beyond estimate assumption.',
  coverage: 195000, liability: 73450,
  lines: [
    { item: 'Room — Cardiac Ward C-412 (3 nights)', est: 22500, cur: 30000, note: '+1 night' },
    { item: 'Doctor — consultation & procedure fees', est: 45000, cur: 45000, note: '' },
    { item: 'Procedure — PTCA, cath lab charges', est: 68000, cur: 68000, note: '' },
    { item: 'Implant — drug-eluting stent', est: 65000, cur: 65000, note: 'NPPA capped' },
    { item: 'Consumables — balloons, guidewire, sheath', est: 18000, cur: 33950, note: '+1 pre-dilatation balloon' },
    { item: 'Laboratory — CBC, KFT, lipid, troponin ×2', est: 6500, cur: 6500, note: '' },
    { item: 'Radiology — ECHO, chest X-ray', est: 5200, cur: 5200, note: '' },
    { item: 'Pharmacy', est: 9800, cur: 9800, note: '' },
    { item: 'Nursing & monitoring', est: 5000, cur: 5000, note: '' },
  ],
};

const insuranceCases = [
  { id: 'PA-2026-1142', patient: 'Kavitha Raman', uhid: 'MER-2026-008421', insurer: 'Star Health', policy: 'SH-FHO-77219043', procedure: 'PTCA + DES', estimate: '₹2,45,000', requested: '₹2,68,450', approved: '₹1,95,000', liability: '₹73,450', submitted: '12 Sep 09:05', status: 'Query Raised', missing: 'Angioplasty procedure report', risk: '9%', exec: 'R. Sundar' },
  { id: 'PA-2026-1098', patient: 'Murugan Selvam', uhid: 'MER-2026-007733', insurer: 'ICICI Lombard', policy: 'IL-HA-5583201', procedure: 'CABG', estimate: '₹4,85,000', requested: '₹4,20,000', approved: '₹3,65,000', liability: '₹1,20,000', submitted: '05 Sep 08:40', status: 'Partially Approved', missing: 'Previous treatment document', risk: '18%', exec: 'R. Sundar' },
  { id: 'PA-2026-1120', patient: 'Fathima Begum', uhid: 'MER-2026-009104', insurer: 'HDFC Ergo', policy: 'HE-OPT-330912', procedure: 'Total hip replacement', estimate: '₹3,10,000', requested: '₹3,10,000', approved: '₹2,90,000', liability: '₹20,000', submitted: '06 Sep 15:20', status: 'Approved', missing: '—', risk: '4%', exec: 'L. Fathima' },
  { id: 'PA-2026-1131', patient: 'Lakshmi Narayanan', uhid: 'MER-2026-008877', insurer: 'Aditya Birla Health', policy: 'AB-ACT-9012331', procedure: 'AV fistula', estimate: '₹85,000', requested: '₹85,000', approved: '—', liability: '—', submitted: '08 Sep 10:02', status: 'Pending', missing: '—', risk: '12%', exec: 'L. Fathima' },
  { id: 'PA-2026-1139', patient: 'Arun Prakash', uhid: 'MER-2026-009230', insurer: 'Bajaj Allianz', policy: 'BA-HG-4471820', procedure: 'ACL reconstruction', estimate: '₹1,95,000', requested: '₹1,95,000', approved: '₹1,95,000', liability: '₹0', submitted: '09 Sep 17:44', status: 'Approved', missing: '—', risk: '3%', exec: 'R. Sundar' },
  { id: 'PA-2026-1136', patient: 'Meenakshi Sundaram', uhid: 'MER-2026-005098', insurer: 'Star Health', policy: 'SH-SCR-11820934', procedure: 'Chemotherapy cycle 4', estimate: '₹1,12,000', requested: '₹1,12,000', approved: '₹1,12,000', liability: '₹0', submitted: '09 Sep 12:10', status: 'Approved', missing: '—', risk: '2%', exec: 'L. Fathima' },
  { id: 'PA-2026-1104', patient: 'Joseph Antony', uhid: 'MER-2026-007415', insurer: 'Niva Bupa', policy: 'NB-RA-2290187', procedure: 'Stroke management', estimate: '₹2,60,000', requested: '₹2,95,000', approved: '₹2,60,000', liability: '₹35,000', submitted: '11 Sep 09:15', status: 'Additional Documents', missing: 'Rehabilitation plan, MRI report', risk: '22%', exec: 'R. Sundar' },
  { id: 'PA-2026-1140', patient: 'Priyanka Das', uhid: 'MER-2026-009511', insurer: 'Care Health', policy: 'CH-JOY-6631007', procedure: 'LSCS', estimate: '₹95,000', requested: '₹95,000', approved: '₹95,000', liability: '₹0', submitted: '09 Sep 08:50', status: 'Approved', missing: '—', risk: '2%', exec: 'L. Fathima' },
  { id: 'PA-2026-1145', patient: 'Karthikeyan M', uhid: 'MER-2026-009344', insurer: 'Star Health', policy: 'SH-FHO-77003121', procedure: 'URSL', estimate: '₹78,000', requested: '₹78,000', approved: '—', liability: '—', submitted: '—', status: 'Missing Documents', missing: 'KYC, policy copy', risk: '31%', exec: 'Unassigned' },
  { id: 'PA-2026-1147', patient: 'Rebecca Thomas', uhid: 'MER-2026-009488', insurer: 'Niva Bupa', policy: 'NB-RA-2210993', procedure: 'Septoplasty', estimate: '₹62,000', requested: '₹62,000', approved: '₹58,000', liability: '₹4,000', submitted: '11 Sep 07:30', status: 'Approved', missing: '—', risk: '5%', exec: 'L. Fathima' },
  { id: 'PA-2026-1149', patient: 'Suresh Kumar V', uhid: 'MER-2026-009655', insurer: 'ICICI Lombard', policy: 'IL-HA-5590112', procedure: 'Knee arthroscopy', estimate: '₹1,40,000', requested: '₹1,40,000', approved: '—', liability: '—', submitted: '—', status: 'Draft', missing: 'Consultant note', risk: '—', exec: 'R. Sundar' },
  { id: 'PA-2026-1093', patient: 'Bhuvaneswari R', uhid: 'MER-2026-006901', insurer: 'Star Health', policy: 'SH-FHO-77110852', procedure: 'Hysterectomy', estimate: '₹1,85,000', requested: '₹1,85,000', approved: '₹0', liability: '₹1,85,000', submitted: '03 Sep 11:00', status: 'Rejected', missing: 'Pre-existing disease waiting period', risk: '—', exec: 'L. Fathima' },
  { id: 'PA-2026-1150', patient: 'Ganesan T', uhid: 'MER-2026-009661', insurer: 'HDFC Ergo', policy: 'HE-OPT-331240', procedure: 'Spinal fusion L4-L5', estimate: '₹5,20,000', requested: '₹5,20,000', approved: '—', liability: '—', submitted: '12 Sep 10:30', status: 'High Denial Risk', missing: 'Conservative treatment history', risk: '44%', exec: 'R. Sundar' },
];

const agents = [
  { id: 'AG-01', name: 'Appointment Agent', v: '2.3.1', owner: 'Front Office', tier: 'Low', status: 'Active', success: '96.8%', lastRun: '11:19', runs: 412 },
  { id: 'AG-02', name: 'Patient Access Agent', v: '1.8.0', owner: 'Patient Experience', tier: 'Low', status: 'Active', success: '95.1%', lastRun: '11:18', runs: 380 },
  { id: 'AG-03', name: 'Pre-registration Agent', v: '1.4.2', owner: 'Front Office', tier: 'Low', status: 'Active', success: '97.4%', lastRun: '11:12', runs: 96 },
  { id: 'AG-04', name: 'Employee Service Agent', v: '1.6.0', owner: 'HR', tier: 'Low', status: 'Active', success: '93.2%', lastRun: '11:17', runs: 141 },
  { id: 'AG-05', name: 'Feedback Agent', v: '1.2.0', owner: 'Quality', tier: 'Medium', status: 'Active', success: '91.7%', lastRun: '11:04', runs: 58 },
  { id: 'AG-06', name: 'Queue / Flow Agent', v: '2.0.4', owner: 'Operations', tier: 'Low', status: 'Active', success: '98.9%', lastRun: '11:20', runs: 1290 },
  { id: 'AG-07', name: 'Insurance Preauth Agent', v: '2.1.0', owner: 'Insurance Desk', tier: 'High', status: 'Active', success: '92.4%', lastRun: '11:15', runs: 74 },
  { id: 'AG-08', name: 'Billing Transparency Agent', v: '1.9.3', owner: 'Finance', tier: 'High', status: 'Suspended', success: '89.0%', lastRun: '10:42', runs: 210 },
  { id: 'AG-09', name: 'Discharge Orchestration Agent', v: '3.0.2', owner: 'Operations', tier: 'High', status: 'Active', success: '94.2%', lastRun: '11:19', runs: 61 },
  { id: 'AG-10', name: 'Diagnostic Coordination Agent', v: '1.5.1', owner: 'Diagnostics', tier: 'Medium', status: 'Active', success: '95.6%', lastRun: '11:16', runs: 322 },
  { id: 'AG-11', name: 'Follow-up Agent', v: '1.3.0', owner: 'Patient Experience', tier: 'Low', status: 'Active', success: '94.0%', lastRun: '10:58', runs: 88 },
  { id: 'AG-12', name: 'Contact Centre Agent', v: '2.2.0', owner: 'Contact Centre', tier: 'Medium', status: 'Active', success: '90.3%', lastRun: '11:20', runs: 264 },
  { id: 'AG-13', name: 'AI Trainer Agent', v: '1.1.0', owner: 'Nursing Education', tier: 'Low', status: 'Active', success: '—', lastRun: '10:30', runs: 19 },
  { id: 'AG-14', name: 'Analytics Agent', v: '1.7.0', owner: 'Management', tier: 'Medium', status: 'Active', success: '93.8%', lastRun: '11:11', runs: 47 },
  { id: 'AG-15', name: 'Forecasting Agent', v: '1.0.6', owner: 'Operations', tier: 'Medium', status: 'Active', success: '—', lastRun: '06:00', runs: 1 },
  { id: 'AG-16', name: 'Radiology Screening Agent', v: '0.9.0', owner: 'Radiology', tier: 'High', status: 'Silent Validation', success: '—', lastRun: '11:08', runs: 52 },
  { id: 'AG-17', name: 'Voice Documentation Agent', v: '1.2.4', owner: 'Medical Records', tier: 'Medium', status: 'Active', success: '96.1%', lastRun: '11:02', runs: 33 },
  { id: 'AG-18', name: 'Nursing Handover Agent', v: '0.8.2', owner: 'Nursing', tier: 'Medium', status: 'Pilot', success: '—', lastRun: '07:05', runs: 12 },
];

const dischargeTrace = [
  { t: '09:02', who: 'Dr. Arjun Menon', what: 'Marked likely discharge (Doctor Workspace)', sys: 'EMR', res: 'Intent recorded', k: 'human' },
  { t: '09:03', who: 'Orchestrator', what: 'Identity ✓ Consent ✓ Intent: discharge.coordinate → Discharge Orchestration Agent v3.0.2', sys: 'Policy Gateway', res: 'Allowed', k: 'policy' },
  { t: '09:03', who: 'Discharge Agent', what: 'Checked pending investigations', sys: 'LIS', res: '0 pending', k: 'tool' },
  { t: '09:04', who: 'Discharge Agent', what: 'Pharmacy clearance requested', sys: 'Pharmacy', res: 'Cleared 09:31', k: 'tool' },
  { t: '09:04', who: 'Discharge Agent', what: 'Billing assembly triggered', sys: 'Billing', res: 'Provisional bill ₹2,68,450', k: 'tool' },
  { t: '09:05', who: 'Discharge Agent', what: 'Insurance status checked', sys: 'Insurance / TPA', res: 'Enhancement query raised 09:05', k: 'warn' },
  { t: '09:06', who: 'Discharge Agent', what: 'Housekeeping notified (bed C-412 turnaround pre-alert)', sys: 'Facilities', res: 'Acknowledged', k: 'tool' },
  { t: '09:06', who: 'Discharge Agent', what: 'Transport requirement detected (wheelchair, family car)', sys: 'Patient Transport', res: 'Slot 4:00 PM held', k: 'tool' },
  { t: '09:07', who: 'Discharge Agent', what: 'Discharge summary draft generated (v1, 7 sections)', sys: 'LLM', res: 'Groundedness 97%', k: 'ai' },
  { t: '09:08', who: 'Discharge Agent', what: 'Doctor approval requested', sys: 'Human Approval', res: 'Waiting', k: 'human' },
  { t: '10:14', who: 'Insurance Preauth Agent', what: 'Handoff: enhancement packet prepared for R. Sundar', sys: 'Insurance / TPA', res: 'Awaiting human submit', k: 'human' },
  { t: '11:19', who: 'Discharge Agent', what: 'ETA recomputed on dependency change', sys: 'Forecast model', res: '4:20 PM (±35 m)', k: 'ai' },
];

const exceptions = [
  { group: 'Discharges blocked', n: 12, items: [
    { ref: 'Kavitha Raman · C-412', owner: 'R. Sundar', age: '1 h 42 m', pri: 'High', reason: 'Insurance enhancement query', next: 'Resubmit with angio report' },
    { ref: 'Fathima Begum · O-207', owner: 'S. Devi', age: '52 m', pri: 'High', reason: 'Enoxaparin stock-out', next: 'Central pharmacy transfer' },
    { ref: 'Arun Prakash · O-212', owner: 'K. Meena', age: '1 h 05 m', pri: 'Medium', reason: 'Implant not posted', next: 'Post ₹48,500 implant' },
    { ref: 'Lakshmi Narayanan · N-305', owner: 'M. Ganesh', age: '25 m', pri: 'Medium', reason: 'KFT in analyser queue', next: 'Expedite queue #14' },
  ]},
  { group: 'Insurance cases ageing', n: 5, items: [
    { ref: 'Murugan Selvam · PA-1098', owner: 'R. Sundar', age: '7 d', pri: 'High', reason: 'Partial approval ₹3,65,000', next: 'Appeal with treatment history' },
    { ref: 'Joseph Antony · PA-1104', owner: 'R. Sundar', age: '26 h', pri: 'High', reason: 'Additional documents', next: 'Attach MRI + rehab plan' },
    { ref: 'Ganesan T · PA-1150', owner: 'R. Sundar', age: '50 m', pri: 'High', reason: 'Denial risk 44%', next: 'Supervisor review before submit' },
  ]},
  { group: 'Critical values awaiting acknowledgement', n: 3, items: [
    { ref: 'Venkatesan P · P-118', owner: 'Dr. Ramesh Pillai', age: '14 m', pri: 'Critical', reason: 'K⁺ 6.1 mmol/L', next: 'Acknowledge in EMR' },
    { ref: 'ER-2026-4412 · Bay 3', owner: 'Dr. on duty ER', age: '6 m', pri: 'Critical', reason: 'Troponin-I 2.4 ng/mL', next: 'Acknowledge · cardiology call' },
  ]},
  { group: 'Complaints nearing SLA', n: 8, items: [
    { ref: 'FB-2026-0471 · Divya Shankar', owner: 'Billing Manager', age: '22 m / 30 m', pri: 'High', reason: 'Waited 2 h for final bill', next: 'Call family · itemised explanation' },
    { ref: 'FB-2026-0468 · OP Cardiology', owner: 'Front Office Lead', age: '41 m / 60 m', pri: 'Medium', reason: 'Wait past promised slot', next: 'Service recovery call' },
  ]},
  { group: 'Long-stay patients', n: 6, items: [
    { ref: 'Venkatesan P · 14 d', owner: 'Dr. Ramesh Pillai', age: '14 d', pri: 'Medium', reason: 'Home O₂ decision pending', next: 'Family counselling 2 PM' },
  ]},
  { group: 'Radiology delays', n: 4, items: [
    { ref: 'RAD-2026-7731 · CT Abdomen', owner: 'Dr. Hemalatha Devi', age: '1 h 20 m', pri: 'Medium', reason: 'Report past 60-min target', next: 'Assign second reader' },
    { ref: 'RAD-2026-7728 · MRI Brain', owner: 'Dr. Hemalatha Devi', age: '55 m', pri: 'Medium', reason: 'Scanner 2 maintenance', next: 'Reschedule to scanner 1' },
  ]},
];

const auditEvents = [
  { ts: '11:19:41', user: 'system', agent: 'AG-09 Discharge', patient: 'MER-2026-008421', action: 'ETA recomputed', sys: 'Forecast', result: 'OK', approval: '—', id: 'AUD-2026-091241' },
  { ts: '11:18:07', user: 'anitha.kumar', agent: '—', patient: 'MER-2026-008421', action: 'Patient accessed (Nurse Workspace)', sys: 'EMR', result: 'Granted', approval: '—', id: 'AUD-2026-091240' },
  { ts: '11:16:52', user: 'r.sundar', agent: 'AG-07 Preauth', patient: 'MER-2026-007733', action: 'Document retrieved · Preauth SOP v2.4', sys: 'Knowledge', result: 'OK', approval: '—', id: 'AUD-2026-091239' },
  { ts: '11:15:30', user: 'k.meena', agent: '—', patient: 'MER-2026-009511', action: 'Attempted clinical record access', sys: 'EMR', result: 'DENIED · no relationship', approval: '—', id: 'AUD-2026-091238' },
  { ts: '11:14:12', user: 'system', agent: 'AG-08 Billing', patient: '—', action: 'Agent suspended · AI-2026-0048', sys: 'Orchestrator', result: 'Suspended', approval: 'CIO', id: 'AUD-2026-091237' },
  { ts: '11:12:48', user: 'dr.arjun.menon', agent: 'AG-17 Voice', patient: 'MER-2026-008421', action: 'Dictation transcribed · 38 s · Tamil/English', sys: 'Voice', result: 'Confidence 94%', approval: 'Pending sign', id: 'AUD-2026-091236' },
  { ts: '11:10:05', user: 'patient', agent: 'AG-02 Access', patient: 'MER-2026-008421', action: 'AI response generated (WhatsApp) · discharge status', sys: 'WhatsApp', result: 'Read-only', approval: '—', id: 'AUD-2026-091235' },
  { ts: '11:08:33', user: 'system', agent: 'AG-16 Radiology', patient: 'MER-2026-009718', action: 'Study screened · flagged for priority', sys: 'PACS', result: 'Silent mode', approval: 'Radiologist', id: 'AUD-2026-091234' },
  { ts: '11:05:19', user: 'dr.deepa.krishnan', agent: 'AG-09 Discharge', patient: 'MER-2026-005098', action: 'Approval requested · discharge summary v2', sys: 'Human Approval', result: 'Pending', approval: 'Pending', id: 'AUD-2026-091233' },
  { ts: '11:02:44', user: 'patient', agent: '—', patient: 'MER-2026-008421', action: 'Consent changed · Billing notifications ON', sys: 'Consent', result: 'OK', approval: '—', id: 'AUD-2026-091232' },
  { ts: '10:58:10', user: 'system', agent: 'AG-04 Employee', patient: '—', action: 'Low confidence 42% · refused · routed to Insurance Supervisor', sys: 'Knowledge', result: 'Refused', approval: '—', id: 'AUD-2026-091231' },
  { ts: '10:42:16', user: 'system', agent: 'AG-08 Billing', patient: 'MER-2026-009602', action: 'Policy violation · tariff explanation mismatch', sys: 'Billing', result: 'Incident opened', approval: '—', id: 'AUD-2026-091230' },
];

const knowledgeDocs = [
  { title: 'Inpatient Discharge SOP', dept: 'Operations', v: '3.1', eff: '01 Jul 2026', review: '01 Jan 2027', status: 'Published', lang: 'EN · TA' },
  { title: 'Insurance Preauthorisation SOP', dept: 'Insurance Desk', v: '2.4', eff: '15 Aug 2026', review: '15 Feb 2027', status: 'Published', lang: 'EN' },
  { title: 'Medication Safety — High-alert drugs', dept: 'Nursing', v: '4.0', eff: '15 Aug 2026', review: '15 Nov 2026', status: 'Conflict', lang: 'EN · TA' },
  { title: 'Medication Safety — Ward administration', dept: 'Nursing', v: '3.2', eff: '01 Aug 2026', review: '01 Feb 2027', status: 'Conflict', lang: 'EN · TA' },
  { title: 'Tariff Schedule FY26-27', dept: 'Finance', v: '1.3', eff: '01 Apr 2026', review: '01 Oct 2026', status: 'Published', lang: 'EN' },
  { title: 'HR Leave Policy', dept: 'HR', v: '5.0', eff: '01 Jan 2026', review: '01 Jan 2027', status: 'Published', lang: 'EN · TA' },
  { title: 'Visiting Hours & Attendant Policy', dept: 'Patient Experience', v: '2.0', eff: '01 Jun 2026', review: '01 Dec 2026', status: 'Published', lang: 'EN · TA · HI' },
  { title: 'Diagnostic Preparation Guide', dept: 'Diagnostics', v: '1.8', eff: '10 May 2026', review: '10 Nov 2026', status: 'Review due', lang: 'EN · TA' },
  { title: 'Emergency Codes & Procedures', dept: 'Emergency', v: '2.2', eff: '01 Mar 2026', review: '01 Sep 2026', status: 'Expired', lang: 'EN' },
  { title: 'NABH Patient Rights Charter', dept: 'Quality', v: '1.0', eff: '01 Feb 2026', review: '01 Feb 2027', status: 'Published', lang: 'EN · TA' },
];

const evalRows = [
  { agent: 'Discharge Orchestration', success: '94.2%', ground: '97.1%', override: '4.8%', esc: '3.2%', latency: '2.8 s', viol: 0 },
  { agent: 'Insurance Preauth', success: '92.4%', ground: '96.3%', override: '7.1%', esc: '5.5%', latency: '3.4 s', viol: 0 },
  { agent: 'Appointment', success: '96.8%', ground: '98.0%', override: '1.2%', esc: '2.1%', latency: '1.6 s', viol: 0 },
  { agent: 'Patient Access', success: '95.1%', ground: '97.5%', override: '—', esc: '4.0%', latency: '1.9 s', viol: 0 },
  { agent: 'Billing Transparency', success: '89.0%', ground: '91.2%', override: '9.4%', esc: '6.8%', latency: '2.2 s', viol: 1 },
  { agent: 'Diagnostic Coordination', success: '95.6%', ground: '97.9%', override: '2.0%', esc: '2.6%', latency: '2.0 s', viol: 0 },
  { agent: 'Contact Centre', success: '90.3%', ground: '94.7%', override: '—', esc: '8.9%', latency: '1.4 s', viol: 0 },
  { agent: 'Employee Service', success: '93.2%', ground: '96.8%', override: '—', esc: '6.1%', latency: '1.7 s', viol: 0 },
];

const costs = [
  { k: 'LLM', v: 5240 }, { k: 'Voice', v: 1840 }, { k: 'Agent execution', v: 980 }, { k: 'Vision', v: 610 }, { k: 'Embedding', v: 420 },
];

const roadmap = [
  { phase: 'Phase 1', when: '0–3 months', theme: 'Prove it works', items: ['Knowledge foundation', 'Patient assistant', 'Employee service', 'Health identifier', 'Flow instrumentation'], owner: 'AI Programme Lead', risk: 'Low', data: 'Ready', integ: 'HMS read' },
  { phase: 'Phase 2', when: '3–6 months', theme: 'Fix the money', items: ['Preauthorisation', 'Billing transparency', 'Discharge orchestration', 'AI trainer', 'Contact centre'], owner: 'Finance Head · COO', risk: 'Medium', data: 'Partial', integ: 'Billing · TPA write + approval' },
  { phase: 'Phase 3', when: '6–12 months', theme: 'Fix the flow', items: ['Diagnostic coordination', 'Voice follow-up', 'Command centre forecasting', 'Voice documentation'], owner: 'COO · Medical Director', risk: 'Medium', data: 'Building', integ: 'LIS · RIS · Telephony' },
  { phase: 'Phase 4', when: '12–24 months', theme: 'Scale', items: ['Nursing handover', 'Ambient documentation', 'Predictive rostering', 'Expanded agent ecosystem'], owner: 'Nursing Supt · CIO', risk: 'High', data: 'Depends on 1–3', integ: 'EMR write + approval' },
];

const mgmtKpis = [
  { k: 'Discharge turnaround', cur: '3 h 48 m', target: '3 h 00 m', trend: 'down', delta: '−22 m vs Aug', link: 'Discharge board' },
  { k: 'Preauth turnaround', cur: '5 h 10 m', target: '4 h', trend: 'down', delta: '−48 m vs Aug', link: 'Insurance' },
  { k: 'Billing variance > 10%', cur: '14% of IP bills', target: '8%', trend: 'down', delta: '−3 pts', link: 'Billing' },
  { k: 'OP wait past slot', cur: '23 min', target: '15 min', trend: 'down', delta: '−6 min', link: 'Queue' },
  { k: 'First-pass preauth acceptance', cur: '78%', target: '85%', trend: 'up', delta: '+5 pts', link: 'Insurance' },
  { k: 'Patient satisfaction (NPS)', cur: '41', target: '55', trend: 'up', delta: '+7', link: 'Feedback' },
  { k: 'AI task success', cur: '93.6%', target: '95%', trend: 'up', delta: '+1.1 pts', link: 'AI Evaluation' },
  { k: 'Human override rate', cur: '5.2%', target: '< 6%', trend: 'flat', delta: '0.0', link: 'AI Evaluation' },
];

return { TODAY, NOW, doctors, patients, kavitha, kavithaTimeline, kavithaBill, insuranceCases, agents, dischargeTrace, exceptions, auditEvents, knowledgeDocs, evalRows, costs, roadmap, mgmtKpis };
})();
window.__mm2.store = (function(){
// Hospital in-memory hospital data layer. DEMO ENVIRONMENT • SYNTHETIC DATA • NOT FOR CLINICAL USE
const D = window.__mm2.data;
const mul = s => () => { s |= 0; s = s + 0x6D2B79F5 | 0; let t = Math.imul(s ^ s >>> 15, 1 | s); t = t + Math.imul(t ^ t >>> 7, 61 | t) ^ t; return ((t ^ t >>> 14) >>> 0) / 4294967296; };
const pad = n => String(n).padStart(2, '0');
const fmt = ts => { const d = Math.floor(ts / 1440), m = ((ts % 1440) + 1440) % 1440; const hm = pad(Math.floor(m / 60)) + ':' + pad(m % 60); return d === 0 ? hm : (12 + d) + ' Sep ' + hm; };
const ago = (ts, now) => { const m = Math.max(0, now - ts); if (m < 60) return m + ' m'; if (m < 1440) return Math.floor(m / 60) + ' h ' + pad(m % 60) + ' m'; return Math.floor(m / 1440) + ' d ' + Math.floor((m % 1440) / 60) + ' h'; };
const inr = n => '₹' + Math.round(n).toLocaleString('en-IN');
const FIRST = ['Anbu', 'Bhavani', 'Chandran', 'Dhanalakshmi', 'Ezhil', 'Gomathi', 'Hari', 'Indira', 'Jeyaraman', 'Kalpana', 'Loganathan', 'Malar', 'Natarajan', 'Oviya', 'Pandian', 'Radha', 'Sekar', 'Thenmozhi', 'Uma', 'Velu', 'Yamuna', 'Ashok', 'Geetha', 'Iqbal', 'Jancy', 'Kumaran', 'Latha', 'Manoj', 'Nirmala', 'Ponnusamy', 'Rani', 'Sathish', 'Tamilselvi', 'Vasanthi', 'Xavier'];
const LAST = ['Subramanian', 'Krishnan', 'Pillai', 'Rajendran', 'Devi', 'Kumar', 'Mani', 'Raj', 'Selvam', 'Moorthy', 'Nadar', 'Chettiar', 'Gounder', 'Iyengar', 'Fernandes', 'Basha', 'Naidu', 'Reddy', 'Nair', 'Menon'];
const DEPTS = ['Cardiology', 'Cardiac Surgery', 'Orthopaedics', 'General Surgery', 'Nephrology', 'Oncology', 'Neurology', 'Obstetrics', 'Pulmonology', 'Paediatrics', 'Gastroenterology', 'Radiology', 'Emergency', 'Urology', 'ENT'];
const INSURERS = ['Star Health', 'ICICI Lombard', 'HDFC Ergo', 'Aditya Birla Health', 'Bajaj Allianz', 'Niva Bupa', 'Care Health', 'ManipalCigna', 'Self-pay', 'Self-pay'];
const TESTS = [['CBC', 'Hb', 'g/dL', 11, 15.5], ['KFT', 'Creatinine', 'mg/dL', 0.6, 1.3], ['LFT', 'ALT', 'U/L', 7, 56], ['Lipid profile', 'LDL', 'mg/dL', 60, 130], ['Troponin-I', 'Troponin-I', 'ng/mL', 0, 0.04], ['Electrolytes', 'K⁺', 'mmol/L', 3.5, 5.1], ['HbA1c', 'HbA1c', '%', 4, 5.7], ['Blood sugar', 'Glucose', 'mg/dL', 70, 140], ['Urine culture', 'Growth', 'CFU', 0, 0], ['D-dimer', 'D-dimer', 'ng/mL', 0, 500]];
const MODS = ['CT', 'MRI', 'X-ray', 'USG', 'ECHO'];
const DRUGS = ['Aspirin 75 mg', 'Clopidogrel 75 mg', 'Atorvastatin 40 mg', 'Metoprolol 25 mg', 'Pantoprazole 40 mg', 'Enoxaparin 40 mg', 'Paracetamol 650 mg', 'Ceftriaxone 1 g', 'Furosemide 40 mg', 'Insulin glargine', 'Ondansetron 4 mg', 'Amlodipine 5 mg'];
const AGENT_EXTRA = [
  { id: 'AG-19', name: 'Discharge Summary Agent', v: '1.1.0', owner: 'Medical Records', tier: 'High', status: 'Active', success: '95.3%', lastRun: '11:07', runs: 44 },
  { id: 'AG-20', name: 'Claim Denial Agent', v: '1.0.2', owner: 'Insurance Desk', tier: 'Medium', status: 'Active', success: '91.0%', lastRun: '10:50', runs: 22 },
  { id: 'AG-21', name: 'Management Copilot', v: '1.3.0', owner: 'Management', tier: 'Medium', status: 'Active', success: '94.4%', lastRun: '11:19', runs: 31 },
];
const TYPE_OF = n => /Copilot|Access|Employee|Contact|Trainer/.test(n) ? 'Answerer' : /Summary|Handover|Voice|Analytics/.test(n) ? 'Summariser' : /Billing|Preauth|Claim/.test(n) ? 'Drafter' : /Screening|Queue|Feedback|Forecast/.test(n) ? 'Monitor' : /Discharge|Diagnostic|Appointment|Follow|Pre-reg/.test(n) ? 'Workflow Agent' : 'Decision Support';
const TOOLS = ['Patient Search', 'Appointment API', 'HMS', 'EMR', 'LIS', 'RIS', 'PACS', 'Billing', 'Insurance / TPA', 'Pharmacy', 'Housekeeping', 'Scheduling', 'Messaging', 'Notification', 'Document Generator'];
const ROLES = ['Patient', 'Doctor', 'Nurse', 'Front Office', 'Billing', 'Finance Manager', 'Insurance', 'Radiologist', 'Laboratory', 'Pathologist', 'Pharmacy', 'Store Manager', 'Procurement Officer', 'HR Manager', 'Canteen Manager', 'Hospital Management', 'AI Administrator', 'Governance Officer', 'IT Administrator', 'Auditor'];
const APPROVER = { 'Discharge summary': 'Doctor', 'Prescription': 'Doctor', 'Billing release': 'Billing', 'Billing void': 'Hospital Management', 'Refund': 'Hospital Management', 'Preauth submission': 'Insurance', 'Claim appeal': 'Insurance', 'Nursing handover': 'Nurse', 'Staff roster': 'Hospital Management', 'Agent publish': 'Governance Officer', 'Knowledge publish': 'Governance Officer', 'Stock adjustment': 'Store Manager', 'Purchase approval': 'Procurement Officer', 'Leave approval': 'HR Manager' };

function createStore(onChange) {
  const R = mul(20260912); const rnd = R; const int = (a, b) => a + Math.floor(rnd() * (b - a + 1)); const pick = a => a[Math.floor(rnd() * a.length)];
  const S = { clock: 680, day: 'Fri 12 Sep 2026', notifications: [], audit: [], lisDegraded: true };
  let seq = 0; const nid = p => p + '-' + String(++seq).padStart(4, '0');

  // Doctors 30
  S.doctors = D.doctors.map(d => ({ ...d })); for (let i = S.doctors.length; i < 30; i++) { const dept = DEPTS[i % DEPTS.length]; S.doctors.push({ id: 'DR-' + pad(40 + i), name: 'Dr. ' + pick(FIRST) + ' ' + pick(LAST), dept, role: 'Consultant ' + dept }); }
  S.departments = DEPTS.map((n, i) => ({ id: 'DEP-' + pad(i + 1), name: n, head: S.doctors.find(d => d.dept === n)?.name || '—', beds: 0 }));
  // Staff 75
  const SROLES = ['Staff Nurse', 'Front Office Executive', 'Insurance Executive', 'Billing Executive', 'Lab Technician', 'Radiographer', 'Pharmacist', 'Housekeeping', 'Porter', 'HR Executive', 'Ward Secretary', 'Contact Centre Agent'];
  S.staff = [['ST-001', 'Anitha Kumar', 'Staff Nurse', 'Cardiac Ward'], ['ST-002', 'R. Sundar', 'Insurance Executive', 'Insurance Desk'], ['ST-003', 'L. Fathima', 'Insurance Supervisor', 'Insurance Desk'], ['ST-004', 'K. Meena', 'Billing Executive', 'Billing'], ['ST-005', 'S. Devi', 'Pharmacist', 'Pharmacy'], ['ST-006', 'M. Ganesh', 'Lab Technician', 'Laboratory'], ['ST-007', 'P. Velu', 'Housekeeping Supervisor', 'Facilities'], ['ST-008', 'A. Raj', 'Porter', 'Patient Transport'], ['ST-009', 'Sheela J', 'Staff Nurse', 'Medical Ward']].map(([id, name, role, unit]) => ({ id, name, role, unit, shift: 'Morning', status: 'On duty' }));
  for (let i = S.staff.length; i < 75; i++) S.staff.push({ id: 'ST-' + pad(i + 1), name: pick(FIRST) + ' ' + pick(LAST), role: pick(SROLES), unit: pick(DEPTS), shift: pick(['Morning', 'Evening', 'Night']), status: rnd() < 0.85 ? 'On duty' : 'On leave' });
  // Beds 108
  S.beds = []; const WARD = { Cardiology: 'C', 'Cardiac Surgery': 'CT', Orthopaedics: 'O', 'General Surgery': 'S', Nephrology: 'N', Oncology: 'ON', Neurology: 'NR', Obstetrics: 'M', Pulmonology: 'P', Paediatrics: 'PD', Gastroenterology: 'G', Urology: 'U', ENT: 'E', Emergency: 'ER' };
  Object.entries(WARD).forEach(([dept, pre]) => { const n = dept === 'Emergency' ? 12 : dept === 'Cardiology' ? 12 : 7; for (let i = 1; i <= n; i++) S.beds.push({ id: pre + '-' + pad(i), dept, status: 'Free', patientId: null, cls: i <= 2 ? 'ICU' : i <= 4 ? 'Private' : 'General' }); });
  // Patients 75
  S.patients = D.patients.map(p => ({ ...p, id: p.uhid, doctorId: (S.doctors.find(d => d.name === p.doctor) || {}).id, blood: pick(['A+', 'B+', 'O+', 'AB+', 'O−']), type: /Discharged/.test(p.status) ? 'Discharged' : 'IP', risk: p.age > 65 ? ['Age > 65'] : [] }));
  for (let i = S.patients.length; i < 75; i++) { const dept = pick(DEPTS.filter(d => d !== 'Radiology')); const doc = pick(S.doctors.filter(d => d.dept === dept) .length ? S.doctors.filter(d => d.dept === dept) : S.doctors); const type = i < 30 ? 'IP' : i < 55 ? 'OP' : i < 68 ? 'ER' : 'Discharged'; const sex = pick(['F', 'M']); S.patients.push({ id: 'MER-2026-' + (3000 + int(100, 6999)), name: pick(FIRST) + ' ' + pick(LAST), age: int(6, 82), sex, lang: pick(['Tamil', 'Tamil', 'Tamil', 'English', 'Telugu', 'Malayalam', 'Hindi']), dept, doctor: doc.name, doctorId: doc.id, insurer: pick(INSURERS), policy: '', procedure: pick(['Observation', 'Elective surgery', 'Medical management', 'Day-care procedure']), status: type === 'IP' ? 'Admitted' : type, type, blood: pick(['A+', 'B+', 'O+', 'AB+']), phone: '+91 9' + int(1000, 9999) + '•••• ' + int(1000, 9999), risk: [], hero: false }); }
  S.patients.forEach(p => { if (!p.phone) p.phone = '+91 98•••• 4410'; if (p.uhid) p.type = p.type || 'IP'; });
  // Assign beds to IP
  S.patients.filter(p => p.type === 'IP').forEach(p => { const pre = WARD[p.dept] || 'S'; let b = p.bed ? S.beds.find(x => x.id === p.bed.split(' ')[0]) : null; if (!b) b = S.beds.find(x => x.id.startsWith(pre + '-') && x.status === 'Free') || S.beds.find(x => x.status === 'Free'); b.status = 'Occupied'; b.patientId = p.id; p.bedId = b.id; if (!p.bed) p.bed = b.id + ' · ' + p.dept; });
  S.beds.filter(b => b.status === 'Free' && rnd() < 0.8 && b.dept !== 'Emergency').slice(0, 45).forEach(b => { b.status = 'Occupied'; b.patientId = 'other'; });
  S.beds.filter(b => b.status === 'Free' && rnd() < 0.2).forEach(b => b.status = 'Cleaning');
  // Appointments 110
  const CH = ['Web', 'Mobile app', 'WhatsApp', 'Front desk', 'Voice'];
  S.appointments = []; for (let i = 0; i < 110; i++) { const p = pick(S.patients); const doc = S.doctors.find(d => d.id === p.doctorId) || pick(S.doctors); const day = int(-3, 2); const t = int(9, 17) * 60 + pick([0, 15, 30, 45]); const ts = day * 1440 + t; const st = ts < S.clock ? pick(['Completed', 'Completed', 'Completed', 'Completed', 'No-show', 'Cancelled']) : pick(['Scheduled', 'Scheduled', 'Confirmed', 'Rescheduled']); S.appointments.push({ id: 'APT-' + (24000 + i), patientId: p.id, patient: p.name, doctorId: doc.id, doctor: doc.name, dept: doc.dept, ts, status: st, channel: pick(CH), type: pick(['New', 'Follow-up', 'Follow-up', 'Review']) }); }
  S.appointments.push({ id: 'APT-23990', patientId: D.kavitha.uhid, patient: 'Kavitha Raman', doctorId: 'DR-014', doctor: 'Dr. Arjun Menon', dept: 'Cardiology', ts: -4 * 1440 + 630, status: 'Completed', channel: 'WhatsApp', type: 'New' });
  const noShow = S.patients[40]; S.appointments.push({ id: 'APT-24110', patientId: noShow.id, patient: noShow.name, doctorId: noShow.doctorId, doctor: noShow.doctor, dept: noShow.dept, ts: 600, status: 'No-show', channel: 'Web', type: 'Follow-up', noShowHandled: false });
  // Encounters 105
  S.encounters = S.appointments.filter(a => a.status === 'Completed').map((a, i) => ({ id: 'ENC-' + (20300 + i), patientId: a.patientId, patient: a.patient, doctor: a.doctor, dept: a.dept, ts: a.ts, type: 'OP consultation', status: 'Closed' }));
  S.patients.filter(p => p.type === 'IP' || p.type === 'ER').forEach((p, i) => S.encounters.push({ id: 'ENC-' + (20481 + i), patientId: p.id, patient: p.name, doctor: p.doctor, dept: p.dept, ts: -int(0, 5) * 1440 + int(480, 1200), type: p.type === 'ER' ? 'Emergency' : 'Inpatient', status: 'Open' }));
  S.patients.find(p => p.id === D.kavitha.uhid).encounter = 'ENC-20481';
  // Admissions 32
  S.admissions = S.patients.filter(p => p.type === 'IP' || p.type === 'Discharged').slice(0, 32).map((p, i) => ({ id: p.admission || 'IP-2026-' + (3300 + i), patientId: p.id, patient: p.name, doctor: p.doctor, dept: p.dept, bed: p.bed || '—', admitted: p.admitted || (int(1, 9) + ' Sep 2026'), status: p.type === 'Discharged' ? 'Discharged' : 'Admitted', estimate: p.uhid === D.kavitha.uhid ? 245000 : int(40, 520) * 1000, blocker: p.type === 'IP' ? null : null }));
  // Emergency 25
  const ACU = ['ESI-2', 'ESI-3', 'ESI-3', 'ESI-4', 'ESI-1'];
  S.emergency = S.patients.filter(p => p.type === 'ER').map((p, i) => ({ id: 'ER-2026-' + (4400 + i), patientId: p.id, patient: p.name, arrival: S.clock - int(10, 300), clinician: pick(S.doctors.filter(d => d.dept === 'Emergency').concat(S.doctors.slice(0, 3))).name, acuity: pick(ACU) + ' (clinician)', complaint: pick(['Chest pain', 'Breathlessness', 'Fall · hip pain', 'Fever 3 days', 'Abdominal pain', 'Road traffic accident', 'Seizure', 'Vomiting'] ), status: pick(['Assessment', 'Awaiting results', 'Awaiting bed', 'Treatment', 'Observation']), bedNeed: rnd() < 0.4 }));
  for (let i = S.emergency.length; i < 25; i++) { const p = pick(S.patients); S.emergency.push({ id: 'ER-2026-' + (4400 + i), patientId: p.id, patient: p.name, arrival: -1440 + int(0, 1400), clinician: pick(S.doctors).name, acuity: pick(ACU) + ' (clinician)', complaint: 'Discharged from ER', status: 'Closed', bedNeed: false }); }
  S.emergency[0].id = 'ER-2026-4412'; S.emergency[0].complaint = 'Chest pain'; S.emergency[0].status = 'Awaiting results';
  // Orders 150, labs 100, radiology 50
  S.orders = []; S.labs = []; S.radiology = [];
  for (let i = 0; i < 150; i++) { const p = pick(S.patients); const isLab = i < 100; const T = pick(TESTS); const ts = -int(0, 2) * 1440 + int(420, 1300); const o = { id: 'ORD-' + (51000 + i), patientId: p.id, patient: p.name, doctor: p.doctor, kind: isLab ? 'Laboratory' : 'Radiology', test: isLab ? T[0] : pick(MODS) + ' ' + pick(['Chest', 'Abdomen', 'Brain', 'Knee', 'Spine']), ts, status: ts < S.clock - 90 ? 'Resulted' : pick(['Ordered', 'Sample collected', 'In progress']), prep: isLab ? (T[0] === 'Lipid profile' ? 'Fasting 12 h' : 'None') : 'As per guide' }; S.orders.push(o);
    if (isLab) { const abnormal = rnd() < 0.18; const v = abnormal ? +(T[4] * (1.1 + rnd() * 0.5)).toFixed(2) : +(T[3] + rnd() * (T[4] - T[3])).toFixed(2); S.labs.push({ id: 'LAB-' + (77000 + i), orderId: o.id, patientId: p.id, patient: p.name, test: T[0], analyte: T[1], value: v, unit: T[2], flag: abnormal ? 'High' : 'Normal', critical: false, ts: o.ts + 90, status: o.status === 'Resulted' ? 'Final' : 'Pending' }); }
    else { const prob = +(rnd() * 0.95).toFixed(2); S.radiology.push({ id: 'RAD-2026-' + (7700 + i), orderId: o.id, patientId: p.id, patient: p.name, modality: o.test.split(' ')[0], study: o.test, received: o.ts + 40, aiStatus: rnd() < 0.85 ? 'Screened' : 'Queued', aiProb: prob, priority: prob > 0.7 ? 'Priority' : 'Routine', model: 'rad-screen v0.9.0', radStatus: o.status === 'Resulted' ? 'Read' : pick(['Unread', 'In review']), final: o.status === 'Resulted' ? pick(['No acute finding', 'Finding confirmed', 'Finding not confirmed']) : '—' }); } }
  // Critical values: Venkatesan K+ 6.1 (unacked), ER troponin (unacked)
  const venk = S.patients.find(p => p.name === 'Venkatesan P'); const erp = S.patients.find(p => p.id === S.emergency[0].patientId);
  S.labs.unshift({ id: 'LAB-76991', patientId: venk.id, patient: venk.name, test: 'Electrolytes', analyte: 'K⁺', value: 6.1, unit: 'mmol/L', flag: 'Critical', critical: true, acked: false, notified: 666, escalated: false, clinician: 'Dr. Ramesh Pillai', ts: 664, status: 'Final' }, { id: 'LAB-76992', patientId: erp.id, patient: erp.name, test: 'Troponin-I', analyte: 'Troponin-I', value: 2.4, unit: 'ng/mL', flag: 'Critical', critical: true, acked: false, notified: 674, escalated: false, clinician: 'Dr. on duty ER', ts: 673, status: 'Final' });
  // Pharmacy 100, procedures 75
  S.pharmacy = Array.from({ length: 100 }, (_, i) => { const p = pick(S.patients); return { id: 'PH-' + (91000 + i), patientId: p.id, patient: p.name, drug: pick(DRUGS), qty: int(1, 30), ts: -int(0, 3) * 1440 + int(480, 1300), status: pick(['Dispensed', 'Dispensed', 'Dispensed', 'Pending', 'Returned']), ward: p.bed || 'OP' }; });
  S.pharmacy.push({ id: 'PH-91100', patientId: 'MER-2026-009104', patient: 'Fathima Begum', drug: 'Enoxaparin 40 mg', qty: 10, ts: 600, status: 'Stock-out', ward: 'O-207' });
  S.procedures = Array.from({ length: 75 }, (_, i) => { const p = pick(S.patients); return { id: 'PRC-' + (6100 + i), patientId: p.id, patient: p.name, name: p.procedure || 'Procedure', surgeon: p.doctor, ts: -int(0, 6) * 1440 + int(480, 1100), status: pick(['Completed', 'Completed', 'Scheduled']), theatre: 'OT-' + int(1, 5) }; });
  // Policies 25, preauths 25+
  S.policies = S.patients.filter(p => p.insurer && p.insurer !== 'Self-pay').slice(0, 25).map((p, i) => ({ id: p.policy || (p.insurer.slice(0, 2).toUpperCase() + '-' + int(100000, 999999)), patientId: p.id, patient: p.name, insurer: p.insurer, sumInsured: int(3, 25) * 100000, room: pick(['Single', 'Twin', 'General']), copay: pick(['0%', '10%', '20%']), waiting: 'Cleared' }));
  S.preauths = D.insuranceCases.map(c => ({ ...c, patientId: c.uhid, createdTs: c.submitted === '—' ? 640 : c.submitted.startsWith('12') ? 545 : -int(1, 7) * 1440 + 600, docs: { required: ['Preauth form', 'Doctor advice', 'Estimate', 'ID + policy', c.missing === '—' ? 'Investigation reports' : c.missing], missing: c.missing === '—' ? [] : c.missing.split(', ') }, completeness: c.missing === '—' ? 100 : int(60, 85), history: [] }));
  for (let i = S.preauths.length; i < 27; i++) { const p = pick(S.patients.filter(x => x.insurer !== 'Self-pay')); const st = pick(['Approved', 'Approved', 'Pending', 'Submitted', 'Rejected', 'Missing Documents']); S.preauths.push({ id: 'PA-2026-' + (1050 + i), patient: p.name, patientId: p.id, insurer: p.insurer, policy: '—', procedure: p.procedure, estimate: inr(int(40, 400) * 1000), requested: '—', approved: st === 'Approved' ? inr(int(40, 300) * 1000) : '—', liability: '—', submitted: fmt(-int(0, 6) * 1440 + 600), status: st, missing: st === 'Missing Documents' ? 'Policy copy' : '—', risk: int(2, 30) + '%', exec: pick(['R. Sundar', 'L. Fathima']), createdTs: -int(0, 6) * 1440 + 600, docs: { required: ['Preauth form', 'Estimate'], missing: st === 'Missing Documents' ? ['Policy copy'] : [] }, completeness: st === 'Missing Documents' ? 70 : 100, history: [] }); }
  // Bills 50, payments 50
  S.bills = []; S.admissions.forEach((a, i) => { const est = a.estimate, act = a.patientId === D.kavitha.uhid ? 268450 : Math.round(est * (0.92 + rnd() * 0.2)); const pa = S.preauths.find(x => x.patientId === a.patientId); const cov = pa && pa.approved !== '—' ? +pa.approved.replace(/[₹,]/g, '') : 0; S.bills.push({ id: 'BILL-' + (30500 + i), patientId: a.patientId, patient: a.patient, admissionId: a.id, estimate: est, actual: act, insurance: Math.min(cov, act), patientShare: act - Math.min(cov, act), status: a.status === 'Discharged' ? 'Settled' : 'Provisional', dispute: null, lines: a.patientId === D.kavitha.uhid ? D.kavithaBill.lines : null }); });
  for (let i = S.bills.length; i < 50; i++) { const p = pick(S.patients.filter(x => x.type === 'OP')); const act = int(800, 12000); S.bills.push({ id: 'BILL-' + (30500 + i), patientId: p.id, patient: p.name, admissionId: '—', estimate: act, actual: act, insurance: 0, patientShare: act, status: pick(['Paid', 'Paid', 'Pending']), dispute: null }); }
  const divya = S.bills.find(b => b.patient === 'Divya Shankar'); if (divya) { divya.dispute = { id: 'DSP-2026-0071', raised: 605, reason: 'Paediatric room rate charged ₹7,500/night; family was quoted ₹6,500 at admission', status: 'Open', owner: 'K. Meena', aiExplanation: 'The FY26-27 tariff (v1.3, effective 1 Apr 2026) sets the paediatric ward room rate at ₹7,500 per night. The ₹6,500 figure quoted verbally at admission was the superseded FY25-26 rate. Two nights were charged. The IV rehydration set and paediatric consultation are itemised separately.' , resolution: null }; divya.status = 'Disputed'; }
  S.payments = S.bills.filter(b => /Paid|Settled/.test(b.status)).map((b, i) => ({ id: 'PAY-' + (41000 + i), billId: b.id, patient: b.patient, amount: b.patientShare, mode: pick(['UPI', 'Card', 'Cash', 'Net banking']), ts: -int(0, 4) * 1440 + int(500, 1200), status: 'Success' })); while (S.payments.length < 50) { const b = pick(S.bills); S.payments.push({ id: 'PAY-' + (41000 + S.payments.length), billId: b.id, patient: b.patient, amount: int(500, 20000), mode: pick(['UPI', 'Card']), ts: -int(0, 4) * 1440 + int(500, 1200), status: pick(['Success', 'Success', 'Refunded', 'Failed']) }); }
  // Discharges
  const DEPKEYS = ['clinical', 'investigations', 'pharmacy', 'billing', 'insurance', 'housekeeping', 'transport', 'summary', 'prescription'];
  const mkDeps = (st) => { const d = {}; DEPKEYS.forEach(k => d[k] = { status: 'done', at: 545 + int(0, 40), note: '' }); d.summary = { status: 'approval', at: 548, note: 'AI draft v1 · awaiting doctor signature' }; d.prescription = { status: 'approval', at: 548, note: 'With discharge summary' };
    if (st === 'Insurance Blocked') { d.insurance = { status: 'blocked', at: 545, note: 'Star Health enhancement query · angioplasty report missing' }; d.billing = { status: 'pending', at: 544, note: 'Provisional ₹2,68,450 · release after insurer' }; d.transport = { status: 'waiting', at: 546, note: 'Wheelchair slot 4:00 PM held' }; }
    if (st === 'Pharmacy Blocked') d.pharmacy = { status: 'blocked', at: 600, note: 'Enoxaparin 40 mg stock-out at ward pharmacy' };
    if (st === 'Lab Blocked') d.investigations = { status: 'blocked', at: 655, note: 'Result pending in analyser queue' };
    if (st === 'Billing Blocked') d.billing = { status: 'blocked', at: 615, note: 'Implant invoice ₹48,500 not posted' };
    if (st === 'Housekeeping Pending') { d.summary.status = 'done'; d.prescription.status = 'done'; d.housekeeping = { status: 'pending', at: 668, note: 'Bed turnaround not started' }; }
    if (st === 'Transport Pending') { d.summary.status = 'done'; d.prescription.status = 'done'; d.transport = { status: 'pending', at: 665, note: 'Wheelchair requested 11:05' }; }
    if (st === 'Ready' || st === 'Completed') { d.summary.status = 'done'; d.prescription.status = 'done'; }
    return d; };
  S.discharges = []; S.patients.filter(p => p.uhid && p.dischargeStatus).forEach((p, i) => { const st = p.dischargeStatus; S.discharges.push({ id: 'DIS-' + (1 + i), patientId: p.id, patient: p.name, doctor: p.doctor, doctorId: p.doctorId, bed: p.bed, admissionId: p.admission, insurer: p.insurer, intentAt: st === 'Completed' ? 480 : 542 + i * 3, eta: p.eta, owner: p.owner, deps: mkDeps(st), completed: st === 'Completed', dischargedAt: st === 'Completed' ? 585 + i * 20 : null, log: [] }); });
  S.patients.filter(p => p.type === 'IP' && !p.uhid).slice(0, 13).forEach((p, i) => { const st = pick(['Doctor Approval', 'Ready', 'Lab Blocked', 'Pharmacy Blocked', 'Housekeeping Pending']); S.discharges.push({ id: 'DIS-' + (20 + i), patientId: p.id, patient: p.name, doctor: p.doctor, doctorId: p.doctorId, bed: p.bed, admissionId: '—', insurer: p.insurer, intentAt: 500 + int(0, 170), eta: fmt(S.clock + int(30, 300)), owner: pick(['Ward nurse', 'Pharmacy', 'Laboratory', 'Housekeeping']), deps: mkDeps(st), completed: false, dischargedAt: null, log: [] }); });
  S.discharges.filter(d => !d.completed).forEach(d => { const b = S.beds.find(x => x.patientId === d.patientId); if (b) b.status = 'Discharge Pending'; });
  // Follow-ups 50, feedback 50
  S.followups = Array.from({ length: 50 }, (_, i) => { const p = pick(S.patients); return { id: 'FU-' + (8800 + i), patientId: p.id, patient: p.name, doctor: p.doctor, ts: int(1, 14) * 1440 + 630, status: pick(['Booked', 'Booked', 'Proposed', 'Reminder sent', 'Missed']), channel: pick(CH) }; });
  const FB = ['Long wait at billing counter', 'Nurse was very supportive', 'Room not cleaned on time', 'Discharge took 5 hours', 'Doctor explained clearly', 'Pharmacy queue too long', 'Parking difficult', 'Food quality', 'Insurance desk unresponsive', 'Excellent ICU care'];
  S.feedback = Array.from({ length: 50 }, (_, i) => { const p = pick(S.patients); const txt = pick(FB); const neg = !/supportive|clearly|Excellent/.test(txt); return { id: 'FB-2026-' + (420 + i), patientId: p.id, patient: p.name, text: txt, sentiment: neg ? 'Negative' : 'Positive', category: /bill|Insurance|Pharmacy/.test(txt) ? 'Billing & Insurance' : /wait|Discharge|queue/.test(txt) ? 'Waiting time' : /clean|Food|Parking/.test(txt) ? 'Facilities' : 'Care', priority: neg ? pick(['High', 'Medium', 'Medium', 'Low']) : 'Low', dept: p.dept, owner: pick(['Front Office Lead', 'Billing Manager', 'Nursing Supt', 'Facilities']), ts: -int(0, 5) * 1440 + int(500, 1200), sla: 60, status: neg ? pick(['Open', 'In progress', 'Resolved', 'Closed']) : 'Closed' }; });
  S.feedback.unshift({ id: 'FB-2026-0471', patientId: 'MER-2026-009602', patient: 'Divya Shankar', text: 'Waited 2 hours for the final bill after discharge; room rate different from what we were told', sentiment: 'Negative', category: 'Billing & Insurance', priority: 'High', dept: 'Paediatrics', owner: 'Billing Manager', ts: 658, sla: 30, status: 'Open' }, { id: 'FB-2026-0468', patientId: S.patients[45].id, patient: S.patients[45].name, text: 'Waited 40 minutes past my slot in OP Cardiology', sentiment: 'Negative', category: 'Waiting time', priority: 'Medium', dept: 'Cardiology', owner: 'Front Office Lead', ts: 639, sla: 60, status: 'Open' });
  // Calls 100 (simulated)
  const INTENTS = ['Appointment booking', 'Report status', 'Bill query', 'Discharge status', 'Insurance status', 'Directions', 'Doctor availability', 'Complaint'];
  S.calls = Array.from({ length: 100 }, (_, i) => { const p = pick(S.patients); const intent = pick(INTENTS); return { id: 'CALL-' + (55000 + i), patientId: p.id, patient: p.name, ts: -int(0, 2) * 1440 + int(480, 1300), duration: int(1, 9) + ' m ' + int(0, 59) + ' s', intent, sentiment: pick(['Neutral', 'Neutral', 'Positive', 'Frustrated']), handled: pick(['AI resolved', 'AI resolved', 'AI → human', 'Human']), summary: intent + ' for ' + p.name + ' · resolved with ' + pick(['status lookup', 'booking', 'callback scheduled', 'transfer to desk']), status: 'Closed' }; });
  S.calls[0] = { ...S.calls[0], patientId: D.kavitha.uhid, patient: 'Raman S (attendant of Kavitha Raman)', ts: 520, intent: 'Bill query', sentiment: 'Frustrated', handled: 'AI → human', summary: 'Attendant asked why the bill exceeds the estimate; agent surfaced itemised variance, transferred to Front Office for explanation', status: 'Closed', duration: '4 m 12 s' };
  // Communications 200
  const CT = ['Appointment reminder', 'Queue update', 'Diagnostic preparation', 'Report ready', 'Discharge status', 'Billing update', 'Follow-up reminder', 'Feedback request'];
  S.comms = Array.from({ length: 200 }, (_, i) => { const p = pick(S.patients); return { id: 'MSG-' + (60000 + i), patientId: p.id, patient: p.name, ts: -int(0, 3) * 1440 + int(420, 1320), channel: pick(['WhatsApp', 'SMS', 'Mobile push', 'Voice', 'Email']), type: pick(CT), lang: p.lang === 'Tamil' ? 'TA' : 'EN', status: pick(['Delivered', 'Read', 'Read', 'Failed']), agent: 'AG-02' }; });
  [[-5 * 1440 + 1152, 'WhatsApp', 'Appointment confirmed', 'EN'], [-3 * 1440 + 630, 'Mobile push', 'Pre-registration complete', 'EN'], [-3 * 1440 + 680, 'WhatsApp', 'Insurance preauth approved ₹1,95,000', 'TA'], [552, 'Mobile push', 'Discharge planning has started', 'TA'], [670, 'WhatsApp', 'Discharge status · expected ~4:20 PM', 'TA']].forEach(([ts, channel, type, lang], i) => S.comms.push({ id: 'MSG-6099' + i, patientId: D.kavitha.uhid, patient: 'Kavitha Raman', ts, channel, type, lang, status: 'Read', agent: 'AG-02' }));
  // Agents 21 with builder config
  S.agents = D.agents.concat(AGENT_EXTRA).map(a => { const type = TYPE_OF(a.name); const tools = TOOLS.filter(() => rnd() < 0.4).map(t => ({ tool: t, read: true, write: /Messaging|Notification|Scheduling|Housekeeping|Document/.test(t), approval: /Billing|Insurance|EMR/.test(t), enabled: true })); const vparts = a.v.split('.'); const versions = [];
    for (let i = 1; i <= +vparts[0]; i++) versions.push({ v: i + '.0.0', created: (1 + i * 20) + ' days ago', author: pick(['AI Engineering', 'Ops Product', 'Clinical Informatics']), changes: pick(['Initial release', 'Added escalation rules', 'New tool: Housekeeping', 'Prompt safety hardening', 'Tamil output']), score: int(84, 97), approval: 'Approved · AI Governance Committee', published: i === +vparts[0] });
    if (versions.length) versions[versions.length - 1].v = a.v;
    return { ...a, type, humanApproval: a.tier === 'High' ? 'Required' : a.tier === 'Medium' ? 'Selective' : 'None', status: a.status === 'Active' ? 'Published' : a.status === 'Suspended' ? 'Disabled' : a.status, statusNote: a.status, config: { identity: { nameEn: a.name, nameTa: a.name === 'Discharge Orchestration Agent' ? 'டிஸ்சார்ஜ் ஒருங்கிணைப்பு முகவர்' : '—', purpose: a.name === 'Discharge Orchestration Agent' ? 'Coordinate inpatient discharge dependencies, identify blockers, predict readiness, initiate parallel operational tasks and escalate exceptions while keeping clinical and billing approval with authorized humans.' : 'Assist ' + a.owner + ' with ' + a.name.replace(' Agent', '').toLowerCase() + ' tasks under human oversight.', type }, instructions: { objective: 'Reduce turnaround and manual coordination for ' + a.owner + '.', system: 'You are the Hospital ' + a.name + '. Operate only on the patient/workflow context provided. Cite sources. Never diagnose, prescribe, triage or sign.', rules: 'Use Tamil when the patient language is Tamil. Prefer structured outputs. Log every tool call.', safety: 'Refuse clinical interpretation. Do not release bills, sign documents or submit to insurers. Mask PHI outside the care team.', escalation: 'Escalate to the human owner when confidence < 70%, a tool fails twice, or an SLA is breached.', refusal: '"I don\'t have enough verified information to answer this safely." then route to a human.' }, knowledge: D.knowledgeDocs.filter(() => rnd() < 0.4).map(k => k.title), tools, memory: { session: 'On · 30 min', patient: 'Encounter-scoped', workflow: 'On', retention: '90 days (audit) · 0 days (conversation)', sensitive: 'No free-text PHI stored' }, access: { roles: a.owner + ', Hospital Management', departments: 'All wards', patients: 'Care-team relationship required', scopes: 'Operational + financial (no clinical write)', env: 'Production' }, model: { model: 'meridian-llm-large', temperature: 0.2, tokens: 8000, fallback: 'meridian-llm-small', latency: '< 3 s p50', cost: '₹' + int(4, 60) + ' / run' }, versions } , evals: [] }; });
  // Knowledge 100
  S.knowledge = D.knowledgeDocs.map((k, i) => ({ ...k, id: 'KD-' + pad(i + 1) })); const KT = ['Infection Control Policy', 'Consent Procedure', 'Blood Transfusion SOP', 'Fall Prevention Protocol', 'Pressure Injury Protocol', 'Code Blue Procedure', 'Bed Management Policy', 'Visitor Policy', 'Radiology Safety Manual', 'Lab Sample Handling', 'Pharmacy Formulary', 'Oxygen Therapy SOP', 'Pain Management Guide', 'Insurance Desk Handbook', 'Cashless Process Guide', 'Estimate Counselling SOP', 'Refund Policy', 'Grievance Redressal Policy', 'Leave Policy Annex', 'Payroll FAQ'];
  for (let i = S.knowledge.length; i < 100; i++) S.knowledge.push({ id: 'KD-' + pad(i + 1), title: pick(KT) + ' ' + pick(['', 'Annex A', 'Part 2', 'Ward edition']).trim(), dept: pick(DEPTS.concat(['HR', 'Finance', 'Quality', 'Nursing'])), v: int(1, 4) + '.' + int(0, 5), eff: pad(int(1, 28)) + ' ' + pick(['Jan', 'Mar', 'May', 'Jul']) + ' 2026', review: '2027', status: pick(['Published', 'Published', 'Published', 'Published', 'Review due', 'Draft']), lang: pick(['EN', 'EN · TA']) });
  // Executions 500, approvals 150, audit 1000
  const STEPS = ['Identity ✓ Consent ✓ Context assembled', 'Retrieved knowledge (2 documents)', 'Tool: HMS lookup', 'Tool: Billing read', 'Tool: Messaging (draft)', 'Policy checks passed (12)', 'Output generated · human review requested'];
  S.executions = []; S.approvals = []; S.audit = [];
  for (let i = 0; i < 500; i++) { const a = pick(S.agents); const p = pick(S.patients); const ts = -int(0, 3) * 1440 + int(360, 1310); const r = rnd(); const status = r < 0.9 ? 'Completed' : r < 0.94 ? 'Failed' : r < 0.98 ? 'Waiting' : 'Escalated'; const ex = { id: 'EXE-2026-' + (117600 + i), agentId: a.id, agent: a.name, version: a.v, patientId: p.id, patient: p.name, ts, status, steps: STEPS.filter(() => rnd() < 0.8).map((s, j) => ({ t: fmt(ts + j), what: s, k: /Tool/.test(s) ? 'tool' : /Policy|Identity/.test(s) ? 'policy' : /human/.test(s) ? 'human' : 'ai', res: 'OK' })), latency: (1 + rnd() * 5).toFixed(1) + ' s', cost: '₹' + int(3, 70), tools: int(1, 6), failure: status === 'Failed' ? pick(['LIS timeout', 'Insurer portal 502', 'Missing consent', 'Agent timeout 30 s']) : null };
    if (status === 'Failed') ex.steps.push({ t: fmt(ts + 5), what: 'Tool failure · ' + ex.failure + ' · retry scheduled', k: 'warn', res: 'Failed' });
    S.executions.push(ex);
    S.audit.push({ id: 'AUD-' + (2026090000 + i * 2), ts, actor: a.id + ' ' + a.name, action: 'Agent execution ' + status.toLowerCase(), entity: p.id, before: '—', after: status, reason: 'Workflow trigger', approval: '—', source: 'Orchestrator', corr: ex.id });
    S.audit.push({ id: 'AUD-' + (2026090001 + i * 2), ts: ts + 1, actor: pick(S.staff).name, action: pick(['Patient record viewed', 'Bill viewed', 'Consent verified', 'Report downloaded', 'Task completed']), entity: p.id, before: '—', after: '—', reason: 'Routine', approval: '—', source: pick(['Web', 'Mobile', 'EMR']), corr: ex.id }); }
  for (let i = 0; i < 140; i++) { const ex = pick(S.executions); const type = pick(Object.keys(APPROVER)); S.approvals.push({ id: 'AP-' + (3000 + i), type, patientId: ex.patientId, patient: ex.patient, agentId: ex.agentId, agent: ex.agent, action: type + ' for ' + ex.patient, risk: pick(['Low', 'Medium', 'High']), reason: 'Agent output requires ' + APPROVER[type] + ' approval', created: ex.ts, sla: 120, owner: APPROVER[type], status: pick(['Approved', 'Approved', 'Approved', 'Rejected', 'Changes requested']), decidedBy: pick(S.staff).name, decidedAt: ex.ts + int(5, 100), execId: ex.id }); }
  // Hero executions + pending approvals
  const kav = D.kavitha; const disK = S.discharges.find(d => d.patientId === kav.uhid);
  const heroExec = { id: 'EXE-2026-118204', agentId: 'AG-09', agent: 'Discharge Orchestration Agent', version: '3.0.2', patientId: kav.uhid, patient: 'Kavitha Raman', encounter: 'ENC-20481', ts: 543, status: 'Waiting', steps: D.dischargeTrace.map(s => ({ t: s.t, what: s.who + ' · ' + s.what, k: s.k, res: s.res, sys: s.sys })), latency: '2.8 s', cost: '₹38', tools: 7, criticalPath: 'Insurance → Billing → Doctor approval', handoffs: [{ from: 'Appointment Agent', to: 'Pre-registration Agent', reason: 'Booking confirmed', ts: -5 * 1440 + 1152, scope: 'Demographics · slot' }, { from: 'Pre-registration Agent', to: 'Insurance Preauth Agent', reason: 'Admission advised', ts: -4 * 1440 + 660, scope: 'Policy · estimate · doctor advice' }, { from: 'Insurance Preauth Agent', to: 'Diagnostic Coordination Agent', reason: 'Preauth approved · pre-procedure tests', ts: -3 * 1440 + 690, scope: 'Orders · prep instructions (TA)' }, { from: 'Diagnostic Coordination Agent', to: 'Discharge Orchestration Agent', reason: 'Doctor discharge intent', ts: 543, scope: 'Encounter · results · bill · insurance state' }, { from: 'Discharge Orchestration Agent', to: 'Insurance Preauth Agent', reason: 'Enhancement query needs human packet', ts: 614, scope: 'Angio report · provisional bill' }] };
  S.executions.unshift(heroExec);
  const pend = (id, type, agentId, agent, action, risk, reason, created, owner) => S.approvals.unshift({ id, type, patientId: kav.uhid, patient: 'Kavitha Raman', agentId, agent, action, risk, reason, created, sla: type === 'Preauth submission' ? 30 : 120, owner: owner || APPROVER[type], status: 'Pending', execId: heroExec.id });
  pend('AP-0001', 'Discharge summary', 'AG-09', 'Discharge Orchestration Agent', 'Sign discharge summary v2 + prescription', 'High', 'AI draft (groundedness 97%) · 2 doctor edits · requires clinician signature', 548);
  pend('AP-0002', 'Preauth submission', 'AG-07', 'Insurance Preauth Agent', 'Submit enhancement ₹73,450 to Star Health with angioplasty report', 'Medium', 'Packet assembled by agent · insurer submission is a human action', 614);
  pend('AP-0003', 'Billing release', 'AG-09', 'Discharge Orchestration Agent', 'Release final bill ₹2,68,450', 'High', 'Locked until insurer responds to enhancement', 544);
  S.approvals.unshift({ id: 'AP-0004', type: 'Discharge summary', patientId: 'MER-2026-005098', patient: 'Meenakshi Sundaram', agentId: 'AG-19', agent: 'Discharge Summary Agent', action: 'Sign discharge summary v2', risk: 'High', reason: 'AI draft · chemotherapy cycle 4', created: 665, sla: 120, owner: 'Doctor', status: 'Pending' }, { id: 'AP-0005', type: 'Discharge summary', patientId: 'MER-2026-007733', patient: 'Murugan Selvam', agentId: 'AG-19', agent: 'Discharge Summary Agent', action: 'Sign CABG discharge summary v1', risk: 'High', reason: 'AI draft · surgeon review', created: 642, sla: 120, owner: 'Doctor', status: 'Pending' }, { id: 'AP-0006', type: 'Claim appeal', patientId: 'MER-2026-007733', patient: 'Murugan Selvam', agentId: 'AG-20', agent: 'Claim Denial Agent', action: 'Submit appeal for ₹55,000 shortfall', 'risk': 'Medium', reason: 'Partial approval · appeal packet ready except family document', created: 620, sla: 240, owner: 'Insurance', status: 'Pending' }, { id: 'AP-0007', type: 'Nursing handover', patientId: 'MER-2026-008421', patient: 'Cardiac Ward (6 patients)', agentId: 'AG-18', agent: 'Nursing Handover Agent', action: 'Confirm morning → evening handover', risk: 'Medium', reason: '1 correction made by nurse', created: 665, sla: 60, owner: 'Nurse', status: 'Pending' }, { id: 'AP-0008', type: 'Staff roster', patientId: '—', patient: 'Nursing · next week', agentId: 'AG-15', agent: 'Forecasting Agent', action: 'Approve predictive roster draft', risk: 'Low', reason: 'Forecast-driven · 3 changes vs baseline', created: 560, sla: 1440, owner: 'Hospital Management', status: 'Pending' }, { id: 'AP-0009', type: 'Preauth submission', patientId: 'MER-2026-009661', patient: 'Ganesan T', agentId: 'AG-07', agent: 'Insurance Preauth Agent', action: 'Submit spinal fusion preauth (denial risk 44%)', risk: 'High', reason: 'Supervisor review advised · conservative treatment history missing', created: 630, sla: 60, owner: 'Insurance', status: 'Pending' });
  D.auditEvents.forEach((e, i) => { const [h, m] = e.ts.split(':'); S.audit.unshift({ id: e.id, ts: +h * 60 + +m, actor: e.user + (e.agent !== '—' ? ' · ' + e.agent : ''), action: e.action, entity: e.patient, before: '—', after: e.result, reason: e.sys, approval: e.approval, source: e.sys, corr: i < 3 ? heroExec.id : '—' }); });
  // Incidents 25, evals 50
  const IK = ['Incorrect tariff explanation', 'Stale SOP retrieved', 'Wrong language output', 'Tool timeout loop', 'PHI in log line', 'Over-confident answer', 'Duplicate notification', 'Escalation not triggered'];
  S.incidents = [{ id: 'AI-2026-0048', severity: 'High', agentId: 'AG-08', agent: 'Billing Transparency Agent', version: '1.9.3', patient: 'Divya Shankar · FB-2026-0471', description: 'Quoted superseded paediatric room rate (₹6,500 vs ₹7,500) in a family-facing explanation', detected: 642, owner: 'Knowledge Manager + Finance', containment: 'Agent disabled 11:14 (CIO approval); explanations held for Billing review', root: 'Suspected: Tariff v1.2 chunk not retired at v1.3 publish', resolution: '—', corrective: 'Regression suite Tariff-140 · publish-time chunk retirement check', status: 'Investigating' }];
  for (let i = 1; i < 25; i++) { const a = pick(S.agents); S.incidents.push({ id: 'AI-2026-' + pad(10 + i, 4), severity: pick(['Low', 'Low', 'Medium', 'Medium', 'High']), agentId: a.id, agent: a.name, version: a.v, patient: pick(S.patients).name, description: pick(IK), detected: -int(1, 60) * 1440 + 600, owner: pick(['AI Engineering', 'Knowledge Manager', 'CISO', 'Quality Head']), containment: 'Version pinned · monitoring', root: pick(['Prompt gap', 'Knowledge staleness', 'Integration fault', 'Config error']), resolution: 'Fixed in next version', corrective: 'Eval case added', status: pick(['Closed', 'Closed', 'Resolved', 'Contained', 'Open']) }); }
  S.evals = Array.from({ length: 50 }, (_, i) => { const a = pick(S.agents); return { id: 'EV-' + (700 + i), agentId: a.id, agent: a.name, version: a.v, ts: -int(0, 30) * 1440 + 180, cases: int(40, 220), accuracy: int(86, 98), groundedness: int(90, 99), hallucination: (rnd() * 3).toFixed(1), refusal: int(85, 99), escalation: int(85, 99), completion: int(85, 98), toolSuccess: int(92, 100), latency: (1 + rnd() * 3).toFixed(1) + ' s', override: (rnd() * 9).toFixed(1), result: rnd() < 0.9 ? 'Pass' : 'Fail' }; });
  S.evals.forEach(e => { const a = S.agents.find(x => x.id === e.agentId); if (a) a.evals.push(e); });
  // Notifications
  const notify = (title, body, priority, source, link, ts) => S.notifications.unshift({ id: nid('NT'), ts: ts ?? S.clock, title, body, priority, source, link, read: false });
  [['Critical value unacknowledged', 'K⁺ 6.1 mmol/L · Venkatesan P · Dr. Ramesh Pillai · 14 m', 'Critical', 'Laboratory', { page: 'laboratory', id: 'LAB-76991' }, 666], ['Critical value unacknowledged', 'Troponin-I 2.4 ng/mL · ER-2026-4412 · 6 m', 'Critical', 'Laboratory', { page: 'laboratory', id: 'LAB-76992' }, 674], ['Approval required', 'Sign discharge summary v2 · Kavitha Raman', 'High', 'Discharge Orchestration Agent', { page: 'approvals', id: 'AP-0001' }, 548], ['Insurance packet ready', 'Enhancement ₹73,450 · Kavitha Raman · awaiting submit', 'High', 'Insurance Preauth Agent', { page: 'approvals', id: 'AP-0002' }, 614], ['Agent disabled', 'Billing Transparency Agent v1.9.3 · AI-2026-0048', 'High', 'AI Governance', { page: 'incidents', id: 'AI-2026-0048' }, 674], ['Complaint nearing SLA', 'FB-2026-0471 · Divya Shankar · 22 m of 30 m', 'High', 'Feedback Agent', { page: 'feedback', id: 'FB-2026-0471' }, 670], ['Discharge blocked', 'Fathima Begum · Enoxaparin stock-out', 'Medium', 'Discharge Orchestration Agent', { page: 'discharge', id: 'DIS-3' }, 600], ['Knowledge conflict', 'Medication Safety v3.2 vs v4.0 · Nursing', 'Medium', 'Knowledge', { page: 'knowledge', id: 'KD-03' }, 580], ['No-show detected', noShow.name + ' · 10:00 · reschedule offered', 'Low', 'Appointment Agent', { page: 'appointments', id: 'APT-24110' }, 615], ['LIS connector degraded', 'Result feed 7 min stale · manual fallback active', 'Medium', 'Integrations', { page: 'integrations' }, 660]].forEach(([t, b, p, s, l, ts]) => notify(t, b, p, s, l, ts));
  S.notifications.slice(5).forEach(n => n.read = rnd() < 0.5);

  // ---------- helpers & derived ----------
  const st = { S };
  const emit = () => onChange && onChange();
  const audit = (actor, action, entity, before, after, reason, approval, source, corr) => { S.audit.unshift({ id: 'AUD-2026-' + (91300 + S.audit.length), ts: S.clock, actor, action, entity, before: before ?? '—', after: after ?? '—', reason: reason || '—', approval: approval || '—', source: source || 'Web', corr: corr || '—' }); };
  const dischargeState = d => { if (d.completed) return { label: 'Completed', k: 'done' }; if (d.paused) return { label: 'Paused · clinical', k: 'blocked', dep: 'clinical' }; const v = Object.entries(d.deps); const b = v.find(([, x]) => x.status === 'blocked'); if (b) return { label: 'Blocked · ' + b[0], k: 'blocked', dep: b[0] }; const a = v.find(([, x]) => x.status === 'approval'); if (a) return { label: 'Approval required · ' + a[0], k: 'approval', dep: a[0] }; const p = v.find(([, x]) => x.status === 'pending' || x.status === 'waiting'); if (p) return { label: 'In progress · ' + p[0], k: 'pending', dep: p[0] }; return { label: 'Ready', k: 'ready' }; };
  const patient = id => S.patients.find(p => p.id === id);
  const recomputeEta = d => { const s = dischargeState(d); if (s.k === 'ready') d.eta = 'Now'; else if (s.k === 'blocked') d.eta = fmt(S.clock + 180); else if (s.k === 'approval') d.eta = fmt(S.clock + 75); else d.eta = fmt(S.clock + 40); };
  const log = (d, who, what, k) => d.log.unshift({ t: fmt(S.clock), who, what, k: k || 'tool' });
  const exec = id => S.executions.find(e => e.id === id);
  const step = (exId, who, what, k, res, sys) => { const e = exec(exId); if (e) e.steps.push({ t: fmt(S.clock), what: who + ' · ' + what, k, res, sys }); };

  const api = {
    S, fmt, ago, inr, dischargeState, patient,
    clock: () => S.clock,
    exceptions() { const now = S.clock, out = [];
      S.discharges.filter(d => !d.completed).forEach(d => { const s = dischargeState(d); if (s.k === 'blocked') out.push({ id: d.id, group: 'Discharge blocked', ref: d.patient + ' · ' + (d.bed || '').split(' ')[0], owner: d.owner, age: ago(d.deps[s.dep].at, now), pri: 'High', reason: d.deps[s.dep].note, patientId: d.patientId, workflow: 'Discharge', agent: 'AG-09', next: 'Open case', sla: '30 m', page: 'discharge-case' }); });
      S.preauths.filter(p => /Query|Additional|Missing|High Denial|Partially/.test(p.status)).forEach(p => out.push({ id: p.id, group: 'Insurance ageing', ref: p.patient + ' · ' + p.id, owner: p.exec, age: ago(p.createdTs, now), pri: /High Denial|Partially/.test(p.status) ? 'High' : 'Medium', reason: p.status + (p.missing !== '—' ? ' · ' + p.missing : ''), patientId: p.patientId, workflow: 'Preauthorisation', agent: 'AG-07', next: 'Open case', sla: '2 h', page: 'insurance' }));
      S.labs.filter(l => l.critical && !l.acked).forEach(l => out.push({ id: l.id, group: 'Critical value unacknowledged', ref: l.patient + ' · ' + l.analyte + ' ' + l.value + ' ' + l.unit, owner: l.clinician, age: ago(l.notified, now), pri: 'Critical', reason: l.escalated ? 'Escalated to department head' : 'Acknowledgement timer running (15 m)', patientId: l.patientId, workflow: 'Critical value', agent: 'AG-10', next: 'Acknowledge', sla: '15 m', page: 'laboratory' }));
      S.bills.filter(b => b.dispute && b.dispute.status === 'Open').forEach(b => out.push({ id: b.id, group: 'Billing dispute', ref: b.patient + ' · ' + b.dispute.id, owner: b.dispute.owner, age: ago(b.dispute.raised, now), pri: 'High', reason: b.dispute.reason, patientId: b.patientId, workflow: 'Billing', agent: 'AG-08', next: 'Review', sla: '60 m', page: 'billing' }));
      S.feedback.filter(f => f.status === 'Open' && f.sentiment === 'Negative' && f.ts > 0).forEach(f => out.push({ id: f.id, group: 'Complaint past SLA', ref: f.id + ' · ' + f.patient, owner: f.owner, age: ago(f.ts, now) + ' / ' + f.sla + ' m', pri: f.priority, reason: f.text, patientId: f.patientId, workflow: 'Service recovery', agent: 'AG-05', next: 'Call patient', sla: f.sla + ' m', page: 'feedback' }));
      S.executions.filter(e => e.status === 'Failed' && e.ts > 0).slice(0, 4).forEach(e => out.push({ id: e.id, group: 'Failed agent execution', ref: e.agent + ' · ' + e.patient, owner: 'AI Operations', age: ago(e.ts, now), pri: 'Medium', reason: e.failure, patientId: e.patientId, workflow: 'Agent run', agent: e.agentId, next: 'Retry', sla: '30 m', page: 'runs' }));
      S.knowledge.filter(k => k.status === 'Conflict').slice(0, 1).forEach(k => out.push({ id: k.id, group: 'Knowledge conflict', ref: 'Medication Safety v3.2 vs v4.0', owner: 'Nursing Superintendent', age: ago(580, now), pri: 'Medium', reason: 'Contradictory double-check rules · withheld from answers', patientId: '—', workflow: 'Knowledge', agent: '—', next: 'Owner review', sla: '2 d', page: 'knowledge' }));
      S.incidents.filter(i => /Open|Investigating/.test(i.status)).forEach(i => out.push({ id: i.id, group: 'AI safety incident', ref: i.id + ' · ' + i.agent, owner: i.owner, age: ago(i.detected, now), pri: i.severity, reason: i.description, patientId: '—', workflow: 'AI incident', agent: i.agentId, next: 'Investigate', sla: '4 h', page: 'incidents' }));
      if (S.pharmacy.some(p => p.status === 'Stock-out')) out.push({ id: 'PH-91100', group: 'Pharmacy stock-out', ref: 'Enoxaparin 40 mg · Ortho ward', owner: 'S. Devi', age: ago(600, now), pri: 'High', reason: 'Blocks Fathima Begum discharge', patientId: 'MER-2026-009104', workflow: 'Pharmacy', agent: 'AG-09', next: 'Transfer stock', sla: '45 m', page: 'pharmacy' });
      if (S.lisDegraded) out.push({ id: 'INT-LIS', group: 'Integration degraded', ref: 'LIS result feed', owner: 'Integration Engineer', age: ago(660, now), pri: 'Medium', reason: 'Results 7 min stale · manual fallback', patientId: '—', workflow: 'Integrations', agent: '—', next: 'Retry connector', sla: '30 m', page: 'integrations' });
      return out; },
    kpis() { const ip = S.patients.filter(p => p.type === 'IP').length; const occ = S.beds.filter(b => b.status === 'Occupied' || b.status === 'Discharge Pending' || b.status === 'Reserved').length; const dis = S.discharges.filter(d => !d.completed); const blocked = dis.filter(d => dischargeState(d).k === 'blocked').length; const ready = dis.filter(d => dischargeState(d).k === 'ready').length; const crit = S.labs.filter(l => l.critical && !l.acked).length; const pend = S.approvals.filter(a => a.status === 'Pending').length; const todayEx = S.executions.filter(e => e.ts >= 0); const auto = Math.round(100 * todayEx.filter(e => e.status === 'Completed').length / Math.max(1, todayEx.length));
      return { apptsToday: S.appointments.filter(a => a.ts >= 0 && a.ts < 1440).length, opdWaiting: 23 + (S.clock - 680 > 0 ? -Math.min(8, Math.floor((S.clock - 680) / 15)) : 0), er: S.emergency.filter(e => e.status !== 'Closed').length, admissions: S.admissions.filter(a => a.status === 'Admitted').length, ip, bedsFree: S.beds.filter(b => b.status === 'Free').length, bedsTotal: S.beds.length, occ, occPct: Math.round(100 * occ / S.beds.length), dischargeCandidates: dis.length, dischargeReady: ready, dischargeBlocked: blocked, completedToday: S.discharges.filter(d => d.completed && d.dischargedAt >= 0).length, insuranceAgeing: S.preauths.filter(p => /Query|Additional|Missing|Partially|High Denial/.test(p.status)).length, preauthPending: S.preauths.filter(p => /Pending|Draft|Submitted/.test(p.status)).length, disputes: S.bills.filter(b => b.dispute && b.dispute.status === 'Open').length, critical: crit, complaints: S.feedback.filter(f => f.status === 'Open').length, approvals: pend, exceptions: api.exceptions().length, execsToday: todayEx.length, automation: auto, agentsActive: S.agents.filter(a => a.status === 'Published').length, agentsDisabled: S.agents.filter(a => a.status === 'Disabled').length, pressure: blocked > 3 || crit > 1 ? 'High' : 'Moderate', turnaround: blocked >= 4 ? '3 h 48 m' : blocked >= 2 ? '3 h 21 m' : '2 h 58 m' }; },
    can(role, what) { const M = { approve: type => APPROVER[type] === role || (role === 'Hospital Management' && /roster|Purchase|Stock|Leave|void|Refund/i.test(type)) || (role === 'Finance Manager' && /Billing|Refund|Purchase/i.test(type)), clinical: ['Doctor', 'Nurse', 'Radiologist', 'Laboratory', 'Pharmacy'].includes(role), lab: ['Laboratory', 'Pathologist'].includes(role), pharmacy: ['Pharmacy', 'Nurse'].includes(role), frontOffice: ['Front Office', 'Hospital Management', 'Doctor', 'Nurse'].includes(role), admit: ['Doctor', 'Front Office', 'Hospital Management'].includes(role), payments: ['Billing', 'Front Office', 'Hospital Management'].includes(role), financial: ['Billing', 'Finance Manager', 'Insurance', 'Hospital Management', 'Front Office', 'Auditor'].includes(role), inventory: ['Store Manager', 'Procurement Officer', 'Pharmacy', 'Laboratory', 'Hospital Management'].includes(role), hr: ['HR Manager', 'Hospital Management'].includes(role), canteen: ['Canteen Manager', 'Nurse', 'Hospital Management'].includes(role), admin: ['IT Administrator', 'Hospital Management'].includes(role), pathologist: ['Pathologist'].includes(role), configureAgents: ['AI Administrator'].includes(role), governance: ['Governance Officer', 'AI Administrator', 'Hospital Management'].includes(role), discharge: ['Nurse', 'Front Office', 'Hospital Management', 'Doctor'].includes(role), acknowledge: ['Doctor'].includes(role), resolveDispute: ['Billing', 'Hospital Management'].includes(role), radiology: ['Radiologist', 'Doctor'].includes(role) }; const v = M[what]; return typeof v === 'function' ? v : !!v; },
    // ---------- actions ----------
    approve(id, role, note) { const a = S.approvals.find(x => x.id === id); if (!a || a.status !== 'Pending') return; a.status = 'Approved'; a.decidedBy = role; a.decidedAt = S.clock; a.note = note; const d = S.discharges.find(x => x.patientId === a.patientId && !x.completed);
      if (a.type === 'Discharge summary' && d) { d.deps.summary = { status: 'done', at: S.clock, note: 'Signed by ' + role }; d.deps.prescription = { status: 'done', at: S.clock, note: 'Signed with summary' }; log(d, role, 'Signed discharge summary and prescription', 'human'); step(a.execId, role, 'Discharge summary + prescription signed', 'human', 'Approved'); notify('Discharge summary signed', a.patient + ' · summary v2 signed by ' + role, 'Medium', 'Discharge Orchestration Agent', { page: 'discharge-case', id: d.id }); }
      if (a.type === 'Preauth submission') { const p = S.preauths.find(x => x.patientId === a.patientId && /Query|Missing|Draft|High Denial|Pending/.test(x.status)); if (p) { p.status = 'Submitted · awaiting insurer'; p.submitted = fmt(S.clock); p.missing = '—'; p.docs.missing = []; p.completeness = 100; p.history.unshift({ t: fmt(S.clock), e: 'Submitted by ' + role + ' (human) with agent-prepared packet' }); if (d) { d.deps.insurance.note = 'Enhancement submitted ' + fmt(S.clock) + ' · awaiting Star Health'; log(d, role, 'Enhancement submitted to insurer', 'human'); } step(a.execId, 'Insurance Preauth Agent', 'Human submitted enhancement to insurer · watching response', 'tool', 'Submitted', 'Insurance / TPA'); notify('Enhancement submitted', a.patient + ' · Star Health · awaiting response (simulate in Demo controls)', 'Medium', 'Insurance Preauth Agent', { page: 'insurance', id: p.id }); } }
      if (a.type === 'Billing release') { const b = S.bills.find(x => x.patientId === a.patientId && x.status !== 'Settled'); if (b) { b.status = 'Released'; } if (d) { d.deps.billing = { status: 'done', at: S.clock, note: 'Final bill released by ' + role }; log(d, role, 'Final bill released', 'human'); } step(a.execId, role, 'Final bill released', 'human', 'Released', 'Billing'); notify('Final bill released', a.patient + ' · ' + (b ? inr(b.actual) : ''), 'Medium', 'Billing', { page: 'billing', id: b && b.id }); S.comms.push({ id: nid('MSG'), patientId: a.patientId, patient: a.patient, ts: S.clock, channel: 'WhatsApp', type: 'Billing update · final bill ready', lang: 'TA', status: 'Delivered', agent: 'AG-02' }); }
      if (a.type === 'Nursing handover') notify('Handover confirmed', 'Cardiac Ward · morning → evening', 'Low', 'Nursing Handover Agent', { page: 'approvals' });
      if (a.type === 'Claim appeal') { const p = S.preauths.find(x => x.patientId === a.patientId); if (p) { p.status = 'Appeal submitted'; p.history.unshift({ t: fmt(S.clock), e: 'Appeal submitted by ' + role }); } }
      if (a.type === 'Agent publish') { const ag = S.agents.find(x => x.id === a.agentId); if (ag) { ag.status = 'Published'; ag.statusNote = 'Active'; ag.config.versions.forEach(v => v.approval = v.published ? 'Approved · ' + role : v.approval); } }
      if (a.type === 'Billing void') { const b = S.bills.find(x => x.id === a.entityId); if (b) { b.status = 'Voided'; b.voidReason = a.reason; } }
      if (a.type === 'Refund') { const b = S.bills.find(x => x.id === a.entityId); if (b) { S.payments.unshift({ id: nid('PAY'), billId: b.id, patient: b.patient, amount: a.amount, mode: 'Refund · original mode', ts: S.clock, status: 'Refunded' }); b.paid = Math.max(0, (b.paid || 0) - a.amount); } }
      if (d) { recomputeEta(d); api._checkReady(d); }
      audit(role, 'Approval granted · ' + a.type, a.patientId, 'Pending', 'Approved', a.action, role, 'Approval Centre', a.execId); emit(); },
    reject(id, role, reason) { const a = S.approvals.find(x => x.id === id); if (!a || a.status !== 'Pending') return; a.status = 'Rejected'; a.decidedBy = role; a.decidedAt = S.clock; a.note = reason; const d = S.discharges.find(x => x.patientId === a.patientId && !x.completed); if (d && a.type === 'Discharge summary') { d.deps.summary = { status: 'blocked', at: S.clock, note: 'Rejected by doctor: ' + reason + ' · agent regenerating draft' }; log(d, role, 'Rejected draft: ' + reason, 'warn'); step(a.execId, role, 'Draft rejected · ' + reason + ' · regenerating v3', 'warn', 'Rejected'); } notify('Approval rejected', a.action + ' · ' + reason, 'Medium', 'Approval Centre', { page: 'approvals', id }); audit(role, 'Approval rejected · ' + a.type, a.patientId, 'Pending', 'Rejected', reason, role, 'Approval Centre', a.execId); emit(); },
    requestChanges(id, role, reason) { const a = S.approvals.find(x => x.id === id); if (!a || a.status !== 'Pending') return; a.status = 'Changes requested'; a.note = reason; a.decidedBy = role; a.decidedAt = S.clock; step(a.execId, role, 'Changes requested · ' + reason, 'human', 'Revising'); setTimeout(() => { S.approvals.unshift({ ...a, id: a.id + 'R', status: 'Pending', created: S.clock, reason: 'Revised after: ' + reason, note: undefined }); notify('Revised draft ready', a.action + ' (revised)', 'Medium', a.agent, { page: 'approvals', id: a.id + 'R' }); emit(); }, 1500); audit(role, 'Changes requested · ' + a.type, a.patientId, 'Pending', 'Changes requested', reason, role, 'Approval Centre', a.execId); emit(); },
    simulateInsurer(patientId, outcome) { const p = S.preauths.find(x => x.patientId === patientId && /Submitted|Pending|Query|Appeal/.test(x.status)); if (!p) return false; const d = S.discharges.find(x => x.patientId === patientId && !x.completed); const b = S.bills.find(x => x.patientId === patientId);
      if (outcome === 'approve') { p.status = 'Approved'; p.approved = p.requested !== '—' ? p.requested : p.estimate; p.liability = '₹0'; p.history.unshift({ t: fmt(S.clock), e: 'Insurer approved ' + p.approved }); if (b) { b.insurance = b.actual; b.patientShare = 0; } if (d) { d.deps.insurance = { status: 'done', at: S.clock, note: 'Approved by ' + p.insurer + ' ' + fmt(S.clock) }; if (d.deps.billing.status !== 'done') d.deps.billing = { status: 'approval', at: S.clock, note: 'Final bill ready · awaiting Billing release' }; log(d, p.insurer, 'Final approval received', 'tool'); const ap = S.approvals.find(x => x.patientId === patientId && x.type === 'Billing release' && x.status === 'Pending'); if (ap) ap.reason = 'Insurer approved · bill can be released'; } step('EXE-2026-118204', 'Insurance Preauth Agent', 'Insurer response: approved · dependency cleared', 'tool', 'Approved', 'Insurance / TPA'); S.comms.push({ id: nid('MSG'), patientId, patient: p.patient, ts: S.clock, channel: 'WhatsApp', type: 'Insurance approved · discharge moving ahead', lang: 'TA', status: 'Delivered', agent: 'AG-02' }); notify('Insurer approved', p.patient + ' · ' + p.insurer + ' approved ' + p.approved + ' · patient notified in Tamil', 'High', 'Insurance Preauth Agent', { page: 'discharge-case', id: d && d.id }); }
      else { p.status = 'Rejected'; p.history.unshift({ t: fmt(S.clock), e: 'Insurer rejected enhancement' }); if (d) { d.deps.insurance = { status: 'blocked', at: S.clock, note: 'Enhancement rejected · patient liability counselling needed' }; log(d, p.insurer, 'Enhancement rejected', 'warn'); } notify('Insurer rejected', p.patient + ' · appeal packet being prepared by Claim Denial Agent', 'High', 'Claim Denial Agent', { page: 'insurance', id: p.id }); S.approvals.unshift({ id: nid('AP'), type: 'Claim appeal', patientId, patient: p.patient, agentId: 'AG-20', agent: 'Claim Denial Agent', action: 'Submit appeal · ' + p.insurer, risk: 'Medium', reason: 'Enhancement rejected · appeal packet assembled', created: S.clock, sla: 240, owner: 'Insurance', status: 'Pending' }); }
      if (d) { recomputeEta(d); api._checkReady(d); } audit('Insurance / TPA (simulated)', 'Insurer response · ' + outcome, patientId, 'Submitted', p.status, 'Demo control', '—', 'TPA connector', 'EXE-2026-118204'); emit(); return true; },
    _checkReady(d) { if (d.paused) return; const open = Object.entries(d.deps).filter(([, x]) => x.status !== 'done'); if (open.length && open.every(([k, x]) => (k === 'transport' || k === 'housekeeping') && (x.status === 'waiting' || x.status === 'pending'))) { open.forEach(([k]) => { d.deps[k] = { status: 'done', at: S.clock, note: k === 'transport' ? 'Porter dispatched · wheelchair at ward' : 'Team B dispatched' }; log(d, 'Discharge Agent', k === 'transport' ? 'Transport confirmed · porter dispatched' : 'Housekeeping dispatched', 'tool'); step('EXE-2026-118204', 'Discharge Agent', (k === 'transport' ? 'Transport slot confirmed · porter dispatched' : 'Housekeeping dispatched'), 'tool', 'Done', k === 'transport' ? 'Patient Transport' : 'Facilities'); }); } const s = dischargeState(d); if (s.k === 'ready' && !d.readyNotified) { d.readyNotified = true; d.eta = 'Now'; step('EXE-2026-118204', 'Discharge Agent', 'All dependencies cleared · patient ready for discharge', 'ai', 'READY'); notify('Ready for discharge', d.patient + ' · all dependencies cleared · ward can release', 'High', 'Discharge Orchestration Agent', { page: 'discharge-case', id: d.id }); S.comms.push({ id: nid('MSG'), patientId: d.patientId, patient: d.patient, ts: S.clock, channel: 'WhatsApp', type: 'நீங்கள் இப்போது வீட்டுக்கு செல்லலாம் · discharge ready', lang: 'TA', status: 'Delivered', agent: 'AG-02' }); } },
    dischargePatient(dId, role) { const d = S.discharges.find(x => x.id === dId); if (!d || dischargeState(d).k !== 'ready') return; d.completed = true; d.dischargedAt = S.clock; const p = patient(d.patientId); if (p) { p.type = 'Discharged'; p.status = 'Discharged ' + fmt(S.clock); const b = S.beds.find(x => x.patientId === p.id); if (b) { b.status = 'Cleaning'; b.patientId = null; setTimeout(() => { b.status = 'Free'; emit(); }, 4000); } } const ad = S.admissions.find(x => x.patientId === d.patientId); if (ad) ad.status = 'Discharged'; const fu = { id: nid('FU'), patientId: d.patientId, patient: d.patient, doctor: d.doctor, ts: 7 * 1440 + 630, status: 'Booked', channel: 'Mobile app' }; S.followups.unshift(fu); S.comms.push({ id: nid('MSG'), patientId: d.patientId, patient: d.patient, ts: S.clock, channel: 'WhatsApp', type: 'Follow-up 19 Sep 10:30 confirmed · feedback request', lang: 'TA', status: 'Delivered', agent: 'AG-11' }); const ex = exec('EXE-2026-118204'); if (ex && ex.patientId === d.patientId) { ex.status = 'Completed'; step(ex.id, 'Follow-up Agent', 'Handoff: follow-up booked 19 Sep 10:30 · feedback request scheduled', 'ai', 'Booked'); ex.handoffs.push({ from: 'Discharge Orchestration Agent', to: 'Follow-up Agent', reason: 'Patient discharged', ts: S.clock, scope: 'Summary · medications · follow-up slot' }); } log(d, role, 'Patient discharged · bed released to Command Centre', 'human'); notify('Patient discharged', d.patient + ' · bed ' + (d.bed || '').split(' ')[0] + ' to housekeeping · follow-up booked', 'Medium', 'Discharge', { page: 'command' }); audit(role, 'Patient discharged', d.patientId, 'Ready', 'Discharged', 'All dependencies cleared', '—', 'Discharge board', 'EXE-2026-118204'); emit(); },
    markLikelyDischarge(patientId, role) { const p = patient(patientId); if (!p || S.discharges.some(d => d.patientId === patientId && !d.completed)) return; const deps = {}; DEPKEYS.forEach(k => deps[k] = { status: 'pending', at: S.clock, note: 'Requested by agent' }); deps.clinical = { status: 'done', at: S.clock, note: 'Intent by ' + p.doctor }; const d = { id: nid('DIS'), patientId, patient: p.name, doctor: p.doctor, doctorId: p.doctorId, bed: p.bed, admissionId: '—', insurer: p.insurer, intentAt: S.clock, eta: fmt(S.clock + 150), owner: 'Discharge Orchestration Agent', deps, completed: false, dischargedAt: null, log: [] }; S.discharges.unshift(d); const bedI = S.beds.find(x => x.patientId === patientId); if (bedI) bedI.status = 'Discharge Pending'; const ex = { id: nid('EXE-2026'), agentId: 'AG-09', agent: 'Discharge Orchestration Agent', version: '3.0.2', patientId, patient: p.name, ts: S.clock, status: 'Running', steps: [{ t: fmt(S.clock), what: 'Orchestrator · Identity ✓ Consent ✓ Intent discharge.coordinate → Discharge Agent', k: 'policy', res: 'Allowed' }], latency: '—', cost: '₹12', tools: 0, handoffs: [] }; S.executions.unshift(ex); log(d, p.doctor, 'Marked likely discharge', 'human'); audit(role, 'Discharge intent recorded', patientId, '—', 'Likely discharge', 'Doctor Workspace', '—', 'EMR', ex.id); emit();
      const plan = [['investigations', 'LIS', 'Pending investigations checked', '0 pending'], ['pharmacy', 'Pharmacy', 'Clearance requested', 'Cleared'], ['billing', 'Billing', 'Bill assembly triggered', 'Provisional bill'], ['insurance', 'Insurance / TPA', p.insurer === 'Self-pay' ? 'Self-pay · no insurer step' : 'Final approval requested', p.insurer === 'Self-pay' ? 'N/A' : 'Awaiting insurer'], ['housekeeping', 'Facilities', 'Bed turnaround pre-alert', 'Acknowledged'], ['transport', 'Patient Transport', 'Requirement detected', 'Slot held'], ['summary', 'LLM', 'Discharge summary draft v1 generated', 'Groundedness 96%']];
      plan.forEach(([k, sys, what, res], i) => setTimeout(() => { S.clock += 1; d.deps[k] = { status: k === 'summary' ? 'approval' : k === 'insurance' && p.insurer !== 'Self-pay' ? 'waiting' : 'done', at: S.clock, note: res }; if (k === 'summary') d.deps.prescription = { status: 'approval', at: S.clock, note: 'With summary' }; ex.steps.push({ t: fmt(S.clock), what: 'Discharge Agent · ' + what, k: k === 'summary' ? 'ai' : 'tool', res, sys }); ex.tools++; if (k === 'summary') { ex.status = 'Waiting'; S.approvals.unshift({ id: nid('AP'), type: 'Discharge summary', patientId, patient: p.name, agentId: 'AG-09', agent: 'Discharge Orchestration Agent', action: 'Sign discharge summary v1', risk: 'High', reason: 'AI draft · groundedness 96%', created: S.clock, sla: 120, owner: 'Doctor', status: 'Pending', execId: ex.id }); S.approvals.unshift({ id: nid('AP'), type: 'Billing release', patientId, patient: p.name, agentId: 'AG-09', agent: 'Discharge Orchestration Agent', action: 'Release final bill', risk: 'High', reason: 'After insurer response', created: S.clock, sla: 120, owner: 'Billing', status: 'Pending', execId: ex.id }); d.deps.billing.status = 'approval'; notify('Approval required', 'Sign discharge summary v1 · ' + p.name, 'High', 'Discharge Orchestration Agent', { page: 'approvals' }); } recomputeEta(d); emit(); }, 700 * (i + 1))); },
    acknowledgeCritical(labId, role) { const l = S.labs.find(x => x.id === labId); if (!l) return; l.acked = true; l.ackedBy = role; l.ackedAt = S.clock; notify('Critical value acknowledged', l.patient + ' · ' + l.analyte + ' ' + l.value + ' · escalation stopped', 'Medium', 'Diagnostic Coordination Agent', { page: 'laboratory', id: labId }); audit(role, 'Critical value acknowledged', l.patientId, 'Unacknowledged', 'Acknowledged', l.analyte + ' ' + l.value + ' ' + l.unit, '—', 'EMR'); emit(); },
    simulateCritical() { const e = S.emergency.find(x => x.status !== 'Closed' && !S.labs.some(l => l.patientId === x.patientId && l.critical)); if (!e) return; const l = { id: nid('LAB'), patientId: e.patientId, patient: e.patient, test: 'Blood sugar', analyte: 'Glucose', value: 38, unit: 'mg/dL', flag: 'Critical', critical: true, acked: false, notified: S.clock, escalated: false, clinician: e.clinician, ts: S.clock, status: 'Final' }; S.labs.unshift(l); notify('Critical value', l.patient + ' · Glucose 38 mg/dL · ' + e.clinician + ' notified · 15 m timer', 'Critical', 'Diagnostic Coordination Agent', { page: 'laboratory', id: l.id }); audit('AG-10 Diagnostic Coordination', 'Critical-value rule triggered', l.patientId, '—', 'Clinician notified', 'Glucose < 50', '—', 'LIS'); emit(); },
    advanceTime(min) { S.clock += min; S.labs.filter(l => l.critical && !l.acked && !l.escalated && S.clock - l.notified >= 15).forEach(l => { l.escalated = true; notify('Escalated to department head', l.patient + ' · ' + l.analyte + ' ' + l.value + ' unacknowledged for 15 m', 'Critical', 'Diagnostic Coordination Agent', { page: 'laboratory', id: l.id }); audit('AG-10 Diagnostic Coordination', 'Critical value escalated', l.patientId, 'Clinician notified', 'Department head notified', '15-minute acknowledgement timer', '—', 'Orchestrator'); }); S.feedback.filter(f => f.status === 'Open' && f.ts > 0 && S.clock - f.ts > f.sla && !f.breached).forEach(f => { f.breached = true; notify('Complaint SLA breached', f.id + ' · ' + f.patient, 'High', 'Feedback Agent', { page: 'feedback', id: f.id }); }); emit(); },
    toggleLis() { S.lisDegraded = !S.lisDegraded; notify(S.lisDegraded ? 'LIS connector degraded' : 'LIS connector restored', S.lisDegraded ? 'Result feed stale · manual fallback' : 'Result feed healthy', 'Medium', 'Integrations', { page: 'integrations' }); emit(); },
    createComplaint() { const p = pick(S.patients); const f = { id: nid('FB-2026'), patientId: p.id, patient: p.name, text: 'Pharmacy queue over 40 minutes for discharge medicines', sentiment: 'Negative', category: 'Waiting time', priority: 'Medium', dept: 'Pharmacy', owner: 'Pharmacy Lead', ts: S.clock, sla: 60, status: 'Open', ai: 'Classified: Waiting time · Pharmacy · Medium · routed to Pharmacy Lead (human resolves)' }; S.feedback.unshift(f); notify('New complaint classified', f.patient + ' · Pharmacy · Medium · owner assigned', 'Medium', 'Feedback Agent', { page: 'feedback', id: f.id }); audit('AG-05 Feedback', 'Complaint classified and routed', p.id, '—', 'Open', 'AI classification', '—', 'Feedback'); emit(); },
    resolveDispute(billId, role, resolution) { const b = S.bills.find(x => x.id === billId); if (!b || !b.dispute) return; b.dispute.status = 'Resolved'; b.dispute.resolution = resolution; b.dispute.resolvedBy = role; b.status = 'Settled'; const f = S.feedback.find(x => x.patientId === b.patientId && x.status === 'Open'); if (f) { f.status = 'Resolved'; f.resolution = resolution; } S.comms.push({ id: nid('MSG'), patientId: b.patientId, patient: b.patient, ts: S.clock, channel: 'WhatsApp', type: 'Billing dispute resolved · itemised explanation sent', lang: 'TA', status: 'Delivered', agent: 'AG-08' }); notify('Dispute resolved', b.patient + ' · ' + resolution, 'Medium', 'Billing', { page: 'billing', id: billId }); audit(role, 'Billing dispute resolved', b.patientId, 'Open', 'Resolved', resolution, role, 'Billing'); emit(); },
    rescheduleNoShow(apptId, slot) { const a = S.appointments.find(x => x.id === apptId); if (!a) return; a.status = 'Rescheduled'; a.noShowHandled = true; const n = { ...a, id: nid('APT'), ts: 1440 + slot, status: 'Confirmed', channel: 'WhatsApp', type: a.type }; S.appointments.unshift(n); S.comms.push({ id: nid('MSG'), patientId: a.patientId, patient: a.patient, ts: S.clock, channel: 'WhatsApp', type: 'Appointment rescheduled · ' + fmt(1440 + slot), lang: 'TA', status: 'Delivered', agent: 'AG-01' }); notify('No-show rescheduled', a.patient + ' → tomorrow ' + fmt(1440 + slot).slice(-5), 'Low', 'Appointment Agent', { page: 'appointments', id: n.id }); audit('AG-01 Appointment', 'No-show rescheduled (patient selected slot)', a.patientId, 'No-show', 'Rescheduled', 'WhatsApp offer accepted', '—', 'WhatsApp'); emit(); },
    runAgent(agentId, patientId, role) { const a = S.agents.find(x => x.id === agentId); const p = patient(patientId) || S.patients[0]; const ex = { id: nid('EXE-2026'), agentId, agent: a.name, version: a.v, patientId: p.id, patient: p.name, ts: S.clock, status: 'Running', steps: [{ t: fmt(S.clock), what: 'Orchestrator · Identity ✓ Consent ✓ Context assembled', k: 'policy', res: 'Allowed' }], latency: '—', cost: '₹' + int(5, 40), tools: 0, handoffs: [] }; S.executions.unshift(ex); a.lastRun = fmt(S.clock); a.runs++; audit(role, 'Agent run triggered', p.id, '—', 'Running', a.name, '—', 'Agent Registry', ex.id); emit();
      const plan = a.name.includes('Discharge') ? [['LIS', 'Pending investigations checked', '0 pending', 'tool'], ['Pharmacy', 'Clearance requested', 'Cleared', 'tool'], ['Billing', 'Provisional bill assembled', inr(S.bills.find(b => b.patientId === p.id)?.actual || 0), 'tool'], ['Insurance / TPA', 'Status checked', S.preauths.find(x => x.patientId === p.id)?.status || 'Self-pay', 'tool'], ['LLM', 'Discharge summary draft generated', 'Groundedness 96%', 'ai'], ['Human Approval', 'Doctor approval requested', 'Waiting', 'human']] : [['Knowledge', 'Retrieved 2 governed documents', 'Cited', 'tool'], ['HMS', 'Context lookup', 'OK', 'tool'], ['LLM', 'Output generated', 'Confidence 93%', 'ai'], ['Human Approval', a.humanApproval === 'None' ? 'No approval required (low risk)' : 'Human review requested', a.humanApproval === 'None' ? 'Done' : 'Waiting', 'human']];
      plan.forEach(([sys, what, res, k], i) => setTimeout(() => { if (a.status === 'Draft') { a.status = 'Testing'; a.statusNote = 'Testing'; } ex.steps.push({ t: fmt(S.clock), what: a.name + ' · ' + what, k, res, sys }); ex.tools++; if (i === plan.length - 1) { ex.status = res === 'Waiting' ? 'Waiting' : 'Completed'; ex.latency = (1.2 + i * 0.4).toFixed(1) + ' s'; if (res === 'Waiting') { S.approvals.unshift({ id: nid('AP'), type: a.name.includes('Discharge') ? 'Discharge summary' : 'Nursing handover', patientId: p.id, patient: p.name, agentId, agent: a.name, action: a.name.includes('Discharge') ? 'Sign discharge summary (playground run)' : 'Review agent output', risk: a.tier, reason: 'Generated by ' + a.name + ' ' + a.v, created: S.clock, sla: 120, owner: a.name.includes('Discharge') ? 'Doctor' : 'Nurse', status: 'Pending', execId: ex.id }); notify('Approval required', a.name + ' · ' + p.name, 'Medium', a.name, { page: 'approvals' }); } } emit(); }, 600 * (i + 1)));
      return ex.id; },
    runEvaluation(agentId, role) { const a = S.agents.find(x => x.id === agentId); a.evalRunning = true; emit(); setTimeout(() => { const e = { id: nid('EV'), agentId, agent: a.name, version: a.v, ts: S.clock, cases: 120, accuracy: int(90, 97), groundedness: int(94, 99), hallucination: (rnd() * 1.5).toFixed(1), refusal: int(92, 99), escalation: int(90, 99), completion: int(90, 97), toolSuccess: int(96, 100), latency: (1.5 + rnd() * 1.5).toFixed(1) + ' s', override: (rnd() * 5).toFixed(1), result: 'Pass' }; a.evals.unshift(e); S.evals.unshift(e); a.evalRunning = false; const v = a.config.versions[a.config.versions.length - 1]; if (v) v.score = e.accuracy; notify('Evaluation complete', a.name + ' · ' + e.cases + ' cases · accuracy ' + e.accuracy + '% · Pass', 'Low', 'AI Evaluation', { page: 'agent', id: agentId }); audit(role, 'Evaluation run', agentId, '—', 'Pass ' + e.accuracy + '%', '120 synthetic cases', '—', 'AI Evaluation', e.id); emit(); }, 1800); },
    publishAgent(agentId, role, changes) { const a = S.agents.find(x => x.id === agentId); if (!api.publishRequest(agentId, role)) return; const parts = a.v.split('.').map(Number); const nv = a.status === 'Draft' || a.status === 'Testing' ? '1.0.0' : parts[0] + '.' + (parts[1] + 1) + '.0'; a.config.versions.forEach(v => v.published = false); a.config.versions.push({ v: nv, created: 'today ' + fmt(S.clock), author: role, changes: changes || 'Published from builder', score: a.evals[0]?.accuracy || '—', approval: 'Approved · ' + role, published: true }); a.v = nv; a.status = 'Published'; a.statusNote = 'Active'; notify('Agent published', a.name + ' v' + nv, 'Medium', 'Agent Registry', { page: 'agent', id: agentId }); audit(role, 'Agent published', agentId, a.config.versions.length > 1 ? 'v' + a.config.versions[a.config.versions.length - 2].v : 'Draft', 'v' + nv, changes || '—', role, 'Agent Builder'); emit(); },
    rollbackAgent(agentId, role) { const a = S.agents.find(x => x.id === agentId); const vs = a.config.versions; if (vs.length < 2) return; vs.pop(); vs[vs.length - 1].published = true; a.v = vs[vs.length - 1].v; notify('Agent rolled back', a.name + ' → v' + a.v, 'Medium', 'Agent Registry', { page: 'agent', id: agentId }); audit(role, 'Agent rollback', agentId, '—', 'v' + a.v, 'Manual rollback', role, 'Agent Builder'); emit(); },
    setAgentStatus(agentId, status, role) { const a = S.agents.find(x => x.id === agentId); const before = a.status; a.status = status; a.statusNote = status === 'Published' ? 'Active' : status; audit(role, 'Agent status changed', agentId, before, status, '—', role, 'Agent Registry'); emit(); },
    saveAgent(agentId, patch, role) { const a = S.agents.find(x => x.id === agentId); Object.keys(patch).forEach(k => Object.assign(a.config[k], patch[k])); if (patch.identity && patch.identity.nameEn) a.name = patch.identity.nameEn; audit(role, 'Agent draft saved', agentId, '—', 'Draft', Object.keys(patch).join(', '), '—', 'Agent Builder'); emit(); },
    createAgent(cfg, role) { const id = 'AG-' + (S.agents.length + 1); const base = S.agents.find(x => x.name === 'Discharge Orchestration Agent'); const a = { id, name: cfg.nameEn, v: '0.1.0', owner: 'Operations', tier: cfg.tier || 'Medium', status: 'Draft', statusNote: 'Draft', success: '—', lastRun: '—', runs: 0, type: cfg.type || 'Workflow Agent', humanApproval: 'Required', config: JSON.parse(JSON.stringify(base.config)), evals: [] }; a.config.identity = { nameEn: cfg.nameEn, nameTa: cfg.nameTa || '—', purpose: cfg.purpose, type: cfg.type || 'Workflow Agent' }; a.config.tools = TOOLS.map(t => ({ tool: t, read: (cfg.tools || []).includes(t), write: /Messaging|Housekeeping|Scheduling/.test(t) && (cfg.tools || []).includes(t), approval: /Billing|Insurance/.test(t), enabled: (cfg.tools || []).includes(t) })); a.config.versions = []; S.agents.unshift(a); notify('Agent draft created', cfg.nameEn + ' · Draft', 'Low', 'Agent Registry', { page: 'agent', id }); audit(role, 'Agent created (draft)', id, '—', 'Draft', cfg.nameEn, '—', 'Agent Builder'); emit(); return id; },
    mutate(fn, actor, action, entity, after, note) { fn(S); if (action) audit(actor, action, entity, '—', after || '—', note || '—', '—', 'Web'); emit(); },
    notify(t, b, p, s, l) { notify(t, b, p, s, l); emit(); },
    markRead(id) { const n = S.notifications.find(x => x.id === id); if (n) n.read = true; emit(); },
    markAllRead() { S.notifications.forEach(n => n.read = true); emit(); },
    radiologistFinal(radId, decision, role) { const r = S.radiology.find(x => x.id === radId); if (!r) return; r.radStatus = 'Read'; r.final = decision; r.readBy = role; audit(role, 'Radiology final read', r.patientId, r.aiStatus, decision, 'AI screening ' + r.aiProb, role, 'PACS'); notify('Study finalised', r.study + ' · ' + r.patient + ' · ' + decision, 'Low', 'Radiology', { page: 'radiology', id: radId }); emit(); },
    ask(q, role) { const s = q.toLowerCase(); const now = S.clock; const links = []; let answer, sources = ['Live synthetic dataset'];
      const dis = S.discharges.filter(d => !d.completed); const blocked = dis.filter(d => dischargeState(d).k === 'blocked');
      if (/kavitha|not ready|why is .* discharge/.test(s)) { const d = S.discharges.find(x => x.patientId === D.kavitha.uhid); const st = dischargeState(d); const open = Object.entries(d.deps).filter(([, x]) => x.status !== 'done'); answer = d.completed ? 'Kavitha Raman was discharged at ' + fmt(d.dischargedAt) + '. Follow-up booked 19 Sep 10:30 with Dr. Arjun Menon; feedback request sent.' : 'Kavitha Raman (C-412) is ' + st.label.toLowerCase() + '. Open dependencies: ' + (open.map(([k, x]) => k + ' (' + x.status + (x.note ? ' — ' + x.note : '') + ')').join('; ') || 'none') + '. Predicted ready ' + d.eta + '. Doctors sign, Billing releases; the agent coordinates.'; links.push({ label: 'Discharge case', page: 'discharge-case', id: d.id }, { label: 'Patient 360', page: 'patient', id: d.patientId }, { label: 'Agent trace', page: 'execution', id: 'EXE-2026-118204' }); sources = ['Discharge board (live)', 'TPA status', 'Approval queue']; }
      else if (/blocked|delay/.test(s) && /discharge/.test(s)) { answer = blocked.length + ' of ' + dis.length + ' active discharges are blocked: ' + blocked.map(d => d.patient + ' — ' + dischargeState(d).dep + ' (' + d.deps[dischargeState(d).dep].note + ')').join('; ') + '. Median turnaround today ' + api.kpis().turnaround + '.'; blocked.forEach(d => links.push({ label: d.patient, page: 'discharge-case', id: d.id })); sources = ['Discharge board (live)']; }
      else if (/insurance|preauth/.test(s)) { const old = S.preauths.filter(p => /Query|Additional|Missing|Partially|High Denial|Submitted/.test(p.status) && now - p.createdTs > 120); answer = old.length + ' insurance cases have been open for more than 2 hours: ' + old.map(p => p.patient + ' — ' + p.status + ' (' + ago(p.createdTs, now) + ')').join('; ') + '.'; old.forEach(p => links.push({ label: p.id, page: 'insurance', id: p.id })); sources = ['Preauthorisation queue', 'TPA feed']; }
      else if (/critical/.test(s)) { const c = S.labs.filter(l => l.critical && !l.acked); answer = c.length ? c.length + ' critical results await acknowledgement: ' + c.map(l => l.patient + ' — ' + l.analyte + ' ' + l.value + ' ' + l.unit + ' (' + l.clinician + ', ' + ago(l.notified, now) + (l.escalated ? ', escalated' : '') + ')').join('; ') + '. I can notify, not interpret.' : 'All critical results have been acknowledged.'; c.forEach(l => links.push({ label: l.patient, page: 'laboratory', id: l.id })); sources = ['LIS critical-value log']; }
      else if (/agent/.test(s) && /fail/.test(s)) { const f = S.executions.filter(e => e.status === 'Failed' && e.ts >= 0); answer = f.length + ' agent executions failed today: ' + f.slice(0, 5).map(e => e.agent + ' (' + e.failure + ')').join('; ') + (f.length > 5 ? ' …' : '') + '. Most are LIS timeouts while the connector is degraded.'; f.slice(0, 5).forEach(e => links.push({ label: e.id, page: 'execution', id: e.id })); sources = ['Agent Runs', 'Observability']; }
      else if (/approval/.test(s)) { const p = S.approvals.filter(a => a.status === 'Pending'); answer = p.length + ' workflows require human approval: ' + p.slice(0, 6).map(a => a.type + ' — ' + a.patient + ' (' + a.owner + ', ' + ago(a.created, now) + ')').join('; ') + '.'; links.push({ label: 'Approval Centre', page: 'approvals' }); sources = ['Approval queue']; }
      else if (/waiting longer|wait/.test(s)) { answer = 'OP Cardiology is running 41 minutes past slot (Dr. Arjun Menon, 2 patients waiting beyond 30 minutes). Emergency: 3 patients waiting for beds over 60 minutes. Queue Agent has sent wait-time updates to 6 patients.'; links.push({ label: 'Appointments', page: 'appointments' }, { label: 'Emergency', page: 'emergency' }); sources = ['Queue Agent', 'OP schedule']; }
      else if (/discharge/.test(s)) { answer = dis.length + ' active discharge cases; ' + dis.filter(d => dischargeState(d).k === 'ready').length + ' ready, ' + blocked.length + ' blocked, ' + dis.filter(d => dischargeState(d).k === 'approval').length + ' awaiting approval.'; links.push({ label: 'Discharge board', page: 'discharge' }); }
      else { answer = "I don't have enough verified information to answer this safely. I have routed your question to the Operations duty manager and logged a knowledge gap."; sources = ['No governed source matched (confidence 38%)']; audit('Management Copilot', 'Query refused · low confidence', '—', '—', 'Routed to human', q, '—', 'Command bar'); }
      audit(role + ' · Management Copilot', 'Ask Hospital AI', '—', '—', 'Answered', q, '—', 'Command bar'); return { q, answer, links, sources, ts: fmt(now), confidence: links.length ? int(88, 96) + '%' : '38%' }; },
    reset() { window.location.reload(); },
    dispense(txId, role) { const t = S.pharmacy.find(x => x.id === txId); if (!t || t.status !== 'Pending') return; const key = t.drug.split(' ')[0].toLowerCase(); const it = (S.items || []).find(i => i.name.toLowerCase().startsWith(key)); if (it && it.expiryState === 'Expired') { notify('Dispense blocked', t.drug + ' · batch ' + it.batch + ' expired · substitute batch required', 'High', 'Pharmacy', { page: 'pharmacy', id: txId }); audit(role, 'Dispense BLOCKED · expired batch', t.patientId, 'Pending', 'Pending', it.batch, '—', 'Pharmacy'); emit(); return; } if (it && it.qty < t.qty) { notify('Dispense blocked', t.drug + ' · only ' + it.qty + ' ' + it.unit + ' in stock · requisition suggested', 'High', 'Pharmacy', { page: 'inventory', id: it.id }); audit(role, 'Dispense BLOCKED · insufficient stock', t.patientId, 'Pending', 'Pending', it.name, '—', 'Pharmacy'); emit(); return; } t.status = 'Dispensed'; t.by = role; t.batch = it ? it.batch : '—'; if (it) { it.qty -= t.qty; it.value = it.qty * it.price; it.status = it.qty === 0 ? 'Out of stock' : it.qty < it.min ? 'Low stock' : 'In stock'; S.stockTx.unshift({ id: nid('STX'), itemId: it.id, item: it.name, type: 'Issue', qty: -t.qty, from: it.store, to: t.ward, by: role, ts: S.clock, status: 'Posted', reason: txId }); if (it.status !== 'In stock') notify(it.status, it.name + ' · ' + it.qty + ' ' + it.unit + ' left after dispensing', 'Medium', 'Inventory', { page: 'inventory', id: it.id }); } const b = S.bills.find(x => x.patientId === t.patientId && /Provisional|Pending/.test(x.status)); const price = it ? it.price * t.qty : 120; if (b) { b.actual += price; b.patientShare = Math.max(0, b.actual - b.insurance); if (b.lines) { const ln = b.lines.find(l => /Pharmacy/.test(l.item)); if (ln) ln.cur += price; } } audit(role, 'Medication dispensed · stock deducted · charged ' + inr(price), t.patientId, 'Pending', 'Dispensed', t.drug + (it ? ' · ' + it.batch : ''), '—', 'Pharmacy', txId); emit(); },
    createMaster(coll, data, role) { const list = S[coll]; if (!list) return { error: 'Unknown collection' }; const req = Object.entries(data).filter(([k, v]) => k !== 'active' && (v === '' || v == null)); if (req.length) return { error: 'Required: ' + req.map(([k]) => k).join(', ') }; const nameKey = data.name != null ? 'name' : data.title != null ? 'title' : null; if (nameKey && list.some(x => String(x[nameKey] || '').toLowerCase() === String(data[nameKey]).toLowerCase())) return { error: 'Duplicate: a record named "' + data[nameKey] + '" already exists' }; const pre = { patients: 'MER-2026-', doctors: 'DR-', staff: 'ST-', departments: 'DEP-', services: 'SV-', vendors: 'VEN-', insurers: 'INS-', taxes: 'TAX-', employees: 'EMP-' }[coll] || coll.slice(0, 3).toUpperCase() + '-'; const id = pre + String(list.length + 1 + int(0, 9)).padStart(3, '0'); const rec = { id, ...data, active: true, status: data.status || 'Active', created: fmt(S.clock), createdBy: role }; if (coll === 'patients') Object.assign(rec, { uhid: id, type: 'OP', status: 'Registered', risk: [], hero: false, doctorId: (S.doctors.find(d => d.name === data.doctor) || {}).id, procedure: '—' }); if (coll === 'doctors') Object.assign(rec, { role: 'Consultant ' + data.dept }); if (coll === 'taxes') Object.assign(rec, { code: id, igst: +data.cgst + +data.sgst, rate: +data.cgst + +data.sgst, type: 'GST', inclusive: false, effective: S.day.slice(4), note: 'DEMO CONFIGURATION' }); if (coll === 'staff') { S.employees && S.employees.unshift({ ...rec, empId: 'EMP-' + String(S.employees.length + 1).padStart(3, '0'), designation: data.role, joined: '2026-09-13', employment: 'Active', manager: S.doctors[0].name, location: 'Kolathur', qualification: 'Graduate', salary: '₹30,000', today: 'Present', shift: 'Morning' }); } list.unshift(rec); notify('Record created', coll + ' · ' + (rec.name || rec.title || id), 'Low', 'Administration', { page: ({ patients: 'patients', doctors: 'clinical', staff: 'users', departments: 'departments', services: 'services', vendors: 'vendors', insurers: 'insurers', taxes: 'tax' })[coll] || 'command', id }); audit(role, coll + ' record created', id, '—', 'Active', rec.name || rec.title || '—', '—', 'Administration'); emit(); return { id }; },
    updateMaster(coll, id, patch, role) { const r = (S[coll] || []).find(x => x.id === id); if (!r) return; const before = JSON.stringify(Object.fromEntries(Object.keys(patch).map(k => [k, r[k]]))); Object.assign(r, patch); audit(role, coll + ' record edited', id, before, JSON.stringify(patch), '—', '—', 'Administration'); emit(); },
    ingest(id, role, title) { let k = S.knowledge.find(x => x.id === id); if (!k) { k = { id: 'KD-' + String(S.knowledge.length + 1).padStart(2, '0'), title: title || 'Uploaded document ' + (S.knowledge.length + 1), dept: 'Knowledge Manager', v: '1.0', eff: S.day.slice(4), review: '2027', status: 'Uploaded', lang: 'EN', chunks: 0, retrievals: 0 }; S.knowledge.unshift(k); audit(role, 'Document uploaded', k.id, '—', 'Uploaded', k.title, '—', 'Knowledge base'); } const steps = [['Processing', 'Parse + PII/PHI scan'], ['Processing', 'Chunk (58) + embed'], [rnd() < 0.15 ? 'Failed' : 'Indexed', 'Index + validate']]; emit(); steps.forEach(([stt, what], i) => setTimeout(() => { k.status = stt; if (stt === 'Indexed') { k.chunks = 58; k.indexed = fmt(S.clock); notify('Document indexed', k.title + ' · 58 chunks · awaiting owner approval to publish', 'Low', 'Knowledge base', { page: 'knowledge', id: k.id }); setTimeout(() => { if (k.status === 'Indexed') { k.status = 'Draft'; emit(); } }, 1500); } if (stt === 'Failed') notify('Ingestion failed', k.title + ' · OCR failed on page 3 · retry available', 'Medium', 'Knowledge base', { page: 'knowledge', id: k.id }); audit('Knowledge pipeline', 'Ingestion · ' + what, k.id, '—', stt, '—', '—', 'Knowledge base'); emit(); }, 900 * (i + 1))); return k.id; },
    // ---------- ERP transactions ----------
    escalate(id, role) { const a = S.approvals.find(x => x.id === id); if (!a || a.status !== 'Pending') return; a.escalated = true; a.escalatedTo = ({ Doctor: 'Medical Director', Billing: 'Finance Head', Insurance: 'Insurance Supervisor', Nurse: 'Nursing Superintendent' })[a.owner] || 'COO'; a.reason += ' · ESCALATED to ' + a.escalatedTo; notify('Approval escalated', a.type + ' · ' + a.patient + ' → ' + a.escalatedTo, 'High', 'Approval Centre', { page: 'approvals', id }); audit(role, 'Approval escalated · ' + a.type, a.patientId, a.owner, a.escalatedTo, 'SLA pressure', '—', 'Approval Centre', a.execId); emit(); },
    createAppointment(f, role) { const p = patient(f.patientId) || S.patients[0]; const doc = S.doctors.find(d => d.id === f.doctorId) || S.doctors[0]; const ts = f.day * 1440 + f.min; const clash = S.appointments.find(a => a.doctorId === doc.id && a.ts === ts && /Scheduled|Confirmed/.test(a.status)); if (clash) return { error: 'Slot conflict: ' + doc.name + ' already has ' + clash.patient + ' at ' + fmt(ts) + '. Overbooking requires Front Office lead.' }; const a = { id: nid('APT'), patientId: p.id, patient: p.name, doctorId: doc.id, doctor: doc.name, dept: doc.dept, ts, status: f.walkIn ? 'Checked-in' : 'Confirmed', channel: f.walkIn ? 'Walk-in' : 'Front desk', type: f.type || 'New', token: f.walkIn ? 'T-' + (40 + S.appointments.filter(x => x.status === 'Checked-in').length) : null }; S.appointments.unshift(a); S.comms.push({ id: nid('MSG'), patientId: p.id, patient: p.name, ts: S.clock, channel: 'WhatsApp', type: 'Appointment confirmed · ' + fmt(ts) + ' · ' + doc.name, lang: p.lang === 'Tamil' ? 'TA' : 'EN', status: 'Delivered', agent: 'AG-01' }); notify('Appointment ' + (f.walkIn ? 'walk-in checked in' : 'confirmed'), p.name + ' · ' + doc.name + ' · ' + fmt(ts), 'Low', 'Appointment Agent', { page: 'appointments', id: a.id }); audit(role, 'Appointment created', p.id, '—', a.status, doc.name + ' ' + fmt(ts), '—', 'Front Office', a.id); emit(); return { id: a.id }; },
    advanceAppointment(id, role) { const a = S.appointments.find(x => x.id === id); if (!a) return; const flow = { Scheduled: 'Confirmed', Confirmed: 'Checked-in', 'Checked-in': 'In consultation', 'In consultation': 'Completed' }; const before = a.status, next = flow[before]; if (!next) return; a.status = next; if (next === 'Checked-in') a.token = 'T-' + (40 + S.appointments.filter(x => x.status === 'Checked-in').length); if (next === 'Completed') { const e = { id: nid('ENC'), patientId: a.patientId, patient: a.patient, doctor: a.doctor, dept: a.dept, ts: S.clock, type: 'OP consultation', status: 'Closed', signedBy: role }; S.encounters.unshift(e); S.bills.unshift({ id: nid('BILL'), patientId: a.patientId, patient: a.patient, admissionId: '—', estimate: 800, actual: 800, insurance: 0, patientShare: 800, status: 'Pending', dispute: null }); S.comms.push({ id: nid('MSG'), patientId: a.patientId, patient: a.patient, ts: S.clock, channel: 'Mobile push', type: 'Consultation complete · prescription + bill ready', lang: 'EN', status: 'Delivered', agent: 'AG-02' }); } audit(role, 'Appointment → ' + next, a.patientId, before, next, '—', '—', 'Front Office', a.id); emit(); },
    createAdmission(f, role) { const p = patient(f.patientId); if (!p) return; const pre = ({ Cardiology: 'C', 'Cardiac Surgery': 'CT', Orthopaedics: 'O', 'General Surgery': 'S', Nephrology: 'N', Oncology: 'ON', Neurology: 'NR', Obstetrics: 'M', Pulmonology: 'P', Paediatrics: 'PD', Gastroenterology: 'G', Urology: 'U', ENT: 'E' })[f.dept] || 'S'; const b = S.beds.find(x => x.id.startsWith(pre + '-') && x.status === 'Free' && (!f.cls || x.cls === f.cls)) || S.beds.find(x => x.id.startsWith(pre + '-') && x.status === 'Free') || S.beds.find(x => x.status === 'Free' && x.dept !== 'Emergency'); if (!b) return { error: 'No free bed in ' + f.dept + '. Bed manager notified; patient placed on waiting list.' }; const outlier = b.dept !== f.dept; b.status = 'Reserved'; b.patientId = p.id; const ad = { id: nid('IP-2026'), patientId: p.id, patient: p.name, doctor: p.doctor, dept: f.dept, bed: b.id + ' · ' + b.dept + (outlier ? ' (outlier bed)' : ''), admitted: S.day.slice(4), status: 'Approved', estimate: f.estimate || 120000 }; if (outlier) notify('Outlier bed assigned', p.name + ' (' + f.dept + ') placed in ' + b.dept + ' ward · bed ' + b.id + ' · no free ' + f.dept + ' bed · transfer when available', 'Medium', 'Bed manager', { page: 'facilities' }); S.admissions.unshift(ad); const bill = { id: nid('BILL'), patientId: p.id, patient: p.name, admissionId: ad.id, estimate: ad.estimate, actual: Math.round(ad.estimate * 0.35), insurance: 0, patientShare: Math.round(ad.estimate * 0.35), status: 'Provisional', dispute: null }; S.bills.unshift(bill); if (p.insurer && p.insurer !== 'Self-pay') { S.preauths.unshift({ id: nid('PA-2026'), patient: p.name, patientId: p.id, insurer: p.insurer, policy: p.policy || '—', procedure: f.reason || 'Admission', estimate: inr(ad.estimate), requested: inr(ad.estimate), approved: '—', liability: '—', submitted: '—', status: 'Draft', missing: 'Doctor advice', risk: '12%', exec: 'R. Sundar', createdTs: S.clock, docs: { required: ['Preauth form', 'Doctor advice', 'Estimate', 'ID + policy'], missing: ['Doctor advice'] }, completeness: 75, history: [{ t: fmt(S.clock), e: 'Draft assembled by Preauthorisation Agent on admission' }] }); notify('Preauth draft assembled', p.name + ' · ' + p.insurer + ' · missing doctor advice', 'Medium', 'Insurance Preauth Agent', { page: 'insurance' }); }
      setTimeout(() => { b.status = 'Occupied'; ad.status = 'Admitted'; p.type = 'IP'; p.status = 'Admitted'; p.bed = ad.bed; p.bedId = b.id; p.dept = f.dept; p.procedure = f.reason || p.procedure; S.encounters.unshift({ id: nid('ENC'), patientId: p.id, patient: p.name, doctor: p.doctor, dept: f.dept, ts: S.clock, type: 'Inpatient', status: 'Open' }); const er = S.emergency.find(x => x.patientId === p.id && x.status !== 'Closed'); if (er) er.status = 'Admitted'; notify('Patient admitted', p.name + ' · bed ' + b.id + ' · ' + f.dept, 'Medium', 'Admissions', { page: 'patient', id: p.id }); audit(role, 'Admission completed · bed occupied', p.id, 'Reserved', 'Occupied', b.id, '—', 'HMS', ad.id); emit(); }, 1500);
      audit(role, 'Admission created · bed reserved' + (outlier ? ' · OUTLIER BED (' + b.dept + ' ward)' : ''), p.id, p.type, 'Reserved ' + b.id, f.reason || '—', '—', 'HMS', ad.id); emit(); return { id: ad.id }; },
    recordPayment(billId, amount, mode, role) { const b = S.bills.find(x => x.id === billId); if (!b) return; b.paid = (b.paid || 0) + amount; S.payments.unshift({ id: nid('PAY'), billId, patient: b.patient, amount, mode, ts: S.clock, status: 'Success' }); if (b.paid >= b.patientShare) b.status = b.admissionId === '—' ? 'Paid' : 'Settled'; else b.status = 'Part-paid'; S.comms.push({ id: nid('MSG'), patientId: b.patientId, patient: b.patient, ts: S.clock, channel: 'SMS', type: 'Receipt ' + inr(amount) + ' · ' + mode, lang: 'EN', status: 'Delivered', agent: '—' }); audit(role, 'Payment recorded', b.patientId, inr(b.paid - amount), inr(b.paid), mode, '—', 'Billing', billId); emit(); },
    requestVoid(billId, reason, role) { const b = S.bills.find(x => x.id === billId); if (!b) return; S.approvals.unshift({ id: nid('AP'), type: 'Billing void', patientId: b.patientId, patient: b.patient, agentId: '—', agent: 'Billing (human request)', action: 'Void bill ' + b.id + ' · ' + inr(b.actual), risk: 'High', reason, created: S.clock, sla: 240, owner: 'Hospital Management', status: 'Pending', entityId: billId }); b.status = 'Void requested'; notify('Void requested', b.id + ' · requires Finance approval', 'Medium', 'Billing', { page: 'approvals' }); audit(role, 'Bill void requested', b.patientId, 'Active', 'Void requested', reason, 'Pending', 'Billing', billId); emit(); },
    requestRefund(billId, amount, reason, role) { const b = S.bills.find(x => x.id === billId); if (!b) return; S.approvals.unshift({ id: nid('AP'), type: 'Refund', patientId: b.patientId, patient: b.patient, agentId: '—', agent: 'Billing (human request)', action: 'Refund ' + inr(amount) + ' on ' + b.id, risk: 'Medium', reason, created: S.clock, sla: 240, owner: 'Hospital Management', status: 'Pending', entityId: billId, amount }); notify('Refund requested', b.patient + ' · ' + inr(amount), 'Medium', 'Billing', { page: 'approvals' }); audit(role, 'Refund requested', b.patientId, '—', inr(amount), reason, 'Pending', 'Billing', billId); emit(); },
    submitClaim(patientId, role) { const p = S.preauths.find(x => x.patientId === patientId && x.status === 'Approved'); if (!p) return; p.status = 'Claim Submitted'; p.history.unshift({ t: fmt(S.clock), e: 'Claim submitted with final bill and signed summary' }); notify('Claim submitted', p.patient + ' · ' + p.insurer, 'Low', 'Claim Denial Agent', { page: 'insurance', id: p.id }); audit(role, 'Claim submitted', patientId, 'Approved', 'Claim Submitted', '—', '—', 'TPA portal'); emit(); },
    simulateSettlement(patientId) { const p = S.preauths.find(x => x.patientId === patientId && p_ok(x)); function p_ok(x) { return x.status === 'Claim Submitted'; } if (!p) return false; p.status = 'Settled'; p.history.unshift({ t: fmt(S.clock), e: 'Insurer settled ' + p.approved }); const b = S.bills.find(x => x.patientId === patientId && x.admissionId !== '—'); if (b) { b.status = 'Settled'; S.payments.unshift({ id: nid('PAY'), billId: b.id, patient: b.patient, amount: b.insurance, mode: 'Insurer NEFT', ts: S.clock, status: 'Success' }); } notify('Claim settled', p.patient + ' · ' + p.insurer + ' · ' + p.approved, 'Low', 'Insurance / TPA (simulated)', { page: 'insurance', id: p.id }); audit('Insurance / TPA (simulated)', 'Claim settled', patientId, 'Claim Submitted', 'Settled', '—', '—', 'TPA connector'); emit(); return true; },
    pauseDischarge(dId, role, reason) { const d = S.discharges.find(x => x.id === dId); if (!d || d.completed) return; d.paused = !d.paused; if (d.paused) { d.pauseReason = reason; log(d, role, 'Workflow PAUSED · ' + reason, 'warn'); step('EXE-2026-118204', role, 'Clinical deterioration reported · agent paused all outbound coordination', 'warn', 'Paused'); notify('Discharge paused', d.patient + ' · ' + reason + ' · agent stopped; family message held', 'High', 'Discharge Orchestration Agent', { page: 'discharge-case', id: dId }); } else { log(d, role, 'Workflow resumed', 'human'); step('EXE-2026-118204', role, 'Workflow resumed by clinician', 'human', 'Resumed'); } audit(role, d.paused ? 'Discharge paused' : 'Discharge resumed', d.patientId, d.paused ? 'Active' : 'Paused', d.paused ? 'Paused' : 'Active', reason || '—', '—', 'Discharge board', 'EXE-2026-118204'); emit(); },
    advanceLab(labId, role) { const l = S.labs.find(x => x.id === labId); if (!l) return; const flow = ['Ordered', 'Collected', 'Processing', 'Result available', 'Verified', 'Released']; const cur = l.status === 'Pending' ? 'Ordered' : l.status === 'Final' ? 'Released' : l.status; const i = flow.indexOf(cur); if (i < 0 || i === flow.length - 1) return; l.status = flow[i + 1]; if (l.status === 'Released') { l.status = 'Final'; const o = S.orders.find(x => x.id === l.orderId); if (o) o.status = 'Resulted'; S.comms.push({ id: nid('MSG'), patientId: l.patientId, patient: l.patient, ts: S.clock, channel: 'WhatsApp', type: 'Report ready · ' + l.test + ' (no interpretation)', lang: 'TA', status: 'Delivered', agent: 'AG-10' }); } audit(role, 'Lab ' + cur + ' → ' + (l.status === 'Final' ? 'Released' : l.status), l.patientId, cur, l.status, '—', '—', 'LIS', l.orderId); emit(); },
    amendLab(labId, value, reason, role) { const l = S.labs.find(x => x.id === labId); if (!l) return; l.history = l.history || []; l.history.unshift({ t: fmt(S.clock), v: l.value, by: role, reason }); const before = l.value; l.value = value; l.version = (l.version || 1) + 1; notify('Result amended', l.patient + ' · ' + l.analyte + ' ' + before + ' → ' + value + ' · clinician notified', 'High', 'Laboratory', { page: 'laboratory', id: labId }); audit(role, 'Lab result amended (v' + l.version + ')', l.patientId, String(before), String(value), reason, '—', 'LIS', l.orderId); emit(); },
    returnMedication(txId, role) { const t = S.pharmacy.find(x => x.id === txId); if (!t || t.status !== 'Dispensed') return; t.status = 'Returned'; const b = S.bills.find(x => x.patientId === t.patientId && x.status === 'Provisional'); if (b) { b.actual -= 250; b.patientShare = Math.max(0, b.actual - b.insurance); } audit(role, 'Medication returned · charge reversed', t.patientId, 'Dispensed', 'Returned', t.drug, '—', 'Pharmacy', txId); emit(); },
    cancelExecution(exId, role) { const e = S.executions.find(x => x.id === exId); if (!e || !/Running|Waiting|Queued/.test(e.status)) return; e.status = 'Cancelled'; e.steps.push({ t: fmt(S.clock), what: role + ' · execution cancelled', k: 'human', res: 'Cancelled' }); S.approvals.filter(a => a.execId === exId && a.status === 'Pending').forEach(a => { a.status = 'Cancelled'; }); audit(role, 'Agent execution cancelled', e.patientId, 'Running', 'Cancelled', '—', '—', 'Agent Runs', exId); emit(); },
    simulateAgentFailure() { const a = S.agents.find(x => x.name.includes('Diagnostic')); const p = S.patients[5]; const ex = { id: nid('EXE-2026'), agentId: a.id, agent: a.name, version: a.v, patientId: p.id, patient: p.name, ts: S.clock, status: 'Failed', failure: 'LIS timeout after 3 retries', steps: [{ t: fmt(S.clock), what: 'Orchestrator · context assembled', k: 'policy', res: 'OK' }, { t: fmt(S.clock), what: a.name + ' · Tool: LIS lookup', k: 'tool', res: 'Timeout' }, { t: fmt(S.clock), what: a.name + ' · retry ×3 failed · manual fallback task created for Laboratory', k: 'warn', res: 'Failed' }], latency: '30.0 s', cost: '₹6', tools: 3, handoffs: [] }; S.executions.unshift(ex); notify('Agent execution failed', a.name + ' · LIS timeout · manual fallback task created', 'High', 'AI Observability', { page: 'execution', id: ex.id }); audit('Orchestrator', 'Agent execution failed', p.id, 'Running', 'Failed', 'LIS timeout', '—', 'Orchestrator', ex.id); emit(); },
    breakGlass(patientId, role, reason) { S.breakGlass = S.breakGlass || {}; S.breakGlass[role + ':' + patientId] = S.clock; notify('Break-glass access', role + ' opened clinical record ' + patientId + ' · ' + reason + ' · Governance notified', 'High', 'Policy Gateway', { page: 'audit' }); audit(role, 'BREAK-GLASS clinical access', patientId, 'Masked', 'Unmasked (30 min)', reason, 'Post-hoc review', 'Policy Gateway'); emit(); },
    hasBreakGlass(patientId, role) { return !!(S.breakGlass && S.breakGlass[role + ':' + patientId] != null && S.clock - S.breakGlass[role + ':' + patientId] <= 30); },
    publishRequest(agentId, role) { const a = S.agents.find(x => x.id === agentId); if (a.tier === 'High' && role !== 'Governance Officer') { a.status = 'Approval'; a.statusNote = 'Awaiting governance approval'; S.approvals.unshift({ id: nid('AP'), type: 'Agent publish', patientId: '—', patient: a.name, agentId: a.id, agent: a.name, action: 'Publish ' + a.name + ' (risk tier High)', risk: 'High', reason: 'Evaluation ' + (a.evals[0] ? a.evals[0].accuracy + '% · Pass' : 'not run'), created: S.clock, sla: 1440, owner: 'Governance Officer', status: 'Pending' }); notify('Publish requires governance approval', a.name + ' · risk tier High', 'Medium', 'Agent Registry', { page: 'approvals' }); audit(role, 'Publish requested', agentId, a.status, 'Approval', 'High-tier agent', 'Pending', 'Agent Builder'); emit(); return false; } return true; },
  };
  api._h = { audit, notify, emit, nid, rnd, int, pick, step, patient, fmt, ago, inr };
  return api;
}

return { fmt, ago, inr, ROLES, APPROVER, createStore };
})();
window.__mm2.views = (function(){
// Page view-models for the Hospital prototype. Pure functions over the store API.
const G = 'oklch(0.4 0.12 150)', GB = 'oklch(0.95 0.04 150)', A = 'oklch(0.5 0.13 70)', AB = 'oklch(0.96 0.05 80)', RD = 'oklch(0.45 0.17 25)', RB = 'oklch(0.96 0.03 25)', V = 'oklch(0.45 0.1 300)', VB = 'oklch(0.96 0.03 300)', N = '#52585e', NB = '#eef0f1', T = 'oklch(0.45 0.1 200)', TB = 'oklch(0.95 0.03 200)';
const pill = s => { s = String(s || ''); if (/^(Approved|Published|Completed|Done|Ready|Delivered|Read|Success|Final|Normal|Healthy|Pass|Closed|Resolved|Settled|Paid|Confirmed|On duty|Free|Active|Cleared|Booked|Released|Granted|Allowed|Dispensed|Screened)/.test(s)) return { bg: GB, fg: G }; if (/Pending|Waiting|Partially|Query|Submitted|Approval|In progress|Review|Scheduled|Proposed|Draft|Testing|Queued|Unread|Investigating|Contained|Observation|Cleaning|Pilot|Running|Changes|Provisional|Sample|Ordered|Reminder|Awaiting|Assessment|Treatment|Rescheduled|Medium|Occupied|Silent|Appeal/.test(s)) return { bg: AB, fg: A }; if (/Rejected|Blocked|Failed|Critical|High|Denied|Missing|Expired|Conflict|Suspended|Disabled|Open|Escalated|Stock-out|No-show|Cancelled|Frustrated|Degraded|Disputed|On leave|Negative|Priority|Refused|Returned/.test(s)) return { bg: RB, fg: RD }; if (/AI|Agent/.test(s)) return { bg: VB, fg: V }; return { bg: NB, fg: N }; };
const c = (t, o = {}) => ({ t: t == null ? '—' : String(t), c: o.c || '', bg: o.bg || '', m: !!o.m, b: !!o.b, pill: !!o.p });
const P = s => ({ ...c(s), ...pill(s), pill: true });
const kv = (k, v, col) => ({ k, v: v == null ? '—' : String(v), c: col || '' });

const NAV = [
  { g: 'Main', items: [['command', 'Command Centre'], ['patients', 'Patients'], ['appointments', 'Appointments'], ['admissions', 'Admissions'], ['emergency', 'Emergency'], ['clinical', 'Clinical'], ['diagnostics', 'Diagnostics'], ['laboratory', 'Laboratory'], ['radiology', 'Radiology'], ['pharmacy', 'Pharmacy'], ['billing', 'Billing'], ['insurance', 'Insurance'], ['discharge', 'Discharge'], ['feedback', 'Feedback & Service Recovery'], ['contact', 'Contact Centre'], ['staff', 'Staff'], ['hr', 'HR & Employee Service'], ['facilities', 'Facilities'], ['reports', 'Reports']] },
  { g: 'AI Platform', items: [['chat', 'Hospital Assistant'], ['ai-command', 'AI Command Centre'], ['agents', 'Agents'], ['orchestrator', 'Orchestrator'], ['runs', 'Agent Runs'], ['approvals', 'Agent Approvals'], ['exceptions', 'Exceptions'], ['knowledge', 'Knowledge Base'], ['ai-analytics', 'Assistant Analytics'], ['evals', 'AI Evaluations'], ['observability', 'AI Observability'], ['cost', 'AI Cost & Usage'], ['incidents', 'AI Incidents'], ['risk', 'AI Risk Register'], ['governance', 'Governance'], ['audit', 'Audit Trail'], ['trainer', 'AI Trainer']] },
  { g: 'Data', items: [['data-patient', 'Patient Data'], ['data-ops', 'Operational Data'], ['data-clinical', 'Clinical Data'], ['data-financial', 'Financial Data'], ['analytics', 'Analytics'], ['forecasting', 'Forecasting'], ['scenario', 'Scenario Simulator'], ['beforeafter', 'Before vs After'], ['data-quality', 'Data Quality']] },
  { g: 'Administration', items: [['users', 'Users'], ['roles', 'Roles'], ['permissions', 'Permissions'], ['identity', 'Identity & Consent'], ['integrations', 'Integrations'], ['notifications', 'Notifications'], ['config', 'Configuration']] },
];
// Which roles see which pages (everything else = restricted state)
const ALL = ['Doctor', 'Nurse', 'Front Office', 'Billing', 'Insurance', 'Radiologist', 'Laboratory', 'Pharmacy', 'Hospital Management', 'AI Administrator', 'Governance Officer'];
const ROLE_PAGES = {
  Doctor: ['command', 'patients', 'appointments', 'admissions', 'emergency', 'clinical', 'diagnostics', 'laboratory', 'radiology', 'pharmacy', 'discharge', 'approvals', 'notifications', 'knowledge', 'trainer', 'hr', 'exceptions'],
  Nurse: ['command', 'patients', 'admissions', 'emergency', 'clinical', 'diagnostics', 'laboratory', 'pharmacy', 'discharge', 'approvals', 'notifications', 'knowledge', 'trainer', 'hr', 'facilities', 'exceptions'],
  'Front Office': ['command', 'patients', 'appointments', 'admissions', 'emergency', 'billing', 'insurance', 'discharge', 'feedback', 'contact', 'notifications', 'knowledge', 'hr', 'trainer'],
  Billing: ['command', 'patients', 'admissions', 'billing', 'insurance', 'discharge', 'feedback', 'approvals', 'notifications', 'knowledge', 'data-financial', 'hr', 'exceptions'],
  Insurance: ['command', 'patients', 'admissions', 'billing', 'insurance', 'discharge', 'approvals', 'notifications', 'knowledge', 'hr', 'exceptions'],
  Radiologist: ['command', 'patients', 'diagnostics', 'radiology', 'notifications', 'knowledge', 'hr', 'trainer'],
  Laboratory: ['command', 'patients', 'diagnostics', 'laboratory', 'notifications', 'knowledge', 'hr', 'trainer', 'exceptions', 'integrations'],
  Pharmacy: ['command', 'patients', 'pharmacy', 'discharge', 'notifications', 'knowledge', 'hr', 'trainer', 'exceptions'],
  'Hospital Management': null, 'AI Administrator': null, 'Governance Officer': ['command', 'ai-command', 'ai-analytics', 'chat', 'agents', 'orchestrator', 'runs', 'approvals', 'exceptions', 'knowledge', 'evals', 'observability', 'cost', 'incidents', 'risk', 'governance', 'audit', 'identity', 'roles', 'permissions', 'users', 'integrations', 'notifications', 'config', 'data-quality', 'analytics', 'reports', 'feedback'],
};
const allowed = (role, page) => { const l = ROLE_PAGES[role]; if (l === null) return true; if (!l) return false; const base = page.replace(/^(discharge-case|execution|agent|patient|approval)$/, m => ({ 'discharge-case': 'discharge', execution: 'runs', agent: 'agents', patient: 'patients', approval: 'approvals' })[m]); return l.includes(base) || ['ask', 'portal', 'chat'].includes(page); };
const HOME = { Patient: 'portal', Doctor: 'clinical', Nurse: 'clinical', 'Front Office': 'appointments', Billing: 'billing', Insurance: 'insurance', Radiologist: 'radiology', Laboratory: 'laboratory', Pharmacy: 'pharmacy', 'Hospital Management': 'command', 'AI Administrator': 'ai-command', 'Governance Officer': 'governance' };

const RISKS = [['Wrong patient context', 'High', 'CIO', 'UHID + relationship check on every turn', 'Low'], ['Hallucination in patient-facing answers', 'High', 'Knowledge Manager', 'Citations required; refusal below 70%; groundedness monitor', 'Medium'], ['Outdated SOP retrieved', 'Medium', 'Knowledge Manager', 'Expiry enforcement; conflict detection', 'Medium ↑'], ['Unauthorised access via agent', 'High', 'CISO', 'Policy + tool gateway; RBAC tests', 'Low'], ['Incorrect Tamil translation', 'Medium', 'Patient Experience', 'Bilingual output; nurse verifies', 'Low'], ['Agent tool misuse', 'High', 'AI Programme Lead', 'Allow-listed tools; approval tier for writes', 'Low'], ['Model degradation', 'Medium', 'AI Engineering', 'Weekly eval suites; pinned versions', 'Low'], ['Integration failure (LIS, TPA)', 'Medium', 'Integration Engineer', 'Manual fallback with SLA timers', 'Medium']];
const PERMS = { Doctor: { can: ['View assigned patients', 'Create clinical notes', 'Approve discharge summary', 'Sign prescription', 'Acknowledge critical values'], cannot: ['Release final bill', 'Approve insurance submission', 'Configure agents'] }, Nurse: { can: ['View ward patients', 'Record vitals / MAR', 'Confirm handover', 'Release discharged patient'], cannot: ['Sign clinical documents', 'Release bills', 'View financial detail'] }, 'Front Office': { can: ['Register patients', 'Book / reschedule', 'View estimates', 'Raise service recovery'], cannot: ['View clinical notes', 'Approve anything financial'] }, Billing: { can: ['View financial information', 'Explain bills', 'Release final bill', 'Resolve disputes'], cannot: ['Sign clinical documents', 'View clinical notes', 'Submit to insurers'] }, Insurance: { can: ['View preauth cases', 'Submit to insurer', 'Submit appeals'], cannot: ['Release bills', 'Sign clinical documents'] }, Radiologist: { can: ['Read studies', 'Finalise reports', 'Override AI priority'], cannot: ['Approve financial items', 'Configure agents'] }, Laboratory: { can: ['Collect / process / verify / release results', 'Amend results with reason', 'Phone critical values'], cannot: ['Interpret results to patients', 'View billing'] }, Pharmacy: { can: ['Dispense', 'Accept returns', 'Transfer stock', 'Clear discharge medication'], cannot: ['Change prescriptions', 'View billing detail'] }, 'Hospital Management': { can: ['View all dashboards', 'Approve rosters', 'View aggregate AI activity'], cannot: ['Make clinical decisions', 'Configure agents', 'View clinical notes by default'] }, 'AI Administrator': { can: ['Configure agents', 'Manage tools & knowledge', 'Evaluate', 'Publish / rollback'], cannot: ['Make clinical decisions', 'Approve clinical or financial items'] }, 'Governance Officer': { can: ['View audit', 'Manage risk register', 'Suspend agents (with CIO)', 'Publish knowledge'], cannot: ['Edit agent instructions', 'Make clinical decisions'] }, Patient: { can: ['View own record', 'Book', 'Pay', 'Manage consent'], cannot: ['View other patients', 'See AI clinical interpretation'] } };

function buildList(page, api, ctx) {
  const S = api.S, now = api.clock(), { role, q } = ctx; const fmt = api.fmt, ago = t => api.ago(t, now), inr = api.inr;
  const open = (kind, id) => () => ctx.open(kind, id); const go = (p, id) => () => ctx.go(p, id);
  let L = { title: '', sub: '', cols: [], rows: [], stats: [], banner: null, filters: [] };
  const withF = (vals, key) => { L.filters = ['All'].concat(vals).map(v => ({ label: v, active: (ctx.filter || 'All') === v, on: () => ctx.setFilter(v) })); return r => (ctx.filter || 'All') === 'All' || String(r[key]) === ctx.filter; };
  switch (page) {
    case 'patients': { const f = withF(['IP', 'OP', 'ER', 'Discharged'], 'type'); L.title = 'Patients'; L.sub = S.patients.length + ' patients · shared Patient 360 across every module'; L.cols = [['UHID', 150], ['Name', 160], ['Age · Sex', 70], ['Language', 80], ['Department', 130], ['Doctor', 160], ['Insurer', 130], ['Status', 150]]; L.rows = S.patients.filter(f).map(p => ({ id: p.id, cells: [c(p.id, { m: 1 }), c(p.name, { b: 1 }), c(p.age + ' · ' + p.sex), c(p.lang), c(p.dept), c(p.doctor), c(p.insurer), P(p.status)], go: go('patient', p.id) })); break; }
    case 'appointments': { const f = withF(['Scheduled', 'Confirmed', 'Checked-in', 'In consultation', 'Completed', 'No-show', 'Rescheduled', 'Cancelled'], 'status'); L.title = 'Appointments'; L.create = 'New appointment / walk-in'; L.sub = S.appointments.filter(a => a.ts >= 0 && a.ts < 1440).length + ' today · Appointment Agent handles booking, reminders, no-show detection and rescheduling'; L.cols = [['ID', 90], ['Patient', 160], ['Doctor', 160], ['Department', 120], ['Time', 110], ['Type', 80], ['Channel', 90], ['Status', 110]]; L.rows = S.appointments.filter(f).sort((a, b) => Math.abs(a.ts - now) - Math.abs(b.ts - now)).map(a => ({ id: a.id, cells: [c(a.id, { m: 1 }), c(a.patient, { b: 1 }), c(a.doctor), c(a.dept), c(fmt(a.ts), { m: 1 }), c(a.type), c(a.channel), P(a.status)], go: open('appointment', a.id) })); break; }
    case 'admissions': { L.title = 'Admissions'; L.sub = S.admissions.filter(a => a.status === 'Admitted').length + ' current inpatients · blockers derived from discharge, insurance and billing state'; L.cols = [['Admission', 110], ['Patient', 160], ['Department', 120], ['Doctor', 160], ['Bed', 110], ['Admitted', 100], ['Estimate', 90], ['Blocker', 220], ['Status', 100]]; L.rows = S.admissions.map(a => { const d = S.discharges.find(x => x.patientId === a.patientId && !x.completed); const st = d ? api.dischargeState(d) : null; const pa = S.preauths.find(x => x.patientId === a.patientId); const bl = st && st.k === 'blocked' ? st.label : pa && /Missing|Query|Pending/.test(pa.status) ? 'Insurance · ' + pa.status : '—'; return { id: a.id, cells: [c(a.id, { m: 1 }), c(a.patient, { b: 1 }), c(a.dept), c(a.doctor), c(a.bed), c(a.admitted), c(inr(a.estimate), { m: 1 }), c(bl, { c: bl === '—' ? '' : RD }), P(a.status)], go: open('admission', a.id) }; }); break; }
    case 'emergency': { L.title = 'Emergency'; L.sub = 'Acuity is set by the treating clinician. AI surfaces results, timers and bed availability only.'; L.stats = [['Active', S.emergency.filter(e => e.status !== 'Closed').length], ['Awaiting bed', S.emergency.filter(e => e.status === 'Awaiting bed').length], ['Critical unacked', S.labs.filter(l => l.critical && !l.acked).length, RD], ['ER beds free', S.beds.filter(b => b.dept === 'Emergency' && b.status === 'Free').length]].map(([k, v, col]) => ({ k, v, c: col || '' })); L.cols = [['Case', 110], ['Patient', 160], ['Arrival', 90], ['Waiting', 80], ['Complaint', 160], ['Clinician', 160], ['Acuity', 120], ['Critical', 130], ['Status', 120]]; L.rows = S.emergency.filter(e => e.status !== 'Closed').map(e => { const cr = S.labs.find(l => l.patientId === e.patientId && l.critical && !l.acked); return { id: e.id, cells: [c(e.id, { m: 1 }), c(e.patient, { b: 1 }), c(fmt(e.arrival), { m: 1 }), c(ago(e.arrival), { m: 1 }), c(e.complaint), c(e.clinician), c(e.acuity), cr ? P('Critical · ' + cr.analyte) : c('—'), P(e.status)], go: open('emergency', e.id) }; }); break; }
    case 'clinical': { const mine = role === 'Doctor' ? S.patients.filter(p => p.doctorId === 'DR-014' && p.type === 'IP') : S.patients.filter(p => p.type === 'IP'); L.title = role === 'Nurse' ? 'Nurse Workspace · Cardiac Ward census' : 'Clinical · ' + (role === 'Doctor' ? 'Dr. Arjun Menon' : 'inpatient census'); L.sub = 'AI drafts are DRAFT — HUMAN REVIEW REQUIRED. Signing, prescribing and acknowledgement stay with clinicians.'; const pend = S.approvals.filter(a => a.status === 'Pending' && a.owner === (role === 'Nurse' ? 'Nurse' : 'Doctor')); L.stats = [['Inpatients', mine.length], ['Pending signatures', pend.length, A], ['Critical alerts', S.labs.filter(l => l.critical && !l.acked).length, RD], ['Discharge candidates', S.discharges.filter(d => !d.completed && mine.some(p => p.id === d.patientId)).length], ['AI drafts', pend.length + 2, V]].map(([k, v, col]) => ({ k, v, c: col || '' })); L.cols = [['Bed', 110], ['Patient', 170], ['Age', 50], ['Procedure / reason', 220], ['Pending results', 110], ['Discharge', 200], ['Approvals', 120], ['Critical', 120]]; L.rows = mine.map(p => { const d = S.discharges.find(x => x.patientId === p.id && !x.completed); const ap = S.approvals.filter(a => a.patientId === p.id && a.status === 'Pending').length; const cr = S.labs.find(l => l.patientId === p.id && l.critical && !l.acked); const pr = S.orders.filter(o => o.patientId === p.id && o.status !== 'Resulted').length; return { id: p.id, cells: [c((p.bed || '').split(' ')[0], { m: 1 }), c(p.name, { b: 1 }), c(p.age), c(p.procedure), c(pr ? pr + ' pending' : '—'), d ? P(api.dischargeState(d).label) : c('—'), ap ? P(ap + ' pending') : c('—'), cr ? P('Critical') : c('—')], go: go('patient', p.id) }; }); if (role === 'Doctor') L.banner = { kind: 'info', text: 'Voice dictation and Doctor Copilot are available inside each patient record. Every generated document requires your signature.' }; break; }
    case 'diagnostics': { const f = withF(['Ordered', 'Sample collected', 'In progress', 'Resulted'], 'status'); L.title = 'Diagnostic coordination'; L.sub = 'Diagnostic Coordination Agent sequences tests, sends preparation instructions (Tamil/English) and detects preparation failures. It never interprets results for patients.'; L.cols = [['Order', 90], ['Patient', 160], ['Test', 170], ['Kind', 90], ['Preparation', 110], ['Ordered', 100], ['Queue / ETA', 110], ['Status', 130]]; L.rows = S.orders.filter(f).sort((a, b) => b.ts - a.ts).map(o => ({ id: o.id, cells: [c(o.id, { m: 1 }), c(o.patient, { b: 1 }), c(o.test), c(o.kind), c(o.prep), c(fmt(o.ts), { m: 1 }), c(o.status === 'Resulted' ? '—' : '#' + (3 + (o.id.charCodeAt(6) % 9)) + ' · ' + fmt(now + 25 + (o.id.charCodeAt(7) % 40)), { m: 1 }), P(o.status)], go: open('order', o.id) })); break; }
    case 'laboratory': { const f = withF(['Critical', 'High', 'Normal'], 'flag'); L.title = 'Laboratory'; L.sub = S.labs.length + ' results · critical-value rules notify the clinician and start a 15-minute acknowledgement timer, then escalate to the department head'; if (S.lisDegraded) L.banner = { kind: 'error', text: 'LIS connector degraded — results shown may be up to 7 minutes stale. Manual fallback workflow active; verify critical values by phone with the laboratory.', action: 'Retry connector', on: () => api.toggleLis() }; L.cols = [['Result', 90], ['Patient', 160], ['Test', 110], ['Analyte', 90], ['Value', 90], ['Flag', 90], ['Clinician', 150], ['Acknowledged', 130], ['Time', 100], ['Status', 90]]; L.rows = S.labs.filter(f).sort((a, b) => (b.critical - a.critical) || b.ts - a.ts).slice(0, 60).map(l => ({ id: l.id, cells: [c(l.id, { m: 1 }), c(l.patient, { b: 1 }), c(l.test), c(l.analyte), c(l.value + ' ' + l.unit, { m: 1, c: l.critical ? RD : '' }), P(l.flag), c(l.clinician || '—'), l.critical ? (l.acked ? P('Acknowledged') : P(l.escalated ? 'Escalated · ' + ago(l.notified) : 'Timer · ' + ago(l.notified))) : c('—'), c(fmt(l.ts), { m: 1 }), P(l.status)], go: open('lab', l.id) })); break; }
    case 'radiology': { const f = withF(['Priority', 'Routine'], 'priority'); L.title = 'Radiology worklist'; L.sub = 'AI SCREENING / PRIORITIZATION ONLY — AI output does not constitute a diagnosis. The radiologist remains the final decision-maker.'; L.banner = { kind: 'warn', text: 'Radiology Screening Agent v0.9.0 is in silent validation. Probability and priority are shown to radiologists only and never to patients or referring clinicians as findings.' }; L.cols = [['Study', 120], ['Patient', 160], ['Modality', 70], ['Study', 130], ['Received', 100], ['AI status', 90], ['Priority', 90], ['AI probability', 100], ['Radiologist', 90], ['Final status', 160]]; L.rows = S.radiology.filter(f).sort((a, b) => (b.priority === 'Priority') - (a.priority === 'Priority') || b.received - a.received).map(r => ({ id: r.id, cells: [c(r.id, { m: 1 }), c(r.patient, { b: 1 }), c(r.modality), c(r.study), c(fmt(r.received), { m: 1 }), P(r.aiStatus), P(r.priority), c(r.aiStatus === 'Screened' ? (r.aiProb * 100).toFixed(0) + '%' : '—', { m: 1 }), P(r.radStatus), c(r.final)], go: open('radiology', r.id) })); break; }
    case 'pharmacy': { const f = withF(['Dispensed', 'Pending', 'Stock-out', 'Returned'], 'status'); L.title = 'Pharmacy'; L.sub = S.pharmacy.length + ' transactions · discharge medication clearance and stock-outs feed the discharge dependency graph'; L.cols = [['Txn', 90], ['Patient', 160], ['Drug', 170], ['Qty', 50], ['Ward', 130], ['Time', 100], ['Status', 110]]; L.rows = S.pharmacy.filter(f).sort((a, b) => (a.status === 'Stock-out' ? -1 : 1) - (b.status === 'Stock-out' ? -1 : 1) || b.ts - a.ts).slice(0, 60).map(p => ({ id: p.id, cells: [c(p.id, { m: 1 }), c(p.patient, { b: 1 }), c(p.drug), c(p.qty), c(p.ward), c(fmt(p.ts), { m: 1 }), P(p.status)], go: open('pharmacy', p.id) })); break; }
    case 'billing': { const f = withF(['Provisional', 'Released', 'Part-paid', 'Disputed', 'Paid', 'Settled', 'Pending', 'Void requested', 'Voided'], 'status'); L.title = 'Billing'; L.sub = 'Estimate vs actual on every bill · AI writes plain-language explanations; the billing executive releases and responds'; L.stats = [['Provisional', S.bills.filter(b => b.status === 'Provisional').length], ['Disputes open', S.bills.filter(b => b.dispute && b.dispute.status === 'Open').length, RD], ['Variance > 10%', S.bills.filter(b => b.actual > b.estimate * 1.1).length, A], ['Insurance share', inr(S.bills.reduce((s, b) => s + b.insurance, 0))], ['Patient share', inr(S.bills.reduce((s, b) => s + b.patientShare, 0))]].map(([k, v, col]) => ({ k, v, c: col || '' })); L.cols = [['Bill', 100], ['Patient', 160], ['Admission', 110], ['Estimate', 100], ['Actual', 100], ['Variance', 90], ['Insurance', 100], ['Patient', 100], ['Status', 110]]; L.rows = S.bills.filter(f).map(b => { const v = b.actual - b.estimate; return { id: b.id, cells: [c(b.id, { m: 1 }), c(b.patient, { b: 1 }), c(b.admissionId, { m: 1 }), c(inr(b.estimate), { m: 1 }), c(inr(b.actual), { m: 1 }), c((v >= 0 ? '+' : '') + Math.round(100 * v / b.estimate) + '%', { m: 1, c: v > b.estimate * 0.1 ? RD : '' }), c(inr(b.insurance), { m: 1 }), c(inr(b.patientShare), { m: 1 }), P(b.dispute && b.dispute.status === 'Open' ? 'Disputed' : b.status)], go: open('bill', b.id) }; }); break; }
    case 'insurance': { const f = withF(['Pending', 'Submitted · awaiting insurer', 'Query Raised', 'Missing Documents', 'Additional Documents', 'High Denial Risk', 'Approved', 'Rejected'], 'status'); L.title = 'Insurance · preauthorisation'; L.sub = 'Preauthorisation Assembly Agent collects, checks completeness and scores denial risk. A human always submits.'; L.stats = [['Pending', S.preauths.filter(p => /Pending|Draft/.test(p.status)).length], ['Awaiting insurer', S.preauths.filter(p => /Submitted/.test(p.status)).length, A], ['Missing documents', S.preauths.filter(p => /Missing|Additional|Query/.test(p.status)).length, A], ['High denial risk', S.preauths.filter(p => parseInt(p.risk) >= 25).length, RD], ['Approved', S.preauths.filter(p => p.status === 'Approved').length, G], ['Rejected', S.preauths.filter(p => p.status === 'Rejected').length, RD]].map(([k, v, col]) => ({ k, v, c: col || '' })); L.cols = [['Case', 100], ['Patient', 150], ['Insurer', 130], ['Procedure', 150], ['Requested', 90], ['Approved', 90], ['Completeness', 90], ['Denial risk', 80], ['Age', 80], ['Owner', 90], ['Status', 170]]; L.rows = S.preauths.filter(f).map(p => ({ id: p.id, cells: [c(p.id, { m: 1 }), c(p.patient, { b: 1 }), c(p.insurer), c(p.procedure), c(p.requested, { m: 1 }), c(p.approved, { m: 1 }), c(p.completeness + '%', { m: 1, c: p.completeness < 100 ? A : '' }), c(p.risk, { m: 1, c: parseInt(p.risk) >= 25 ? RD : '' }), c(ago(p.createdTs), { m: 1 }), c(p.exec), P(p.status)], go: open('preauth', p.id) })); break; }
    case 'discharge': { L.title = 'Discharge command centre'; L.sub = S.discharges.filter(d => !d.completed).length + ' active cases · dependency graph, predicted ready time and critical path by Discharge Orchestration Agent v3.0.2'; const act = S.discharges.filter(d => !d.completed); const k = s => act.filter(d => api.dischargeState(d).k === s).length; L.stats = [['Ready', k('ready'), G], ['Blocked', k('blocked'), RD], ['Approval required', k('approval'), A], ['In progress', k('pending')], ['Completed today', S.discharges.filter(d => d.completed && d.dischargedAt >= 0).length]].map(([k2, v, col]) => ({ k: k2, v, c: col || '' })); L.cols = [['Patient · bed', 190], ['Doctor', 150], ['Insurer', 120], ['Intent', 70], ['Predicted ready', 90], ['Critical path', 200], ['Pending', 70], ['Owner', 150], ['Age', 80], ['Status', 190]]; L.rows = S.discharges.filter(d => !d.completed).concat(S.discharges.filter(d => d.completed)).map(d => { const st = api.dischargeState(d); const pend = Object.entries(d.deps).filter(([, x]) => x.status !== 'done'); const cp = pend.map(([k]) => k).slice(0, 3).join(' → ') || 'Clear'; return { id: d.id, cells: [c(d.patient + ' · ' + (d.bed || '').split(' ')[0], { b: 1 }), c(d.doctor), c(d.insurer), c(fmt(d.intentAt), { m: 1 }), c(d.completed ? fmt(d.dischargedAt) : d.eta, { m: 1 }), c(cp), c(pend.length, { m: 1 }), c(d.owner), c(d.completed ? '—' : ago(d.intentAt), { m: 1 }), P(st.label)], go: go('discharge-case', d.id) }; }); break; }
    case 'feedback': { const f = withF(['Open', 'In progress', 'Resolved', 'Closed'], 'status'); L.title = 'Feedback & service recovery'; L.sub = 'Feedback Agent classifies, prioritises and routes. Sensitive complaints are always resolved by a human owner.'; L.cols = [['Case', 110], ['Patient', 150], ['Feedback', 280], ['Category', 130], ['Sentiment', 90], ['Priority', 80], ['Owner', 130], ['Age / SLA', 110], ['Status', 100]]; L.rows = S.feedback.filter(f).map(fb => ({ id: fb.id, cells: [c(fb.id, { m: 1 }), c(fb.patient, { b: 1 }), c(fb.text), c(fb.category), P(fb.sentiment), P(fb.priority), c(fb.owner), c(fb.status === 'Open' && fb.ts > 0 ? ago(fb.ts) + ' / ' + fb.sla + ' m' : '—', { m: 1, c: fb.breached ? RD : '' }), P(fb.status)], go: open('feedback', fb.id) })); break; }
    case 'contact': { L.title = 'Contact centre'; L.sub = 'SIMULATED TELEPHONY DATA · Contact Centre Agent transcribes, detects intent and sentiment, and assists the human agent'; L.stats = [['Calls today', S.calls.filter(x => x.ts >= 0).length], ['AI resolved', S.calls.filter(x => x.handled === 'AI resolved').length, G], ['Transferred to human', S.calls.filter(x => x.handled === 'AI → human').length, A], ['Frustrated callers', S.calls.filter(x => x.sentiment === 'Frustrated').length, RD]].map(([k, v, col]) => ({ k, v, c: col || '' })); L.cols = [['Call', 100], ['Caller', 200], ['Time', 100], ['Duration', 80], ['Intent', 140], ['Sentiment', 90], ['Handled', 110], ['AI summary', 300]]; L.rows = S.calls.sort((a, b) => b.ts - a.ts).map(x => ({ id: x.id, cells: [c(x.id, { m: 1 }), c(x.patient, { b: 1 }), c(fmt(x.ts), { m: 1 }), c(x.duration, { m: 1 }), c(x.intent), P(x.sentiment), c(x.handled), c(x.summary)], go: open('call', x.id) })); break; }
    case 'staff': { const f = withF(['On duty', 'On leave'], 'status'); L.title = 'Staff'; L.sub = S.staff.length + ' staff · roster, shifts and role assignments'; L.cols = [['ID', 80], ['Name', 170], ['Role', 180], ['Unit', 160], ['Shift', 90], ['Status', 100]]; L.rows = S.staff.filter(f).map(s => ({ id: s.id, cells: [c(s.id, { m: 1 }), c(s.name, { b: 1 }), c(s.role), c(s.unit), c(s.shift), P(s.status)], go: open('staff', s.id) })); break; }
    case 'hr': { L.title = 'HR & Employee Service'; L.sub = 'Employee Service Agent answers from governed HR knowledge (Leave Policy v5.0, Payroll FAQ). Low-confidence questions are routed to HR.'; L.cols = [['Time', 80], ['Employee', 160], ['Question', 320], ['Source', 200], ['Confidence', 90], ['Outcome', 160]]; L.rows = [[fmt(now - 4), 'Anitha Kumar', 'How many casual leave days do I have left?', 'HR Leave Policy v5.0 §3 + HRMS', '98%', 'Answered · 4 days'], [fmt(now - 22), 'R. Sundar', 'What is the new policy for cashless robotic surgery packages?', 'No authoritative source', '42%', 'Refused · routed to Insurance Supervisor'], [fmt(now - 35), 'K. Meena', 'When is September payroll credited?', 'Payroll FAQ v2.1', '96%', 'Answered · 30 Sep'], [fmt(now - 61), 'S. Devi', 'How do I replace my ID card?', 'Facility Handbook v1.2', '93%', 'Answered · ticket raised'], [fmt(now - 90), 'P. Velu', 'Night shift allowance rate?', 'HR Leave Policy v5.0 §7', '95%', 'Answered'], [fmt(now - 140), 'New joiner · Ortho', 'Onboarding checklist for nurses', 'Onboarding SOP v1.4', '97%', 'Answered · 12 items']].map((r, i) => ({ id: 'HR' + i, cells: [c(r[0], { m: 1 }), c(r[1], { b: 1 }), c(r[2]), c(r[3]), c(r[4], { m: 1, c: parseInt(r[4]) < 70 ? RD : '' }), P(r[5])], go: open('hr', i) })); break; }
    case 'facilities': { const f = withF(['Occupied', 'Reserved', 'Discharge Pending', 'Free', 'Cleaning'], 'status'); L.title = 'Facilities · beds & housekeeping'; L.sub = S.beds.length + ' beds · turnaround is triggered by discharge orchestration'; L.stats = [['Occupied', S.beds.filter(b => b.status === 'Occupied').length], ['Free', S.beds.filter(b => b.status === 'Free').length, G], ['Discharge pending', S.beds.filter(b => b.status === 'Discharge Pending').length, A], ['Cleaning', S.beds.filter(b => b.status === 'Cleaning').length, A], ['Scanner 2', 'Maintenance', RD]].map(([k, v, col]) => ({ k, v, c: col || '' })); L.cols = [['Bed', 90], ['Department', 150], ['Class', 90], ['Patient', 200], ['Status', 100]]; L.rows = S.beds.filter(f).map(b => { const p = b.patientId && b.patientId !== 'other' ? api.patient(b.patientId) : null; return { id: b.id, cells: [c(b.id, { m: 1 }), c(b.dept), c(b.cls), c(p ? p.name : b.patientId === 'other' ? 'Occupied (other)' : '—'), P(b.status)], go: p ? go('patient', p.id) : open('bed', b.id) }; }); break; }
    case 'reports': { L.title = 'Reports'; L.sub = 'Scheduled and on-demand reports built from the shared data foundation'; L.cols = [['Report', 260], ['Domain', 120], ['Frequency', 100], ['Last run', 100], ['Owner', 160], ['Status', 100]]; L.rows = [['Daily operations summary', 'Operations', 'Daily 06:00', fmt(360), 'COO office', 'Ready'], ['Discharge turnaround by blocker', 'Operations', 'Daily', fmt(360), 'Operations', 'Ready'], ['Preauth first-pass acceptance', 'Finance', 'Weekly', '08 Sep', 'Insurance Desk', 'Ready'], ['Estimate variance > 10%', 'Finance', 'Weekly', '08 Sep', 'Finance', 'Ready'], ['Critical-value acknowledgement times', 'Quality', 'Weekly', '08 Sep', 'Quality Head', 'Ready'], ['AI value & cost (board pack)', 'AI', 'Monthly', '01 Sep', 'AI Programme Lead', 'Draft'], ['NABH patient rights compliance', 'Quality', 'Quarterly', '01 Jul', 'Quality Head', 'Ready']].map((r, i) => ({ id: 'R' + i, cells: [c(r[0], { b: 1 }), c(r[1]), c(r[2]), c(r[3], { m: 1 }), c(r[4]), P(r[5])], go: go(['command', 'discharge', 'insurance', 'billing', 'laboratory', 'cost', 'knowledge'][i]) })); break; }
    case 'ai-command': { const k = api.kpis(); L.title = 'AI Command Centre'; L.sub = 'Agent activity across the hospital today · every run is auditable, every high-risk action is human-gated'; L.stats = [['Executions today', k.execsToday], ['Automation rate', k.automation + '%'], ['Agents active', k.agentsActive, G], ['Disabled', k.agentsDisabled, RD], ['Approvals waiting', k.approvals, A], ['Exceptions', k.exceptions, RD]].map(([k2, v, col]) => ({ k: k2, v, c: col || '' })); L.cols = [['Agent', 200], ['Type', 110], ['Runs today', 80], ['Success', 70], ['Waiting', 70], ['Failed', 60], ['Last run', 80], ['Human approval', 100], ['Status', 120]]; L.rows = S.agents.map(a => { const ex = S.executions.filter(e => e.agentId === a.id && e.ts >= 0); return { id: a.id, cells: [c(a.name, { b: 1 }), c(a.type), c(a.runs, { m: 1 }), c(a.success, { m: 1 }), c(ex.filter(e => e.status === 'Waiting').length, { m: 1 }), c(ex.filter(e => e.status === 'Failed').length, { m: 1, c: ex.some(e => e.status === 'Failed') ? RD : '' }), c(a.lastRun, { m: 1 }), c(a.humanApproval), P(a.statusNote || a.status)], go: go('agent', a.id) }; }); break; }
    case 'agents': { const f = withF(['Draft', 'Testing', 'Approval', 'Published', 'Disabled', 'Silent Validation', 'Pilot'], 'status'); L.title = 'Agents'; L.sub = S.agents.length + ' agents · registry, builder, evaluation, versions'; L.cols = [['Agent', 200], ['Type', 110], ['Version', 60], ['Owner', 130], ['Risk tier', 70], ['Status', 110], ['Last run', 70], ['Success', 70], ['Human approval', 100], ['Tools', 50], ['Knowledge', 70]]; L.rows = S.agents.filter(f).map(a => ({ id: a.id, cells: [c(a.name, { b: 1 }), c(a.type), c(a.v, { m: 1 }), c(a.owner), c(a.tier, { c: a.tier === 'High' ? RD : a.tier === 'Medium' ? A : G, b: 1 }), P(a.status), c(a.lastRun, { m: 1 }), c(a.success, { m: 1 }), c(a.humanApproval), c(a.config.tools.filter(t => t.enabled).length, { m: 1 }), c(a.config.knowledge.length, { m: 1 })], go: go('agent', a.id) })); break; }
    case 'runs': { const f = withF(['Queued', 'Running', 'Waiting', 'Completed', 'Failed', 'Blocked', 'Escalated', 'Cancelled'], 'status'); L.title = 'Agent runs'; L.sub = S.executions.length + ' executions · click a run for the full trace'; L.cols = [['Execution', 130], ['Agent', 190], ['Version', 60], ['Patient', 160], ['Started', 100], ['Steps', 50], ['Tools', 50], ['Latency', 70], ['Cost', 60], ['Status', 100]]; L.rows = S.executions.filter(f).slice(0, 120).map(e => ({ id: e.id, cells: [c(e.id, { m: 1 }), c(e.agent, { b: 1 }), c(e.version, { m: 1 }), c(e.patient), c(fmt(e.ts), { m: 1 }), c(e.steps.length, { m: 1 }), c(e.tools, { m: 1 }), c(e.latency, { m: 1 }), c(e.cost, { m: 1 }), P(e.status)], go: go('execution', e.id) })); break; }
    case 'exceptions': { const ex = api.exceptions(); const f = withF([...new Set(ex.map(e => e.group))], 'group'); L.title = 'Exception centre'; L.sub = ex.length + ' open exceptions across workflows, agents and integrations'; L.cols = [['Type', 170], ['Reference', 200], ['Owner', 140], ['Age', 80], ['Priority', 70], ['Reason', 260], ['Workflow', 110], ['Agent', 60], ['Next action', 110], ['SLA', 60]]; L.rows = ex.filter(f).map(e => ({ id: e.id, cells: [c(e.group, { b: 1 }), c(e.ref), c(e.owner), c(e.age, { m: 1 }), P(e.pri), c(e.reason), c(e.workflow), c(e.agent, { m: 1 }), c(e.next + ' →', { c: T }), c(e.sla, { m: 1 })], go: e.page === 'discharge-case' ? go('discharge-case', e.id) : open(e.page === 'laboratory' ? 'lab' : e.page === 'insurance' ? 'preauth' : e.page === 'billing' ? 'bill' : e.page === 'feedback' ? 'feedback' : e.page === 'runs' ? 'execution' : e.page === 'incidents' ? 'incident' : e.page === 'knowledge' ? 'knowledge' : e.page === 'pharmacy' ? 'pharmacy' : 'integration', e.id) })); break; }
    case 'knowledge': { const f = withF(['Published', 'Indexed', 'Processing', 'Uploaded', 'Review due', 'Conflict', 'Expired', 'Draft', 'Inactive', 'Failed'], 'status'); L.title = 'Knowledge base'; L.sub = S.knowledge.length + ' governed documents · pipeline Upload → Parse → Chunk → Embed → Index → Available to RAG · every assistant answer cites document, section, version and effective date'; L.upload = true; L.stats = [['Published', S.knowledge.filter(k => k.status === 'Published').length, G], ['Review due', S.knowledge.filter(k => k.status === 'Review due').length, A], ['Conflicts', S.knowledge.filter(k => k.status === 'Conflict').length, RD], ['Expired', S.knowledge.filter(k => k.status === 'Expired').length, RD]].map(([k, v, col]) => ({ k, v, c: col || '' })); L.cols = [['ID', 70], ['Title', 240], ['Department', 120], ['Ver', 50], ['Effective', 90], ['Indexed', 90], ['Chunks', 60], ['Retrievals', 70], ['Lang', 60], ['Status', 100]]; L.rows = S.knowledge.filter(f).sort((a, b) => (a.status === 'Published') - (b.status === 'Published')).map(k => ({ id: k.id, cells: [c(k.id, { m: 1 }), c(k.title, { b: 1 }), c(k.dept), c(k.v, { m: 1 }), c(k.eff, { m: 1 }), c(k.indexed || k.eff, { m: 1 }), c(k.chunks || 40 + (k.id.charCodeAt(4) % 90), { m: 1 }), c(k.retrievals || 0, { m: 1 }), c(k.lang), P(k.status)], go: open('knowledge', k.id) })); break; }
    case 'evals': { L.title = 'AI evaluations'; L.sub = S.evals.length + ' evaluation runs on synthetic test cases · nightly regression on every published version'; L.cols = [['Run', 80], ['Agent', 190], ['Version', 60], ['When', 100], ['Cases', 50], ['Accuracy', 70], ['Grounded', 70], ['Halluc.', 60], ['Refusal', 60], ['Escalation', 70], ['Tool ✓', 60], ['Latency', 60], ['Override', 60], ['Result', 70]]; L.rows = S.evals.map(e => ({ id: e.id, cells: [c(e.id, { m: 1 }), c(e.agent, { b: 1 }), c(e.version, { m: 1 }), c(fmt(e.ts), { m: 1 }), c(e.cases, { m: 1 }), c(e.accuracy + '%', { m: 1 }), c(e.groundedness + '%', { m: 1 }), c(e.hallucination + '%', { m: 1 }), c(e.refusal + '%', { m: 1 }), c(e.escalation + '%', { m: 1 }), c(e.toolSuccess + '%', { m: 1 }), c(e.latency, { m: 1 }), c(e.override + '%', { m: 1 }), P(e.result)], go: go('agent', e.agentId) })); break; }
    case 'observability': { const t = S.executions.filter(e => e.ts >= 0); const f = withF(['Failed', 'Waiting', 'Escalated'], 'status'); L.title = 'AI observability'; L.sub = 'Live operations view · executions, failures, latency, queues'; L.stats = [['Executions today', t.length], ['Success', Math.round(100 * t.filter(e => e.status === 'Completed').length / Math.max(1, t.length)) + '%', G], ['Failed', t.filter(e => e.status === 'Failed').length, RD], ['Escalated', t.filter(e => e.status === 'Escalated').length, A], ['p50 latency', '2.1 s'], ['Approval queue', S.approvals.filter(a => a.status === 'Pending').length, A], ['Tool failures (1 h)', S.lisDegraded ? 3 : 0, S.lisDegraded ? RD : G], ['Uptime 30 d', '99.7%']].map(([k, v, col]) => ({ k, v, c: col || '' })); L.cols = [['Execution', 130], ['Agent', 190], ['Patient', 160], ['Started', 90], ['Failure', 180], ['Latency', 70], ['Cost', 60], ['Status', 100]]; L.rows = t.filter(e => e.status !== 'Completed').filter(f).map(e => ({ id: e.id, cells: [c(e.id, { m: 1 }), c(e.agent, { b: 1 }), c(e.patient), c(fmt(e.ts), { m: 1 }), c(e.failure || '—', { c: e.failure ? RD : '' }), c(e.latency, { m: 1 }), c(e.cost, { m: 1 }), P(e.status)], go: go('execution', e.id) })); break; }
    case 'cost': { L.title = 'AI cost & usage'; L.sub = 'Month to date ₹1,04,300 · 71% of ₹1,47,000 budget · projected ₹1,39,000'; L.stats = [['Today', '₹9,090'], ['LLM', '₹5,240'], ['Voice', '₹1,840'], ['Agent execution', '₹980'], ['Vision', '₹610'], ['Per discharge', '₹38'], ['Per preauth packet', '₹52'], ['Budget alert', 'None', G]].map(([k, v, col]) => ({ k, v, c: col || '' })); L.cols = [['Agent', 200], ['Department', 140], ['Model', 150], ['Runs today', 80], ['Tokens (k)', 80], ['Cost today', 90], ['Avg / run', 80], ['MTD', 90], ['Trend', 80]]; L.rows = S.agents.map(a => { const runs = a.runs || 0; const per = 4 + (a.id.charCodeAt(4) % 30); return { id: a.id, cells: [c(a.name, { b: 1 }), c(a.owner), c(a.config.model.model), c(runs, { m: 1 }), c(Math.round(runs * 3.2), { m: 1 }), c(inr(runs * per), { m: 1 }), c('₹' + per, { m: 1 }), c(inr(runs * per * 11), { m: 1 }), c(['▲ 4%', '▼ 2%', '— 0%'][a.id.charCodeAt(4) % 3], { m: 1 })], go: go('agent', a.id) }; }); break; }
    case 'incidents': { const f = withF(['Open', 'Investigating', 'Contained', 'Resolved', 'Closed'], 'status'); L.title = 'AI incidents'; L.sub = S.incidents.length + ' incidents · Open → Investigating → Contained → Resolved → Closed'; L.cols = [['Incident', 110], ['Severity', 70], ['Agent', 190], ['Version', 60], ['Patient / workflow', 170], ['Description', 260], ['Detected', 100], ['Owner', 150], ['Status', 110]]; L.rows = S.incidents.filter(f).map(i => ({ id: i.id, cells: [c(i.id, { m: 1 }), P(i.severity), c(i.agent, { b: 1 }), c(i.version, { m: 1 }), c(i.patient), c(i.description), c(fmt(i.detected), { m: 1 }), c(i.owner), P(i.status)], go: open('incident', i.id) })); break; }
    case 'risk': { L.title = 'AI risk register'; L.sub = '8 risks · reviewed by the AI Governance Committee on 05 Sep 2026'; L.cols = [['Risk', 240], ['Severity', 80], ['Owner', 160], ['Mitigation', 340], ['Residual', 90]]; L.rows = RISKS.map((r, i) => ({ id: 'RK' + i, cells: [c(r[0], { b: 1 }), P(r[1]), c(r[2]), c(r[3]), c(r[4], { c: /Medium/.test(r[4]) ? A : G })], go: open('risk', i) })); break; }
    case 'audit': { L.title = 'Audit trail'; L.sub = S.audit.length + ' events · every AI response, agent step, access decision, approval and consent change · searchable'; L.cols = [['Time', 90], ['User / agent', 200], ['Action', 240], ['Entity', 130], ['Before', 90], ['After', 120], ['Reason', 160], ['Approval', 90], ['Source', 100], ['Correlation', 130]]; L.rows = S.audit.slice(0, 200).map(a => ({ id: a.id, cells: [c(fmt(a.ts), { m: 1 }), c(a.actor), c(a.action, { b: 1 }), c(a.entity, { m: 1 }), c(a.before), c(a.after, { c: /DENIED|Rejected|Refused|Suspended|Failed/.test(a.after) ? RD : '' }), c(a.reason), c(a.approval), c(a.source), c(a.corr, { m: 1, c: a.corr !== '—' ? T : '' })], go: a.corr && a.corr.startsWith('EXE') ? go('execution', a.corr) : open('audit', a.id) })); break; }
    case 'ai-analytics': { const log = S.aiLog || []; const f = withF(['answered', 'denied', 'no-answer', 'conflict', 'boundary', 'error'], 'status'); const days = ctx.days || 'Today'; const inWin = l => days === 'Today' ? l.ts >= 0 : days === '7 days' ? l.ts >= -7 * 1440 : true; const win = log.filter(inWin); L.title = 'Assistant analytics'; L.sub = 'Conversations, voice vs text, retrieval quality and feedback for Hospital Assistant · this session'; L.stats = [['Queries', win.length], ['Voice', win.filter(l => l.voice).length], ['Text', win.filter(l => !l.voice).length], ['RAG', win.filter(l => l.intent === 'rag').length], ['Structured', win.filter(l => l.intent === 'structured').length], ['Hybrid', win.filter(l => l.intent === 'hybrid').length], ['Grounded', win.filter(l => l.status === 'answered').length, G], ['No answer', win.filter(l => l.status === 'no-answer').length, A], ['Denied', win.filter(l => l.status === 'denied').length, RD], ['Errors', win.filter(l => l.status === 'error').length, RD], ['👍', win.filter(l => l.feedback === 'up').length, G], ['👎', win.filter(l => l.feedback === 'down').length, RD]].map(([k, v2, col]) => ({ k, v: v2, c: col || '' })); const topDocs = {}; win.forEach(l => (l.sources || []).forEach(d => { topDocs[d] = (topDocs[d] || 0) + 1; })); L.banner = win.length ? { kind: 'info', text: 'Most retrieved: ' + Object.entries(topDocs).sort((a, b) => b[1] - a[1]).slice(0, 4).map(([d, n]) => d + ' (' + n + ')').join(' · ') } : { kind: 'info', text: 'No assistant queries yet this session. Open Hospital Assistant and ask a question or use the microphone.' }; L.cols = [['Time', 70], ['Role', 120], ['Question', 320], ['Mode', 60], ['Intent', 90], ['Outcome', 110], ['Sources', 220], ['Latency', 60], ['Feedback', 70]]; L.rows = win.filter(f).map((l, i) => ({ id: 'AIL' + i, cells: [c(fmt(l.ts), { m: 1 }), c(l.role), c(l.q, { b: 1 }), c(l.voice ? 'Voice' : 'Text'), c(l.intent.replace('unauthorized-probe', 'permission')), P(l.status === 'answered' ? 'Grounded' : l.status === 'denied' ? 'Denied' : l.status === 'no-answer' ? 'No answer' : l.status), c((l.sources || []).join(' · ') || '—'), c(l.latency, { m: 1 }), c(l.feedback === 'up' ? '👍' : l.feedback === 'down' ? '👎' : '—')], go: go('chat') })); break; }
    case 'trainer': { L.title = 'AI Trainer'; L.sub = 'Role-based competency: module → AI tutor → scenario → quiz → score → human sign-off'; L.cols = [['Role', 130], ['Competency', 220], ['Module', 200], ['Learners', 70], ['Avg score', 80], ['Sign-off', 160], ['Status', 100]]; L.rows = [['Staff Nurse', 'Reviewing AI handover drafts', 'Nursing Handover Agent · safe use', 42, '88%', 'Nursing Superintendent', 'In progress'], ['Insurance Executive', 'Preauth completeness & denial risk', 'Preauth Agent walkthrough', 6, '92%', 'L. Fathima', 'Completed'], ['Billing Executive', 'Explaining estimate variance', 'Billing transparency scenarios', 9, '85%', 'Finance Head', 'In progress'], ['Doctor', 'Signing AI-drafted summaries', 'Draft review + voice dictation', 30, '90%', 'Medical Director', 'Completed'], ['Front Office', 'Ask Hospital AI for patient status', 'Patient access channels', 18, '94%', 'Front Office Lead', 'Completed']].map((r, i) => ({ id: 'TR' + i, cells: [c(r[0], { b: 1 }), c(r[1]), c(r[2]), c(r[3], { m: 1 }), c(r[4], { m: 1 }), c(r[5]), P(r[6])], go: open('trainer', i) })); break; }
    case 'data-patient': { L.title = 'Patient data'; L.sub = 'Master patient index · identity resolution · consent · lineage from HMS + EMR'; L.stats = [['Patients', S.patients.length], ['Duplicate suspects', 2, A], ['Consent verified', S.patients.length - 3, G], ['Missing phone', 1, A]].map(([k, v, col]) => ({ k, v, c: col || '' })); L.cols = [['UHID', 150], ['Name', 160], ['Source', 80], ['Consent', 110], ['Language', 80], ['Quality', 80]]; L.rows = S.patients.map((p, i) => ({ id: p.id, cells: [c(p.id, { m: 1 }), c(p.name, { b: 1 }), c('HMS'), P(i % 25 === 3 ? 'Pending' : 'Verified'), c(p.lang), c(i % 25 === 3 ? '82%' : '100%', { m: 1 })], go: go('patient', p.id) })); break; }
    case 'data-ops': { L.title = 'Operational data'; L.sub = 'Beds, queues, turnaround and flow events feeding the Command Centre and forecasting features'; L.cols = [['Dataset', 220], ['Records', 90], ['Freshness', 100], ['Source', 120], ['Quality', 80], ['Consumers', 260]]; L.rows = [['Bed status', S.beds.length, '2 min', 'HMS', '99%', 'Command Centre · Forecasting · Discharge'], ['Appointments', S.appointments.length, '1 min', 'HMS', '98%', 'Queue Agent · Portal'], ['Discharge events', S.discharges.length, 'Live', 'Orchestrator', '100%', 'Discharge board · Analytics'], ['Emergency arrivals', S.emergency.length, '3 min', 'ER module', '97%', 'Emergency · Forecasting'], ['Housekeeping tasks', 41, '5 min', 'Facilities', '95%', 'Discharge · Facilities']].map((r, i) => ({ id: 'DO' + i, cells: [c(r[0], { b: 1 }), c(r[1], { m: 1 }), c(r[2], { m: 1 }), c(r[3]), c(r[4], { m: 1 }), c(r[5])], go: open('dataset', i) })); break; }
    case 'data-clinical': { L.title = 'Clinical data'; L.sub = 'Orders, results, medications, procedures · read-only for AI, write requires clinician approval'; L.cols = [['Dataset', 220], ['Records', 90], ['Freshness', 100], ['Source', 120], ['AI access', 160], ['Quality', 80]]; L.rows = [['Orders', S.orders.length, '2 min', 'EMR', 'Read', '99%'], ['Laboratory results', S.labs.length, S.lisDegraded ? '7 min · degraded' : '1 min', 'LIS', 'Read', S.lisDegraded ? '91%' : '99%'], ['Radiology studies', S.radiology.length, '4 min', 'RIS / PACS', 'Read · screening (silent)', '98%'], ['Medications', S.pharmacy.length, '3 min', 'Pharmacy', 'Read', '99%'], ['Procedures', S.procedures.length, '10 min', 'OT module', 'Read', '97%'], ['Discharge summaries', S.discharges.length, 'Live', 'EMR', 'Draft only · clinician signs', '100%']].map((r, i) => ({ id: 'DC' + i, cells: [c(r[0], { b: 1 }), c(r[1], { m: 1 }), c(r[2], { m: 1, c: /degraded/.test(r[2]) ? RD : '' }), c(r[3]), c(r[4]), c(r[5], { m: 1 })], go: open('dataset', i) })); break; }
    case 'data-financial': { L.title = 'Financial data'; L.sub = 'Bills, payments, policies, preauths · estimate variance and denial features'; L.cols = [['Payment', 100], ['Patient', 160], ['Bill', 100], ['Amount', 100], ['Mode', 90], ['Time', 100], ['Status', 90]]; L.rows = S.payments.map(p => ({ id: p.id, cells: [c(p.id, { m: 1 }), c(p.patient, { b: 1 }), c(p.billId, { m: 1 }), c(inr(p.amount), { m: 1 }), c(p.mode), c(fmt(p.ts), { m: 1 }), P(p.status)], go: open('bill', p.billId) })); break; }
    case 'data-quality': { L.title = 'Data quality'; L.sub = 'Rules evaluated hourly · failures create exceptions for data owners'; L.cols = [['Rule', 260], ['Domain', 110], ['Pass rate', 80], ['Failures', 70], ['Owner', 150], ['Status', 100]]; L.rows = [['Every inpatient has an active bed', 'Operations', '100%', 0, 'Bed manager', 'Pass'], ['Every insured admission has a preauth case', 'Finance', '96%', 1, 'Insurance Desk', 'Warning'], ['Phone number present and verified', 'Patient', '98.7%', 1, 'Front Office', 'Warning'], ['Consent record < 12 months old', 'Governance', '99%', 1, 'Governance Officer', 'Pass'], ['Lab result linked to order', 'Clinical', '100%', 0, 'Laboratory', 'Pass'], ['Knowledge doc within review date', 'Knowledge', '93%', 7, 'Knowledge Manager', 'Failed']].map((r, i) => ({ id: 'DQ' + i, cells: [c(r[0], { b: 1 }), c(r[1]), c(r[2], { m: 1 }), c(r[3], { m: 1 }), c(r[4]), P(r[5] === 'Warning' ? 'Review' : r[5])], go: open('rule', i) })); break; }
    case 'users': { L.title = 'Users'; L.sub = S.staff.length + ' users · SSO · role and department scoped'; L.cols = [['ID', 80], ['Name', 170], ['Role', 180], ['Department', 160], ['MFA', 60], ['Last login', 100], ['Status', 100]]; L.rows = S.staff.map((s, i) => ({ id: s.id, cells: [c(s.id, { m: 1 }), c(s.name, { b: 1 }), c(s.role), c(s.unit), c(i % 9 === 4 ? 'No' : 'Yes', { c: i % 9 === 4 ? RD : '' }), c(fmt(now - 5 - i * 7), { m: 1 }), P(s.status === 'On duty' ? 'Active' : 'Inactive')], go: open('staff', s.id) })); break; }
    case 'roles': { L.title = 'Roles'; L.sub = 'Role definitions used by the policy gateway'; L.cols = [['Role', 170], ['Can', 380], ['Cannot', 340], ['Users', 60]]; L.rows = Object.entries(PERMS).map(([r, p], i) => ({ id: r, cells: [c(r, { b: 1 }), c(p.can.join(' · ')), c(p.cannot.join(' · '), { c: RD }), c(r === 'Patient' ? S.patients.length : 3 + i * 4, { m: 1 })], go: open('role', r) })); break; }
    case 'permissions': { L.title = 'Permissions'; L.sub = 'RBAC + ABAC matrix · relationship and consent checks apply on top of role permissions'; const acts = ['View clinical record', 'Sign clinical documents', 'Release final bill', 'Verify / release lab result', 'Dispense / return medication', 'Submit to insurer', 'Configure agents', 'Publish knowledge', 'View aggregate analytics', 'Acknowledge critical value']; L.cols = [['Permission', 200]].concat(ALL.map(r => [r.replace('Hospital ', ''), 90])); L.rows = acts.map((a, i) => ({ id: 'PM' + i, cells: [c(a, { b: 1 })].concat(ALL.map(r => { const yes = { 'View clinical record': ['Doctor', 'Nurse', 'Radiologist', 'Laboratory', 'Pharmacy'], 'Sign clinical documents': ['Doctor'], 'Release final bill': ['Billing'], 'Verify / release lab result': ['Laboratory'], 'Dispense / return medication': ['Pharmacy', 'Nurse'], 'Submit to insurer': ['Insurance'], 'Configure agents': ['AI Administrator'], 'Publish knowledge': ['Governance Officer', 'AI Administrator'], 'View aggregate analytics': ['Hospital Management', 'Governance Officer', 'AI Administrator', 'Billing'], 'Acknowledge critical value': ['Doctor'] }[a].includes(r); return yes ? P('Allowed') : c('—'); })), go: open('permission', i) })); break; }
    case 'identity': { L.title = 'Identity & consent'; L.sub = 'Health identifier (UHID), OTP verification and per-purpose consent for every patient'; L.cols = [['UHID', 150], ['Patient', 160], ['Verified via', 110], ['WhatsApp', 80], ['Reminders', 80], ['Billing', 80], ['AI interpretation', 110], ['Third-party', 90], ['Last verified', 110]]; L.rows = S.patients.slice(0, 40).map((p, i) => ({ id: p.id, cells: [c(p.id, { m: 1 }), c(p.name, { b: 1 }), c(['Mobile OTP', 'Front desk', 'Web OTP'][i % 3]), P(i % 7 === 5 ? 'Off' : 'On'), P('On'), P('On'), P('Off'), P('Off'), c(fmt(-i * 300 + 520), { m: 1 })], go: go('patient', p.id) })); break; }
    case 'integrations': { L.title = 'Integrations'; L.sub = 'Prototype connectors · not production integrations · manual fallback workflows exist for every feed'; L.cols = [['System', 160], ['Direction', 120], ['Freshness', 100], ['Calls today', 80], ['Errors (1 h)', 80], ['Fallback', 200], ['Status', 110]]; const rows = [['HMS', 'Read', '4 min', 3120, 0, '—', 'Healthy'], ['EMR', 'Read · draft write', '2 min', 1880, 0, '—', 'Healthy'], ['LIS', 'Read', S.lisDegraded ? '7 min' : '1 min', 640, S.lisDegraded ? 3 : 0, 'Phone verification of critical values', S.lisDegraded ? 'Degraded' : 'Healthy'], ['RIS / PACS', 'Read', '4 min', 210, 0, '—', 'Healthy'], ['Billing', 'Read · write + approval', '1 min', 940, 0, '—', 'Healthy'], ['Insurance / TPA portal', 'Read · human submit', '3 min', 120, 0, 'Manual portal submission', 'Healthy'], ['Pharmacy', 'Read', '3 min', 410, 0, '—', 'Healthy'], ['WhatsApp Business', 'Send · receive', 'Live', 1460, 1, 'SMS', 'Healthy'], ['Telephony (simulated)', 'Receive', 'Live', 100, 0, 'Human agent', 'Healthy'], ['HRMS', 'Read', '15 min', 88, 0, '—', 'Healthy']]; L.rows = rows.map((r, i) => ({ id: 'INT-' + r[0].split(' ')[0].toUpperCase(), cells: [c(r[0], { b: 1 }), c(r[1]), c(r[2], { m: 1 }), c(r[3], { m: 1 }), c(r[4], { m: 1, c: r[4] ? RD : '' }), c(r[5]), P(r[6])], go: open('integration', r[0]) })); break; }
    case 'notifications': { L.title = 'Notification centre'; L.sub = S.notifications.filter(n => !n.read).length + ' unread · every notification deep-links to its record'; L.cols = [['Time', 80], ['Priority', 80], ['Title', 220], ['Detail', 360], ['Source', 180], ['State', 80]]; L.rows = S.notifications.map(n => ({ id: n.id, cells: [c(fmt(n.ts), { m: 1 }), P(n.priority), c(n.title, { b: 1 }), c(n.body), c(n.source), P(n.read ? 'Read' : 'Unread')], go: () => { api.markRead(n.id); ctx.follow(n.link); } })); break; }
    case 'config': { L.title = 'Configuration'; L.sub = 'Platform settings · changes are audited and require AI Administrator role'; L.cols = [['Setting', 260], ['Value', 300], ['Scope', 120], ['Changed', 100], ['By', 160]]; L.rows = [['Refusal confidence threshold', '70%', 'All agents', '02 Sep', 'AI Governance Committee'], ['Critical-value acknowledgement timer', '15 minutes → escalate to department head', 'Laboratory', '15 Aug', 'Medical Director'], ['Discharge turnaround target', '3 h from doctor intent', 'Operations', '01 Jul', 'COO'], ['Estimate variance alert', '> 5% notify · > 10% counselling required', 'Billing', '01 Apr', 'Finance Head'], ['Default patient language', 'Tamil (fallback English)', 'Channels', '01 Jul', 'Patient Experience'], ['PHI masking outside care team', 'On', 'Policy gateway', '01 Jul', 'CISO'], ['Conversation retention', '0 days (audit metadata 90 days)', 'Memory', '01 Jul', 'CISO']].map((r, i) => ({ id: 'CF' + i, cells: [c(r[0], { b: 1 }), c(r[1]), c(r[2]), c(r[3], { m: 1 }), c(r[4])], go: open('config', i) })); break; }
    default: L.title = page; L.sub = 'Page not yet mapped'; }
  if (q) { const s = q.toLowerCase(); L.rows = L.rows.filter(r => r.cells.some(x => x.t.toLowerCase().includes(s))); }
  L.count = L.rows.length; L.empty = L.rows.length === 0; L.cols = L.cols.map(([l, w]) => ({ l, w: w + 'px' }));
  L.grid = L.cols.map(x => 'minmax(0,' + x.w.replace('px', 'fr') + ')').join(' ');
  return L;
}

function buildDetail(kind, id, api, ctx) {
  const S = api.S, now = api.clock(), { role } = ctx; const fmt = api.fmt, ago = t => api.ago(t, now), inr = api.inr;
  const D = { title: '', sub: '', badges: [], facts: [], sections: [], actions: [], related: [], ai: null, image: null, steps: [] };
  const badge = s => ({ t: s, ...pill(s) }); const go = (p, i) => () => ctx.go(p, i);
  const act = (label, on, o = {}) => ({ label, on, primary: !!o.primary, disabled: !!o.disabled, hint: o.hint || '' });
  const needRole = r => role === r ? {} : { disabled: true, hint: 'Requires ' + r + ' role' };
  switch (kind) {
    case 'appointment': { const a = S.appointments.find(x => x.id === id); if (!a) break; D.title = a.patient + ' · ' + fmt(a.ts); D.sub = a.id + ' · ' + a.doctor + ' · ' + a.dept; D.badges = [badge(a.status), badge(a.channel)]; D.facts = [kv('Type', a.type), kv('Channel', a.channel), kv('Token', a.token || '—'), kv('Lifecycle', 'Requested → Confirmed → Checked-in → In consultation → Completed'), kv('Delete policy', 'Appointments are cancelled, never deleted — history stays in the patient timeline'), kv('Reminder', a.ts > now ? 'Scheduled ' + fmt(a.ts - 1440) : 'Sent'), kv('Queue', a.status === 'Confirmed' && a.ts < now + 120 ? 'Position 3 · ETA ' + fmt(now + 24) : '—')]; D.steps = [{ t: fmt(a.ts - 2 * 1440), who: 'Appointment Agent', what: 'Booked via ' + a.channel, k: 'ai' }, { t: fmt(a.ts - 1440), who: 'Appointment Agent', what: 'Reminder sent (' + (api.patient(a.patientId)?.lang === 'Tamil' ? 'Tamil' : 'English') + ')', k: 'tool' }].concat(a.status === 'No-show' ? [{ t: fmt(a.ts + 20), who: 'Appointment Agent', what: 'No-show detected · WhatsApp reschedule offer sent with 3 slots', k: 'warn' }] : a.status === 'Rescheduled' ? [{ t: fmt(now), who: 'Patient', what: 'Selected new slot on WhatsApp', k: 'human' }] : []); if (a.status === 'No-show' && !a.noShowHandled) D.actions = [act('Patient picks 10:30 tomorrow', () => api.rescheduleNoShow(a.id, 630), { primary: true }), act('Patient picks 4:15 PM tomorrow', () => api.rescheduleNoShow(a.id, 975))]; else if (/Scheduled|Confirmed|Checked-in|In consultation/.test(a.status)) D.actions = [act(({ Scheduled: 'Confirm', Confirmed: 'Check in · issue token', 'Checked-in': 'Start consultation', 'In consultation': 'Complete · close encounter + bill' })[a.status], () => api.advanceAppointment(a.id, role), { primary: true, ...(api.can(role, 'frontOffice') ? {} : { disabled: true, hint: 'Requires Front Office or clinician role' }) }), act('Cancel appointment', () => api.mutate(s => { a.status = 'Cancelled'; }, role, 'Appointment cancelled', a.patientId, 'Cancelled'))]; D.related = [{ label: 'Patient 360', on: go('patient', a.patientId) }]; break; }
    case 'admission': { const a = S.admissions.find(x => x.id === id); if (!a) break; const d = S.discharges.find(x => x.patientId === a.patientId && !x.completed); const pa = S.preauths.find(x => x.patientId === a.patientId); const b = S.bills.find(x => x.admissionId === a.id); D.title = a.patient; D.sub = a.id + ' · ' + a.dept + ' · ' + a.doctor; D.badges = [badge(a.status)]; D.facts = [kv('Bed', a.bed), kv('Admitted', a.admitted), kv('Estimate', inr(a.estimate)), kv('Running bill', b ? inr(b.actual) : '—'), kv('Insurance', pa ? pa.insurer + ' · ' + pa.status : 'Self-pay'), kv('Discharge', d ? api.dischargeState(d).label : 'Not started')]; D.steps = [{ t: a.admitted, who: 'Front Office', what: 'Admission request · room selected · estimate counselled', k: 'human' }, { t: a.admitted, who: 'Insurance Preauth Agent', what: pa ? 'Preauth ' + pa.id + ' · ' + pa.status : 'Self-pay · no preauth', k: 'ai' }, { t: a.admitted, who: 'Bed manager', what: 'Bed ' + a.bed + ' assigned', k: 'tool' }]; D.related = [{ label: 'Patient 360', on: go('patient', a.patientId) }].concat(d ? [{ label: 'Discharge case', on: go('discharge-case', d.id) }] : []); if (!d && a.status === 'Admitted') D.actions = [act('Mark likely discharge (doctor)', () => api.markLikelyDischarge(a.patientId, role), { primary: true, ...needRole('Doctor') })]; break; }
    case 'emergency': { const e = S.emergency.find(x => x.id === id); if (!e) break; const labs = S.labs.filter(l => l.patientId === e.patientId); D.title = e.patient + ' · ' + e.id; D.sub = e.complaint + ' · arrived ' + fmt(e.arrival) + ' (' + ago(e.arrival) + ')'; D.badges = [badge(e.status), badge(e.acuity)]; D.facts = [kv('Clinician', e.clinician), kv('Acuity', e.acuity + ' — set by clinician, not AI'), kv('Bed required', e.bedNeed ? 'Yes · ' + S.beds.filter(b => b.status === 'Free' && b.dept !== 'Emergency').length + ' free' : 'No'), kv('Family updated', fmt(e.arrival + 30) + ' · SMS')]; D.sections = [{ title: 'Diagnostics', items: labs.length ? labs.map(l => kv(l.test + ' · ' + l.analyte, l.value + ' ' + l.unit + ' · ' + l.flag + (l.critical ? (l.acked ? ' · acknowledged' : ' · UNACKNOWLEDGED') : ''), l.critical && !l.acked ? RD : '')) : [kv('Orders', 'None resulted yet')] }]; const cr = labs.find(l => l.critical && !l.acked); if (cr) D.actions = [act('Acknowledge critical value', () => api.acknowledgeCritical(cr.id, role), { primary: true, ...needRole('Doctor') })]; if (e.status !== 'Admitted') D.actions.push(act('Admit to ward (clinician decision)', () => ctx.admit(e.patientId), { ...(api.can(role, 'admit') ? {} : { disabled: true, hint: 'Requires Doctor or Front Office' }) })); D.related = [{ label: 'Patient 360', on: go('patient', e.patientId) }]; break; }
    case 'order': { const o = S.orders.find(x => x.id === id); if (!o) break; const p = api.patient(o.patientId); D.title = o.test + ' · ' + o.patient; D.sub = o.id + ' · ordered by ' + o.doctor + ' · ' + fmt(o.ts); D.badges = [badge(o.status), badge(o.kind)]; D.facts = [kv('Preparation', o.prep), kv('Queue', o.status === 'Resulted' ? 'Done' : '#' + (3 + (o.id.charCodeAt(6) % 9))), kv('ETA', o.status === 'Resulted' ? '—' : fmt(now + 25 + (o.id.charCodeAt(7) % 40))), kv('Patient language', p ? p.lang : '—')]; D.steps = [{ t: fmt(o.ts), who: o.doctor, what: 'Test ordered', k: 'human' }, { t: fmt(o.ts + 1), who: 'Diagnostic Coordination Agent', what: 'Preparation instructions sent · ' + (p && p.lang === 'Tamil' ? 'தமிழ் · ' : '') + o.prep, k: 'ai' }, { t: fmt(o.ts + 15), who: 'Patient', what: 'Confirmed on WhatsApp', k: 'human' }, { t: fmt(o.ts + 20), who: 'Diagnostic Coordination Agent', what: 'Queue assigned · grouped with other pending tests · ETA sent', k: 'tool' }].concat(o.status === 'Resulted' ? [{ t: fmt(o.ts + 90), who: 'LIS / RIS', what: 'Result received · critical-value rules evaluated · clinician notified', k: 'tool' }, { t: fmt(o.ts + 95), who: 'Diagnostic Coordination Agent', what: 'Patient told the report is ready — no interpretation given', k: 'ai' }] : []); D.related = [{ label: 'Patient 360', on: go('patient', o.patientId) }]; break; }
    case 'lab': { const l = S.labs.find(x => x.id === id); if (!l) break; D.title = l.analyte + ' ' + l.value + ' ' + l.unit + ' · ' + l.patient; D.sub = l.id + ' · ' + l.test + ' · ' + fmt(l.ts); D.badges = [badge(l.flag), badge(l.status)]; D.facts = [kv('Clinician', l.clinician || '—'), kv('Critical rule', l.critical ? 'Triggered · notified ' + fmt(l.notified) : 'Not triggered'), kv('Acknowledged', l.acked ? 'Yes · ' + l.ackedBy + ' ' + fmt(l.ackedAt) : l.critical ? 'No · timer ' + ago(l.notified) + ' / 15 m' : '—'), kv('Escalation', l.escalated ? 'Department head notified' : l.critical ? 'Pending timer' : '—')]; if (l.critical) D.steps = [{ t: fmt(l.ts), who: 'LIS', what: 'Result received', k: 'tool' }, { t: fmt(l.notified), who: 'Diagnostic Coordination Agent', what: 'Critical-value rule triggered · ' + l.clinician + ' notified (push + call)', k: 'ai' }, { t: fmt(l.notified), who: 'Orchestrator', what: '15-minute acknowledgement timer started', k: 'policy' }].concat(l.escalated ? [{ t: fmt(l.notified + 15), who: 'Orchestrator', what: 'Escalated to department head', k: 'warn' }] : []).concat(l.acked ? [{ t: fmt(l.ackedAt), who: l.ackedBy, what: 'Acknowledged · escalation stopped · audit created', k: 'human' }] : []); if (l.critical && !l.acked) D.actions.push(act('Acknowledge', () => api.acknowledgeCritical(l.id, role), { primary: true, ...needRole('Doctor') })); if (l.status !== 'Final') D.actions.push(act('Advance: ' + ({ Pending: 'Collected', Ordered: 'Collected', Collected: 'Processing', Processing: 'Result available', 'Result available': 'Verified', Verified: 'Released' })[l.status], () => api.advanceLab(l.id, role), { primary: !l.critical, ...(api.can(role, 'lab') ? {} : { disabled: true, hint: 'Requires Laboratory role' }) })); else D.actions.push(act('Amend result (versioned, reason required)', () => ctx.prompt('Amend ' + l.analyte, 'Reason for amendment', r => api.amendLab(l.id, +(l.value * 0.9).toFixed(2), r || 'Re-run on analyser 1')), { ...(api.can(role, 'lab') ? {} : { disabled: true, hint: 'Requires Laboratory role' }) })); D.facts.push(kv('Version', 'v' + (l.version || 1) + (l.history ? ' · ' + l.history.length + ' amendment(s)' : '')), kv('Delete policy', 'Finalised results are never deleted; amendments create a new version with audit')); D.ai = { title: 'Boundary', text: 'The agent notifies and times acknowledgement. It does not interpret the value or suggest treatment.' }; D.related = [{ label: 'Patient 360', on: go('patient', l.patientId) }]; break; }
    case 'radiology': { const r = S.radiology.find(x => x.id === id); if (!r) break; D.title = r.study + ' · ' + r.patient; D.sub = r.id + ' · received ' + fmt(r.received); D.badges = [badge(r.aiStatus), badge(r.priority), badge(r.radStatus)]; D.image = 'Radiology image placeholder · ' + r.modality; D.facts = [kv('Accession · DICOM', (r.accession || '—') + ' · ' + (r.dicom || '—')), kv('Viewed', r.viewed ? 'Opened by radiologist' : 'Unviewed'), kv('AI screening', r.aiStatus === 'Screened' ? 'Probability ' + (r.aiProb * 100).toFixed(0) + '% · ' + r.priority : 'Queued'), kv('Model', r.model + ' · silent validation'), kv('Timestamp', fmt(r.received + 6)), kv('Radiologist', r.radStatus + (r.readBy ? ' · ' + r.readBy : '')), kv('Final decision', r.final)]; D.ai = { title: 'AI SCREENING / PRIORITIZATION ONLY', text: 'AI output does not constitute a diagnosis. The radiologist remains the final decision-maker. This result is never shown to patients.' }; if (r.radStatus !== 'Read') D.actions = [act('Finalise: no acute finding', () => api.radiologistFinal(r.id, 'No acute finding', role), { ...needRole('Radiologist') }), act('Finalise: finding confirmed', () => api.radiologistFinal(r.id, 'Finding confirmed', role), { primary: true, ...needRole('Radiologist') }), act('Finalise: finding not confirmed', () => api.radiologistFinal(r.id, 'Finding not confirmed', role), { ...needRole('Radiologist') })]; D.related = [{ label: 'Patient 360', on: go('patient', r.patientId) }]; break; }
    case 'pharmacy': { const p = S.pharmacy.find(x => x.id === id); if (!p) break; D.title = p.drug + ' · ' + p.patient; D.sub = p.id + ' · ' + p.ward + ' · ' + fmt(p.ts); D.badges = [badge(p.status)]; D.facts = [kv('Quantity', p.qty), kv('Ward', p.ward), kv('Batch · expiry', 'B' + p.id.slice(-4) + ' · 03/2027')]; if (p.status === 'Pending') D.actions = [act('Dispense · deduct stock · charge bill', () => api.dispense(p.id, role), { primary: true, ...(api.can(role, 'pharmacy') ? {} : { disabled: true, hint: 'Requires Pharmacy or Nurse role' }) })]; if (p.batch) D.facts.push(kv('Dispensed batch', p.batch)); if (p.status === 'Dispensed') D.actions = [act('Accept return · reverse charge', () => api.returnMedication(p.id, role), { ...(api.can(role, 'pharmacy') ? {} : { disabled: true, hint: 'Requires Pharmacy or Nurse role' }) })]; if (p.status === 'Stock-out') { D.ai = { title: 'Discharge dependency', text: 'This stock-out blocks Fathima Begum (O-207). Central pharmacy has 60 units; transfer takes ~25 minutes.' }; D.actions = [act('Transfer from central pharmacy', () => api.mutate(s => { p.status = 'Dispensed'; const d = s.discharges.find(x => x.patientId === p.patientId && !x.completed); if (d) { d.deps.pharmacy = { status: 'done', at: s.clock, note: 'Stock transferred' }; d.log.unshift({ t: fmt(s.clock), who: 'S. Devi', what: 'Stock transferred · dispensed', k: 'human' }); } }, role, 'Stock transfer · Enoxaparin', p.patientId, 'Dispensed', 'Central → ward'), { primary: true })]; } D.related = [{ label: 'Patient 360', on: go('patient', p.patientId) }]; break; }
    case 'bill': { const b = S.bills.find(x => x.id === id); if (!b) break; const v = b.actual - b.estimate; D.title = b.patient + ' · ' + inr(b.actual); D.sub = b.id + ' · ' + b.admissionId; D.badges = [badge(b.dispute && b.dispute.status === 'Open' ? 'Disputed' : b.status)]; D.facts = [kv('Estimated', inr(b.estimate)), kv('Actual', inr(b.actual)), kv('Variance', (v >= 0 ? '+' : '') + inr(v) + ' (' + Math.round(100 * v / b.estimate) + '%)', v > b.estimate * 0.05 ? RD : ''), kv('Insurance', inr(b.insurance)), kv('Patient', inr(b.patientShare))]; if (b.lines) D.sections.push({ title: 'Line items · estimate → actual', items: b.lines.map(l => kv(l.item, inr(l.est) + ' → ' + inr(l.cur) + (l.note ? ' · ' + l.note : ''), l.cur > l.est ? RD : '')) }); if (api.billTax) { const tx = api.billTax(b); D.sections.push({ title: 'Tax (central configuration · demo rates)', items: tx.rows.filter(r => r.cgst + r.sgst > 0).map(r => kv(r.item + ' · ' + r.code, 'taxable ' + inr(r.taxable) + ' · CGST ' + inr(r.cgst) + ' · SGST ' + inr(r.sgst))).concat([kv('Exempt (SAC 9993)', inr(tx.rows.filter(r => r.cgst + r.sgst === 0).reduce((a, r) => a + r.taxable, 0))), kv('Total tax · gross', inr(tx.cgst + tx.sgst) + ' · ' + inr(tx.gross), '#15181b')]) }); } D.ai = { title: 'AI GENERATED · plain-language explanation · Billing Transparency Agent', text: b.dispute ? b.dispute.aiExplanation : b.patientId === 'MER-2026-008421' ? 'The bill is ₹23,450 above the estimate: a second balloon was needed before the stent (₹15,950, consumables) and one extra ward night on the doctor\'s advice (₹7,500). The stent is priced at the government-capped rate. Star Health approved ₹1,95,000; the remaining ₹73,450 is under enhancement.' : 'Charges follow Tariff FY26-27 v1.3. ' + (v > 0 ? 'The variance comes from an extra day of stay and additional consumables.' : 'The bill is within the counselled estimate.'), meta: 'Tariff FY26-27 v1.3 · confidence 95% · the billing executive remains responsible for the final response' }; if (b.dispute) { D.sections.push({ title: 'Dispute ' + b.dispute.id + ' · ' + b.dispute.status, items: [kv('Raised', fmt(b.dispute.raised)), kv('Reason', b.dispute.reason), kv('Owner', b.dispute.owner), kv('Resolution', b.dispute.resolution || 'Pending human review')] }); if (b.dispute.status === 'Open') D.actions = [act('Resolve: honour quoted rate (₹2,000 adjustment)', () => api.resolveDispute(b.id, role, 'Quoted rate honoured · ₹2,000 goodwill adjustment · tariff communication fixed'), { primary: true, ...needRole('Billing') }), act('Resolve: explain, no adjustment', () => api.resolveDispute(b.id, role, 'Itemised explanation accepted by family'), { ...needRole('Billing') })]; } else if (b.status === 'Provisional' && !b.dispute) D.actions = [act('Raise dispute (patient)', () => api.mutate(s => { b.dispute = { id: 'DSP-' + s.clock, raised: s.clock, reason: 'Patient questions consumables charge', status: 'Open', owner: 'K. Meena', aiExplanation: 'Consumables are itemised per the cath-lab log; the extra balloon is documented in the procedure note.', resolution: null }; b.status = 'Disputed'; }, 'Patient', 'Billing dispute raised', b.patientId, 'Open'))]; const due = Math.max(0, b.patientShare - (b.paid || 0)); D.facts.push(kv('Paid', inr(b.paid || 0) + ' · due ' + inr(due)), kv('Delete policy', 'Bills are voided or refunded with Finance approval, never deleted')); if (!/Voided|Void requested/.test(b.status)) { if (due > 0 && /Released|Pending|Part-paid|Paid/.test(b.status)) D.actions.push(act('Record payment · ' + inr(Math.min(due, 5000)) + ' UPI', () => api.recordPayment(b.id, Math.min(due, 5000), 'UPI', role), { primary: true, ...(api.can(role, 'payments') ? {} : { disabled: true, hint: 'Requires Billing or Front Office' }) }), act('Record payment · full ' + inr(due) + ' card', () => api.recordPayment(b.id, due, 'Card', role), { ...(api.can(role, 'payments') ? {} : { disabled: true, hint: 'Requires Billing or Front Office' }) })); if ((b.paid || 0) > 0) D.actions.push(act('Request refund ₹500 (Finance approval)', () => ctx.prompt('Refund on ' + b.id, 'Reason', r => api.requestRefund(b.id, 500, r || 'Duplicate charge', role)), needRole('Billing'))); D.actions.push(act('Request void (Finance approval)', () => ctx.prompt('Void ' + b.id, 'Reason for void', r => api.requestVoid(b.id, r || 'Raised on wrong encounter', role)), needRole('Billing'))); } D.related = [{ label: 'Patient 360', on: go('patient', b.patientId) }].concat(S.approvals.filter(a => a.patientId === b.patientId && a.type === 'Billing release' && a.status === 'Pending').map(a => ({ label: 'Release approval ' + a.id, on: go('approvals', a.id) }))); break; }
    case 'preauth': { const p = S.preauths.find(x => x.id === id); if (!p) break; D.title = p.patient + ' · ' + p.procedure; D.sub = p.id + ' · ' + p.insurer + ' · ' + p.policy; D.badges = [badge(p.status), badge('Denial risk ' + p.risk)]; D.facts = [kv('Estimate', p.estimate), kv('Requested', p.requested), kv('Approved', p.approved), kv('Patient liability', p.liability), kv('Completeness', p.completeness + '%'), kv('Human owner', p.exec), kv('Submitted', p.submitted), kv('Age', ago(p.createdTs))]; D.sections = [{ title: 'Documents', items: p.docs.required.map(d => kv(d, p.docs.missing.includes(d) ? 'Missing' : 'Collected', p.docs.missing.includes(d) ? RD : G)) }, { title: 'Denial-risk indicators · model preauth-denial v0.9', items: [kv('Documentation', p.docs.missing.length ? '+' + (p.docs.missing.length * 9) + ' pts · missing documents' : 'Complete'), kv('Package variance', /CABG|Spinal/.test(p.procedure) ? '+5 pts · above insurer package' : 'Within package'), kv('Threshold', 'Supervisor review at 25%')] }]; if (p.history.length) D.sections.push({ title: 'Insurer history', items: p.history.map(h => kv(h.t, h.e)) }); D.steps = [{ t: fmt(p.createdTs), who: 'Preauthorisation Assembly Agent', what: 'Admission detected · clinical + financial information collected · insurer requirements checked', k: 'ai' }, { t: fmt(p.createdTs + 2), who: 'Preauthorisation Assembly Agent', what: p.docs.missing.length ? 'Missing: ' + p.docs.missing.join(', ') + ' · requested from ward / family' : 'Submission draft assembled · risk ' + p.risk, k: p.docs.missing.length ? 'warn' : 'ai' }, { t: fmt(p.createdTs + 5), who: 'Human Approval', what: 'Human review by ' + p.exec + ' → executive submits', k: 'human' }]; const ap = S.approvals.find(a => a.patientId === p.patientId && /Preauth|Claim/.test(a.type) && a.status === 'Pending'); if (ap) D.actions.push(act('Open approval ' + ap.id, go('approvals', ap.id), { primary: true })); if (p.status === 'Approved' && S.discharges.some(d => d.patientId === p.patientId && d.completed)) D.actions.push(act('Submit claim (final bill + signed summary)', () => api.submitClaim(p.patientId, role), { primary: true, ...needRole('Insurance') })); if (p.status === 'Claim Submitted') D.actions.push(act('Simulate insurer settlement', () => api.simulateSettlement(p.patientId), { primary: true })); D.facts.push(kv('Lifecycle', 'Verification → Preauth → Approved / Query / Rejected → Claim → Settled')); if (/Submitted ·|Appeal/.test(p.status)) D.actions.push(act('Simulate insurer: approve', () => api.simulateInsurer(p.patientId, 'approve'), { primary: !ap }), act('Simulate insurer: reject', () => api.simulateInsurer(p.patientId, 'reject'))); D.related = [{ label: 'Patient 360', on: go('patient', p.patientId) }].concat(S.discharges.filter(d => d.patientId === p.patientId && !d.completed).map(d => ({ label: 'Discharge case', on: go('discharge-case', d.id) }))); break; }
    case 'feedback': { const f = S.feedback.find(x => x.id === id); if (!f) break; D.title = f.patient + ' · ' + f.category; D.sub = f.id + ' · ' + fmt(f.ts); D.badges = [badge(f.status), badge(f.priority), badge(f.sentiment)]; D.facts = [kv('Feedback', f.text), kv('Department', f.dept), kv('Owner', f.owner), kv('SLA', f.sla + ' m' + (f.breached ? ' · BREACHED' : ''), f.breached ? RD : ''), kv('Resolution', f.resolution || '—')]; D.ai = { title: 'AI classification · Feedback Agent', text: f.ai || ('Classified as ' + f.category + ' · ' + f.priority + ' · routed to ' + f.owner + '. Sensitive complaints are resolved by the human owner; the agent drafts the acknowledgement only.') }; if (f.status === 'Open' || f.status === 'In progress') D.actions = [act('Call patient · service recovery', () => api.mutate(s => { f.status = 'In progress'; }, role, 'Service recovery call', f.patientId, 'In progress'), { disabled: f.status === 'In progress' }), act('Resolve with patient response', () => api.mutate(s => { f.status = 'Resolved'; f.resolution = 'Apology + explanation · patient satisfied'; s.comms.push({ id: 'MSG-' + s.clock, patientId: f.patientId, patient: f.patient, ts: s.clock, channel: 'WhatsApp', type: 'Service recovery response', lang: 'TA', status: 'Delivered', agent: 'AG-05' }); }, role, 'Complaint resolved', f.patientId, 'Resolved'), { primary: true })]; D.related = [{ label: 'Patient 360', on: go('patient', f.patientId) }].concat(S.bills.filter(b => b.patientId === f.patientId && b.dispute).map(b => ({ label: 'Billing dispute', on: () => ctx.open('bill', b.id) }))); break; }
    case 'call': { const x = S.calls.find(y => y.id === id); if (!x) break; D.title = x.patient; D.sub = x.id + ' · ' + fmt(x.ts) + ' · ' + x.duration + ' · SIMULATED TELEPHONY DATA'; D.badges = [badge(x.sentiment), badge(x.handled)]; D.facts = [kv('Intent', x.intent), kv('Reason', x.summary)]; D.sections = [{ title: 'Transcript (simulated)', items: [kv('Caller', 'I want to know about ' + x.intent.toLowerCase() + '.'), kv('AI', 'I can help with that. Let me check the record for ' + x.patient.split(' (')[0] + '.'), kv('AI', x.handled === 'AI → human' ? 'This needs a person — transferring you to the desk with the context.' : 'Here is the status. Anything else?')] }, { title: 'Agent assist', items: [kv('Recommended action', x.handled === 'AI → human' ? 'Explain itemised variance · offer callback from Billing' : 'None · resolved'), kv('Escalation', x.sentiment === 'Frustrated' ? 'Flag for service recovery' : '—')] }]; D.actions = [act('Schedule callback · creates follow-up task', () => api.mutate(s => { s.followups.unshift({ id: 'FU-' + s.clock, patientId: x.patientId, patient: x.patient, doctor: 'Contact centre', ts: s.clock + 120, status: 'Reminder sent', channel: 'Voice' }); }, role, 'Callback scheduled', x.patientId, 'Follow-up created'))]; D.related = [{ label: 'Patient 360', on: go('patient', x.patientId) }]; break; }
    case 'staff': { const s = S.staff.find(x => x.id === id); if (!s) break; D.title = s.name; D.sub = s.id + ' · ' + s.role + ' · ' + s.unit; D.badges = [badge(s.status)]; D.facts = [kv('Shift', s.shift), kv('Role scope', PERMS[s.role.includes('Nurse') ? 'Nurse' : s.role.includes('Billing') ? 'Billing' : s.role.includes('Insurance') ? 'Insurance' : 'Front Office'].can.join(' · '))]; D.related = [{ label: 'Permissions', on: go('permissions') }]; break; }
    case 'knowledge': { const k = S.knowledge.find(x => x.id === id); if (!k) break; D.title = k.title; D.sub = k.id + ' · ' + k.dept + ' · v' + k.v; D.badges = [badge(k.status), badge(k.lang)]; D.facts = [kv('Effective', k.eff), kv('Review due', k.review), kv('Owner', k.dept), kv('Last indexed', k.indexed || k.eff), kv('Chunks', k.chunks || 40 + (k.id.charCodeAt(4) % 90)), kv('Retrievals (session)', k.retrievals || 0), kv('Delete policy', 'Governed documents are retired or deactivated, never deleted — past answers cite them')]; const gov = ['Governance Officer', 'AI Administrator'].includes(role); if (/Published|Indexed/.test(k.status)) D.actions.push(act('Deactivate (hide from RAG)', () => api.mutate(s => { k.status = 'Inactive'; }, role, 'Knowledge deactivated', k.id, 'Inactive'), gov ? {} : { disabled: true, hint: 'Requires Governance Officer or AI Administrator' })); if (k.status === 'Inactive') D.actions.push(act('Reactivate', () => api.mutate(s => { k.status = 'Published'; }, role, 'Knowledge reactivated', k.id, 'Published'), { primary: true, ...(gov ? {} : { disabled: true, hint: 'Requires Governance Officer or AI Administrator' }) })); if (k.status === 'Failed') D.actions.push(act('Retry ingestion', () => api.ingest(k.id, role), { primary: true })); D.actions.push(act('Delete', () => api.notify('Cannot delete', k.title + ': governed documents are retired, not deleted. Use Deactivate; the audit trail and past citations remain intact.', 'Medium', 'Knowledge base', { page: 'knowledge', id: k.id }), { hint: 'Explains policy' })); if (k.status === 'Conflict') { D.ai = { title: 'Conflict detected', text: 'Medication Safety v3.2 requires a second-nurse double-check for all high-alert medications; v4.0 limits it to IV administration. Both documents are withheld from answers on this topic until the Nursing Superintendent resolves the conflict. The platform does not silently choose the newer document.' }; D.actions = [act('Resolve: v4.0 supersedes v3.2', () => api.mutate(s => { s.knowledge.filter(x => /Medication Safety/.test(x.title)).forEach(x => x.status = x.v === '4.0' ? 'Published' : 'Expired'); }, role, 'Knowledge conflict resolved', k.id, 'v4.0 published', 'Nursing Superintendent decision'), { primary: true, ...(['Governance Officer', 'AI Administrator', 'Nurse'].includes(role) ? {} : { disabled: true, hint: 'Requires Governance Officer or Nursing role' }) })]; } else if (k.status === 'Draft' || k.status === 'Review due') D.actions = [act(k.status === 'Draft' ? 'Publish (owner approval)' : 'Renew review date', () => api.mutate(s => { k.status = 'Published'; k.review = '2027'; }, role, 'Knowledge published', k.id, 'Published'), { primary: true, ...(['Governance Officer', 'AI Administrator'].includes(role) ? {} : { disabled: true, hint: 'Requires Governance Officer' }) })]; D.steps = ['Upload', 'OCR', 'Classify', 'PII/PHI scan', 'Chunk', 'Embed', 'Index', 'Validate', 'Owner approves', k.status === 'Draft' ? 'Publish (pending)' : 'Published', 'Monitor', 'Review'].map((s, i) => ({ t: '', who: 'Step ' + (i + 1), what: s, k: /Owner|Publish/.test(s) ? 'human' : 'tool' })); break; }
    case 'incident': { const i = S.incidents.find(x => x.id === id); if (!i) break; const flow = ['Open', 'Investigating', 'Contained', 'Resolved', 'Closed']; const idx = flow.indexOf(i.status); D.title = i.id + ' · ' + i.agent; D.sub = i.description; D.badges = [badge(i.severity), badge(i.status)]; D.facts = [kv('Version', i.version), kv('Patient / workflow', i.patient), kv('Detected', fmt(i.detected)), kv('Owner', i.owner), kv('Containment', i.containment), kv('Root cause', i.root), kv('Resolution', i.resolution), kv('Corrective action', i.corrective)]; D.steps = flow.map((s, j) => ({ t: '', who: s, what: j < idx ? 'done' : j === idx ? 'current' : 'pending', k: j < idx ? 'tool' : j === idx ? 'warn' : 'pending' })); if (idx < flow.length - 1) D.actions = [act('Move to ' + flow[idx + 1], () => api.mutate(s => { i.status = flow[idx + 1]; if (flow[idx + 1] === 'Resolved') i.resolution = 'Fix verified · regression suite passed'; if (flow[idx + 1] === 'Closed') { const a = s.agents.find(x => x.id === i.agentId); if (a && a.status === 'Disabled') { a.status = 'Published'; a.statusNote = 'Active'; } } }, role, 'Incident status → ' + flow[idx + 1], i.id, flow[idx + 1]), { primary: true, ...(['AI Administrator', 'Governance Officer', 'Hospital Management'].includes(role) ? {} : { disabled: true, hint: 'Requires AI Administrator or Governance Officer' }) })]; D.related = [{ label: 'Agent', on: go('agent', i.agentId) }]; break; }
    case 'integration': { const lis = /LIS/.test(id); D.title = id; D.sub = 'Prototype connector · not a production integration'; D.badges = [badge(lis && S.lisDegraded ? 'Degraded' : 'Healthy')]; D.facts = [kv('Fallback', lis ? 'Phone verification of critical values · manual result entry' : 'Manual workflow with SLA timers'), kv('Owner', 'Integration Engineer')]; if (lis) D.actions = [act(S.lisDegraded ? 'Retry connector' : 'Simulate outage', () => api.toggleLis(), { primary: true })]; break; }
    case 'risk': { const r = RISKS[id]; D.title = r[0]; D.badges = [badge(r[1])]; D.facts = [kv('Owner', r[2]), kv('Mitigation', r[3]), kv('Residual', r[4]), kv('Last review', '05 Sep 2026 · AI Governance Committee')]; break; }
    case 'role': { const p = PERMS[id]; D.title = id; D.sections = [{ title: 'Can', items: p.can.map(x => kv('✓', x, G)) }, { title: 'Cannot', items: p.cannot.map(x => kv('✕', x, RD)) }]; D.actions = [act('Switch to this role', () => ctx.setRole(id), { primary: true })]; break; }
    case 'audit': { const a = S.audit.find(x => x.id === id); if (!a) break; D.title = a.action; D.sub = a.id + ' · ' + fmt(a.ts); D.facts = [kv('Actor', a.actor), kv('Entity', a.entity), kv('Before', a.before), kv('After', a.after), kv('Reason', a.reason), kv('Approval', a.approval), kv('Source', a.source), kv('Correlation', a.corr)]; if (a.entity && a.entity.startsWith('MER')) D.related = [{ label: 'Patient 360', on: go('patient', a.entity) }]; break; }
    case 'bed': { const b = S.beds.find(x => x.id === id); D.title = 'Bed ' + b.id; D.sub = b.dept + ' · ' + b.cls; D.badges = [badge(b.status)]; D.facts = [kv('Lifecycle', 'Available → Reserved → Occupied → Discharge Pending → Cleaning → Available')]; if (b.status === 'Cleaning') D.actions = [act('Mark cleaned', () => api.mutate(s => { b.status = 'Free'; }, role, 'Bed cleaned', b.id, 'Free'), { primary: true })]; break; }
    default: { D.title = String(id); D.sub = kind; D.facts = [kv('Detail', 'Reference record · read-only in this prototype')]; }
  }
  return D;
}

return { pill, NAV, allowed, HOME, buildList, buildDetail };
})();
window.__mm2.erp = (function(){
// ERP extension: administration masters, tax, vendors, procurement, inventory/stores, LIS (samples, analyzers, reagents, QC), HR, canteen, claims.
const ERP_ROLES = ['Pathologist', 'Finance Manager', 'Store Manager', 'Procurement Officer', 'HR Manager', 'Canteen Manager', 'IT Administrator', 'Auditor'];
function installErp(api) {
  const S = api.S, { audit, notify, emit, nid, rnd, int, pick, fmt, inr } = api._h; const pad = n => String(n).padStart(2, '0');
  // ---- Administration masters ----
  S.designations = ['Consultant', 'Senior Resident', 'Staff Nurse', 'Nursing Supervisor', 'Lab Technician', 'Pathologist', 'Pharmacist', 'Billing Executive', 'Insurance Executive', 'Store Keeper', 'Procurement Officer', 'HR Executive', 'Canteen Supervisor', 'IT Administrator'].map((n, i) => ({ id: 'DSG-' + pad(i + 1), name: n, grade: 'G' + (1 + i % 6), active: true }));
  S.branches = [{ id: 'BR-01', name: 'Main hospital', city: 'Chennai', beds: 300, active: true }, { id: 'BR-02', name: 'OPD Centre', city: 'Chennai', beds: 0, active: true }];
  S.services = [['SV-001', 'OP consultation · consultant', 'Consultation', 800, 'exempt'], ['SV-002', 'Room · General ward / night', 'Room', 4500, 'exempt'], ['SV-003', 'Room · Private / night', 'Room', 10000, 'exempt'], ['SV-004', 'ICU / night', 'Room', 18000, 'exempt'], ['SV-010', 'CBC', 'Laboratory', 450, 'exempt'], ['SV-011', 'KFT', 'Laboratory', 650, 'exempt'], ['SV-012', 'Lipid profile', 'Laboratory', 900, 'exempt'], ['SV-013', 'Troponin-I', 'Laboratory', 1400, 'exempt'], ['SV-014', 'Electrolytes', 'Laboratory', 500, 'exempt'], ['SV-020', 'ECHO', 'Radiology', 2800, 'exempt'], ['SV-021', 'CT Abdomen (contrast)', 'Radiology', 8500, 'exempt'], ['SV-030', 'PTCA · cath lab charges', 'Procedure', 68000, 'exempt'], ['SV-031', 'Drug-eluting stent (NPPA)', 'Implant', 65000, 'GST-12'], ['SV-040', 'Pharmacy · medicines', 'Pharmacy', 0, 'GST-12'], ['SV-041', 'Consumables', 'Consumables', 0, 'GST-12'], ['SV-050', 'Patient meal · standard', 'Canteen', 180, 'GST-5'], ['SV-051', 'Ambulance', 'Transport', 2500, 'GST-5-transport'], ['SV-060', 'Health check package', 'Package', 4999, 'GST-18']].map(([id, name, cat, price, tax]) => ({ id, name, category: cat, price, taxCode: tax, active: true }));
  S.taxes = [['GST-0', 'Healthcare services (exempt)', 'Exempt', 0, 0, 0, '9993', 'Clinical, room, diagnostics'], ['GST-5', 'GST 5% · food', 'GST', 2.5, 2.5, 5, '9963', 'Canteen meals'], ['GST-5-transport', 'GST 5% · transport', 'GST', 2.5, 2.5, 5, '9964', 'Ambulance (non-emergency)'], ['GST-12', 'GST 12% · medicines & implants', 'GST', 6, 6, 12, '3004 / 9021', 'Pharmacy, implants, consumables'], ['GST-18', 'GST 18% · non-clinical', 'GST', 9, 9, 18, '9993', 'Wellness packages, cosmetic, parking'], ['CESS-0', 'Cess (none)', 'Cess', 0, 0, 0, '—', '—']].map(([code, name, type, cgst, sgst, rate, hsn, applies]) => ({ code, name, type, cgst, sgst, igst: rate, rate, hsn, applies, inclusive: false, effective: '01 Apr 2026', active: code !== 'CESS-0', note: 'DEMO CONFIGURATION · not legal advice · confirm with tax counsel' }));
  S.paymentMethods = ['Cash', 'UPI', 'Card', 'Net banking', 'Insurer NEFT', 'Cheque'].map((m, i) => ({ id: 'PM-' + (i + 1), name: m, active: m !== 'Cheque', gateway: /UPI|Card|Net/.test(m) ? 'Razorpay (simulated)' : '—' }));
  S.insurers = [['Star Health', 'Medi Assist TPA', 'cashless', 'In-network', 'Active', '01 Apr 2026', '31 Mar 2027', 'Preauth for IP > ₹25,000', '10% co-pay above 60'], ['ICICI Lombard', 'Direct', 'cashless', 'In-network', 'Active', '01 Apr 2026', '31 Mar 2027', 'Preauth for all IP', 'Room rent cap ₹7,500'], ['HDFC Ergo', 'Paramount TPA', 'cashless + reimbursement', 'In-network', 'Active', '01 Jan 2026', '31 Dec 2026', 'Preauth for all IP', 'Implant cap per policy'], ['Aditya Birla Health', 'Direct', 'cashless', 'In-network', 'Active', '01 Apr 2026', '31 Mar 2027', 'Preauth for IP', '—'], ['Bajaj Allianz', 'Health India TPA', 'cashless', 'In-network', 'Active', '01 Jul 2025', '30 Jun 2026', 'Preauth for IP', 'Contract expired · renewal pending'], ['Niva Bupa', 'Direct', 'cashless', 'In-network', 'Active', '01 Apr 2026', '31 Mar 2027', 'Preauth for IP', '—'], ['Care Health', 'Direct', 'reimbursement', 'Out-of-network', 'Inactive', '01 Apr 2025', '31 Mar 2026', 'Reimbursement only', 'Contract not renewed'], ['ManipalCigna', 'MDIndia TPA', 'cashless', 'In-network', 'Active', '01 Apr 2026', '31 Mar 2027', 'Preauth for IP > ₹50,000', '—'], ['CMCHIS (Govt)', 'UIIC', 'cashless', 'Empanelled', 'Active', '01 Jan 2026', '31 Dec 2026', 'Package-based', 'Package rates apply']].map(([name, tpa, method, network, status, eff, exp, auth, rules], i) => ({ id: 'INS-' + pad(i + 1), name, tpa, method, network, status: /expired/i.test(rules) ? 'Expiring' : status, effective: eff, expiry: exp, auth, rules, contact: 'claims@' + name.toLowerCase().replace(/[^a-z]/g, '') + '.example', phone: '1800-' + int(100, 999) + '-' + int(1000, 9999) }));
  // ---- Vendors ----
  const VT = ['Medical Supplies', 'Pharmacy', 'Laboratory', 'Equipment', 'Maintenance', 'IT', 'Housekeeping', 'Security', 'Food/Canteen', 'General Supplies'];
  const VN = ['Chennai Surgicals', 'MedPlus Distributors', 'BioLab Reagents', 'Siemens Healthineers (service)', 'Roche Diagnostics', 'Apex Facility Services', 'SecureGuard', 'Annapoorna Caterers', 'OfficeMart', 'Beckman Coulter (service)', 'Cipla Distributors', 'Sun Pharma Distributors', 'Kavin Linen Services', 'TechNet IT Solutions', 'Sri Balaji Vegetables', 'Aster Gas Supplies', 'Medtronic India', 'Abbott Rapid Kits', 'CleanPro Housekeeping', 'PrimeCare Equipment'];
  S.vendors = VN.map((n, i) => ({ id: 'VEN-' + pad(i + 1), name: n, type: VT[i % VT.length], contact: pick(['R. Kumar', 'S. Priya', 'A. Rahman', 'D. Joseph']), phone: '+91 9' + int(1000, 9999) + '•••• ' + int(1000, 9999), email: 'sales@' + n.toLowerCase().replace(/[^a-z]/g, '') + '.example', gstin: '33' + 'ABCDE'.slice(0, 5) + int(1000, 9999) + 'F1Z' + int(1, 9), pan: 'ABCDE' + int(1000, 9999) + 'F', terms: pick(['Net 30', 'Net 45', 'Net 60', 'Advance']), contractStart: '01 Apr 2026', contractEnd: i % 7 === 3 ? '30 Sep 2026' : '31 Mar 2027', status: i % 9 === 8 ? 'Inactive' : i % 7 === 3 ? 'Expiring' : 'Active', rating: (3.4 + rnd() * 1.5).toFixed(1), spendMtd: int(50, 900) * 1000 }));
  // ---- Stores & inventory ----
  S.stores = ['Main Store', 'Pharmacy Store', 'OT Store', 'ICU Store', 'Lab Store', 'Emergency Store', 'Ward Store · Cardiac', 'Canteen Store'].map((n, i) => ({ id: 'STR-' + pad(i + 1), name: n, keeper: pick(S.staff).name, active: true }));
  const ITEMS = [['Paracetamol 650 mg tab', 'Medicine', 'tab', 'Pharmacy Store', 4200, 2000, 6000, 1.2], ['Enoxaparin 40 mg inj', 'Medicine', 'vial', 'Pharmacy Store', 18, 60, 300, 385], ['Aspirin 75 mg tab', 'Medicine', 'tab', 'Pharmacy Store', 3100, 1500, 5000, 0.6], ['Clopidogrel 75 mg tab', 'Medicine', 'tab', 'Pharmacy Store', 900, 1000, 4000, 3.8], ['Atorvastatin 40 mg tab', 'Medicine', 'tab', 'Pharmacy Store', 2600, 1000, 4000, 4.1], ['Ceftriaxone 1 g inj', 'Medicine', 'vial', 'Pharmacy Store', 240, 200, 800, 48], ['Insulin glargine', 'Medicine', 'pen', 'Pharmacy Store', 0, 40, 160, 620], ['Ondansetron 4 mg inj', 'Medicine', 'amp', 'Pharmacy Store', 610, 300, 1000, 9], ['IV cannula 20G', 'Consumable', 'pc', 'Main Store', 1800, 800, 3000, 22], ['Syringe 5 ml', 'Consumable', 'pc', 'Main Store', 5400, 2000, 8000, 4], ['N95 mask', 'PPE', 'pc', 'Main Store', 620, 1000, 4000, 28], ['Nitrile gloves M', 'PPE', 'box', 'Main Store', 140, 100, 400, 380], ['Surgical gown', 'Surgical', 'pc', 'OT Store', 210, 150, 500, 140], ['Suture 3-0 vicryl', 'Surgical', 'pc', 'OT Store', 85, 100, 400, 260], ['PTCA balloon 3.0×15', 'Consumable', 'pc', 'OT Store', 6, 8, 24, 15950], ['Drug-eluting stent 3.0×18', 'Implant', 'pc', 'OT Store', 9, 6, 20, 65000], ['CBC reagent pack (Sysmex)', 'Reagent', 'pack', 'Lab Store', 4, 6, 20, 8200], ['Creatinine reagent (Cobas)', 'Reagent', 'kit', 'Lab Store', 7, 4, 16, 6400], ['Troponin-I cartridge', 'Reagent', 'box', 'Lab Store', 3, 5, 20, 12500], ['Lipid panel reagent', 'Reagent', 'kit', 'Lab Store', 5, 4, 12, 7100], ['EDTA tube (lavender)', 'Lab supply', 'pc', 'Lab Store', 2400, 1500, 6000, 6], ['Serum tube (red)', 'Lab supply', 'pc', 'Lab Store', 1900, 1500, 6000, 7], ['Urine culture plate', 'Lab supply', 'pc', 'Lab Store', 120, 100, 400, 45], ['QC material · Level 2', 'QC', 'vial', 'Lab Store', 2, 4, 12, 3900], ['CT contrast (iohexol) 100 ml', 'Radiology supply', 'vial', 'Main Store', 34, 30, 120, 1150], ['Oxygen cylinder D-type', 'Equipment', 'pc', 'Emergency Store', 22, 15, 40, 900], ['Rice (raw)', 'Canteen', 'kg', 'Canteen Store', 140, 100, 400, 58], ['Dal (toor)', 'Canteen', 'kg', 'Canteen Store', 22, 40, 150, 140], ['Milk', 'Canteen', 'L', 'Canteen Store', 60, 80, 200, 62], ['A4 paper', 'Office', 'ream', 'Main Store', 48, 30, 120, 260]];
  const expiryOf = i => i % 11 === 2 ? 'Expired' : i % 7 === 1 ? 'Near expiry' : 'OK';
  S.items = ITEMS.map(([name, cat, unit, store, qty, min, max, price], i) => ({ id: 'ITM-' + pad(i + 1), name, category: cat, unit, store, qty, min, max, reorder: Math.round(min * 1.2), price, value: qty * price, batch: 'B' + int(1000, 9999), expiry: expiryOf(i) === 'Expired' ? '31 Aug 2026' : expiryOf(i) === 'Near expiry' ? '15 Oct 2026' : pick(['03/2027', '08/2027', '12/2027']), expiryState: expiryOf(i), supplier: pick(S.vendors.filter(v => v.status === 'Active')).name, rack: 'R' + int(1, 9) + '-' + int(1, 30), status: qty === 0 ? 'Out of stock' : qty < min ? 'Low stock' : qty > max ? 'Overstock' : 'In stock' }));
  S.stockTx = []; for (let i = 0; i < 60; i++) { const it = pick(S.items); const type = pick(['Issue', 'Issue', 'Receipt', 'Transfer', 'Return', 'Adjustment']); S.stockTx.push({ id: 'STX-' + (70000 + i), itemId: it.id, item: it.name, type, qty: int(1, 40) * (type === 'Issue' ? -1 : 1), from: type === 'Receipt' ? pick(S.vendors).name : it.store, to: type === 'Issue' ? pick(['Cardiac Ward', 'ICU', 'OT-2', 'Emergency', 'Laboratory']) : type === 'Transfer' ? pick(S.stores).name : it.store, by: pick(S.staff).name, ts: -int(0, 5) * 1440 + int(480, 1200), status: type === 'Adjustment' && rnd() < 0.5 ? 'Pending approval' : 'Posted', reason: type === 'Adjustment' ? pick(['Count variance', 'Damaged', 'Expired write-off']) : '—' }); }
  // ---- Procurement ----
  const PRST = ['Draft', 'Pending Approval', 'Approved', 'Ordered', 'Partially Received', 'Received', 'Closed', 'Rejected', 'Cancelled'];
  S.purchases = []; for (let i = 0; i < 16; i++) { const it = pick(S.items); const v = S.vendors.find(x => x.name === it.supplier) || pick(S.vendors); const st = i < 3 ? 'Pending Approval' : PRST[int(2, 8)]; const qty = it.max; const amount = qty * it.price; const tax = S.taxes.find(t => t.code === (/Medicine|Implant|Consumable|Reagent/.test(it.category) ? 'GST-12' : /Canteen/.test(it.category) ? 'GST-5' : 'GST-18')); S.purchases.push({ id: 'PR-2026-' + (300 + i), itemId: it.id, item: it.name, qty, unit: it.unit, dept: pick(['Pharmacy', 'Laboratory', 'OT', 'ICU', 'Canteen', 'Stores']), requester: pick(S.staff).name, vendorId: v.id, vendor: v.name, amount, taxCode: tax.code, taxAmt: Math.round(amount * tax.rate / 100), total: Math.round(amount * (1 + tax.rate / 100)), status: st, po: /Ordered|Received|Closed|Partially/.test(st) ? 'PO-2026-' + (1100 + i) : '—', grn: /Received|Closed|Partially/.test(st) ? 'GRN-2026-' + (900 + i) : '—', invoice: /Closed/.test(st) ? 'INV-' + v.id + '-' + int(100, 999) : '—', paid: st === 'Closed', ts: -int(0, 20) * 1440 + 600, history: [] }); }
  // ---- LIS: test master, panels, samples, analyzers, reagents, QC ----
  S.labTests = [['CBC', 'Hb', 'Hematology', 'EDTA whole blood', 'Lavender', 'g/dL', '11–15.5', '<7', '>20', 'Sysmex XN-1000', 60, 'Numeric', 'SV-010'], ['KFT', 'Creatinine', 'Biochemistry', 'Serum', 'Red', 'mg/dL', '0.6–1.3', '—', '>6', 'Cobas c311', 90, 'Numeric', 'SV-011'], ['Lipid profile', 'LDL', 'Biochemistry', 'Serum (fasting)', 'Red', 'mg/dL', '<130', '—', '—', 'Cobas c311', 120, 'Numeric', 'SV-012'], ['Troponin-I', 'Troponin-I', 'Biochemistry', 'Serum', 'Red', 'ng/mL', '<0.04', '—', '>0.4', 'Abbott i-STAT', 45, 'Numeric', 'SV-013'], ['Electrolytes', 'K⁺', 'Biochemistry', 'Serum', 'Red', 'mmol/L', '3.5–5.1', '<2.8', '>6.0', 'Cobas c311', 60, 'Numeric', 'SV-014'], ['LFT', 'ALT', 'Biochemistry', 'Serum', 'Red', 'U/L', '7–56', '—', '>1000', 'Cobas c311', 90, 'Numeric', 'SV-011'], ['HbA1c', 'HbA1c', 'Biochemistry', 'EDTA whole blood', 'Lavender', '%', '4–5.7', '—', '—', 'Bio-Rad D-10', 120, 'Numeric', 'SV-011'], ['Blood sugar', 'Glucose', 'Biochemistry', 'Fluoride plasma', 'Grey', 'mg/dL', '70–140', '<50', '>400', 'Cobas c311', 45, 'Numeric', 'SV-011'], ['Urine culture', 'Growth', 'Microbiology', 'Mid-stream urine', 'Sterile cup', 'CFU/mL', 'No growth', '—', '—', 'Manual', 2880, 'Text', 'SV-011'], ['D-dimer', 'D-dimer', 'Hematology', 'Citrate plasma', 'Blue', 'ng/mL', '<500', '—', '—', 'Sysmex CS-2500', 60, 'Numeric', 'SV-011'], ['Thyroid profile', 'TSH', 'Immunology', 'Serum', 'Red', 'mIU/L', '0.4–4.0', '—', '—', 'Cobas e411', 180, 'Numeric', 'SV-011'], ['Dengue NS1', 'NS1 antigen', 'Serology', 'Serum', 'Red', '—', 'Negative', '—', '—', 'Manual (rapid)', 60, 'Positive/Negative', 'SV-011']].map(([panel, analyte, section, specimen, container, unit, ref, cl, ch, analyzer, tat, rtype, svc], i) => ({ id: 'LT-' + pad(i + 1), code: panel.toUpperCase().replace(/[^A-Z0-9]/g, '').slice(0, 6), panel, analyte, section, specimen, container, unit, ref, critLow: cl, critHigh: ch, analyzer, tat, resultType: rtype, serviceId: svc, method: pick(['Photometric', 'Impedance', 'ECLIA', 'Culture', 'Immunoassay']), active: true }));
  S.panels = [['CBC', ['Hb', 'WBC', 'Platelets', 'RBC', 'MCV']], ['LFT', ['ALT', 'AST', 'Bilirubin', 'ALP', 'Albumin']], ['KFT', ['Creatinine', 'Urea', 'Uric acid', 'eGFR']], ['Lipid profile', ['Total cholesterol', 'LDL', 'HDL', 'Triglycerides']], ['Thyroid profile', ['TSH', 'T3', 'T4']], ['Diabetes profile', ['Fasting glucose', 'PP glucose', 'HbA1c']], ['Urine routine', ['Colour', 'pH', 'Protein', 'Glucose', 'Microscopy']], ['Coagulation profile', ['PT', 'INR', 'aPTT']]].map(([n, tests], i) => ({ id: 'PN-' + pad(i + 1), name: n, tests, count: tests.length }));
  S.analyzers = [['Sysmex XN-1000', 'Hematology', 'Sysmex', 'XN-1000', 'Online', 'HL7 v2.5 (simulated)', 'CBC, D-dimer'], ['Cobas c311', 'Biochemistry', 'Roche', 'c311', 'Online', 'ASTM E1394 (simulated)', 'KFT, LFT, Lipid, Electrolytes, Glucose'], ['Cobas e411', 'Immunology', 'Roche', 'e411', 'Maintenance', 'ASTM (simulated)', 'TSH, T3, T4'], ['Abbott i-STAT', 'Point of care', 'Abbott', 'i-STAT 1', 'Online', 'API (simulated)', 'Troponin-I, electrolytes'], ['Bio-Rad D-10', 'Biochemistry', 'Bio-Rad', 'D-10', 'Offline', 'ASTM (simulated)', 'HbA1c'], ['Sysmex CS-2500', 'Hematology', 'Sysmex', 'CS-2500', 'Calibration', 'HL7 (simulated)', 'PT, aPTT, D-dimer']].map(([name, section, mfr, model, status, iface, tests], i) => ({ id: 'ANL-' + pad(i + 1), name, section, manufacturer: mfr, model, serial: 'SN' + int(100000, 999999), location: 'Lab · ' + section, vendor: /Sysmex/.test(mfr) ? 'Siemens Healthineers (service)' : /Roche/.test(mfr) ? 'Roche Diagnostics' : 'PrimeCare Equipment', installed: '2024-0' + (i + 2), calibration: i === 5 ? 'Due today' : '01 Sep 2026', maintenance: '15 Aug 2026', nextMaint: i === 2 ? 'Today (in progress)' : '15 Nov 2026', status, comm: status === 'Online' ? 'Connected' : status === 'Offline' ? 'Disconnected' : 'Paused', iface, tests, downtimeH: status === 'Online' ? 0 : int(1, 6), errors24h: status === 'Offline' ? 3 : 0 }));
  S.reagents = S.items.filter(i => i.category === 'Reagent' || i.category === 'QC').map(i => ({ ...i, lot: 'L' + int(10000, 99999), machine: /CBC/.test(i.name) ? 'Sysmex XN-1000' : /Troponin/.test(i.name) ? 'Abbott i-STAT' : 'Cobas c311', opened: pick(['—', '05 Sep 2026']), storage: '2–8 °C' }));
  S.qc = Array.from({ length: 12 }, (_, i) => { const a = pick(S.analyzers.filter(x => x.status === 'Online')); const fail = i % 5 === 4; return { id: 'QC-' + (500 + i), analyzer: a.name, test: pick(a.tests.split(', ')), level: pick(['L1', 'L2']), lot: 'L' + int(10000, 99999), value: (fail ? 1.35 : 0.98 + rnd() * 0.06).toFixed(2), expected: '0.95–1.05', result: fail ? 'Fail' : 'Pass', operator: pick(S.staff.filter(s => /Lab/.test(s.role))).name || 'M. Ganesh', ts: -int(0, 2) * 1440 + int(360, 480) }; });
  // Samples for every lab order
  const REJ = ['Insufficient quantity', 'Hemolyzed', 'Clotted', 'Wrong container', 'Mislabeled'];
  S.samples = S.labs.map((l, i) => { const t = S.labTests.find(x => x.panel === l.test) || S.labTests[0]; const st = l.status === 'Final' ? 'Released' : i % 13 === 5 ? 'Sample Rejected' : pick(['Pending Collection', 'Sample Collected', 'Sample Received', 'Processing', 'Machine Processing', 'Result Entered', 'Technical Validation', 'Pathologist Validation']); return { id: 'SMP-' + (88000 + i), accession: 'ACC' + String(260000 + i), barcode: '*' + (260000 + i) + '*', labId: l.id, orderId: l.orderId, patientId: l.patientId, patient: l.patient, test: l.test, specimen: t.specimen, container: t.container, priority: l.critical ? 'STAT' : rnd() < 0.15 ? 'STAT' : 'Routine', collector: pick(['Anitha Kumar', 'Sheela J', 'M. Ganesh']), collectedAt: l.ts - 60, receivedAt: l.ts - 40, analyzer: t.analyzer, status: st, rejectReason: st === 'Sample Rejected' ? pick(REJ) : null, tatTarget: t.tat, history: [{ t: fmt(l.ts - 90), e: 'Order accepted · billing/insurance cleared' }, { t: fmt(l.ts - 60), e: 'Collected · barcode printed · patient verified (UHID + wristband)' }] }; });
  S.labs.forEach((l, i) => { l.sampleId = S.samples[i].id; l.accession = S.samples[i].accession; });
  // ---- HR ----
  S.employees = S.staff.map((s, i) => ({ ...s, empId: 'EMP-' + pad(i + 1), designation: s.role, joined: (2015 + i % 10) + '-0' + (1 + i % 9) + '-01', employment: i % 23 === 7 ? 'Resigned' : i % 17 === 5 ? 'On leave' : 'Active', manager: S.doctors[i % 5].name, location: 'Kolathur', qualification: /Nurse/.test(s.role) ? 'B.Sc Nursing' : /Lab/.test(s.role) ? 'DMLT' : /Pharm/.test(s.role) ? 'B.Pharm' : 'Graduate', salary: '₹' + (25 + (i % 12) * 5) + ',000', today: i % 17 === 5 ? 'On leave' : i % 11 === 3 ? 'Absent' : i % 9 === 4 ? 'Late' : 'Present' }));
  S.leaves = Array.from({ length: 10 }, (_, i) => { const e = S.employees[i * 5 + 2]; return { id: 'LV-' + (200 + i), empId: e.empId, employee: e.name, type: pick(['Casual', 'Sick', 'Earned']), from: (14 + i) + ' Sep', to: (14 + i + int(0, 2)) + ' Sep', days: int(1, 3), reason: pick(['Family function', 'Fever', 'Personal', 'Travel']), status: i < 4 ? 'Pending' : pick(['Approved', 'Approved', 'Rejected']), approver: 'HR Manager' }; });
  S.holidays = [['02 Oct', 'Gandhi Jayanti'], ['20 Oct', 'Deepavali'], ['25 Dec', 'Christmas'], ['14 Jan', 'Pongal']].map(([d, n]) => ({ d, n }));
  S.payroll = S.employees.slice(0, 20).map(e => ({ empId: e.empId, employee: e.name, month: 'Aug 2026', gross: e.salary, deductions: '₹' + int(2, 6) + ',000', net: '₹' + (parseInt(e.salary.replace(/[₹,]/g, '')) - 4000).toLocaleString('en-IN'), status: 'Paid' }));
  // ---- Canteen ----
  S.menu = [['Idli + sambar', 'Breakfast', 60, 'Standard'], ['Pongal', 'Breakfast', 60, 'Standard'], ['Oats porridge (low salt)', 'Breakfast', 55, 'Cardiac'], ['Rice, sambar, poriyal', 'Lunch', 120, 'Standard'], ['Khichdi (soft)', 'Lunch', 90, 'Soft'], ['Diabetic thali', 'Lunch', 130, 'Diabetic'], ['Renal thali (low K)', 'Lunch', 140, 'Renal'], ['Chapati + dal', 'Dinner', 110, 'Standard'], ['Clear soup + bread', 'Dinner', 70, 'Liquid'], ['Fruit bowl', 'Snacks', 50, 'Standard'], ['Buttermilk', 'Snacks', 25, 'Standard'], ['Staff meal · lunch', 'Lunch', 80, 'Staff']].map(([name, meal, price, diet], i) => ({ id: 'MN-' + pad(i + 1), name, meal, price, diet, active: true }));
  const DIET = p => /Card/.test(p.dept) ? 'Cardiac' : /Nephro/.test(p.dept) ? 'Renal' : /Paed/.test(p.dept) ? 'Soft' : p.age > 70 ? 'Soft' : 'Standard';
  S.diets = S.patients.filter(p => p.type === 'IP').map(p => ({ patientId: p.id, patient: p.name, bed: (p.bed || '').split(' ')[0], diet: DIET(p), allergies: 'None', orderedBy: p.doctor, since: p.admitted || '10 Sep 2026' }));
  S.meals = []; S.diets.forEach((d, i) => ['Breakfast', 'Lunch', 'Dinner'].forEach((m, j) => { const item = S.menu.find(x => x.meal === m && x.diet === d.diet) || S.menu.find(x => x.meal === m); const due = [480, 750, 1140][j]; S.meals.push({ id: 'ML-' + (4000 + i * 3 + j), patientId: d.patientId, patient: d.patient, bed: d.bed, meal: m, item: item.name, diet: d.diet, due, status: due + 40 < S.clock ? 'Delivered' : due < S.clock ? 'Out for delivery' : 'Scheduled', kind: 'Patient' }); }));
  for (let i = 0; i < 25; i++) S.meals.push({ id: 'ML-' + (4300 + i), patientId: '—', patient: pick(S.staff).name, bed: 'Staff canteen', meal: 'Lunch', item: 'Staff meal · lunch', diet: 'Staff', due: 780, status: S.clock > 780 ? 'Delivered' : 'Scheduled', kind: 'Staff' });
  // ---- Claims (from preauths that reached claim) ----
  S.preauths.forEach((p, i) => { if (/Approved|Partially|Rejected|Settled|Claim/.test(p.status) && i % 3 === 0 && !p.claimNo) { p.claimNo = 'CLM-2026-' + (5100 + i); p.authNo = 'AUTH' + int(100000, 999999); p.claimStatus = p.status === 'Rejected' ? 'Rejected' : p.status === 'Partially Approved' ? 'Partially Approved' : i % 6 === 0 ? 'Under Review' : 'Settled'; p.claimed = p.requested; p.paidAmt = p.claimStatus === 'Settled' ? p.approved : '—'; p.rejectReason = p.status === 'Rejected' ? 'Pre-existing disease · 24-month waiting period not completed (policy clause 4.2)' : null; } });
  // ---- KPI helpers ----
  api.erpKpis = () => ({ items: S.items.length, stockValue: S.items.reduce((a, i) => a + i.value, 0), low: S.items.filter(i => i.status === 'Low stock').length, out: S.items.filter(i => i.status === 'Out of stock').length, nearExp: S.items.filter(i => i.expiryState === 'Near expiry').length, expired: S.items.filter(i => i.expiryState === 'Expired').length, pendingPO: S.purchases.filter(p => /Pending|Approved/.test(p.status)).length, pendingGRN: S.purchases.filter(p => /Ordered|Partially/.test(p.status)).length, employees: S.employees.length, active: S.employees.filter(e => e.employment === 'Active').length, present: S.employees.filter(e => /Present|Late/.test(e.today)).length, absent: S.employees.filter(e => e.today === 'Absent').length, onLeave: S.employees.filter(e => e.today === 'On leave').length, pendingLeave: S.leaves.filter(l => l.status === 'Pending').length, mealsToday: S.meals.length, delivered: S.meals.filter(m => m.status === 'Delivered').length, pendingMeals: S.meals.filter(m => m.status !== 'Delivered').length, patientMeals: S.meals.filter(m => m.kind === 'Patient').length, staffMeals: S.meals.filter(m => m.kind === 'Staff').length, labOrders: S.samples.length, pendingCollection: S.samples.filter(s => s.status === 'Pending Collection').length, processing: S.samples.filter(s => /Processing/.test(s.status)).length, validation: S.samples.filter(s => /Validation|Result Entered/.test(s.status)).length, rejected: S.samples.filter(s => s.status === 'Sample Rejected').length, released: S.samples.filter(s => s.status === 'Released').length, delayed: S.samples.filter(s => s.status !== 'Released' && S.clock - s.collectedAt > s.tatTarget).length, machinesOnline: S.analyzers.filter(a => a.status === 'Online').length, machinesOffline: S.analyzers.filter(a => a.status !== 'Online').length, qcFail: S.qc.filter(q => q.result === 'Fail').length, tatCompliance: Math.round(100 * S.samples.filter(s => s.status !== 'Released' ? S.clock - s.collectedAt <= s.tatTarget : true).length / S.samples.length), claimsSubmitted: S.preauths.filter(p => p.claimNo).length, claimsRejected: S.preauths.filter(p => p.claimStatus === 'Rejected').length, claimsQuery: S.preauths.filter(p => /Query|Under Review/.test(p.claimStatus || p.status)).length, payables: S.purchases.filter(p => p.grn !== '—' && !p.paid).reduce((a, p) => a + p.total, 0), receivablesPatient: S.bills.reduce((a, b) => a + Math.max(0, b.patientShare - (b.paid || 0)), 0), receivablesInsurer: S.bills.filter(b => b.status !== 'Settled').reduce((a, b) => a + b.insurance, 0), taxCollected: S.bills.reduce((a, b) => a + Math.round((b.lines ? 0.12 * b.lines.filter(l => /Implant|Pharmacy|Consumables/.test(l.item)).reduce((x, l) => x + l.cur, 0) : b.actual * 0.03)), 0) });
  api.billTax = b => { const lines = b.lines || [{ item: 'Services', cur: b.actual }]; const rows = lines.map(l => { const code = /Implant|Pharmacy|Consumables/.test(l.item) ? 'GST-12' : /meal|Canteen/i.test(l.item) ? 'GST-5' : 'GST-0'; const t = S.taxes.find(x => x.code === code); const taxable = l.cur; const cgst = Math.round(taxable * t.cgst / 100), sgst = Math.round(taxable * t.sgst / 100); return { item: l.item, code, taxable, cgst, sgst, total: taxable + cgst + sgst }; }); return { rows, taxable: rows.reduce((a, r) => a + r.taxable, 0), cgst: rows.reduce((a, r) => a + r.cgst, 0), sgst: rows.reduce((a, r) => a + r.sgst, 0), gross: rows.reduce((a, r) => a + r.total, 0) }; };
  // ---- Actions ----
  const recompute = it => { it.value = it.qty * it.price; it.status = it.qty === 0 ? 'Out of stock' : it.qty < it.min ? 'Low stock' : it.qty > it.max ? 'Overstock' : 'In stock'; };
  Object.assign(api, {
    issueStock(itemId, qty, dept, role) { const it = S.items.find(x => x.id === itemId); if (!it) return; if (it.expiryState === 'Expired') { notify('Issue blocked', it.name + ' · batch ' + it.batch + ' is expired · quarantine and write-off required', 'High', 'Inventory', { page: 'inventory', id: itemId }); audit(role, 'Stock issue BLOCKED · expired batch', itemId, it.qty, it.qty, 'Expired', '—', 'Inventory'); emit(); return; } const q = Math.min(qty, it.qty); it.qty -= q; recompute(it); S.stockTx.unshift({ id: nid('STX'), itemId, item: it.name, type: 'Issue', qty: -q, from: it.store, to: dept, by: role, ts: S.clock, status: 'Posted', reason: '—' }); if (it.status === 'Low stock' || it.status === 'Out of stock') notify(it.status, it.name + ' · ' + it.qty + ' ' + it.unit + ' left (min ' + it.min + ') · reorder suggested', 'Medium', 'Inventory', { page: 'inventory', id: itemId }); audit(role, 'Stock issued · ' + q + ' ' + it.unit + ' → ' + dept, itemId, it.qty + q, it.qty, '—', '—', 'Inventory'); emit(); },
    transferStock(itemId, qty, toStore, role) { const it = S.items.find(x => x.id === itemId); if (!it || it.qty < qty) return; S.stockTx.unshift({ id: nid('STX'), itemId, item: it.name, type: 'Transfer', qty, from: it.store, to: toStore, by: role, ts: S.clock, status: 'Posted', reason: '—' }); audit(role, 'Stock transfer · ' + qty + ' ' + it.unit + ' ' + it.store + ' → ' + toStore, itemId, it.store, toStore, '—', '—', 'Inventory'); emit(); },
    adjustStock(itemId, delta, reason, role) { const it = S.items.find(x => x.id === itemId); if (!it) return; S.stockTx.unshift({ id: nid('STX'), itemId, item: it.name, type: 'Adjustment', qty: delta, from: it.store, to: it.store, by: role, ts: S.clock, status: 'Pending approval', reason }); S.approvals.unshift({ id: nid('AP'), type: 'Stock adjustment', patientId: '—', patient: it.name, agentId: '—', agent: 'Stores (human request)', action: 'Adjust ' + it.name + ' by ' + (delta > 0 ? '+' : '') + delta + ' ' + it.unit, risk: 'Medium', reason, created: S.clock, sla: 240, owner: 'Store Manager', status: 'Pending', entityId: itemId, amount: delta }); audit(role, 'Stock adjustment requested', itemId, it.qty, it.qty + delta, reason, 'Pending', 'Inventory'); emit(); },
    requestPurchase(itemId, role) { const it = S.items.find(x => x.id === itemId); if (!it) return; const v = S.vendors.find(x => x.name === it.supplier) || S.vendors[0]; const qty = it.max - it.qty; const tax = S.taxes.find(t => t.code === (/Medicine|Implant|Consumable|Reagent|QC/.test(it.category) ? 'GST-12' : /Canteen/.test(it.category) ? 'GST-5' : 'GST-18')); const pr = { id: nid('PR-2026'), itemId, item: it.name, qty, unit: it.unit, dept: it.store, requester: role, vendorId: v.id, vendor: v.name, amount: qty * it.price, taxCode: tax.code, taxAmt: Math.round(qty * it.price * tax.rate / 100), total: Math.round(qty * it.price * (1 + tax.rate / 100)), status: 'Pending Approval', po: '—', grn: '—', invoice: '—', paid: false, ts: S.clock, history: [{ t: fmt(S.clock), e: 'Requisition raised by ' + role }] }; S.purchases.unshift(pr); S.approvals.unshift({ id: nid('AP'), type: 'Purchase approval', patientId: '—', patient: it.name, agentId: '—', agent: 'Procurement (human request)', action: 'Approve PR ' + pr.id + ' · ' + qty + ' ' + it.unit + ' · ' + inr(pr.total) + ' incl. ' + tax.code, risk: pr.total > 100000 ? 'High' : 'Medium', reason: 'Reorder · stock ' + it.qty + ' below min ' + it.min, created: S.clock, sla: 1440, owner: 'Procurement Officer', status: 'Pending', entityId: pr.id }); notify('Purchase requisition raised', pr.id + ' · ' + it.name + ' · awaiting approval', 'Low', 'Procurement', { page: 'procurement', id: pr.id }); audit(role, 'Purchase requisition created', pr.id, '—', 'Pending Approval', it.name, 'Pending', 'Procurement'); emit(); return pr.id; },
    advancePurchase(prId, role) { const p = S.purchases.find(x => x.id === prId); if (!p) return; const flow = { 'Pending Approval': 'Approved', Approved: 'Ordered', Ordered: 'Received', 'Partially Received': 'Received', Received: 'Closed' }; const before = p.status, next = flow[before]; if (!next) return; p.status = next; if (next === 'Ordered') p.po = nid('PO-2026'); if (next === 'Received') { p.grn = nid('GRN-2026'); const it = S.items.find(x => x.id === p.itemId); if (it) { it.qty += p.qty; recompute(it); S.stockTx.unshift({ id: nid('STX'), itemId: it.id, item: it.name, type: 'Receipt', qty: p.qty, from: p.vendor, to: it.store, by: role, ts: S.clock, status: 'Posted', reason: p.grn + ' · QC passed' }); } } if (next === 'Closed') { p.invoice = 'INV-' + p.vendorId + '-' + int(100, 999); p.paid = true; } p.history.unshift({ t: fmt(S.clock), e: before + ' → ' + next + ' by ' + role }); audit(role, 'Procurement ' + before + ' → ' + next, p.id, before, next, p.item, '—', 'Procurement', p.po); emit(); },
    // LIS
    advanceSample(sampleId, role) { const s = S.samples.find(x => x.id === sampleId); if (!s) return; const l = S.labs.find(x => x.id === s.labId); const flow = ['Pending Collection', 'Sample Collected', 'Sample Received', 'Processing', 'Machine Processing', 'Result Entered', 'Technical Validation', 'Pathologist Validation', 'Released']; const i = flow.indexOf(s.status); if (i < 0 || i === flow.length - 1) return; const before = s.status; s.status = flow[i + 1];
      if (s.status === 'Sample Collected') { s.collectedAt = S.clock; s.history.unshift({ t: fmt(S.clock), e: 'Collected by ' + role + ' · barcode ' + s.barcode + ' · patient verified' }); }
      if (s.status === 'Sample Received') { s.receivedAt = S.clock; s.history.unshift({ t: fmt(S.clock), e: 'Received in lab · barcode scanned · accession ' + s.accession }); }
      if (s.status === 'Machine Processing') { const a = S.analyzers.find(x => x.name === s.analyzer); if (a && a.status !== 'Online') { s.status = 'Processing'; notify('Analyzer unavailable', s.analyzer + ' is ' + a.status + ' · ' + s.test + ' queued; manual entry or backup analyzer', 'Medium', 'LIS', { page: 'lab-analyzers', id: a.id }); emit(); return; } s.history.unshift({ t: fmt(S.clock), e: 'ANALYZER SIMULATOR · order sent to ' + s.analyzer + ' (' + (a ? a.iface : '') + ')' }); setTimeout(() => { s.history.unshift({ t: fmt(S.clock), e: 'Result message received · matched patient ' + s.patientId + ' · accession ' + s.accession + ' · unit ' + (l ? l.unit : '') + ' ✓' }); s.status = 'Result Entered'; if (l) { l.status = 'Result Entered'; if (l.flag === 'Critical' && !l.notified) { l.notified = S.clock; } } const rg = S.reagents.find(r => r.machine === s.analyzer); if (rg) { const it = S.items.find(x => x.id === rg.id); if (it && it.qty > 0) { it.qty -= 1; recompute(it); rg.qty = it.qty; S.stockTx.unshift({ id: nid('STX'), itemId: it.id, item: it.name, type: 'Issue', qty: -1, from: 'Lab Store', to: s.analyzer, by: 'LIS consumption rule', ts: S.clock, status: 'Posted', reason: s.accession }); if (it.status !== 'In stock') notify(it.status, it.name + ' · ' + it.qty + ' left', 'Medium', 'Lab inventory', { page: 'lab-reagents', id: it.id }); } } audit('Analyzer simulator', 'Result received and matched', s.patientId, 'Machine Processing', 'Result Entered', s.accession, '—', 'LIS interface', s.id); emit(); }, 1500); }
      if (s.status === 'Technical Validation') s.history.unshift({ t: fmt(S.clock), e: 'Technical validation by ' + role });
      if (s.status === 'Pathologist Validation') s.history.unshift({ t: fmt(S.clock), e: 'Pathologist review by ' + role });
      if (s.status === 'Released') { s.releasedAt = S.clock; s.history.unshift({ t: fmt(S.clock), e: 'Released by ' + role + ' · doctor + patient notified' }); if (l) { l.status = 'Final'; const o = S.orders.find(x => x.id === l.orderId); if (o) o.status = 'Resulted'; } S.comms.push({ id: nid('MSG'), patientId: s.patientId, patient: s.patient, ts: S.clock, channel: 'WhatsApp', type: 'Report ready · ' + s.test, lang: 'TA', status: 'Delivered', agent: 'AG-10' }); notify('Report released', s.patient + ' · ' + s.test + ' · TAT ' + (S.clock - s.collectedAt) + ' m (target ' + s.tatTarget + ')', 'Low', 'LIS', { page: 'lab-queue', id: s.id }); }
      if (l && s.status !== 'Released' && s.status !== 'Result Entered') l.status = s.status;
      audit(role, 'Sample ' + before + ' → ' + s.status, s.patientId, before, s.status, s.accession, '—', 'LIS', s.id); emit(); },
    rejectSample(sampleId, reason, role) { const s = S.samples.find(x => x.id === sampleId); if (!s) return; s.status = 'Sample Rejected'; s.rejectReason = reason; s.history.unshift({ t: fmt(S.clock), e: 'REJECTED · ' + reason + ' · by ' + role + ' · recollection requested' }); const n = { ...s, id: nid('SMP'), accession: 'ACC' + int(261000, 269999), barcode: '*recollect*', status: 'Pending Collection', rejectReason: null, collectedAt: S.clock, history: [{ t: fmt(S.clock), e: 'Recollection created from ' + s.id + ' (' + reason + ')' }] }; n.barcode = '*' + n.accession.slice(3) + '*'; S.samples.unshift(n); notify('Sample rejected · recollection', s.patient + ' · ' + s.test + ' · ' + reason, 'Medium', 'LIS', { page: 'lab-queue', id: n.id }); audit(role, 'Sample rejected · ' + reason, s.patientId, s.status, 'Sample Rejected', 'Recollection ' + n.id, '—', 'LIS', s.id); emit(); },
    setAnalyzer(id, status, role) { const a = S.analyzers.find(x => x.id === id); if (!a) return; const before = a.status; a.status = status; a.comm = status === 'Online' ? 'Connected' : status === 'Offline' ? 'Disconnected' : 'Paused'; if (status !== 'Online') notify('Analyzer ' + status, a.name + ' · ' + a.section + ' · tests routed to manual/backup', 'Medium', 'LIS', { page: 'lab-analyzers', id }); audit(role, 'Analyzer status ' + before + ' → ' + status, id, before, status, '—', '—', 'Lab equipment'); emit(); },
    runQc(analyzerId, role) { const a = S.analyzers.find(x => x.id === analyzerId); if (!a) return; const fail = rnd() < 0.2; S.qc.unshift({ id: nid('QC'), analyzer: a.name, test: a.tests.split(', ')[0], level: 'L2', lot: 'L' + int(10000, 99999), value: (fail ? 1.31 : 1.0 + rnd() * 0.04).toFixed(2), expected: '0.95–1.05', result: fail ? 'Fail' : 'Pass', operator: role, ts: S.clock }); if (fail) { a.status = 'Error'; notify('QC failed', a.name + ' · patient results blocked until QC passes', 'High', 'LIS QC', { page: 'lab-analyzers', id: analyzerId }); } audit(role, 'QC run · ' + (fail ? 'FAIL' : 'Pass'), analyzerId, '—', fail ? 'Fail' : 'Pass', a.name, '—', 'LIS QC'); emit(); },
    // HR
    markAttendance(empId, state, role) { const e = S.employees.find(x => x.empId === empId); if (!e) return; const b = e.today; e.today = state; audit(role, 'Attendance ' + b + ' → ' + state, empId, b, state, '—', '—', 'HR'); emit(); },
    requestLeave(empId, role) { const e = S.employees.find(x => x.empId === empId); if (!e) return; const lv = { id: nid('LV'), empId, employee: e.name, type: 'Casual', from: '16 Sep', to: '16 Sep', days: 1, reason: 'Personal', status: 'Pending', approver: 'HR Manager' }; S.leaves.unshift(lv); S.approvals.unshift({ id: nid('AP'), type: 'Leave approval', patientId: '—', patient: e.name, agentId: '—', agent: 'HR (human request)', action: 'Approve 1 day casual leave · ' + e.name, risk: 'Low', reason: 'Balance 4 days · roster impact none', created: S.clock, sla: 1440, owner: 'HR Manager', status: 'Pending', entityId: lv.id }); audit(role, 'Leave requested', empId, '—', 'Pending', 'Casual 1 day', 'Pending', 'HR'); emit(); },
    // Canteen
    advanceMeal(id, role) { const m = S.meals.find(x => x.id === id); if (!m) return; const flow = { Scheduled: 'In kitchen', 'In kitchen': 'Out for delivery', 'Out for delivery': 'Delivered' }; const b = m.status; m.status = flow[b] || b; if (m.status === 'Delivered') { const rice = S.items.find(x => x.name === 'Rice (raw)'); if (rice && /Lunch|Dinner/.test(m.meal) && rice.qty > 0) { rice.qty -= 0.25; recompute(rice); } } audit(role, 'Meal ' + b + ' → ' + m.status, m.patientId, b, m.status, m.item, '—', 'Canteen'); emit(); },
    setDiet(patientId, diet, role) { const d = S.diets.find(x => x.patientId === patientId); if (!d) return; const b = d.diet; d.diet = diet; S.meals.filter(m => m.patientId === patientId && m.status === 'Scheduled').forEach(m => { const it = S.menu.find(x => x.meal === m.meal && x.diet === diet) || S.menu.find(x => x.meal === m.meal); m.item = it.name; m.diet = diet; }); notify('Diet changed', d.patient + ' · ' + b + ' → ' + diet + ' · kitchen schedule updated', 'Low', 'Canteen', { page: 'canteen' }); audit(role, 'Diet plan changed', patientId, b, diet, '—', '—', 'Canteen'); emit(); },
    // Claims
    advanceClaim(paId, role) { const p = S.preauths.find(x => x.id === paId); if (!p) return; const flow = { undefined: 'Claim Ready', 'Claim Ready': 'Submitted', Submitted: 'Under Review', 'Under Review': 'Query Raised', 'Query Raised': 'Approved', Approved: 'Settled', 'Partially Approved': 'Settled' }; const b = p.claimStatus; p.claimStatus = flow[b] || b; if (!p.claimNo) p.claimNo = nid('CLM-2026'); if (p.claimStatus === 'Settled') { p.paidAmt = p.approved; const bill = S.bills.find(x => x.patientId === p.patientId && x.admissionId !== '—'); if (bill) bill.status = 'Settled'; } p.history.unshift({ t: fmt(S.clock), e: 'Claim ' + (b || 'created') + ' → ' + p.claimStatus }); audit(role, 'Claim ' + (b || 'created') + ' → ' + p.claimStatus, p.patientId, b || '—', p.claimStatus, p.claimNo, '—', 'TPA portal (simulated)'); emit(); },
    // master data
    toggleActive(coll, id, role) { const list = S[coll]; const r = list && list.find(x => x.id === id); if (!r) return; const key = 'active' in r ? 'active' : 'status'; const b = r[key]; if (key === 'active') r.active = !r.active; else r.status = r.status === 'Inactive' ? 'Active' : 'Inactive'; audit(role, coll + ' ' + (key === 'active' ? (r.active ? 'activated' : 'deactivated') : r.status), id, String(b), String(r[key]), '—', '—', 'Administration'); emit(); },
  });
  // approvals hook: extend approve() for ERP types
  const origApprove = api.approve.bind(api);
  api.approve = (id, role, note) => { const a = S.approvals.find(x => x.id === id); if (a && a.status === 'Pending') { if (a.type === 'Stock adjustment') { const it = S.items.find(x => x.id === a.entityId); if (it) { it.qty = Math.max(0, it.qty + a.amount); recompute(it); const tx = S.stockTx.find(t => t.itemId === it.id && t.status === 'Pending approval'); if (tx) tx.status = 'Posted'; } } if (a.type === 'Purchase approval') { const p = S.purchases.find(x => x.id === a.entityId); if (p) { p.status = 'Approved'; p.history.unshift({ t: fmt(S.clock), e: 'Approved by ' + role }); } } if (a.type === 'Leave approval') { const lv = S.leaves.find(x => x.id === a.entityId); if (lv) { lv.status = 'Approved'; const e = S.employees.find(x => x.empId === lv.empId); if (e) e.leaveBalance = (e.leaveBalance || 4) - lv.days; } } } return origApprove(id, role, note); };
  return api;
}
const ERP_APPROVER = { 'Stock adjustment': 'Store Manager', 'Purchase approval': 'Procurement Officer', 'Leave approval': 'HR Manager' };

return { ERP_ROLES, installErp, ERP_APPROVER };
})();
window.__mm2.views2 = (function(){
// ERP pages: administration masters, finance, procurement, inventory, LIS, HR, canteen.
const { pill } = window.__mm2.views;
const G = 'oklch(0.4 0.12 150)', A = 'oklch(0.5 0.13 70)', RD = 'oklch(0.45 0.17 25)', T = 'oklch(0.45 0.1 200)';
const c = (t, o = {}) => ({ t: t == null ? '—' : String(t), c: o.c || '', bg: o.bg || '', m: !!o.m, b: !!o.b, pill: !!o.p });
const P = s => ({ ...c(s), ...pill(s), pill: true });
const kv = (k, v, col) => ({ k, v: v == null ? '—' : String(v), c: col || '' });
const NAV2 = [
  { g: 'Laboratory (LIS)', items: [['lab-dashboard', 'Lab Dashboard'], ['lab-queue', 'Lab Work Queue'], ['lab-tests', 'Test Master & Panels'], ['lab-analyzers', 'Analyzers & Maintenance'], ['lab-reagents', 'Reagents & QC']] },
  { g: 'Supply chain', items: [['inventory', 'Inventory'], ['stores', 'Stores & Stock Moves'], ['procurement', 'Procurement'], ['vendors', 'Vendors']] },
  { g: 'Finance', items: [['finance', 'Finance Dashboard'], ['claims', 'Claims'], ['tax', 'Tax Configuration']] },
  { g: 'People & services', items: [['hr-dashboard', 'HR Dashboard'], ['employees', 'Employees'], ['attendance', 'Attendance & Leave'], ['canteen', 'Canteen & Diet']] },
  { g: 'Administration', items: [['departments', 'Departments & Designations'], ['services', 'Services & Pricing'], ['insurers', 'Insurers & TPAs'], ['payment-methods', 'Payment Methods']] },
];
const ERP_PAGES = NAV2.flatMap(g => g.items.map(i => i[0]));
const ERP_ROLE_PAGES = { Laboratory: ['lab-dashboard', 'lab-queue', 'lab-tests', 'lab-analyzers', 'lab-reagents', 'inventory'], Pathologist: ['command', 'patients', 'laboratory', 'lab-dashboard', 'lab-queue', 'lab-tests', 'lab-analyzers', 'lab-reagents', 'knowledge', 'notifications', 'chat', 'hr', 'exceptions'], Pharmacy: ['inventory', 'stores', 'procurement'], 'Store Manager': ['command', 'inventory', 'stores', 'procurement', 'vendors', 'approvals', 'notifications', 'knowledge', 'chat', 'hr', 'exceptions', 'reports'], 'Procurement Officer': ['command', 'inventory', 'procurement', 'vendors', 'approvals', 'notifications', 'knowledge', 'chat', 'hr', 'tax', 'reports'], 'Finance Manager': ['command', 'billing', 'insurance', 'claims', 'finance', 'tax', 'procurement', 'vendors', 'approvals', 'notifications', 'knowledge', 'chat', 'hr', 'reports', 'analytics', 'data-financial', 'exceptions', 'audit'], 'HR Manager': ['command', 'hr-dashboard', 'employees', 'attendance', 'staff', 'hr', 'approvals', 'notifications', 'knowledge', 'chat', 'reports', 'users'], 'Canteen Manager': ['command', 'canteen', 'inventory', 'stores', 'procurement', 'notifications', 'knowledge', 'chat', 'hr'], 'IT Administrator': ['command', 'users', 'roles', 'permissions', 'identity', 'integrations', 'notifications', 'config', 'departments', 'services', 'payment-methods', 'tax', 'insurers', 'vendors', 'audit', 'knowledge', 'chat', 'hr', 'data-quality', 'reports'], Auditor: ['command', 'audit', 'approvals', 'billing', 'claims', 'finance', 'procurement', 'vendors', 'inventory', 'stores', 'users', 'roles', 'permissions', 'incidents', 'risk', 'governance', 'reports', 'analytics', 'knowledge', 'chat', 'hr', 'notifications', 'exceptions'], Billing: ['finance', 'claims', 'tax'], Insurance: ['claims', 'insurers'], Nurse: ['canteen', 'lab-queue'], Doctor: ['lab-queue', 'lab-dashboard'], 'Hospital Management': null, 'Governance Officer': ['audit', 'tax', 'insurers', 'vendors'] };
const ERP_HOME = { Pathologist: 'lab-queue', 'Store Manager': 'inventory', 'Procurement Officer': 'procurement', 'Finance Manager': 'finance', 'HR Manager': 'hr-dashboard', 'Canteen Manager': 'canteen', 'IT Administrator': 'users', 'Auditor': 'audit', Laboratory: 'lab-dashboard' };

function buildList2(page, api, ctx) {
  const S = api.S, now = api.clock(), { role } = ctx; const fmt = api.fmt, ago = t => api.ago(t, now), inr = api.inr, K = api.erpKpis();
  const open = (kind, id) => () => ctx.open(kind, id); const go = (p, i) => () => ctx.go(p, i);
  const L = { title: '', sub: '', cols: [], rows: [], stats: [], banner: null, filters: [] };
  const withF = (vals, key) => { L.filters = ['All'].concat(vals).map(v => ({ label: v, active: (ctx.filter || 'All') === v, on: () => ctx.setFilter(v) })); return r => (ctx.filter || 'All') === 'All' || String(r[key]) === ctx.filter; };
  const st = arr => arr.map(([k, v, col, pg]) => ({ k, v, c: col || '', on: pg ? go(pg) : null }));
  switch (page) {
    case 'lab-dashboard': { L.title = 'Laboratory dashboard'; L.sub = 'LIS · every counter opens the work queue filtered · analyzers connected through a clearly labelled simulator'; L.stats = st([['Today\'s orders', K.labOrders, '', 'lab-queue'], ['Pending collection', K.pendingCollection, A, 'lab-queue'], ['Processing', K.processing, '', 'lab-queue'], ['Validation pending', K.validation, A, 'lab-queue'], ['Critical', S.labs.filter(l => l.critical && !l.acked).length, RD, 'laboratory'], ['Rejected', K.rejected, RD, 'lab-queue'], ['Delayed (TAT)', K.delayed, RD, 'lab-queue'], ['Released', K.released, G, 'lab-queue'], ['Machines online', K.machinesOnline, G, 'lab-analyzers'], ['Machines offline / maint.', K.machinesOffline, RD, 'lab-analyzers'], ['QC failures', K.qcFail, RD, 'lab-reagents'], ['TAT compliance', K.tatCompliance + '%', K.tatCompliance > 85 ? G : A, 'lab-queue']]); L.banner = { kind: 'warn', text: 'DEMO / ANALYZER SIMULATOR — HL7/ASTM messages are simulated at the interface boundary; real analyzers replace the simulator without changing the LIS workflow.' }; const bySec = {}; S.samples.forEach(s => { const t = S.labTests.find(x => x.panel === s.test); const sec = t ? t.section : 'Other'; bySec[sec] = bySec[sec] || { n: 0, rel: 0, del: 0, rej: 0, rev: 0 }; bySec[sec].n++; if (s.status === 'Released') bySec[sec].rel++; if (s.status !== 'Released' && now - s.collectedAt > s.tatTarget) bySec[sec].del++; if (s.status === 'Sample Rejected') bySec[sec].rej++; const sv = S.services.find(x => x.id === (t || {}).serviceId); bySec[sec].rev += sv ? sv.price : 500; }); L.cols = [['Section', 160], ['Tests today', 90], ['Released', 80], ['Delayed', 80], ['Rejected', 80], ['Rejection rate', 100], ['Revenue', 100], ['Business question', 320]]; L.rows = Object.entries(bySec).map(([sec, v]) => ({ id: sec, cells: [c(sec, { b: 1 }), c(v.n, { m: 1 }), c(v.rel, { m: 1 }), c(v.del, { m: 1, c: v.del ? RD : '' }), c(v.rej, { m: 1 }), c(Math.round(100 * v.rej / v.n) + '%', { m: 1 }), c(inr(v.rev), { m: 1 }), c(v.del ? 'Where are lab delays? → ' + sec + ' has ' + v.del + ' beyond TAT' : 'Section within TAT')], go: go('lab-queue') })); break; }
    case 'lab-queue': { const f = withF(['Pending Collection', 'Sample Collected', 'Sample Received', 'Processing', 'Machine Processing', 'Result Entered', 'Technical Validation', 'Pathologist Validation', 'Released', 'Sample Rejected'], 'status'); L.title = 'Lab work queue'; L.sub = S.samples.length + ' samples · Order → Collection → Barcode → Received → Analyzer → Result → Technical → Pathologist → Release · STAT first'; L.cols = [['Accession', 100], ['Barcode', 90], ['Patient', 150], ['Test', 110], ['Specimen', 120], ['Priority', 70], ['Analyzer', 130], ['Collected', 80], ['TAT', 90], ['Status', 170]]; L.rows = S.samples.filter(f).sort((a, b) => (b.priority === 'STAT') - (a.priority === 'STAT') || (a.status === 'Released') - (b.status === 'Released')).map(s => { const el = s.status === 'Released' ? (s.releasedAt || s.collectedAt + s.tatTarget - 10) - s.collectedAt : now - s.collectedAt; const late = el > s.tatTarget; return { id: s.id, cells: [c(s.accession, { m: 1 }), c(s.barcode, { m: 1 }), c(s.patient, { b: 1 }), c(s.test), c(s.specimen), P(s.priority === 'STAT' ? 'Priority' : 'Routine'), c(s.analyzer), c(fmt(s.collectedAt), { m: 1 }), c(el + ' / ' + s.tatTarget + ' m', { m: 1, c: late ? RD : '' }), P(s.status)], go: open('sample', s.id) }; }); break; }
    case 'lab-tests': { L.title = 'Test master & panels'; L.sub = S.labTests.length + ' tests · ' + S.panels.length + ' panels · reference and critical ranges drive flags; pricing comes from the central service master'; L.stats = st(S.panels.map(p => [p.name, p.count + ' tests', '', 'lab-tests'])); L.cols = [['Code', 70], ['Test / panel', 120], ['Analyte', 100], ['Section', 110], ['Specimen', 130], ['Container', 80], ['Unit', 70], ['Reference', 90], ['Crit low', 60], ['Crit high', 60], ['Analyzer', 120], ['TAT', 60], ['Price', 70], ['Status', 70]]; L.rows = S.labTests.map(t => { const sv = S.services.find(x => x.id === t.serviceId); return { id: t.id, cells: [c(t.code, { m: 1 }), c(t.panel, { b: 1 }), c(t.analyte), c(t.section), c(t.specimen), c(t.container), c(t.unit), c(t.ref, { m: 1 }), c(t.critLow, { m: 1, c: t.critLow !== '—' ? RD : '' }), c(t.critHigh, { m: 1, c: t.critHigh !== '—' ? RD : '' }), c(t.analyzer), c(t.tat + ' m', { m: 1 }), c(sv ? inr(sv.price) : '—', { m: 1 }), P(t.active ? 'Active' : 'Inactive')], go: open('labtest', t.id) }; }); break; }
    case 'lab-analyzers': { L.title = 'Analyzers & maintenance'; L.sub = 'Connected lab assets · Online → Offline / Maintenance / Calibration / Error / Retired · interface layer simulated (HL7 / ASTM / API)'; L.stats = st([['Online', K.machinesOnline, G], ['Not online', K.machinesOffline, RD], ['Calibration due', S.analyzers.filter(a => /Due/.test(a.calibration)).length, A], ['Maintenance today', S.analyzers.filter(a => /Today/.test(a.nextMaint)).length, A], ['Errors 24 h', S.analyzers.reduce((x, a) => x + a.errors24h, 0), RD]]); L.cols = [['ID', 70], ['Analyzer', 140], ['Section', 110], ['Model · serial', 150], ['Interface', 150], ['Tests', 200], ['Calibration', 100], ['Next maintenance', 120], ['Vendor', 160], ['Downtime', 70], ['Comm', 90], ['Status', 100]]; L.rows = S.analyzers.map(a => ({ id: a.id, cells: [c(a.id, { m: 1 }), c(a.name, { b: 1 }), c(a.section), c(a.model + ' · ' + a.serial, { m: 1 }), c(a.iface), c(a.tests), c(a.calibration, { c: /Due/.test(a.calibration) ? A : '' }), c(a.nextMaint, { c: /Today/.test(a.nextMaint) ? A : '' }), c(a.vendor), c(a.downtimeH + ' h', { m: 1 }), P(a.comm === 'Connected' ? 'Connected' : a.comm === 'Disconnected' ? 'Failed' : 'Pending'), P(a.status === 'Online' ? 'Online' : a.status === 'Error' ? 'Critical' : 'Pending')], go: open('analyzer', a.id) })); break; }
    case 'lab-reagents': { L.title = 'Reagents & QC'; L.sub = 'Reagent lots consumed per test by the LIS consumption rule · expired lots blocked · QC failures block patient results on that analyzer'; L.stats = st([['Reagent lines', S.reagents.length], ['Low stock', S.reagents.filter(r => /Low|Out/.test(r.status)).length, RD, 'lab-reagents'], ['Near expiry', S.reagents.filter(r => r.expiryState === 'Near expiry').length, A], ['Expired', S.reagents.filter(r => r.expiryState === 'Expired').length, RD], ['QC passed (48 h)', S.qc.filter(q => q.result === 'Pass').length, G], ['QC failed', K.qcFail, RD]]); L.cols = [['Item', 200], ['Lot', 80], ['Machine', 130], ['Qty', 60], ['Min', 50], ['Expiry', 90], ['Storage', 70], ['Supplier', 160], ['Stock status', 100], ['Expiry state', 100]]; L.rows = S.reagents.map(r => { const it = S.items.find(x => x.id === r.id) || r; return { id: r.id, cells: [c(r.name, { b: 1 }), c(r.lot, { m: 1 }), c(r.machine), c(it.qty, { m: 1, c: it.qty < it.min ? RD : '' }), c(it.min, { m: 1 }), c(r.expiry, { m: 1 }), c(r.storage), c(r.supplier), P(it.status === 'In stock' ? 'Active' : it.status === 'Low stock' ? 'Pending' : 'Failed'), P(r.expiryState === 'OK' ? 'Active' : r.expiryState === 'Near expiry' ? 'Pending' : 'Expired')], go: open('item', r.id) }; }); L.rows.push(...S.qc.map(q => ({ id: q.id, cells: [c('QC run · ' + q.test + ' ' + q.level, { b: 1 }), c(q.lot, { m: 1 }), c(q.analyzer), c(q.value, { m: 1 }), c('—'), c(fmt(q.ts), { m: 1 }), c(q.expected), c(q.operator), P(q.result), c('—')], go: open('analyzer', (S.analyzers.find(a => a.name === q.analyzer) || {}).id) }))); break; }
    case 'inventory': { const f = withF(['In stock', 'Low stock', 'Out of stock', 'Overstock'], 'status'); L.title = 'Inventory'; L.sub = S.items.length + ' items across ' + S.stores.length + ' stores · every counter is clickable · expired batches cannot be issued'; L.stats = st([['Total items', K.items, '', 'inventory'], ['Stock value', inr(K.stockValue), '', 'inventory'], ['Low stock', K.low, RD, 'inventory'], ['Out of stock', K.out, RD, 'inventory'], ['Near expiry', K.nearExp, A, 'inventory'], ['Expired', K.expired, RD, 'inventory'], ['Pending PO', K.pendingPO, A, 'procurement'], ['Pending GRN', K.pendingGRN, A, 'procurement']]); L.cols = [['Code', 70], ['Item', 200], ['Category', 100], ['Store', 130], ['Qty', 70], ['Min / Max', 90], ['Unit price', 80], ['Value', 90], ['Batch', 70], ['Expiry', 90], ['Rack', 60], ['Supplier', 150], ['Status', 100]]; L.rows = S.items.filter(f).sort((a, b) => ['Out of stock', 'Low stock', 'Overstock', 'In stock'].indexOf(a.status) - ['Out of stock', 'Low stock', 'Overstock', 'In stock'].indexOf(b.status)).map(i => ({ id: i.id, cells: [c(i.id, { m: 1 }), c(i.name, { b: 1 }), c(i.category), c(i.store), c(i.qty + ' ' + i.unit, { m: 1, c: i.qty < i.min ? RD : '' }), c(i.min + ' / ' + i.max, { m: 1 }), c(inr(i.price), { m: 1 }), c(inr(i.value), { m: 1 }), c(i.batch, { m: 1 }), c(i.expiry, { m: 1, c: i.expiryState === 'Expired' ? RD : i.expiryState === 'Near expiry' ? A : '' }), c(i.rack, { m: 1 }), c(i.supplier), P(i.status === 'In stock' ? 'Active' : i.status === 'Overstock' ? 'Pending' : 'Critical · ' + i.status)], go: open('item', i.id) })); break; }
    case 'stores': { const f = withF(['Issue', 'Receipt', 'Transfer', 'Return', 'Adjustment'], 'type'); L.title = 'Stores & stock moves'; L.sub = S.stores.map(s => s.name).join(' · ') + ' · Receipt → put-away → issue → consumption; transfers, returns and approved adjustments'; L.stats = st(S.stores.map(s => [s.name, S.items.filter(i => i.store === s.name).length + ' items', '', 'inventory'])); L.cols = [['Txn', 90], ['Type', 90], ['Item', 200], ['Qty', 60], ['From', 150], ['To', 150], ['By', 130], ['Time', 100], ['Reason', 140], ['Status', 120]]; L.rows = S.stockTx.filter(f).sort((a, b) => b.ts - a.ts).map(t => ({ id: t.id, cells: [c(t.id, { m: 1 }), c(t.type, { b: 1 }), c(t.item), c(t.qty, { m: 1, c: t.qty < 0 ? RD : G }), c(t.from), c(t.to), c(t.by), c(fmt(t.ts), { m: 1 }), c(t.reason), P(t.status === 'Posted' ? 'Completed' : 'Pending approval')], go: open('item', t.itemId) })); break; }
    case 'procurement': { const f = withF(['Pending Approval', 'Approved', 'Ordered', 'Partially Received', 'Received', 'Closed', 'Rejected'], 'status'); L.title = 'Procurement'; L.sub = 'Requisition → Approval → PO → Vendor → GRN + QC → Stock → Invoice → Payable · tax from central configuration'; L.stats = st([['Pending approval', S.purchases.filter(p => p.status === 'Pending Approval').length, A, 'approvals'], ['Open POs', S.purchases.filter(p => /Ordered|Partially/.test(p.status)).length, '', 'procurement'], ['Awaiting invoice/payment', S.purchases.filter(p => p.grn !== '—' && !p.paid).length, A, 'finance'], ['Payables', inr(K.payables), '', 'finance'], ['Vendors active', S.vendors.filter(v => v.status === 'Active').length, G, 'vendors']]); L.cols = [['PR', 100], ['Item', 190], ['Qty', 70], ['Dept', 90], ['Vendor', 170], ['Amount', 90], ['Tax', 80], ['Total', 90], ['PO', 100], ['GRN', 100], ['Invoice', 110], ['Status', 130]]; L.rows = S.purchases.filter(f).map(p => ({ id: p.id, cells: [c(p.id, { m: 1 }), c(p.item, { b: 1 }), c(p.qty + ' ' + p.unit, { m: 1 }), c(p.dept), c(p.vendor), c(inr(p.amount), { m: 1 }), c(p.taxCode + ' ' + inr(p.taxAmt), { m: 1 }), c(inr(p.total), { m: 1 }), c(p.po, { m: 1 }), c(p.grn, { m: 1 }), c(p.invoice, { m: 1 }), P(p.status === 'Closed' ? 'Completed' : p.status)], go: open('purchase', p.id) })); break; }
    case 'vendors': { const f = withF(['Active', 'Expiring', 'Inactive'], 'status'); L.title = 'Vendors'; L.sub = S.vendors.length + ' vendors · GSTIN/PAN visible to finance roles only · contract expiry alerts'; L.cols = [['ID', 70], ['Vendor', 200], ['Type', 130], ['Contact', 110], ['Terms', 70], ['GSTIN', 140], ['Contract', 170], ['Rating', 60], ['Spend MTD', 90], ['Status', 90]]; const fin = api.can(role, 'financial') || api.can(role, 'inventory'); L.rows = S.vendors.filter(f).map(v => ({ id: v.id, cells: [c(v.id, { m: 1 }), c(v.name, { b: 1 }), c(v.type), c(v.contact), c(v.terms), c(fin ? v.gstin : '••••••••••', { m: 1 }), c(v.contractStart + ' → ' + v.contractEnd, { m: 1, c: v.status === 'Expiring' ? A : '' }), c(v.rating, { m: 1 }), c(fin ? inr(v.spendMtd) : '—', { m: 1 }), P(v.status === 'Expiring' ? 'Pending' : v.status)], go: open('vendor', v.id) })); break; }
    case 'finance': { const recv = K.receivablesPatient, ins = K.receivablesInsurer; L.title = 'Finance dashboard'; L.sub = 'Service → bill → tax → insurer / patient responsibility → payment → receipt → finance · refunds and voids via approval'; L.stats = st([['Today\'s revenue', inr(S.payments.filter(p => p.ts >= 0 && p.status === 'Success').reduce((a, p) => a + p.amount, 0)), '', 'data-financial'], ['Monthly revenue', '₹4.8 Cr', '', 'analytics'], ['Patient receivables', inr(recv), A, 'billing'], ['Insurance receivables', inr(ins), A, 'claims'], ['Vendor payables', inr(K.payables), A, 'procurement'], ['Refunds (pending appr.)', S.approvals.filter(a => a.type === 'Refund' && a.status === 'Pending').length, '', 'approvals'], ['Voids (pending appr.)', S.approvals.filter(a => a.type === 'Billing void' && a.status === 'Pending').length, '', 'approvals'], ['Tax collected (est.)', inr(K.taxCollected), '', 'tax'], ['Outstanding total', inr(recv + ins), RD, 'billing']]); L.cols = [['Payment', 100], ['Patient', 160], ['Bill', 100], ['Amount', 100], ['Mode', 130], ['Time', 100], ['Status', 90]]; L.rows = S.payments.slice(0, 80).map(p => ({ id: p.id, cells: [c(p.id, { m: 1 }), c(p.patient, { b: 1 }), c(p.billId, { m: 1 }), c(inr(p.amount), { m: 1 }), c(p.mode.replace(/\d{4,}/g, '••••')), c(fmt(p.ts), { m: 1 }), P(p.status)], go: open('bill', p.billId) })); break; }
    case 'claims': { const f = withF(['Claim Ready', 'Submitted', 'Under Review', 'Query Raised', 'Approved', 'Partially Approved', 'Rejected', 'Settled'], 'claimStatus'); const cl = S.preauths.filter(p => p.claimNo || /Approved|Partially|Rejected/.test(p.status)); L.title = 'Insurance claims'; L.sub = 'Policy → eligibility → authorisation → treatment → bill → claim → submission → review → query / approval / rejection → settlement → patient responsibility'; L.stats = st([['Submitted', K.claimsSubmitted, '', 'claims'], ['Under review / query', K.claimsQuery, A, 'claims'], ['Rejected', K.claimsRejected, RD, 'claims'], ['Settled', cl.filter(p => p.claimStatus === 'Settled').length, G, 'claims'], ['Insurance outstanding', inr(K.receivablesInsurer), A, 'finance'], ['Avg settlement', '11 days', '', 'claims']]); L.cols = [['Claim', 110], ['Patient', 150], ['Insurer · TPA', 170], ['Auth no.', 110], ['Claimed', 90], ['Approved', 90], ['Paid', 90], ['Patient resp.', 90], ['Preauth', 130], ['Claim status', 130]]; L.rows = cl.filter(x => (ctx.filter || 'All') === 'All' || (x.claimStatus || '—') === ctx.filter).map(p => { const ins = S.insurers.find(i => i.name === p.insurer); return { id: p.id, cells: [c(p.claimNo || '— (not yet)', { m: 1 }), c(p.patient, { b: 1 }), c(p.insurer + (ins ? ' · ' + ins.tpa : '')), c(p.authNo || '—', { m: 1 }), c(p.claimed || p.requested, { m: 1 }), c(p.approved, { m: 1 }), c(p.paidAmt || '—', { m: 1 }), c(p.liability, { m: 1 }), P(p.status), P(p.claimStatus || 'Claim Ready')], go: open('preauth', p.id) }; }); break; }
    case 'tax': { L.title = 'Tax configuration'; L.sub = 'Centralised GST rules applied to services, pharmacy, implants, canteen and procurement · DEMO CONFIGURATION, not legal advice'; L.banner = { kind: 'warn', text: 'Rates shown are demo configuration for the prototype. Production configuration must be confirmed with tax counsel and the finance controller before go-live.' }; L.cols = [['Code', 90], ['Name', 220], ['Type', 70], ['CGST', 60], ['SGST', 60], ['IGST', 60], ['HSN / SAC', 100], ['Applies to', 220], ['Effective', 100], ['Inclusive', 70], ['Status', 80]]; L.rows = S.taxes.map(t => ({ id: t.code, cells: [c(t.code, { m: 1 }), c(t.name, { b: 1 }), c(t.type), c(t.cgst + '%', { m: 1 }), c(t.sgst + '%', { m: 1 }), c(t.igst + '%', { m: 1 }), c(t.hsn, { m: 1 }), c(t.applies), c(t.effective, { m: 1 }), c(t.inclusive ? 'Yes' : 'No'), P(t.active ? 'Active' : 'Inactive')], go: open('tax', t.code) })); break; }
    case 'hr-dashboard': { L.title = 'HR dashboard'; L.sub = 'Employee → attendance → leave → approval → payroll · protected by HR role'; L.stats = st([['Total employees', K.employees, '', 'employees'], ['Active', K.active, G, 'employees'], ['Present today', K.present, G, 'attendance'], ['Absent today', K.absent, RD, 'attendance'], ['On leave', K.onLeave, A, 'attendance'], ['New joiners (30 d)', 3, '', 'employees'], ['Exits (30 d)', S.employees.filter(e => e.employment === 'Resigned').length, '', 'employees'], ['Attendance %', Math.round(100 * K.present / K.employees) + '%', '', 'attendance'], ['Overtime hours (wk)', 31, '', 'attendance'], ['Pending leave approvals', K.pendingLeave, A, 'approvals']]); const byDept = {}; S.employees.forEach(e => { byDept[e.unit] = byDept[e.unit] || { n: 0, p: 0, a: 0 }; byDept[e.unit].n++; if (/Present|Late/.test(e.today)) byDept[e.unit].p++; if (e.today === 'Absent') byDept[e.unit].a++; }); L.cols = [['Department / unit', 200], ['Headcount', 80], ['Present', 80], ['Absent', 80], ['Attendance', 90], ['Staffing gap?', 300]]; L.rows = Object.entries(byDept).sort((a, b) => b[1].n - a[1].n).slice(0, 20).map(([d, v]) => ({ id: d, cells: [c(d, { b: 1 }), c(v.n, { m: 1 }), c(v.p, { m: 1 }), c(v.a, { m: 1, c: v.a ? RD : '' }), c(Math.round(100 * v.p / v.n) + '%', { m: 1 }), c(v.a >= 2 ? 'Where are staffing gaps? → ' + d + ' short by ' + v.a : 'Covered')], go: go('attendance') })); break; }
    case 'employees': { const f = withF(['Active', 'On leave', 'Resigned'], 'employment'); L.title = 'Employees'; L.sub = S.employees.length + ' employees · lifecycle Candidate → Employee → Active → Leave / Suspended → Resigned → Archived · salary visible to HR Manager only'; L.cols = [['Emp ID', 80], ['Name', 160], ['Designation', 160], ['Department', 150], ['Joined', 90], ['Manager', 150], ['Qualification', 110], ['Salary', 80], ['Today', 80], ['Status', 90]]; const hr = api.can(role, 'hr'); L.rows = S.employees.filter(f).map(e => ({ id: e.empId, cells: [c(e.empId, { m: 1 }), c(e.name, { b: 1 }), c(e.designation), c(e.unit), c(e.joined, { m: 1 }), c(e.manager), c(e.qualification), c(hr ? e.salary : '••••', { m: 1 }), P(e.today === 'Present' ? 'Active' : e.today === 'Absent' ? 'Failed' : 'Pending'), P(e.employment === 'Resigned' ? 'Inactive' : e.employment)], go: open('employee', e.empId) })); break; }
    case 'attendance': { const f = withF(['Present', 'Late', 'Absent', 'On leave'], 'today'); L.title = 'Attendance & leave'; L.sub = 'Today\'s attendance · leave requests route to HR Manager approval · payroll picks up approved leave'; L.stats = st([['Present', K.present, G], ['Late', S.employees.filter(e => e.today === 'Late').length, A], ['Absent', K.absent, RD], ['On leave', K.onLeave, A], ['Pending leave', K.pendingLeave, A, 'approvals'], ['Holidays next', S.holidays[0].d + ' ' + S.holidays[0].n]]); L.cols = [['Emp ID', 80], ['Name', 160], ['Department', 150], ['Shift', 80], ['Today', 90], ['Leave request', 220], ['Leave status', 100]]; L.rows = S.employees.filter(f).map(e => { const lv = S.leaves.find(l => l.empId === e.empId); return { id: e.empId, cells: [c(e.empId, { m: 1 }), c(e.name, { b: 1 }), c(e.unit), c(e.shift), P(e.today === 'Present' ? 'Active' : e.today === 'Absent' ? 'Failed' : 'Pending'), c(lv ? lv.type + ' · ' + lv.from + '–' + lv.to + ' · ' + lv.reason : '—'), lv ? P(lv.status) : c('—')], go: open('employee', e.empId) }; }); break; }
    case 'canteen': { const f = withF(['Scheduled', 'In kitchen', 'Out for delivery', 'Delivered'], 'status'); L.title = 'Canteen & diet'; L.sub = 'Patient → diet plan → meal schedule → kitchen → delivery → confirmation → consumption · staff meals billed via payroll deduction'; L.stats = st([['Meals today', K.mealsToday, '', 'canteen'], ['Delivered', K.delivered, G, 'canteen'], ['Pending', K.pendingMeals, A, 'canteen'], ['Patient meals', K.patientMeals, '', 'canteen'], ['Staff meals', K.staffMeals, '', 'canteen'], ['Food cost today', inr(K.mealsToday * 62), '', 'inventory'], ['Revenue (staff + visitor)', inr(K.staffMeals * 80 + 3400), '', 'finance'], ['Wastage', '4.2%', A], ['Low stock (kitchen)', S.items.filter(i => i.category === 'Canteen' && /Low|Out/.test(i.status)).length, RD, 'inventory']]); L.cols = [['Meal', 80], ['Patient / staff', 160], ['Bed', 80], ['Meal type', 80], ['Item', 200], ['Diet', 90], ['Due', 70], ['Status', 130]]; L.rows = S.meals.filter(f).sort((a, b) => a.due - b.due).map(m => ({ id: m.id, cells: [c(m.id, { m: 1 }), c(m.patient, { b: 1 }), c(m.bed), c(m.meal), c(m.item), c(m.diet), c(fmt(m.due).slice(-5), { m: 1 }), P(m.status === 'Delivered' ? 'Completed' : m.status === 'Scheduled' ? 'Scheduled' : 'In progress')], go: open('meal', m.id) })); break; }
    case 'departments': { L.title = 'Departments & designations'; L.sub = S.departments.length + ' departments · ' + S.designations.length + ' designations · ' + S.branches.length + ' branches'; L.cols = [['ID', 80], ['Name', 220], ['Kind', 100], ['Head / grade', 200], ['Beds / users', 100], ['Status', 80]]; L.rows = S.departments.map(d => ({ id: d.id, cells: [c(d.id, { m: 1 }), c(d.name, { b: 1 }), c('Department'), c(d.head), c(S.beds.filter(b => b.dept === d.name).length + ' beds', { m: 1 }), P('Active')], go: open('master', 'departments:' + d.id) })).concat(S.designations.map(d => ({ id: d.id, cells: [c(d.id, { m: 1 }), c(d.name, { b: 1 }), c('Designation'), c(d.grade), c(S.employees.filter(e => e.designation === d.name).length + ' users', { m: 1 }), P(d.active ? 'Active' : 'Inactive')], go: open('master', 'designations:' + d.id) }))).concat(S.branches.map(b => ({ id: b.id, cells: [c(b.id, { m: 1 }), c(b.name, { b: 1 }), c('Branch'), c(b.city), c(b.beds + ' beds', { m: 1 }), P('Active')], go: open('master', 'branches:' + b.id) }))); break; }
    case 'services': { const f = withF(['Consultation', 'Room', 'Laboratory', 'Radiology', 'Procedure', 'Implant', 'Pharmacy', 'Canteen', 'Package'], 'category'); L.title = 'Services & pricing'; L.sub = 'Central service master · single source of price and tax code for billing, lab, pharmacy, canteen'; L.cols = [['Code', 80], ['Service', 240], ['Category', 110], ['Price', 90], ['Tax code', 110], ['Rate', 60], ['Status', 80]]; L.rows = S.services.filter(f).map(s => { const t = S.taxes.find(x => x.code === s.taxCode) || S.taxes[0]; return { id: s.id, cells: [c(s.id, { m: 1 }), c(s.name, { b: 1 }), c(s.category), c(s.price ? inr(s.price) : 'per item', { m: 1 }), c(s.taxCode, { m: 1 }), c(t.rate + '%', { m: 1 }), P(s.active ? 'Active' : 'Inactive')], go: open('master', 'services:' + s.id) }; }); break; }
    case 'insurers': { const f = withF(['Active', 'Expiring', 'Inactive'], 'status'); L.title = 'Insurers & TPAs'; L.sub = S.insurers.length + ' providers · network status, contract dates, authorisation and coverage rules drive the preauth agent'; L.cols = [['ID', 70], ['Provider', 150], ['TPA', 140], ['Method', 150], ['Network', 100], ['Contract', 170], ['Authorisation rule', 180], ['Coverage rules', 200], ['Status', 90]]; L.rows = S.insurers.filter(f).map(i => ({ id: i.id, cells: [c(i.id, { m: 1 }), c(i.name, { b: 1 }), c(i.tpa), c(i.method), c(i.network), c(i.effective + ' → ' + i.expiry, { m: 1, c: i.status === 'Expiring' ? A : '' }), c(i.auth), c(i.rules), P(i.status === 'Expiring' ? 'Pending' : i.status)], go: open('master', 'insurers:' + i.id) })); break; }
    case 'payment-methods': { L.title = 'Payment methods'; L.sub = 'Accepted tender types and gateways (simulated)'; L.cols = [['ID', 70], ['Method', 160], ['Gateway', 200], ['Status', 90]]; L.rows = S.paymentMethods.map(p => ({ id: p.id, cells: [c(p.id, { m: 1 }), c(p.name, { b: 1 }), c(p.gateway), P(p.active ? 'Active' : 'Inactive')], go: open('master', 'paymentMethods:' + p.id) })); break; }
    default: return null;
  }
  return L;
}

function buildDetail2(kind, id, api, ctx) {
  const S = api.S, now = api.clock(), { role } = ctx; const fmt = api.fmt, ago = t => api.ago(t, now), inr = api.inr;
  const D = { title: '', sub: '', badges: [], facts: [], sections: [], actions: [], related: [], ai: null, image: null, steps: [] };
  const badge = s => ({ t: s, ...pill(s) }); const go = (p, i) => () => ctx.go(p, i);
  const act = (label, on, o = {}) => ({ label, on, primary: !!o.primary, disabled: !!o.disabled, hint: o.hint || '' });
  const need = (ok, who) => ok ? {} : { disabled: true, hint: 'Requires ' + who };
  switch (kind) {
    case 'sample': { const s = S.samples.find(x => x.id === id); if (!s) return null; const l = S.labs.find(x => x.id === s.labId); const t = S.labTests.find(x => x.panel === s.test); const a = S.analyzers.find(x => x.name === s.analyzer); const el = now - s.collectedAt; D.title = s.test + ' · ' + s.patient; D.sub = s.accession + ' · barcode ' + s.barcode + ' · ' + s.priority; D.badges = [badge(s.status), badge(s.priority === 'STAT' ? 'Priority' : 'Routine')]; D.facts = [kv('Patient', s.patient + ' · ' + s.patientId), kv('Order', s.orderId + ' · ' + (l ? l.test : '')), kv('Specimen · container', s.specimen + ' · ' + s.container), kv('Collector', s.collector + ' · ' + fmt(s.collectedAt)), kv('Analyzer', s.analyzer + (a ? ' · ' + a.status + ' · ' + a.iface : '')), kv('TAT', el + ' m elapsed / ' + s.tatTarget + ' m target', el > s.tatTarget ? RD : ''), kv('Result', l && /Final|Result|Validation/.test(l.status) ? l.analyte + ' ' + l.value + ' ' + l.unit + ' · ' + l.flag + ' (ref ' + (t ? t.ref : '') + ')' : 'Not yet available'), kv('Report', s.status === 'Released' ? 'Released to doctor + patient portal · v' + (l && l.version || 1) : 'Draft — not visible to patient'), kv('Delete policy', 'Samples and results are never deleted; rejections and amendments are versioned with reason')]; if (s.rejectReason) D.facts.push(kv('Rejection', s.rejectReason, RD)); D.steps = s.history.map(h => ({ t: h.t, who: 'LIS', what: h.e, k: /REJECT/.test(h.e) ? 'warn' : /SIMULATOR|Result message/.test(h.e) ? 'ai' : /validation|Pathologist|Released|Collected/.test(h.e) ? 'human' : 'tool' })); const flow = ['Pending Collection', 'Sample Collected', 'Sample Received', 'Processing', 'Machine Processing', 'Result Entered', 'Technical Validation', 'Pathologist Validation', 'Released']; const i = flow.indexOf(s.status); if (i >= 0 && i < flow.length - 1) { const next = flow[i + 1]; const who = next === 'Pathologist Validation' || next === 'Released' ? 'Pathologist' : 'Laboratory'; const ok = next === 'Released' || next === 'Pathologist Validation' ? api.can(role, 'pathologist') : api.can(role, 'lab'); D.actions.push(act(next === 'Machine Processing' ? 'Send to analyzer (simulator)' : next === 'Released' ? 'Release report' : 'Advance → ' + next, () => api.advanceSample(s.id, role), { primary: true, ...need(ok, who) })); if (i >= 1 && i <= 4) D.actions.push(act('Reject sample (reason)', () => ctx.prompt('Reject ' + s.accession, 'Reason: Insufficient quantity · Hemolyzed · Clotted · Wrong container · Mislabeled …', r => api.rejectSample(s.id, r || 'Hemolyzed', role)), need(api.can(role, 'lab'), 'Laboratory'))); } if (s.status === 'Released' && l) D.actions.push(act('Amend released result (versioned)', () => ctx.prompt('Amend ' + l.analyte, 'Reason for amendment', r => api.amendLab(l.id, +(l.value * 0.9).toFixed(2), r || 'Re-run', role)), need(api.can(role, 'pathologist'), 'Pathologist'))); D.ai = { title: 'Boundary', text: 'The LIS matches patient, accession, test and unit before a result is accepted; unmatched results are quarantined. No interpretation is sent to the patient.' }; D.related = [{ label: 'Patient 360', on: go('patient', s.patientId) }, { label: 'Analyzer', on: () => a && ctx.open('analyzer', a.id) }]; break; }
    case 'labtest': { const t = S.labTests.find(x => x.id === id); if (!t) return null; const sv = S.services.find(x => x.id === t.serviceId); const pn = S.panels.find(p => p.name === t.panel); D.title = t.panel + ' · ' + t.analyte; D.sub = t.code + ' · ' + t.section + ' · ' + t.method; D.badges = [badge(t.active ? 'Active' : 'Inactive'), badge(t.resultType)]; D.facts = [kv('Specimen · container', t.specimen + ' · ' + t.container), kv('Unit · reference', t.unit + ' · ' + t.ref), kv('Critical low / high', t.critLow + ' / ' + t.critHigh, RD), kv('Analyzer', t.analyzer), kv('TAT target', t.tat + ' min'), kv('Price · tax', sv ? inr(sv.price) + ' · ' + sv.taxCode + ' (service master ' + sv.id + ')' : '—'), kv('Panel members', pn ? pn.tests.join(', ') : '—'), kv('Age / sex ranges', 'Paediatric and sex-specific ranges configured (demo)')]; D.actions = [act(t.active ? 'Deactivate test' : 'Activate test', () => api.mutate(s => { t.active = !t.active; }, role, 'Lab test ' + (t.active ? 'deactivated' : 'activated'), t.id, String(!t.active)), need(api.can(role, 'pathologist') || api.can(role, 'admin'), 'Pathologist or IT Administrator'))]; D.related = [{ label: 'Service master', on: go('services') }]; break; }
    case 'analyzer': { const a = S.analyzers.find(x => x.id === id); if (!a) return null; const qc = S.qc.filter(q => q.analyzer === a.name); D.title = a.name; D.sub = a.id + ' · ' + a.section + ' · ' + a.model + ' · SN ' + a.serial; D.badges = [badge(a.status === 'Online' ? 'Online' : a.status === 'Error' ? 'Critical' : 'Pending · ' + a.status), badge(a.comm === 'Connected' ? 'Connected' : 'Disconnected')]; D.facts = [kv('Interface', a.iface + ' — DEMO / ANALYZER SIMULATOR'), kv('Supported tests', a.tests), kv('Vendor · service', a.vendor), kv('Installed', a.installed), kv('Calibration', a.calibration, /Due/.test(a.calibration) ? A : ''), kv('Maintenance', 'Last ' + a.maintenance + ' · next ' + a.nextMaint), kv('Downtime (7 d)', a.downtimeH + ' h · errors 24 h: ' + a.errors24h), kv('Queue', S.samples.filter(s => s.analyzer === a.name && /Processing/.test(s.status)).length + ' samples')]; D.sections = [{ title: 'QC runs', items: qc.length ? qc.map(q => kv(q.test + ' ' + q.level + ' · ' + fmt(q.ts), q.value + ' (exp ' + q.expected + ') · ' + q.result + ' · ' + q.operator, q.result === 'Fail' ? RD : G)) : [kv('QC', 'No runs recorded')] }, { title: 'Integration boundary', items: [kv('Order out', 'LIS → interface engine → analyzer (' + a.iface.split(' (')[0] + ')'), kv('Result in', 'analyzer → interface → LIS matching: patient, accession, test, unit'), kv('Failure handling', 'Unknown accession / duplicate / invalid unit → quarantine queue, never auto-attached')] }]; const ok = api.can(role, 'lab') || api.can(role, 'admin'); D.actions = [act('Run QC', () => api.runQc(a.id, role), { primary: true, ...need(ok, 'Laboratory') })]; ['Online', 'Offline', 'Maintenance', 'Calibration', 'Retired'].filter(x => x !== a.status).forEach(x => D.actions.push(act('Set ' + x, () => api.setAnalyzer(a.id, x, role), need(ok, 'Laboratory or IT Administrator')))); D.actions.push(act('Raise service request to vendor', () => api.notify('Service request raised', a.name + ' → ' + a.vendor + ' · breakdown · SLA 4 h', 'Medium', 'Maintenance', { page: 'lab-analyzers', id: a.id }))); D.related = [{ label: 'Vendor', on: () => { const v = S.vendors.find(x => x.name === a.vendor); if (v) ctx.open('vendor', v.id); } }]; break; }
    case 'item': { const it = S.items.find(x => x.id === id); if (!it) return null; const tx = S.stockTx.filter(t => t.itemId === it.id).slice(0, 8); D.title = it.name; D.sub = it.id + ' · ' + it.category + ' · ' + it.store + ' · rack ' + it.rack; D.badges = [badge(it.status === 'In stock' ? 'Active' : it.status === 'Overstock' ? 'Pending' : 'Critical · ' + it.status), badge(it.expiryState === 'OK' ? 'Active' : it.expiryState === 'Near expiry' ? 'Pending' : 'Expired')]; D.facts = [kv('Quantity', it.qty + ' ' + it.unit + ' · min ' + it.min + ' · max ' + it.max + ' · reorder at ' + it.reorder), kv('Value', inr(it.value) + ' @ ' + inr(it.price) + '/' + it.unit), kv('Batch · expiry', it.batch + ' · ' + it.expiry, it.expiryState === 'Expired' ? RD : ''), kv('Supplier', it.supplier), kv('Open PR/PO', S.purchases.filter(p => p.itemId === it.id && !/Closed|Rejected|Cancelled/.test(p.status)).map(p => p.id + ' ' + p.status).join(', ') || 'None')]; D.sections = [{ title: 'Recent stock moves', items: tx.length ? tx.map(t => kv(t.type + ' · ' + fmt(t.ts), (t.qty > 0 ? '+' : '') + t.qty + ' ' + it.unit + ' · ' + t.from + ' → ' + t.to + ' · ' + t.status, t.qty < 0 ? RD : G)) : [kv('Moves', 'None')] }]; const inv = api.can(role, 'inventory'); D.actions = [act('Issue 10 to Cardiac Ward', () => api.issueStock(it.id, 10, 'Cardiac Ward', role), { primary: true, ...need(inv, 'Store Manager / Pharmacy / Laboratory') }), act('Transfer 5 → ICU Store', () => api.transferStock(it.id, 5, 'ICU Store', role), need(inv, 'Store Manager')), act('Adjust −2 (approval required)', () => ctx.prompt('Adjust ' + it.name, 'Reason: count variance · damaged · expired write-off', r => api.adjustStock(it.id, -2, r || 'Count variance', role)), need(inv, 'Store Manager')), act('Raise purchase requisition', () => api.requestPurchase(it.id, role), need(inv || api.can(role, 'financial'), 'Store / Procurement'))]; if (it.expiryState === 'Expired') D.ai = { title: 'Blocked', text: 'Expired batch: issue and dispensing are blocked; write-off adjustment with approval is the only allowed move.' }; D.related = [{ label: 'Stock moves', on: go('stores') }, { label: 'Procurement', on: go('procurement') }]; break; }
    case 'purchase': { const p = S.purchases.find(x => x.id === id); if (!p) return null; const v = S.vendors.find(x => x.id === p.vendorId); D.title = p.id + ' · ' + p.item; D.sub = p.qty + ' ' + p.unit + ' · ' + p.dept + ' · requested by ' + p.requester; D.badges = [badge(p.status === 'Closed' ? 'Completed' : p.status)]; D.facts = [kv('Vendor', p.vendor + (v ? ' · ' + v.terms + ' · rating ' + v.rating : '')), kv('Amount', inr(p.amount)), kv('Tax', p.taxCode + ' · ' + inr(p.taxAmt)), kv('Total', inr(p.total)), kv('PO · GRN · Invoice', p.po + ' · ' + p.grn + ' · ' + p.invoice), kv('Paid', p.paid ? 'Yes' : 'No'), kv('Lifecycle', 'Draft → Pending Approval → Approved → Ordered → (Partially) Received → Closed')]; D.steps = p.history.map(h => ({ t: h.t, who: 'Procurement', what: h.e, k: 'human' })); const next = { 'Pending Approval': ['Approve via Approval Centre', null], Approved: ['Issue purchase order', true], Ordered: ['Receive goods · GRN + QC → stock', true], 'Partially Received': ['Receive remaining', true], Received: ['Post invoice · pay vendor', true] }[p.status]; if (next) { if (next[1]) D.actions.push(act(next[0], () => api.advancePurchase(p.id, role), { primary: true, ...need(api.can(role, 'inventory') || api.can(role, 'financial'), 'Procurement / Store / Finance') })); else D.actions.push(act(next[0], go('approvals'), { primary: true })); } if (!/Closed|Received|Rejected|Cancelled/.test(p.status)) D.actions.push(act('Cancel requisition', () => api.mutate(s => { p.status = 'Cancelled'; }, role, 'PR cancelled', p.id, 'Cancelled'), need(api.can(role, 'inventory'), 'Procurement'))); D.related = [{ label: 'Item', on: () => ctx.open('item', p.itemId) }, { label: 'Vendor', on: () => ctx.open('vendor', p.vendorId) }]; break; }
    case 'vendor': { const v = S.vendors.find(x => x.id === id); if (!v) return null; const fin = api.can(role, 'financial') || api.can(role, 'inventory'); D.title = v.name; D.sub = v.id + ' · ' + v.type; D.badges = [badge(v.status === 'Expiring' ? 'Pending · contract expiring' : v.status)]; D.facts = [kv('Contact', v.contact + ' · ' + v.phone + ' · ' + v.email), kv('GSTIN · PAN', fin ? v.gstin + ' · ' + v.pan : 'masked for ' + role), kv('Payment terms', v.terms), kv('Contract', v.contractStart + ' → ' + v.contractEnd), kv('Performance', 'Rating ' + v.rating + ' · on-time 92% · rejections 1'), kv('Spend MTD', fin ? inr(v.spendMtd) : 'masked'), kv('Open POs', S.purchases.filter(p => p.vendorId === v.id && /Ordered|Partially/.test(p.status)).length)]; D.actions = [act(v.status === 'Inactive' ? 'Activate vendor' : 'Deactivate vendor', () => api.toggleActive('vendors', v.id, role), need(api.can(role, 'inventory') || api.can(role, 'admin'), 'Procurement or IT Administrator'))]; if (v.status === 'Expiring') D.actions.push(act('Renew contract to 31 Mar 2027', () => api.mutate(s => { v.contractEnd = '31 Mar 2027'; v.status = 'Active'; }, role, 'Vendor contract renewed', v.id, '31 Mar 2027'), { primary: true, ...need(api.can(role, 'inventory') || api.can(role, 'financial'), 'Procurement / Finance') })); D.related = [{ label: 'Procurement', on: go('procurement') }]; break; }
    case 'tax': { const t = S.taxes.find(x => x.code === id); if (!t) return null; D.title = t.name; D.sub = t.code + ' · ' + t.type + ' · HSN/SAC ' + t.hsn; D.badges = [badge(t.active ? 'Active' : 'Inactive')]; D.facts = [kv('CGST / SGST / IGST', t.cgst + '% / ' + t.sgst + '% / ' + t.igst + '%'), kv('Applies to', t.applies), kv('Inclusive', t.inclusive ? 'Yes' : 'No'), kv('Effective', t.effective), kv('Services using it', S.services.filter(s => s.taxCode === t.code).length), kv('Note', t.note, A)]; D.actions = [act(t.active ? 'Deactivate' : 'Activate', () => api.mutate(s => { t.active = !t.active; }, role, 'Tax rule ' + (t.active ? 'deactivated' : 'activated'), t.code, String(!t.active)), need(api.can(role, 'financial') || api.can(role, 'admin'), 'Finance Manager or IT Administrator'))]; break; }
    case 'employee': { const e = S.employees.find(x => x.empId === id); if (!e) return null; const hr = api.can(role, 'hr'); const lv = S.leaves.filter(l => l.empId === e.empId); D.title = e.name; D.sub = e.empId + ' · ' + e.designation + ' · ' + e.unit; D.badges = [badge(e.employment === 'Resigned' ? 'Inactive' : e.employment), badge(e.today === 'Present' ? 'Active · present' : e.today === 'Absent' ? 'Failed · absent' : 'Pending · ' + e.today)]; D.facts = [kv('Joined', e.joined), kv('Manager', e.manager), kv('Shift · location', e.shift + ' · ' + e.location), kv('Qualification', e.qualification), kv('Salary', hr ? e.salary + ' gross · payroll Aug: Paid' : 'masked for ' + role), kv('Leave balance', (e.leaveBalance ?? 4) + ' casual days'), kv('Lifecycle', 'Candidate → Employee → Active → Leave / Suspended → Resigned → Archived')]; D.sections = [{ title: 'Leave requests', items: lv.length ? lv.map(l => kv(l.type + ' · ' + l.from + '–' + l.to, l.reason + ' · ' + l.status, l.status === 'Approved' ? G : l.status === 'Rejected' ? RD : A)) : [kv('Leave', 'None')] }]; D.actions = [act('Mark present', () => api.markAttendance(e.empId, 'Present', role), need(hr, 'HR Manager')), act('Mark absent', () => api.markAttendance(e.empId, 'Absent', role), need(hr, 'HR Manager')), act('Request 1 day casual leave (→ HR approval)', () => api.requestLeave(e.empId, role), { primary: true }), act(e.employment === 'Active' ? 'Mark resigned (archive, keep history)' : 'Reactivate', () => api.mutate(s => { e.employment = e.employment === 'Active' ? 'Resigned' : 'Active'; }, role, 'Employment status changed', e.empId, e.employment === 'Active' ? 'Resigned' : 'Active'), need(hr, 'HR Manager'))]; D.related = [{ label: 'Users & access', on: go('users') }]; break; }
    case 'meal': { const m = S.meals.find(x => x.id === id); if (!m) return null; const d = S.diets.find(x => x.patientId === m.patientId); D.title = m.item + ' · ' + m.patient; D.sub = m.meal + ' · due ' + fmt(m.due).slice(-5) + ' · ' + m.bed; D.badges = [badge(m.status === 'Delivered' ? 'Completed' : m.status)]; D.facts = [kv('Diet plan', m.diet + (d ? ' · ordered by ' + d.orderedBy + ' · allergies ' + d.allergies : '')), kv('Kind', m.kind), kv('Flow', 'Diet plan → schedule → kitchen → delivery → confirmation → consumption (rice/dal deducted from Canteen Store)')]; if (m.status !== 'Delivered') D.actions.push(act('Advance → ' + ({ Scheduled: 'In kitchen', 'In kitchen': 'Out for delivery', 'Out for delivery': 'Delivered' })[m.status], () => api.advanceMeal(m.id, role), { primary: true, ...need(api.can(role, 'canteen'), 'Canteen Manager or Nurse') })); if (d) ['Standard', 'Cardiac', 'Renal', 'Diabetic', 'Soft', 'Liquid'].filter(x => x !== d.diet).slice(0, 3).forEach(x => D.actions.push(act('Change diet → ' + x + ' (doctor/nurse)', () => api.setDiet(m.patientId, x, role), need(role === 'Doctor' || role === 'Nurse', 'Doctor or Nurse')))); if (m.patientId !== '—') D.related = [{ label: 'Patient 360', on: go('patient', m.patientId) }]; break; }
    case 'master': { const [coll, rid] = id.split(':'); const r = (S[coll] || []).find(x => x.id === rid); if (!r) return null; D.title = r.name; D.sub = coll + ' · ' + r.id; D.badges = [badge(('active' in r ? r.active : r.status !== 'Inactive') ? 'Active' : 'Inactive')]; D.facts = Object.entries(r).filter(([k]) => !['id', 'name'].includes(k)).slice(0, 10).map(([k, v]) => kv(k, typeof v === 'object' ? JSON.stringify(v) : v)); D.facts.push(kv('Delete policy', 'Master data with history is deactivated, not deleted')); D.actions = [act(('active' in r ? r.active : r.status !== 'Inactive') ? 'Deactivate' : 'Activate', () => api.toggleActive(coll, r.id, role), need(api.can(role, 'admin') || api.can(role, 'financial'), 'IT Administrator'))]; break; }
    default: return null;
  }
  return D;
}

return { NAV2, ERP_PAGES, ERP_ROLE_PAGES, ERP_HOME, buildList2, buildDetail2 };
})();
window.__mm2.erp2 = (function(){
// ERP extension 2: OT/surgery, nursing (vitals, MAR, care plan), blood bank, CSSD, HR credentials, integration architecture, radiology accession.
function installErp2(api) {
  const S = api.S, { audit, notify, emit, nid, rnd, int, pick, fmt, inr } = api._h; const pad = n => String(n).padStart(2, '0');
  const ip = S.patients.filter(p => p.type === 'IP');
  // ---- OT / surgery ----
  S.otRooms = ['OT-1 (Cardiac)', 'OT-2 (General)', 'OT-3 (Ortho)', 'OT-4 (Emergency)', 'Cath Lab 1'].map((n, i) => ({ id: 'OT-' + (i + 1), name: n, status: i === 3 ? 'Available' : i === 1 ? 'Cleaning' : 'In use', nextFree: fmt(S.clock + [90, 20, 150, 0, 60][i]) }));
  const PROC = [['Total hip replacement (left)', 'Orthopaedics', 'Dr. Suresh Babu', 180, 'Yes', 'Hip prosthesis · Zimmer'], ['Laparoscopic cholecystectomy', 'General Surgery', 'Dr. Nithya Rajan', 90, 'No', '—'], ['CABG (triple vessel)', 'Cardiac Surgery', 'Dr. Priya Venkatesh', 300, 'Yes · 4 units PRBC', '—'], ['ACL reconstruction', 'Orthopaedics', 'Dr. Suresh Babu', 120, 'No', 'Interference screws'], ['PTCA + DES', 'Cardiology', 'Dr. Arjun Menon', 75, 'No', 'Drug-eluting stent 3.0×18'], ['LSCS', 'Obstetrics', 'Dr. Anjali Rao', 60, 'Yes · 2 units', '—'], ['Spinal fusion L4-L5', 'Orthopaedics', 'Dr. Suresh Babu', 240, 'Yes · 2 units', 'Pedicle screws ×4'], ['Appendicectomy (emergency)', 'General Surgery', 'Dr. Nithya Rajan', 60, 'No', '—']];
  const STAGES = ['Requested', 'Approved', 'Scheduled', 'Pre-op', 'Consent signed', 'Anaesthesia assessment', 'In OT', 'Procedure', 'Recovery (PACU)', 'Post-op orders', 'Completed'];
  S.surgeries = PROC.map((p, i) => { const pt = ip[(i * 3) % ip.length]; const stage = i === 0 ? 'Pre-op' : i === 1 ? 'In OT' : i === 2 ? 'Recovery (PACU)' : i === 3 ? 'Scheduled' : i === 4 ? 'Completed' : i === 5 ? 'Completed' : i === 6 ? 'Requested' : 'Approved'; return { id: 'SUR-2026-' + (410 + i), patientId: pt.id, patient: pt.name, procedure: p[0], dept: p[1], surgeon: p[2], assistant: pick(S.doctors).name, anaesthetist: 'Dr. Kavya Anand', nursing: 'OT team ' + (1 + i % 3), room: S.otRooms[i % 5].name, start: S.clock + [30, -40, -200, 240, -1500, -2900, 2880, 1500][i], durationMin: p[3], blood: p[4], implant: p[5], stage, stageIdx: STAGES.indexOf(stage), checklist: { preop: stage !== 'Requested' && stage !== 'Approved' && stage !== 'Scheduled', consent: STAGES.indexOf(stage) >= 4, anaesthesia: STAGES.indexOf(stage) >= 5, signIn: STAGES.indexOf(stage) >= 6, timeOut: STAGES.indexOf(stage) >= 7, signOut: STAGES.indexOf(stage) >= 8 }, specimen: i === 1 ? 'Gallbladder → Histopathology' : '—', delayMin: i === 0 ? 25 : 0, emergency: i === 7, cssdSet: ['Ortho major set', 'Lap chole set', 'Cardiac set A', 'Arthroscopy set', 'Cath lab tray', 'LSCS set', 'Spine set', 'Lap appendix set'][i], history: [{ t: fmt(S.clock - 600), e: 'Surgery requested by ' + p[2] }] }; });
  // ---- Nursing ----
  S.vitals = []; ip.forEach((p, i) => { for (let k = 0; k < 3; k++) { const t = S.clock - 240 * k - int(0, 30); S.vitals.push({ id: nid('VT'), patientId: p.id, patient: p.name, bed: (p.bed || '').split(' ')[0], ts: t, hr: 62 + int(0, 40), bp: (105 + int(0, 40)) + '/' + (65 + int(0, 25)), spo2: 93 + int(0, 6), temp: (36.4 + rnd() * 1.6).toFixed(1), rr: 14 + int(0, 8), pain: int(0, 6), by: pick(['Anitha Kumar', 'Sheela J']), ews: 0 }); } });
  S.vitals.forEach(v => { v.ews = (v.hr > 100 ? 1 : 0) + (v.spo2 < 95 ? 1 : 0) + (parseFloat(v.temp) > 38 ? 1 : 0) + (v.rr > 20 ? 1 : 0) + (parseInt(v.bp) < 110 ? 1 : 0); v.flag = v.ews >= 3 ? 'Escalate' : v.ews === 2 ? 'Watch' : 'Normal'; });
  S.mar = []; ip.forEach((p, i) => { const drugs = p.dept === 'Cardiology' ? ['Aspirin 75 mg PO', 'Clopidogrel 75 mg PO', 'Atorvastatin 40 mg PO', 'Metoprolol 25 mg PO'] : ['Paracetamol 650 mg PO', 'Ceftriaxone 1 g IV', 'Pantoprazole 40 mg IV']; drugs.forEach((d, j) => { const due = [480, 720, 840, 1260][j % 4]; S.mar.push({ id: nid('MAR'), patientId: p.id, patient: p.name, bed: (p.bed || '').split(' ')[0], drug: d, due, status: due + 30 < S.clock ? (rnd() < 0.9 ? 'Given' : 'Overdue') : due < S.clock ? 'Due' : 'Scheduled', highAlert: /IV|Insulin|Enoxaparin/.test(d), verified: true, givenBy: due + 30 < S.clock ? 'Anitha Kumar' : '—', prescriber: p.doctor }); }); });
  S.carePlans = ip.map(p => ({ patientId: p.id, patient: p.name, bed: (p.bed || '').split(' ')[0], fallRisk: p.age > 65 ? 'High' : 'Low', pressureRisk: p.age > 70 ? 'Medium' : 'Low', mobility: p.age > 70 ? 'Assisted' : 'Independent', diet: (S.diets.find(d => d.patientId === p.id) || {}).diet || 'Standard', intake: int(800, 2200) + ' mL', output: int(600, 2000) + ' mL', tasks: p.age > 65 ? ['Fall precautions · bed rails', '2-hourly turning', 'Physio referral'] : ['Ambulate TID', 'Wound check'], instructions: 'Continue current medication · review ' + fmt(S.clock + 300) }));
  // ---- Blood bank ----
  const GROUPS = ['A+', 'B+', 'O+', 'AB+', 'O−', 'A−'];
  S.bloodUnits = Array.from({ length: 36 }, (_, i) => ({ id: 'BU-' + (70200 + i), group: GROUPS[i % 6], component: ['PRBC', 'PRBC', 'FFP', 'Platelets', 'PRBC', 'Whole blood'][i % 6], collected: (1 + i % 20) + ' Aug 2026', expiry: i % 9 === 4 ? 'Expired 10 Sep' : (i % 6 === 3 ? '15 Sep 2026' : '30 Sep 2026'), screening: i % 11 === 6 ? 'Pending' : 'Negative (HIV, HBV, HCV, syphilis, malaria)', status: i % 9 === 4 ? 'Expired' : i % 7 === 2 ? 'Reserved' : i % 11 === 6 ? 'Quarantine' : 'Available', reservedFor: i % 7 === 2 ? S.surgeries[2].patient : '—', storage: 'Fridge ' + (1 + i % 3), donor: 'DN-' + (5000 + i) }));
  S.bloodRequests = [{ id: 'BR-2026-081', patientId: S.surgeries[2].patientId, patient: S.surgeries[2].patient, group: 'B+', component: 'PRBC', units: 4, reason: 'CABG', requestedBy: 'Dr. Priya Venkatesh', status: 'Cross-matched · reserved', ts: S.clock - 300, crossmatch: 'Compatible · BU-70204, 70211, 70218, 70225', issued: 0, history: [{ t: fmt(S.clock - 300), e: 'Requested' }, { t: fmt(S.clock - 260), e: 'Compatibility + cross-match done · 4 units reserved' }] }, { id: 'BR-2026-082', patientId: S.surgeries[5].patientId, patient: S.surgeries[5].patient, group: 'O+', component: 'PRBC', units: 2, reason: 'LSCS · post-partum haemorrhage risk', requestedBy: 'Dr. Anjali Rao', status: 'Requested', ts: S.clock - 25, crossmatch: '—', issued: 0, history: [{ t: fmt(S.clock - 25), e: 'Requested' }] }, { id: 'BR-2026-079', patientId: ip[4].id, patient: ip[4].name, group: 'A+', component: 'Platelets', units: 1, reason: 'Thrombocytopenia', requestedBy: ip[4].doctor, status: 'Transfused', ts: S.clock - 900, crossmatch: 'Compatible · BU-70203', issued: 1, reaction: 'None', history: [{ t: fmt(S.clock - 900), e: 'Requested' }, { t: fmt(S.clock - 860), e: 'Issued BU-70203' }, { t: fmt(S.clock - 780), e: 'Transfusion completed · no reaction · vitals recorded' }] }];
  // ---- CSSD ----
  S.cssdSets = ['Ortho major set', 'Lap chole set', 'Cardiac set A', 'Arthroscopy set', 'Cath lab tray', 'LSCS set', 'Spine set', 'Lap appendix set', 'Basic laparotomy set', 'Dressing set ×20'].map((n, i) => ({ id: 'SET-' + pad(i + 1), name: n, items: 12 + i * 3, status: ['Issued to OT', 'In OT', 'Returned · cleaning', 'Sterile · stored', 'Sterile · stored', 'Packing', 'Sterilising (load L-118)', 'Sterile · stored', 'Sterile · stored', 'Sterile · stored'][i], load: i === 6 ? 'L-118' : i < 3 ? 'L-116' : 'L-115', sterileUntil: i === 6 ? '—' : '20 Sep 2026', indicator: i === 6 ? 'Pending' : 'Class 5 pass', assignedSurgery: i < 8 ? S.surgeries[i].id : '—' }));
  S.cssdLoads = [['L-115', 'Autoclave 1', '134 °C · 4 min', 'Complete', 'Pass', fmt(S.clock - 420)], ['L-116', 'Autoclave 2', '134 °C · 4 min', 'Complete', 'Pass', fmt(S.clock - 180)], ['L-117', 'Autoclave 1', '121 °C · 20 min', 'Complete', 'FAIL · biological indicator', fmt(S.clock - 90)], ['L-118', 'Autoclave 2', '134 °C · 4 min', 'Running', 'Pending', fmt(S.clock - 15)]].map(([id, m, cycle, status, ind, t]) => ({ id, machine: m, cycle, status, indicator: ind, ts: t, sets: S.cssdSets.filter(s => s.load === id).map(s => s.name).join(', ') || 'Dressing sets' }));
  // ---- HR credentials ----
  S.credentials = S.doctors.slice(0, 14).map((d, i) => ({ id: 'CRD-' + pad(i + 1), holder: d.name, type: 'TN Medical Council registration', number: 'TNMC ' + (60000 + i * 37), expiry: i === 2 ? '30 Sep 2026' : i === 5 ? '15 Aug 2026' : '31 Mar 2027', status: i === 5 ? 'Expired' : i === 2 ? 'Expiring (17 d)' : 'Valid', training: i % 4 === 1 ? 'BLS due' : 'BLS · ACLS current' })).concat(S.staff.filter(s => /Nurse/.test(s.role)).slice(0, 6).map((s, i) => ({ id: 'CRD-N' + pad(i + 1), holder: s.name, type: 'Nursing Council registration', number: 'TNNMC ' + (30000 + i * 53), expiry: i === 1 ? '05 Oct 2026' : '31 Dec 2026', status: i === 1 ? 'Expiring (22 d)' : 'Valid', training: i === 3 ? 'Infection control due' : 'Current' })));
  // ---- Radiology accession / viewed ----
  S.radiology.forEach((r, i) => { r.accession = 'RAD' + (700000 + i); r.viewed = r.radStatus !== 'Unread'; r.dicom = '1.2.840.' + (100000 + i) + ' · ' + (r.modality === 'CT' ? 320 : r.modality === 'MRI' ? 480 : 2) + ' images'; });
  // ---- Integration architecture ----
  S.integrationArch = [['HIS / HMS', 'Bidirectional', 'REST (prototype)', 'Prototype'], ['EMR / EHR', 'Read · draft write', 'FHIR R4 (prototype)', 'Prototype'], ['LIS', 'Bidirectional', 'HL7 v2 ORU/ORM (prototype)', 'Prototype'], ['Laboratory analyzers', 'Bidirectional', 'ASTM / HL7 via simulator', 'Prototype (simulator)'], ['RIS / PACS', 'Read', 'DICOM / HL7 (prototype)', 'Prototype'], ['Pharmacy', 'Bidirectional', 'REST (prototype)', 'Prototype'], ['Billing / accounting', 'Bidirectional', 'REST (prototype)', 'Prototype'], ['Insurance / TPA portals', 'Human submit · read status', 'Portal / API (simulator)', 'Prototype (simulator)'], ['Payment gateway', 'Outbound', 'Razorpay (simulated)', 'Prototype (simulator)'], ['SMS / Email', 'Outbound', 'Provider API (simulated)', 'Prototype (simulator)'], ['WhatsApp Business', 'Bidirectional', 'Cloud API (simulated)', 'Prototype (simulator)'], ['Voice / telephony', 'Inbound', 'SIP + transcription (simulated)', 'Prototype (simulator)'], ['HR / payroll', 'Read', 'REST (prototype)', 'Prototype'], ['Biometric attendance', 'Inbound', 'Device API', 'Missing'], ['Medical devices / IoT', 'Inbound', 'HL7 / MQTT', 'Missing'], ['Blood bank analyser', 'Inbound', 'ASTM', 'Recommended'], ['AI models (LLM)', 'Request/response', 'Model gateway', 'Prototype (composed answers, no live LLM)'], ['RAG platform', 'Internal', 'Vector index', 'Prototype (lexical retrieval)']].map(([system, dir, iface, cls], i) => ({ id: 'IA-' + pad(i + 1), system, direction: dir, iface, cls, status: cls === 'Missing' ? 'Not connected' : system === 'LIS' && S.lisDegraded ? 'Degraded' : 'Connected (simulated)', lastSync: cls === 'Missing' ? '—' : fmt(S.clock - int(1, 9)), errors: system === 'LIS' && S.lisDegraded ? 3 : 0 }));
  // ---- actions ----
  Object.assign(api, {
    advanceSurgery(id, role) { const s = S.surgeries.find(x => x.id === id); if (!s || s.stage === 'Completed' || s.stage === 'Cancelled') return; const b = s.stage; const next = STAGES[s.stageIdx + 1]; if (next === 'Consent signed' && role !== 'Doctor') { notify('Consent requires the surgeon', s.patient + ' · consent must be taken by the operating surgeon', 'Medium', 'OT', { page: 'ot', id }); return; } if (next === 'In OT') { const set = S.cssdSets.find(x => x.name === s.cssdSet); if (set && !/Sterile|Issued|In OT/.test(set.status)) { notify('OT blocked · instrument set not sterile', s.cssdSet + ' is ' + set.status + ' · CSSD notified', 'High', 'CSSD', { page: 'cssd', id: set.id }); audit(role, 'Surgery start BLOCKED · set not sterile', s.patientId, b, b, s.cssdSet, '—', 'OT'); emit(); return; } if (set) set.status = 'In OT'; if (/Yes/.test(s.blood)) { const br = S.bloodRequests.find(x => x.patientId === s.patientId); if (!br || !/reserved|Issued|Transfused/.test(br.status)) { notify('OT blocked · blood not reserved', s.patient + ' needs ' + s.blood + ' · blood bank request pending', 'High', 'Blood bank', { page: 'bloodbank' }); emit(); return; } } const room = S.otRooms.find(r => r.name === s.room); if (room) room.status = 'In use'; } if (next === 'Recovery (PACU)') { const room = S.otRooms.find(r => r.name === s.room); if (room) { room.status = 'Cleaning'; room.nextFree = fmt(S.clock + 30); } const set = S.cssdSets.find(x => x.name === s.cssdSet); if (set) set.status = 'Returned · cleaning'; if (s.implant !== '—') { const it = S.items.find(i => s.implant.toLowerCase().includes(i.name.split(' ')[0].toLowerCase()) || /stent/i.test(s.implant) && /stent/i.test(i.name)); if (it && it.qty > 0) { it.qty -= 1; it.value = it.qty * it.price; S.stockTx.unshift({ id: nid('STX'), itemId: it.id, item: it.name, type: 'Issue', qty: -1, from: it.store, to: s.room, by: 'OT consumption', ts: S.clock, status: 'Posted', reason: s.id + ' · implant tracked · lot ' + it.batch }); } } const bill = S.bills.find(x => x.patientId === s.patientId && /Provisional/.test(x.status)); if (bill) { const sv = S.services.find(x => /PTCA|Procedure/.test(x.name)); bill.actual += sv ? sv.price : 45000; bill.patientShare = Math.max(0, bill.actual - bill.insurance); } if (s.specimen !== '—') S.orders.unshift({ id: nid('ORD'), patientId: s.patientId, patient: s.patient, doctor: s.surgeon, kind: 'Laboratory', test: 'Histopathology · ' + s.specimen.split(' →')[0], ts: S.clock, status: 'Ordered', prep: 'None' }); } if (next === 'Completed') { S.comms.push({ id: nid('MSG'), patientId: s.patientId, patient: s.patient, ts: S.clock, channel: 'WhatsApp', type: 'Surgery completed · family update (no clinical detail)', lang: 'TA', status: 'Delivered', agent: 'AG-02' }); } s.stage = next; s.stageIdx++; Object.keys(s.checklist).forEach((k, i) => { if (s.stageIdx >= [3, 4, 5, 6, 7, 8][i]) s.checklist[k] = true; }); s.history.unshift({ t: fmt(S.clock), e: b + ' → ' + next + ' by ' + role }); audit(role, 'Surgery ' + b + ' → ' + next, s.patientId, b, next, s.procedure, next === 'Consent signed' ? role : '—', 'OT', s.id); emit(); },
    cancelSurgery(id, reason, role) { const s = S.surgeries.find(x => x.id === id); if (!s) return; const b = s.stage; s.stage = 'Cancelled'; s.history.unshift({ t: fmt(S.clock), e: 'CANCELLED · ' + reason }); const room = S.otRooms.find(r => r.name === s.room); if (room && room.status === 'In use') room.status = 'Available'; notify('Surgery cancelled', s.patient + ' · ' + s.procedure + ' · ' + reason + ' · OT slot released', 'Medium', 'OT', { page: 'ot', id }); audit(role, 'Surgery cancelled', s.patientId, b, 'Cancelled', reason, '—', 'OT', s.id); emit(); },
    recordVitals(patientId, role) { const p = api.patient(patientId); if (!p) return; const v = { id: nid('VT'), patientId, patient: p.name, bed: (p.bed || '').split(' ')[0], ts: S.clock, hr: 70 + int(0, 50), bp: (100 + int(0, 45)) + '/' + (60 + int(0, 30)), spo2: 90 + int(0, 9), temp: (36.5 + rnd() * 2).toFixed(1), rr: 14 + int(0, 12), pain: int(0, 7), by: role }; v.ews = (v.hr > 100 ? 1 : 0) + (v.spo2 < 95 ? 1 : 0) + (parseFloat(v.temp) > 38 ? 1 : 0) + (v.rr > 20 ? 1 : 0) + (parseInt(v.bp) < 110 ? 1 : 0); v.flag = v.ews >= 3 ? 'Escalate' : v.ews === 2 ? 'Watch' : 'Normal'; S.vitals.unshift(v); if (v.flag === 'Escalate') { notify('Early-warning score ' + v.ews + ' · escalate', p.name + ' · ' + p.bed + ' · HR ' + v.hr + ' SpO₂ ' + v.spo2 + '% · ' + p.doctor + ' paged (nurse decision)', 'Critical', 'Nursing', { page: 'nursing', id: patientId }); const d = S.discharges.find(x => x.patientId === patientId && !x.completed); if (d && !d.paused) { d.paused = true; d.pauseReason = 'EWS ' + v.ews + ' — clinical review'; d.log.unshift({ t: fmt(S.clock), who: 'Nursing', what: 'Discharge PAUSED · deterioration (EWS ' + v.ews + ')', k: 'warn' }); } } audit(role, 'Vitals recorded · EWS ' + v.ews, patientId, '—', v.flag, 'HR ' + v.hr + ' BP ' + v.bp + ' SpO2 ' + v.spo2, '—', 'Nursing'); emit(); },
    giveMedication(marId, role) { const m = S.mar.find(x => x.id === marId); if (!m || m.status === 'Given') return; if (m.highAlert && role !== 'Nurse') { notify('Second-nurse check required', m.drug + ' is a high-alert IV medication · administration must be by a nurse', 'Medium', 'Nursing', { page: 'nursing' }); return; } const b = m.status; m.status = 'Given'; m.givenBy = role; m.givenAt = fmt(S.clock); const ph = S.pharmacy.find(x => x.patientId === m.patientId && x.status === 'Pending'); if (ph) { ph.status = 'Dispensed'; } audit(role, 'Medication administered · ' + m.drug, m.patientId, b, 'Given', m.highAlert ? 'High-alert · double-check recorded' : '—', '—', 'MAR', marId); emit(); },
    advanceBlood(id, role) { const r = S.bloodRequests.find(x => x.id === id); if (!r) return; const flow = { Requested: 'Cross-matched · reserved', 'Cross-matched · reserved': 'Issued', Issued: 'Transfused' }; const b = r.status; const next = flow[b]; if (!next) return; if (next === 'Cross-matched · reserved') { const units = S.bloodUnits.filter(u => u.group === r.group && u.component === r.component && u.status === 'Available').slice(0, r.units); if (units.length < r.units) { notify('Insufficient compatible units', r.group + ' ' + r.component + ' · ' + units.length + ' of ' + r.units + ' available · request from regional blood centre', 'High', 'Blood bank', { page: 'bloodbank', id }); audit(role, 'Cross-match BLOCKED · insufficient units', r.patientId, b, b, r.group, '—', 'Blood bank'); emit(); return; } units.forEach(u => { u.status = 'Reserved'; u.reservedFor = r.patient; }); r.crossmatch = 'Compatible · ' + units.map(u => u.id).join(', '); } if (next === 'Issued') { S.bloodUnits.filter(u => u.reservedFor === r.patient && u.status === 'Reserved').forEach(u => u.status = 'Issued'); r.issued = r.units; } if (next === 'Transfused') { S.bloodUnits.filter(u => u.reservedFor === r.patient && u.status === 'Issued').forEach(u => u.status = 'Transfused'); r.reaction = 'None recorded · vitals at 15/30/60 min'; } r.status = next; r.history.unshift({ t: fmt(S.clock), e: b + ' → ' + next + ' by ' + role }); audit(role, 'Blood request ' + b + ' → ' + next, r.patientId, b, next, r.crossmatch, '—', 'Blood bank', id); emit(); },
    discardUnit(id, role) { const u = S.bloodUnits.find(x => x.id === id); if (!u || u.status !== 'Expired') return; u.status = 'Discarded'; audit(role, 'Blood unit discarded (expired)', id, 'Expired', 'Discarded', 'Wastage recorded', '—', 'Blood bank'); emit(); },
    advanceCssd(id, role) { const s = S.cssdSets.find(x => x.id === id); if (!s) return; const flow = { 'Returned · cleaning': 'Packing', Packing: 'Sterilising (load L-119)', 'Sterilising (load L-119)': 'Sterile · stored', 'Sterile · stored': 'Issued to OT', 'Issued to OT': 'In OT', 'In OT': 'Returned · cleaning' }; const b = s.status; const next = flow[b]; if (!next) return; if (b.startsWith('Sterilising')) { s.indicator = 'Class 5 pass'; s.sterileUntil = '27 Sep 2026'; if (!S.cssdLoads.find(l => l.id === 'L-119')) S.cssdLoads.unshift({ id: 'L-119', machine: 'Autoclave 1', cycle: '134 °C · 4 min', status: 'Complete', indicator: 'Pass', ts: fmt(S.clock), sets: s.name }); } if (next.startsWith('Sterilising')) s.load = 'L-119'; s.status = next; audit(role, 'CSSD ' + b + ' → ' + next, s.id, b, next, s.name, '—', 'CSSD'); emit(); },
    renewCredential(id, role) { const c = S.credentials.find(x => x.id === id); if (!c) return; const b = c.status; c.status = 'Renewal submitted'; setTimeout(() => { c.status = 'Valid'; c.expiry = '31 Mar 2028'; notify('Credential verified', c.holder + ' · ' + c.type + ' renewed to ' + c.expiry, 'Low', 'HR', { page: 'credentials', id }); audit('HR verification', 'Credential verified', id, 'Renewal submitted', 'Valid', c.number, '—', 'HR'); emit(); }, 1500); audit(role, 'Credential renewal submitted', id, b, 'Renewal submitted', c.number, '—', 'HR'); emit(); },
    routeAnalyzer(sampleId, role) { const s = S.samples.find(x => x.id === sampleId); if (!s) return; const cur = S.analyzers.find(a => a.name === s.analyzer); const alt = S.analyzers.find(a => a.status === 'Online' && a.tests.split(', ').some(t => s.test.startsWith(t.split(' ')[0]) || t === s.test)) || S.analyzers.find(a => a.status === 'Online'); if (!alt) return; const b = s.analyzer; s.analyzer = alt.name; s.history.unshift({ t: fmt(S.clock), e: 'Rerouted ' + b + ' (' + (cur ? cur.status : '—') + ') → ' + alt.name }); audit(role, 'Sample rerouted to alternate analyzer', s.patientId, b, alt.name, cur ? cur.status : '—', '—', 'LIS'); emit(); },
    bulkApprove(ids, role) { let n = 0; ids.forEach(id => { const a = S.approvals.find(x => x.id === id); if (a && a.status === 'Pending' && api.can(role, 'approve')(a.type) && !/Discharge summary|Prescription/.test(a.type)) { api.approve(id, role, 'Bulk approval'); n++; } }); notify('Bulk approval', n + ' item(s) approved by ' + role + ' · clinical signatures excluded from bulk', 'Low', 'Approval Centre', { page: 'approvals' }); emit(); return n; },
  });
  return api;
}

return { installErp2 };
})();
window.__mm2.views3 = (function(){
// ERP pages 2: OT, nursing, blood bank, CSSD, credentials, integration architecture, radiology worklist detail.
const { pill } = window.__mm2.views;
const G = 'oklch(0.4 0.12 150)', A = 'oklch(0.5 0.13 70)', RD = 'oklch(0.45 0.17 25)';
const c = (t, o = {}) => ({ t: t == null ? '—' : String(t), c: o.c || '', bg: o.bg || '', m: !!o.m, b: !!o.b, pill: !!o.p });
const P = s => ({ ...c(s), ...pill(s), pill: true });
const kv = (k, v, col) => ({ k, v: v == null ? '—' : String(v), c: col || '' });
const NAV3 = [{ g: 'Clinical operations', items: [['ot', 'OT & Surgery'], ['nursing', 'Nursing Workspace'], ['mar', 'Medication Administration'], ['bloodbank', 'Blood Bank'], ['cssd', 'CSSD'], ['credentials', 'Credentials & Training'], ['integration-arch', 'Integration Architecture']] }];
const ERP2_PAGES = NAV3[0].items.map(i => i[0]);
const ERP2_ROLE_PAGES = { Doctor: ['ot', 'nursing', 'mar', 'bloodbank'], Nurse: ['ot', 'nursing', 'mar', 'bloodbank', 'cssd'], Laboratory: ['bloodbank'], Pathologist: ['bloodbank'], 'HR Manager': ['credentials'], 'IT Administrator': ['integration-arch', 'credentials'], 'AI Administrator': ['integration-arch'], 'Governance Officer': ['credentials', 'integration-arch'], Auditor: ['ot', 'bloodbank', 'cssd', 'credentials', 'integration-arch'], 'Store Manager': ['cssd'], 'Hospital Management': null };
function buildList3(page, api, ctx) {
  const S = api.S, now = api.clock(), { role } = ctx; const fmt = api.fmt, ago = t => api.ago(t, now);
  const open = (kind, id) => () => ctx.open(kind, id); const go = (p, i) => () => ctx.go(p, i);
  const L = { title: '', sub: '', cols: [], rows: [], stats: [], banner: null, filters: [] };
  const withF = (vals, key) => { L.filters = ['All'].concat(vals).map(v => ({ label: v, active: (ctx.filter || 'All') === v, on: () => ctx.setFilter(v) })); return r => (ctx.filter || 'All') === 'All' || String(r[key]) === ctx.filter; };
  const st = arr => arr.map(([k, v, col, pg]) => ({ k, v, c: col || '', on: pg ? go(pg) : () => {} }));
  switch (page) {
    case 'ot': { const f = withF(['Requested', 'Approved', 'Scheduled', 'Pre-op', 'In OT', 'Recovery (PACU)', 'Completed', 'Cancelled'], 'stage'); L.title = 'OT & surgery'; L.sub = 'Request → approval → scheduling → pre-op → consent (surgeon) → anaesthesia → OT → procedure → PACU → post-op → billing · sterile set and reserved blood are hard gates'; L.stats = st([['Today\'s cases', S.surgeries.filter(s => s.start > -600 && s.start < 1440).length, '', 'ot'], ['In OT now', S.surgeries.filter(s => /In OT|Procedure/.test(s.stage)).length, A, 'ot'], ['Awaiting consent', S.surgeries.filter(s => s.stageIdx === 3).length, A, 'ot'], ['Delayed', S.surgeries.filter(s => s.delayMin > 0 && s.stage !== 'Completed').length, RD, 'ot'], ['Emergency', S.surgeries.filter(s => s.emergency && s.stage !== 'Completed').length, RD, 'ot'], ['OT utilisation today', '68%', '', 'analytics']].concat(S.otRooms.map(r => [r.name, r.status + ' · free ' + r.nextFree.slice(-5), r.status === 'Available' ? G : r.status === 'Cleaning' ? A : '', 'ot']))); L.cols = [['Case', 110], ['Patient', 150], ['Procedure', 200], ['Surgeon', 150], ['Room', 120], ['Start', 90], ['Duration', 70], ['Blood', 110], ['Implant', 150], ['Checklist', 90], ['Stage', 150]]; L.rows = S.surgeries.filter(f).sort((a, b) => a.start - b.start).map(s => { const done = Object.values(s.checklist).filter(Boolean).length; return { id: s.id, cells: [c(s.id, { m: 1 }), c(s.patient, { b: 1 }), c(s.procedure), c(s.surgeon), c(s.room), c(fmt(s.start), { m: 1 }), c(s.durationMin + ' m', { m: 1 }), c(s.blood), c(s.implant), c(done + ' / 6', { m: 1, c: done < 6 && s.stageIdx >= 6 ? RD : '' }), P(s.stage === 'Completed' ? 'Completed' : s.stage === 'Cancelled' ? 'Cancelled' : /In OT|Procedure/.test(s.stage) ? 'In progress · ' + s.stage : s.stage)], go: open('surgery', s.id) }; }); break; }
    case 'nursing': { const f = withF(['Escalate', 'Watch', 'Normal'], 'flag'); L.title = 'Nursing workspace · Cardiac & medical wards'; L.sub = 'Census, latest vitals with early-warning score, care plan risks, overdue medication and handover. Escalation is a nurse decision; AI drafts handover only.'; const latest = {}; S.vitals.slice().sort((a, b) => b.ts - a.ts).forEach(v => { if (!latest[v.patientId]) latest[v.patientId] = v; }); const rows = Object.values(latest); L.stats = st([['Census (IP)', rows.length, '', 'nursing'], ['EWS escalate', rows.filter(v => v.flag === 'Escalate').length, RD, 'nursing'], ['Watch', rows.filter(v => v.flag === 'Watch').length, A, 'nursing'], ['Medications overdue', S.mar.filter(m => m.status === 'Overdue').length, RD, 'mar'], ['Due now', S.mar.filter(m => m.status === 'Due').length, A, 'mar'], ['High fall risk', S.carePlans.filter(cp => cp.fallRisk === 'High').length, A, 'nursing'], ['Handover', 'AI draft · nurse verifies', '', 'approvals']]); L.cols = [['Bed', 70], ['Patient', 150], ['Last vitals', 80], ['HR', 50], ['BP', 80], ['SpO₂', 60], ['Temp', 60], ['RR', 50], ['Pain', 50], ['EWS', 50], ['Fall / pressure', 110], ['Diet', 80], ['Overdue meds', 90], ['Flag', 100]]; L.rows = rows.filter(f).sort((a, b) => b.ews - a.ews).map(v => { const cp = S.carePlans.find(x => x.patientId === v.patientId) || {}; const od = S.mar.filter(m => m.patientId === v.patientId && m.status === 'Overdue').length; return { id: v.patientId, cells: [c(v.bed, { m: 1 }), c(v.patient, { b: 1 }), c(fmt(v.ts).slice(-5), { m: 1 }), c(v.hr, { m: 1, c: v.hr > 100 ? RD : '' }), c(v.bp, { m: 1 }), c(v.spo2 + '%', { m: 1, c: v.spo2 < 95 ? RD : '' }), c(v.temp, { m: 1, c: parseFloat(v.temp) > 38 ? RD : '' }), c(v.rr, { m: 1 }), c(v.pain, { m: 1 }), c(v.ews, { m: 1, b: 1, c: v.ews >= 3 ? RD : v.ews === 2 ? A : '' }), c((cp.fallRisk || '—') + ' / ' + (cp.pressureRisk || '—')), c(cp.diet || '—'), c(od || '—', { m: 1, c: od ? RD : '' }), P(v.flag === 'Escalate' ? 'Critical · escalate' : v.flag === 'Watch' ? 'Pending · watch' : 'Normal')], go: open('nursingPatient', v.patientId) }; }); break; }
    case 'mar': { const f = withF(['Overdue', 'Due', 'Scheduled', 'Given'], 'status'); L.title = 'Medication administration record'; L.sub = 'Prescription → pharmacy verification → dispensing → MAR → administration → record → audit · high-alert IV drugs need a nurse and a double-check'; L.stats = st([['Overdue', S.mar.filter(m => m.status === 'Overdue').length, RD], ['Due now', S.mar.filter(m => m.status === 'Due').length, A], ['Given today', S.mar.filter(m => m.status === 'Given').length, G], ['High-alert pending', S.mar.filter(m => m.highAlert && m.status !== 'Given').length, RD]]); L.cols = [['Bed', 70], ['Patient', 150], ['Drug · route', 200], ['Due', 70], ['High-alert', 80], ['Prescriber', 150], ['Given by', 130], ['Status', 100]]; L.rows = S.mar.filter(f).sort((a, b) => ['Overdue', 'Due', 'Scheduled', 'Given'].indexOf(a.status) - ['Overdue', 'Due', 'Scheduled', 'Given'].indexOf(b.status) || a.due - b.due).map(m => ({ id: m.id, cells: [c(m.bed, { m: 1 }), c(m.patient, { b: 1 }), c(m.drug), c(fmt(m.due).slice(-5), { m: 1 }), m.highAlert ? P('Critical · double-check') : c('—'), c(m.prescriber), c(m.givenBy + (m.givenAt ? ' · ' + m.givenAt : '')), P(m.status === 'Given' ? 'Completed' : m.status === 'Overdue' ? 'Critical · overdue' : m.status)], go: open('mar', m.id) })); break; }
    case 'bloodbank': { const f = withF(['Available', 'Reserved', 'Issued', 'Transfused', 'Quarantine', 'Expired', 'Discarded'], 'status'); L.title = 'Blood bank'; L.sub = 'Request → compatibility → cross-match → reserve → issue → transfusion → reaction record → traceability · expired units are discarded, never issued'; const grp = {}; S.bloodUnits.filter(u => u.status === 'Available').forEach(u => { grp[u.group + ' ' + u.component] = (grp[u.group + ' ' + u.component] || 0) + 1; }); L.stats = st([['Requests open', S.bloodRequests.filter(r => !/Transfused/.test(r.status)).length, A, 'bloodbank'], ['Available units', S.bloodUnits.filter(u => u.status === 'Available').length, G], ['Reserved', S.bloodUnits.filter(u => u.status === 'Reserved').length, A], ['Quarantine (screening)', S.bloodUnits.filter(u => u.status === 'Quarantine').length, A], ['Expired / wastage', S.bloodUnits.filter(u => /Expired|Discarded/.test(u.status)).length, RD], ['O− available', grp['O− PRBC'] || 0, (grp['O− PRBC'] || 0) < 2 ? RD : G]]); L.banner = { kind: 'info', text: 'Requests: ' + S.bloodRequests.map(r => r.id + ' · ' + r.patient + ' · ' + r.units + '× ' + r.group + ' ' + r.component + ' · ' + r.status).join('  |  ') + ' — open a request from the Requests row below.' }; L.cols = [['Unit', 90], ['Group', 60], ['Component', 100], ['Collected', 100], ['Expiry', 110], ['Screening', 220], ['Storage', 80], ['Reserved for', 150], ['Status', 100]]; L.rows = S.bloodRequests.map(r => ({ id: r.id, cells: [c(r.id, { m: 1, b: 1 }), c(r.group, { b: 1 }), c(r.component), c('REQUEST · ' + r.units + ' units'), c(fmt(r.ts), { m: 1 }), c(r.reason + ' · ' + r.requestedBy), c('—'), c(r.patient, { b: 1 }), P(r.status === 'Transfused' ? 'Completed' : r.status === 'Requested' ? 'Pending · requested' : r.status)], go: open('bloodRequest', r.id) })).concat(S.bloodUnits.filter(f).map(u => ({ id: u.id, cells: [c(u.id, { m: 1 }), c(u.group, { b: 1 }), c(u.component), c(u.collected, { m: 1 }), c(u.expiry, { m: 1, c: /Expired/.test(u.expiry) ? RD : u.expiry.startsWith('15') ? A : '' }), c(u.screening, { c: u.screening === 'Pending' ? A : '' }), c(u.storage), c(u.reservedFor), P(u.status === 'Available' ? 'Active · available' : u.status === 'Transfused' ? 'Completed' : u.status === 'Quarantine' ? 'Pending · quarantine' : u.status)], go: open('bloodUnit', u.id) }))); break; }
    case 'cssd': { const f = withF(['Sterile · stored', 'Issued to OT', 'In OT', 'Returned · cleaning', 'Packing', 'Sterilising (load L-118)', 'Sterilising (load L-119)'], 'status'); L.title = 'CSSD · sterile services'; L.sub = 'Return → cleaning → packing → sterilisation load → indicator → sterile storage → issue to OT · a failed load quarantines every set in it'; L.stats = st([['Sets sterile', S.cssdSets.filter(s => s.status.startsWith('Sterile')).length, G], ['In OT / issued', S.cssdSets.filter(s => /OT/.test(s.status)).length, ''], ['In process', S.cssdSets.filter(s => /cleaning|Packing|Sterilising/.test(s.status)).length, A], ['Loads today', S.cssdLoads.length, ''], ['Load failures', S.cssdLoads.filter(l => /FAIL/.test(l.indicator)).length, RD], ['Autoclaves', '2 · both online', G]]); L.cols = [['Set', 80], ['Name', 180], ['Items', 60], ['Load', 70], ['Indicator', 150], ['Sterile until', 100], ['Assigned surgery', 120], ['Status', 150]]; L.rows = S.cssdSets.filter(f).map(s => ({ id: s.id, cells: [c(s.id, { m: 1 }), c(s.name, { b: 1 }), c(s.items, { m: 1 }), c(s.load, { m: 1 }), c(s.indicator, { c: /FAIL/.test(s.indicator) ? RD : s.indicator === 'Pending' ? A : G }), c(s.sterileUntil, { m: 1 }), c(s.assignedSurgery, { m: 1 }), P(s.status.startsWith('Sterile') ? 'Active · sterile' : /OT/.test(s.status) ? 'In progress · ' + s.status : 'Pending · ' + s.status)], go: open('cssdSet', s.id) })).concat(S.cssdLoads.map(l => ({ id: l.id, cells: [c(l.id, { m: 1, b: 1 }), c('LOAD · ' + l.machine + ' · ' + l.cycle, { b: 1 }), c('—'), c(l.id, { m: 1 }), c(l.indicator, { c: /FAIL/.test(l.indicator) ? RD : l.indicator === 'Pending' ? A : G }), c(l.ts, { m: 1 }), c(l.sets), P(l.status === 'Complete' ? (/FAIL/.test(l.indicator) ? 'Failed' : 'Completed') : 'In progress · running')], go: open('cssdLoad', l.id) }))); break; }
    case 'credentials': { const f = withF(['Valid', 'Expiring (17 d)', 'Expiring (22 d)', 'Expired', 'Renewal submitted'], 'status'); L.title = 'Credentials & mandatory training'; L.sub = 'License expiring → HR alert → manager notification → renewal → verification → audit · expired registration blocks rostering'; L.stats = st([['Tracked', S.credentials.length, ''], ['Expiring ≤ 30 d', S.credentials.filter(x => /Expiring/.test(x.status)).length, A], ['Expired', S.credentials.filter(x => x.status === 'Expired').length, RD], ['Training due', S.credentials.filter(x => /due/.test(x.training)).length, A]]); L.cols = [['ID', 80], ['Holder', 170], ['Credential', 220], ['Number', 120], ['Expiry', 110], ['Mandatory training', 160], ['Status', 140]]; L.rows = S.credentials.filter(f).sort((a, b) => (a.status === 'Valid') - (b.status === 'Valid')).map(x => ({ id: x.id, cells: [c(x.id, { m: 1 }), c(x.holder, { b: 1 }), c(x.type), c(x.number, { m: 1 }), c(x.expiry, { m: 1, c: x.status === 'Expired' ? RD : /Expiring/.test(x.status) ? A : '' }), c(x.training, { c: /due/.test(x.training) ? A : '' }), P(x.status === 'Valid' ? 'Active · valid' : x.status === 'Expired' ? 'Expired' : /Renewal/.test(x.status) ? 'Pending · renewal' : 'Pending · ' + x.status)], go: open('credential', x.id) })); break; }
    case 'integration-arch': { const f = withF(['Prototype', 'Prototype (simulator)', 'Missing', 'Recommended'], 'cls'); L.title = 'Integration architecture'; L.sub = 'Classification is explicit: Prototype = simulated at the interface boundary in this build · Missing = not represented · Recommended = advised for production. Nothing here is a live production integration.'; L.stats = st([['Prototype', S.integrationArch.filter(x => x.cls === 'Prototype').length, ''], ['Simulators', S.integrationArch.filter(x => /simulator/.test(x.cls)).length, A], ['Missing', S.integrationArch.filter(x => x.cls === 'Missing').length, RD], ['Recommended', S.integrationArch.filter(x => x.cls === 'Recommended').length, ''], ['Errors (1 h)', S.integrationArch.reduce((a, x) => a + x.errors, 0), RD]]); L.cols = [['ID', 60], ['System', 190], ['Direction', 150], ['Interface', 220], ['Classification', 200], ['Last sync', 80], ['Errors', 60], ['Status', 150]]; L.rows = S.integrationArch.filter(x => (ctx.filter || 'All') === 'All' || x.cls.startsWith(ctx.filter)).map(x => ({ id: x.id, cells: [c(x.id, { m: 1 }), c(x.system, { b: 1 }), c(x.direction), c(x.iface), c(x.cls, { c: x.cls === 'Missing' ? RD : /simulator/.test(x.cls) ? A : '' }), c(x.lastSync, { m: 1 }), c(x.errors, { m: 1, c: x.errors ? RD : '' }), P(x.status === 'Degraded' ? 'Failed · degraded' : x.status === 'Not connected' ? 'Inactive' : 'Active · simulated')], go: open('integration', x.system) })); break; }
    default: return null;
  }
  return L;
}
function buildDetail3(kind, id, api, ctx) {
  const S = api.S, now = api.clock(), { role } = ctx; const fmt = api.fmt;
  const D = { title: '', sub: '', badges: [], facts: [], sections: [], actions: [], related: [], ai: null, image: null, steps: [] };
  const badge = s => ({ t: s, ...pill(s) }); const go = (p, i) => () => ctx.go(p, i);
  const act = (label, on, o = {}) => ({ label, on, primary: !!o.primary, disabled: !!o.disabled, hint: o.hint || '' });
  const need = (ok, who) => ok ? {} : { disabled: true, hint: 'Requires ' + who };
  switch (kind) {
    case 'surgery': { const s = S.surgeries.find(x => x.id === id); if (!s) return null; const STAGES = ['Requested', 'Approved', 'Scheduled', 'Pre-op', 'Consent signed', 'Anaesthesia assessment', 'In OT', 'Procedure', 'Recovery (PACU)', 'Post-op orders', 'Completed']; const set = S.cssdSets.find(x => x.name === s.cssdSet); const br = S.bloodRequests.find(x => x.patientId === s.patientId); D.title = s.procedure + ' · ' + s.patient; D.sub = s.id + ' · ' + s.dept + ' · ' + s.room + ' · ' + fmt(s.start) + ' · ' + s.durationMin + ' min'; D.badges = [badge(s.stage === 'Completed' ? 'Completed' : s.stage === 'Cancelled' ? 'Cancelled' : 'In progress · ' + s.stage)].concat(s.emergency ? [badge('Critical · emergency OT')] : []).concat(s.delayMin ? [badge('Pending · delayed ' + s.delayMin + ' m')] : []); D.facts = [kv('Team', s.surgeon + ' · assist ' + s.assistant + ' · anaesthesia ' + s.anaesthetist + ' · ' + s.nursing), kv('Blood requirement', s.blood + (br ? ' · ' + br.status : ''), /Yes/.test(s.blood) && !(br && /reserved|Issued|Transfused/.test(br.status)) ? RD : ''), kv('Implant', s.implant + (s.implant !== '—' ? ' · lot tracked to inventory on use' : '')), kv('Instrument set', s.cssdSet + (set ? ' · ' + set.status + ' · ' + set.indicator : ''), set && !/Sterile|Issued|In OT/.test(set.status) ? RD : ''), kv('Specimen', s.specimen), kv('Post-op orders', s.stageIdx >= 9 ? 'Entered by surgeon' : 'Pending'), kv('Delete policy', 'Surgeries are cancelled with reason and kept for OT utilisation reporting')]; D.sections = [{ title: 'Surgical safety checklist (WHO)', items: [['preop', 'Pre-op checklist'], ['consent', 'Consent signed by surgeon'], ['anaesthesia', 'Pre-anaesthesia assessment'], ['signIn', 'Sign-in (before anaesthesia)'], ['timeOut', 'Time-out (before incision)'], ['signOut', 'Sign-out (before leaving OT)']].map(([k, l]) => kv(l, s.checklist[k] ? 'Done' : 'Pending', s.checklist[k] ? G : A)) }]; D.steps = s.history.map(h => ({ t: h.t, who: 'OT', what: h.e, k: /CANCEL|BLOCKED/.test(h.e) ? 'warn' : 'human' })); if (s.stage !== 'Completed' && s.stage !== 'Cancelled') { const next = STAGES[s.stageIdx + 1]; const who = next === 'Consent signed' ? 'Doctor (surgeon)' : /In OT|Procedure|Recovery/.test(next) ? 'Nurse or Doctor' : 'Doctor / OT coordinator'; const ok = next === 'Consent signed' ? role === 'Doctor' : ['Doctor', 'Nurse', 'Hospital Management', 'Front Office'].includes(role); D.actions.push(act('Advance → ' + next, () => api.advanceSurgery(s.id, role), { primary: true, ...need(ok, who) })); D.actions.push(act('Cancel / postpone (reason)', () => ctx.prompt('Cancel ' + s.id, 'Reason: patient unfit · blood unavailable · surgeon unavailable · emergency priority', r => api.cancelSurgery(s.id, r || 'Postponed', role)), need(role === 'Doctor' || role === 'Hospital Management', 'Doctor'))); } D.ai = { title: 'Human control', text: 'Consent, anaesthesia assessment and the safety checklist are clinician actions. The platform enforces sterile-set and blood-reservation gates and tracks implants to inventory; it makes no surgical decisions.' }; D.related = [{ label: 'Patient 360', on: go('patient', s.patientId) }, { label: 'CSSD set', on: () => set && ctx.open('cssdSet', set.id) }, { label: 'Blood request', on: () => br && ctx.open('bloodRequest', br.id) }]; break; }
    case 'nursingPatient': { const p = api.patient(id); if (!p) return null; const vs = S.vitals.filter(v => v.patientId === id).sort((a, b) => b.ts - a.ts); const cp = S.carePlans.find(x => x.patientId === id) || {}; const meds = S.mar.filter(m => m.patientId === id); D.title = p.name + ' · ' + (p.bed || ''); D.sub = p.id + ' · ' + p.procedure + ' · ' + p.doctor; D.badges = [badge(vs[0] ? (vs[0].flag === 'Escalate' ? 'Critical · EWS ' + vs[0].ews : vs[0].flag === 'Watch' ? 'Pending · EWS ' + vs[0].ews : 'Normal · EWS ' + vs[0].ews) : 'No vitals')]; D.facts = [kv('Fall / pressure risk', (cp.fallRisk || '—') + ' / ' + (cp.pressureRisk || '—'), cp.fallRisk === 'High' ? RD : ''), kv('Mobility · diet', (cp.mobility || '—') + ' · ' + (cp.diet || '—')), kv('Intake / output (24 h)', (cp.intake || '—') + ' / ' + (cp.output || '—')), kv('Doctor instructions', cp.instructions || '—'), kv('Care plan tasks', (cp.tasks || []).join(' · '))]; D.sections = [{ title: 'Vitals (latest first)', items: vs.slice(0, 4).map(v => kv(fmt(v.ts).slice(-5) + ' · ' + v.by, 'HR ' + v.hr + ' · BP ' + v.bp + ' · SpO₂ ' + v.spo2 + '% · T ' + v.temp + ' · RR ' + v.rr + ' · pain ' + v.pain + ' · EWS ' + v.ews, v.ews >= 3 ? RD : '')) }, { title: 'Medications (MAR)', items: meds.map(m => kv(fmt(m.due).slice(-5) + ' · ' + m.drug, m.status + (m.givenBy !== '—' ? ' · ' + m.givenBy : '') + (m.highAlert ? ' · high-alert' : ''), m.status === 'Overdue' ? RD : m.status === 'Given' ? G : '')) }]; D.actions = [act('Record vitals (simulated set)', () => api.recordVitals(id, role), { primary: true, ...need(role === 'Nurse' || role === 'Doctor', 'Nurse') }), act('Page doctor · escalate', () => api.notify('Nurse escalation', p.name + ' · ' + (p.bed || '') + ' · ' + role + ' requests review by ' + p.doctor, 'High', 'Nursing', { page: 'nursing', id }), need(role === 'Nurse', 'Nurse')), act('Add nursing note (draft → confirm)', () => ctx.prompt('Nursing note · ' + p.name, 'Note (becomes official only after you confirm)', r => api.mutate(s => { (s.notes = s.notes || []).unshift({ patientId: id, t: fmt(s.clock), by: role, text: r || 'Observed · comfortable' }); }, role, 'Nursing note recorded', id, 'Confirmed', r)), need(role === 'Nurse' || role === 'Doctor', 'Nurse'))]; D.ai = { title: 'Nursing Handover Agent', text: 'Drafts the shift handover from vitals, MAR and notes; the outgoing nurse verifies, edits and confirms before it becomes the handover of record. Escalation thresholds (EWS ≥ 3) alert; the nurse decides.' }; D.related = [{ label: 'Patient 360', on: go('patient', id) }, { label: 'MAR', on: go('mar') }]; break; }
    case 'mar': { const m = S.mar.find(x => x.id === id); if (!m) return null; D.title = m.drug + ' · ' + m.patient; D.sub = m.id + ' · bed ' + m.bed + ' · due ' + fmt(m.due).slice(-5) + ' · prescribed by ' + m.prescriber; D.badges = [badge(m.status === 'Given' ? 'Completed' : m.status === 'Overdue' ? 'Critical · overdue' : m.status)].concat(m.highAlert ? [badge('Critical · high-alert · double-check')] : []); D.facts = [kv('Pharmacy verification', 'Verified · dispensed from Pharmacy Store'), kv('Five rights', 'Patient (wristband scan) · drug · dose · route · time — confirmed at administration'), kv('Given by', m.givenBy + (m.givenAt ? ' · ' + m.givenAt : '')), kv('Delete policy', 'MAR entries are never deleted; omissions and errors are recorded with reason')]; if (m.status !== 'Given') D.actions = [act('Administer · record', () => api.giveMedication(m.id, role), { primary: true, ...need(role === 'Nurse', 'Nurse') }), act('Record omission (reason)', () => ctx.prompt('Omit ' + m.drug, 'Reason: patient refused · NPO · held by doctor', r => api.mutate(s => { m.status = 'Omitted'; m.givenBy = role + ' · ' + (r || 'held'); }, role, 'Medication omitted', m.patientId, 'Omitted', r)), need(role === 'Nurse', 'Nurse'))]; D.related = [{ label: 'Nursing view', on: () => ctx.open('nursingPatient', m.patientId) }]; break; }
    case 'bloodRequest': { const r = S.bloodRequests.find(x => x.id === id); if (!r) return null; D.title = r.units + '× ' + r.group + ' ' + r.component + ' · ' + r.patient; D.sub = r.id + ' · ' + r.reason + ' · ' + r.requestedBy; D.badges = [badge(r.status === 'Transfused' ? 'Completed' : r.status === 'Requested' ? 'Pending · requested' : r.status)]; D.facts = [kv('Compatibility / cross-match', r.crossmatch), kv('Issued', r.issued + ' of ' + r.units), kv('Transfusion reaction', r.reaction || '—'), kv('Traceability', 'Donor → unit → screening → reservation → issue → transfusion → patient record')]; D.steps = r.history.map(h => ({ t: h.t, who: 'Blood bank', what: h.e, k: 'human' })); const next = { Requested: 'Cross-match & reserve', 'Cross-matched · reserved': 'Issue units to ward / OT', Issued: 'Record transfusion + reaction check' }[r.status]; if (next) D.actions = [act(next, () => api.advanceBlood(r.id, role), { primary: true, ...need(['Laboratory', 'Pathologist', 'Nurse', 'Doctor'].includes(role), 'Blood bank / Nurse') })]; D.related = [{ label: 'Patient 360', on: go('patient', r.patientId) }]; break; }
    case 'bloodUnit': { const u = S.bloodUnits.find(x => x.id === id); if (!u) return null; D.title = u.id + ' · ' + u.group + ' ' + u.component; D.sub = 'Donor ' + u.donor + ' · collected ' + u.collected + ' · ' + u.storage; D.badges = [badge(u.status === 'Available' ? 'Active · available' : u.status === 'Quarantine' ? 'Pending · quarantine' : u.status)]; D.facts = [kv('Expiry', u.expiry, /Expired/.test(u.expiry) ? RD : ''), kv('Screening', u.screening), kv('Reserved for', u.reservedFor)]; if (u.status === 'Expired') D.actions = [act('Discard · record wastage', () => api.discardUnit(u.id, role), { primary: true, ...need(['Laboratory', 'Pathologist'].includes(role), 'Blood bank') })]; if (u.status === 'Quarantine') D.actions = [act('Release from quarantine (screening negative)', () => api.mutate(s => { u.screening = 'Negative (HIV, HBV, HCV, syphilis, malaria)'; u.status = 'Available'; }, role, 'Unit released from quarantine', u.id, 'Available'), { primary: true, ...need(['Laboratory', 'Pathologist'].includes(role), 'Blood bank') })]; break; }
    case 'cssdSet': { const s = S.cssdSets.find(x => x.id === id); if (!s) return null; const ld = S.cssdLoads.find(l => l.id === s.load); D.title = s.name; D.sub = s.id + ' · ' + s.items + ' instruments · load ' + s.load; D.badges = [badge(s.status.startsWith('Sterile') ? 'Active · sterile' : 'Pending · ' + s.status)]; D.facts = [kv('Indicator', s.indicator + (ld ? ' · load ' + ld.status : ''), /FAIL/.test(s.indicator) ? RD : ''), kv('Sterile until', s.sterileUntil), kv('Assigned surgery', s.assignedSurgery), kv('Cycle', 'Return → cleaning → packing → sterilisation → indicator → storage → issue')]; const next = { 'Returned · cleaning': 'Pack', Packing: 'Load into autoclave (L-119)', 'Sterilising (load L-119)': 'Unload · read indicator · store', 'Sterilising (load L-118)': 'Unload · read indicator · store', 'Sterile · stored': 'Issue to OT', 'Issued to OT': 'Mark in OT', 'In OT': 'Return for cleaning' }[s.status]; if (next) D.actions = [act(next, () => api.advanceCssd(s.id, role), { primary: true, ...need(['Nurse', 'Store Manager', 'Hospital Management'].includes(role), 'CSSD technician (Nurse / Store Manager)') })]; D.related = [{ label: 'OT', on: go('ot') }]; break; }
    case 'cssdLoad': { const l = S.cssdLoads.find(x => x.id === id); if (!l) return null; D.title = l.id + ' · ' + l.machine; D.sub = l.cycle + ' · ' + l.ts; D.badges = [badge(l.status === 'Running' ? 'In progress · running' : /FAIL/.test(l.indicator) ? 'Failed' : 'Completed')]; D.facts = [kv('Indicator', l.indicator, /FAIL/.test(l.indicator) ? RD : ''), kv('Sets', l.sets), kv('Failure handling', 'Failed biological indicator → all sets quarantined and re-processed · autoclave locked until engineering check')]; break; }
    case 'credential': { const x = S.credentials.find(y => y.id === id); if (!x) return null; D.title = x.holder; D.sub = x.type + ' · ' + x.number; D.badges = [badge(x.status === 'Valid' ? 'Active · valid' : x.status === 'Expired' ? 'Expired' : 'Pending · ' + x.status)]; D.facts = [kv('Expiry', x.expiry, x.status === 'Expired' ? RD : /Expiring/.test(x.status) ? A : ''), kv('Mandatory training', x.training), kv('Workflow', 'Expiring → HR alert → manager notified → renewal → verification → audit'), kv('Rostering', x.status === 'Expired' ? 'BLOCKED until renewed' : 'Allowed', x.status === 'Expired' ? RD : '')]; if (x.status !== 'Valid' && !/Renewal/.test(x.status)) D.actions = [act('Submit renewal for verification', () => api.renewCredential(x.id, role), { primary: true, ...need(role === 'HR Manager' || role === 'Hospital Management', 'HR Manager') })]; if (/due/.test(x.training)) D.actions.push(act('Book mandatory training', () => api.mutate(s => { x.training = x.training.replace(' due', ' booked 20 Sep'); }, role, 'Training booked', x.id, x.training))); D.related = [{ label: 'Employees', on: go('employees') }]; break; }
    default: return null;
  }
  return D;
}

return { NAV3, ERP2_PAGES, ERP2_ROLE_PAGES, buildList3, buildDetail3 };
})();
window.__mm2.erp3 = (function(){
// ERP extension 3 (V2.0 review fixes): facility hierarchy + bed board, employee master + session user, drug master + prescriptions → verification → dispense → MAR,
// encounter clinical record (diagnoses, allergies, notes, orders), charge capture, tax by master, single lab lifecycle, triage / outcomes / death register,
// claim outcomes, GRN inspection + vendor payment approval, credential verification, payment gateway states, derived KPIs, RAG-backed ask(), persistence.
const { answer: ragAnswer } = window.__mm2.rag;
const { APPROVER } = window.__mm2.store;
const VERSION = 'v2.0.3';
const SNAPSHOT_KEY = 'meridian-v2-state';
function installErp3(api) {
  const S = api.S, { audit, notify, emit, nid, rnd, int, pick, fmt, inr } = api._h; const pad = n => String(n).padStart(2, '0');
  const recompute = it => { it.value = it.qty * it.price; it.status = it.qty === 0 ? 'Out of stock' : it.qty < it.min ? 'Low stock' : it.qty > it.max ? 'Overstock' : 'In stock'; };
  const who = role => (S.session && S.session.userId ? S.session.name : role);
  S._v = VERSION;

  // ---------- F-011 facility hierarchy: branch → ward → room → bed ----------
  S.branches.forEach(b => { b.state = 'Tamil Nadu'; b.gstin = b.gstin || '33AAACM1234A1Z' + b.id.slice(-1); });
  S.wards = []; S.rooms = []; const wardOf = {};
  [...new Set(S.beds.map(b => b.dept))].forEach((d, i) => { const w = { id: 'WD-' + pad(i + 1), name: d === 'Emergency' ? 'Emergency bay' : d + ' Ward', dept: d, branchId: 'BR-01', floor: d === 'Emergency' ? 'G' : String(1 + (i % 4)), nurseInCharge: (pick(S.staff.filter(s => /Nurse/.test(s.role))) || {}).name || '—', active: true }; S.wards.push(w); wardOf[d] = w; });
  S.beds.forEach(b => { const w = wardOf[b.dept]; const n = parseInt(b.id.split('-')[1], 10); const cls = b.dept === 'Emergency' ? 'ER bay' : n <= 2 ? 'ICU' : n <= 4 ? 'Private' : 'General'; const roomNo = cls === 'General' ? Math.ceil((n - 4) / 3) : n; const rid = w.id + '-' + ({ ICU: 'I', Private: 'P', General: 'G', 'ER bay': 'E' })[cls] + pad(roomNo); let r = S.rooms.find(x => x.id === rid); if (!r) { r = { id: rid, wardId: w.id, ward: w.name, type: cls, beds: 0, rate: cls === 'ICU' ? 18000 : cls === 'Private' ? 10000 : cls === 'General' ? 4500 : 0, serviceId: cls === 'ICU' ? 'SV-004' : cls === 'Private' ? 'SV-003' : 'SV-002', active: true }; S.rooms.push(r); } r.beds++; b.wardId = w.id; b.ward = w.name; b.roomId = rid; b.cls = cls; b.branchId = 'BR-01'; });
  S.departments.forEach(d => { d.beds = S.beds.filter(b => b.dept === d.name).length; d.branchId = 'BR-01'; });
  S.patients.forEach(p => { p.branchId = 'BR-01'; p.allergies = p.allergies || []; p.flags = p.flags || []; });
  S.admissions.forEach(a => a.branchId = 'BR-01');
  // orphan occupants ('other') become real inpatients with admissions and encounters
  const firsts = S.patients.map(p => p.name.split(' ')[0]), lasts = S.patients.map(p => p.name.split(' ').slice(-1)[0]);
  S.beds.filter(b => b.patientId === 'other').forEach((b, i) => { const doc = S.doctors.find(d => d.dept === b.dept) || pick(S.doctors); const id = 'MER-2026-0' + (9600 + i); const p = { id, uhid: id, name: pick(firsts) + ' ' + pick(lasts), age: int(19, 84), sex: rnd() < 0.5 ? 'F' : 'M', lang: pick(['Tamil', 'Tamil', 'English', 'Telugu']), dept: b.dept, doctor: doc.name, doctorId: doc.id, insurer: pick(['Star Health', 'ICICI Lombard', 'Self-pay', 'HDFC Ergo', 'Self-pay']), procedure: pick(['Medical management', 'Post-operative care', 'Observation', 'IV antibiotics', 'Pain management']), status: 'Admitted', type: 'IP', bed: b.id + ' · ' + b.ward, bedId: b.id, blood: pick(['A+', 'B+', 'O+', 'AB+', 'O−']), phone: '+91 98•••• ' + int(1000, 9999), risk: [], hero: false, branchId: 'BR-01', allergies: [], flags: [] }; S.patients.push(p); b.patientId = id; const ad = { id: 'IP-2026-' + (3400 + i), patientId: id, patient: p.name, doctor: p.doctor, dept: p.dept, bed: p.bed, admitted: (10 + (i % 3)) + ' Sep', status: 'Admitted', estimate: int(40000, 180000), branchId: 'BR-01' }; S.admissions.push(ad); S.encounters.push({ id: 'ENC-' + (20600 + i), patientId: id, patient: p.name, doctor: p.doctor, dept: p.dept, ts: -(1 + (i % 3)) * 1440 + 600, type: 'Inpatient', status: 'Active' }); });

  // ---------- F-004 / F-010 employee master + session user ----------
  S.doctors.forEach((d, i) => { if (!S.employees.some(e => e.name === d.name)) S.employees.push({ id: 'ST-D' + pad(i + 1), empId: 'EMP-D' + pad(i + 1), name: d.name, role: 'Consultant', designation: 'Consultant', unit: d.dept, shift: 'Morning', status: 'On duty', today: 'Present', joined: (2010 + (i % 12)) + '-06-01', employment: 'Active', manager: 'Medical Director', location: 'Kolathur', qualification: 'MD / DM', salary: '₹' + int(250, 480) + ',000', leaveBalance: 12, doctorId: d.id }); });
  const ROLE_OF = r => /Consultant|Resident|Medical Officer/.test(r) ? 'Doctor' : /Nurse|Ward S/.test(r) ? 'Nurse' : /Front Office|Reception/.test(r) ? 'Front Office' : /Insurance/.test(r) ? 'Insurance' : /Billing|Cashier/.test(r) ? 'Billing' : /Lab Tech/.test(r) ? 'Laboratory' : /Patholog/.test(r) ? 'Pathologist' : /Pharmac/.test(r) ? 'Pharmacy' : /Radiograph|Radiolog/.test(r) ? 'Radiologist' : /Store/.test(r) ? 'Store Manager' : /Procure/.test(r) ? 'Procurement Officer' : /HR/.test(r) ? 'HR Manager' : /Finance|Account/.test(r) ? 'Finance Manager' : /Canteen|Diet/.test(r) ? 'Canteen Manager' : /IT/.test(r) ? 'IT Administrator' : /Audit/.test(r) ? 'Auditor' : null;
  const uname = n => n.toLowerCase().replace(/^dr\.?\s*/, '').replace(/[^a-z]+/g, '.').replace(/^\.|\.$/g, '');
  S.users = []; S.employees.filter(e => e.employment !== 'Resigned').forEach(e => { const role = ROLE_OF(e.designation || e.role); if (!role) return; S.users.push({ id: 'USR-' + pad(S.users.length + 1), empId: e.empId, name: e.name, username: uname(e.name), role, dept: e.unit, doctorId: e.doctorId || null, branchId: 'BR-01', mfa: S.users.length % 7 !== 3, status: 'Active', lastLogin: fmt(S.clock - int(5, 900)) }); });
  const ensureUser = (name, role, dept, doctorId) => { let u = S.users.find(x => x.name === name); if (!u) { u = { id: 'USR-' + pad(S.users.length + 1), empId: '—', name, username: uname(name), role, dept, doctorId: doctorId || null, branchId: 'BR-01', mfa: true, status: 'Active', lastLogin: fmt(S.clock - 30) }; S.users.push(u); } else { u.role = role; if (dept) u.dept = dept; if (doctorId) u.doctorId = doctorId; } return u; };
  ensureUser('Dr. Arjun Menon', 'Doctor', 'Cardiology', 'DR-014'); ensureUser('Dr. Priya Venkatesh', 'Doctor', 'Cardiac Surgery', 'DR-003'); ensureUser('Anitha Kumar', 'Nurse', 'Cardiology'); ensureUser('R. Sundar', 'Insurance', 'Insurance Desk'); ensureUser('L. Fathima', 'Insurance', 'Insurance Desk');
  ensureUser('Meera Iyer', 'Hospital Management', 'Administration'); ensureUser('Karthik Rao', 'AI Administrator', 'AI Platform'); ensureUser('Dr. Lakshmi Narayanan', 'Governance Officer', 'Quality'); ensureUser('S. Prabhu', 'IT Administrator', 'IT'); ensureUser('Kavitha Raman', 'Patient', '—');
  ['Billing', 'Front Office', 'Finance Manager', 'HR Manager', 'Store Manager', 'Procurement Officer', 'Pharmacy', 'Laboratory', 'Pathologist', 'Radiologist', 'Canteen Manager', 'Auditor'].forEach((r, i) => { if (!S.users.some(u => u.role === r)) ensureUser(['Divya Prakash', 'Senthil Kumar', 'Ramesh Babu', 'Geetha Rani', 'Mohan Das', 'Vijay Anand', 'Preethi Raj', 'Arun Pandian', 'Dr. Shanthi Devi', 'Dr. Vikram Nair', 'Latha Murugan', 'Naveen Kumar'][i], r, r === 'Pharmacy' ? 'Pharmacy' : r === 'Laboratory' || r === 'Pathologist' ? 'Laboratory' : r === 'Radiologist' ? 'Radiology' : r); });
  S.session = { userId: null, name: 'Hospital Management (demo)', role: 'Hospital Management', dept: null, doctorId: null, branchId: 'BR-01' };
  api.usersForRole = role => S.users.filter(u => u.role === role && u.status === 'Active');
  api.login = (userId, roleFallback) => { const u = S.users.find(x => x.id === userId); if (u) { S.session = { userId: u.id, name: u.name, role: u.role, dept: u.dept, doctorId: u.doctorId, branchId: u.branchId }; u.lastLogin = fmt(S.clock); } else S.session = { userId: null, name: roleFallback + ' (demo)', role: roleFallback, dept: null, doctorId: null, branchId: 'BR-01' }; audit(S.session.name, 'Session started · ' + S.session.role, u ? u.id : '—', '—', 'Active', 'SSO (simulated) · MFA ' + (u && u.mfa ? 'verified' : 'n/a'), '—', 'Web'); emit(); };
  api.stampActors = () => { const s = S.session; S.audit.slice(0, 60).forEach(a => { if (!a.user && s && a.actor === s.role) { a.user = s.name; a.actor = s.name + ' · ' + s.role; } if (!a.origin) a.origin = 'Web · 10.20.' + int(1, 40) + '.' + int(2, 250); }); };
  api.myPatients = () => { const s = S.session; if (s.role === 'Doctor' && s.doctorId) return S.patients.filter(p => p.doctorId === s.doctorId && p.type !== 'Discharged'); if (s.role === 'Nurse') return S.patients.filter(p => p.type === 'IP' && (!s.dept || s.dept === '—' || p.dept === s.dept)); return S.patients.filter(p => p.type === 'IP'); };
  api.setUserStatus = (id, status, role) => { const u = S.users.find(x => x.id === id); if (!u) return; const b = u.status; u.status = status; audit(role, 'User ' + status.toLowerCase(), id, b, status, '—', '—', 'Administration'); emit(); };

  // ---------- F-001 encounter clinical record ----------
  const ICD = [[/PTCA|angioplasty|stent/i, 'I21.4', 'Non-ST elevation myocardial infarction'], [/CABG|bypass/i, 'I25.10', 'Atherosclerotic heart disease'], [/hip/i, 'M16.1', 'Primary osteoarthritis of hip'], [/chole/i, 'K80.20', 'Cholelithiasis without obstruction'], [/knee/i, 'M17.1', 'Primary osteoarthritis of knee'], [/dialysis|renal|nephr/i, 'N18.5', 'Chronic kidney disease, stage 5'], [/pneumonia|COPD|pulmon/i, 'J18.9', 'Pneumonia, unspecified organism'], [/delivery|LSCS|caesar/i, 'O82', 'Encounter for caesarean delivery'], [/chemo|oncol|carcinoma/i, 'C50.9', 'Malignant neoplasm of breast'], [/stroke|neuro/i, 'I63.9', 'Cerebral infarction, unspecified'], [/antibiotic|sepsis|fever/i, 'A41.9', 'Sepsis, unspecified organism'], [/pain/i, 'R52', 'Pain, unspecified'], [/observation|medical/i, 'R69', 'Illness, unspecified']];
  api.currentEncounter = pid => { const list = S.encounters.filter(e => e.patientId === pid).sort((a, b) => b.ts - a.ts); return list.find(e => e.status !== 'Closed') || list[0] || null; };
  S.encounters.forEach(e => { e.diagnoses = e.diagnoses || []; e.notes = e.notes || []; e.orders = e.orders || S.orders.filter(o => o.patientId === e.patientId).map(o => o.id); e.prescriptions = e.prescriptions || []; if (!e.status) e.status = 'Active'; });
  S.patients.forEach((p, i) => { if (p.type === 'OP' || p.type === 'Discharged') return; const e = api.currentEncounter(p.id); if (!e || e.diagnoses.length) return; const m = ICD.find(x => x[0].test(p.procedure || '')) || ICD[ICD.length - 1]; e.diagnoses.push({ code: m[1], desc: m[2], type: 'Primary', by: p.doctor, ts: e.ts }); if (i % 4 === 0) e.diagnoses.push({ code: 'E11.9', desc: 'Type 2 diabetes mellitus without complications', type: 'Secondary', by: p.doctor, ts: e.ts }); if (i % 5 === 1) e.diagnoses.push({ code: 'I10', desc: 'Essential hypertension', type: 'Secondary', by: p.doctor, ts: e.ts }); e.notes.push({ ts: e.ts + 40, by: p.doctor, kind: 'Admission note', text: 'Admitted for ' + (p.procedure || 'evaluation') + '. Haemodynamically stable. Plan: ' + (m[1] === 'I21.4' ? 'dual antiplatelet, statin, cath lab.' : 'as per department protocol; review in morning round.'), status: 'Signed' }); e.notes.push({ ts: S.clock - int(60, 400), by: pick(['Anitha Kumar', 'Sheela J']), kind: 'Nursing note', text: pick(['Patient comfortable, tolerating oral diet.', 'Ambulated with assistance, no complaints.', 'IV site clean, no signs of phlebitis.', 'Pain 2/10, analgesia effective.']), status: 'Signed' }); if (i % 6 === 2 && !p.allergies.length) p.allergies.push(pick(['Penicillin', 'Sulfonamides', 'NSAIDs', 'Iodinated contrast'])); });
  const kav = S.patients.find(p => p.id === 'MER-2026-008421'); if (kav) { kav.allergies = ['Iodinated contrast (mild rash, 2019)']; const e = api.currentEncounter(kav.id); if (e && !e.notes.some(n => /round/i.test(n.kind))) e.notes.push({ ts: -1440 + 1000, by: 'Dr. Arjun Menon', kind: 'Progress note (round)', text: 'Post-PTCA day 2. Chest pain free, ECG unchanged, access site clean. Likely discharge tomorrow if troponin trend continues to fall.', status: 'Signed' }); }
  api.addDiagnosis = (encId, text, role) => { const e = S.encounters.find(x => x.id === encId); if (!e || !text) return; const w = text.trim().split(/\s+/); const code = /^[A-Z]\d\d(\.\d+)?$/i.test(w[0]) ? w[0].toUpperCase() : '—'; const desc = code === '—' ? text.trim() : w.slice(1).join(' ') || 'Unspecified'; e.diagnoses.push({ code, desc, type: e.diagnoses.length ? 'Secondary' : 'Primary', by: who(role), ts: S.clock }); audit(role, 'Diagnosis added · ' + code + ' ' + desc, e.patientId, '—', code, desc, '—', 'EMR', encId); emit(); };
  api.addAllergy = (pid, text, role) => { const p = api.patient(pid); if (!p || !text) return; p.allergies.push(text.trim()); S.prescriptions.filter(r => r.patientId === pid && /Prescribed|Verified|Dispensed/.test(r.status)).forEach(r => { const d = S.drugs.find(x => x.id === r.drugId); if (d && (text.toLowerCase().includes(d.generic.toLowerCase()) || d.generic.toLowerCase().includes(text.toLowerCase().split(' ')[0]))) { r.checks.push('ALLERGY · ' + text); notify('Allergy conflict with active prescription', p.name + ' · ' + d.name + ' · prescriber review required', 'High', 'Pharmacy', { page: 'prescriptions', id: r.id }); } }); notify('Allergy recorded', p.name + ' · ' + text, 'Medium', 'Clinical', { page: 'patient', id: pid }); audit(role, 'Allergy recorded · ' + text, pid, '—', text, '—', '—', 'EMR'); emit(); };
  api.addNote = (encId, text, role, kind) => { const e = S.encounters.find(x => x.id === encId); if (!e || !text) return; e.notes.push({ ts: S.clock, by: who(role), kind: kind || (role === 'Nurse' ? 'Nursing note' : 'Progress note'), text: text.trim(), status: 'Signed' }); audit(role, (kind || (role === 'Nurse' ? 'Nursing note' : 'Progress note')) + ' signed', e.patientId, '—', 'Signed', text.slice(0, 80), '—', 'EMR', encId); emit(); };
  const MODS = ['CT', 'MRI', 'X-RAY', 'USG', 'ECHO'];
  api.createOrder = (pid, text, role) => { const p = api.patient(pid); if (!p) return { error: 'Patient not found' }; const s = (text || '').trim(); if (!s) return { error: 'Enter a test name' }; const stat = /\bstat\b/i.test(s); const name = s.replace(/\bstat\b/i, '').trim(); const lt = S.labTests.find(t => t.panel.toLowerCase() === name.toLowerCase() || t.code === name.toUpperCase().replace(/[^A-Z0-9]/g, '').slice(0, 6)); const mod = MODS.find(m => name.toUpperCase().startsWith(m)); if (!lt && !mod) return { error: 'Unknown test "' + name + '". Lab: ' + S.labTests.map(t => t.panel).join(', ') + '. Imaging: CT …, MRI …, X-ray …, USG …, ECHO.' }; const enc = api.currentEncounter(pid); const o = { id: nid('ORD'), patientId: pid, patient: p.name, doctor: S.session.role === 'Doctor' ? S.session.name : p.doctor, kind: lt ? 'Lab' : 'Radiology', test: lt ? lt.panel : name, prep: lt ? (/fasting/i.test(lt.specimen) ? 'Fasting 8 h' : 'None') : mod === 'CT' ? 'NPO 4 h · creatinine check' : 'None', ts: S.clock, status: 'Ordered', priority: stat ? 'STAT' : 'Routine', encounterId: enc ? enc.id : '—' }; S.orders.unshift(o); if (enc) enc.orders.push(o.id);
    if (lt) { const l = { id: nid('LAB'), orderId: o.id, patientId: pid, patient: p.name, test: lt.panel, analyte: lt.analyte, value: null, unit: lt.unit, flag: 'Pending', critical: false, ts: S.clock, status: 'Ordered' }; S.labs.unshift(l); const acc = 'ACC' + int(270000, 279999); const smp = { id: nid('SMP'), accession: acc, barcode: '*' + acc.slice(3) + '*', labId: l.id, orderId: o.id, patientId: pid, patient: p.name, test: lt.panel, specimen: lt.specimen, container: lt.container, priority: stat ? 'STAT' : 'Routine', collector: '—', collectedAt: S.clock, receivedAt: null, analyzer: lt.analyzer, status: 'Pending Collection', rejectReason: null, tatTarget: lt.tat, history: [{ t: fmt(S.clock), e: 'Ordered by ' + o.doctor + (stat ? ' · STAT' : '') + ' · charge posted' }] }; S.samples.unshift(smp); l.sampleId = smp.id; l.accession = acc; const svc = S.services.find(x => x.id === lt.serviceId); api.postCharge(pid, svc ? svc.name : lt.panel, svc ? svc.price : 500, svc ? svc.taxCode : 'GST-0', 'Laboratory order', o.id); notify('Lab order' + (stat ? ' · STAT' : ''), p.name + ' · ' + lt.panel + ' · sample pending collection', stat ? 'High' : 'Low', 'LIS', { page: 'lab-queue', id: smp.id }); }
    else { const r = { id: nid('RAD-2026'), orderId: o.id, patientId: pid, patient: p.name, modality: mod === 'X-RAY' ? 'X-ray' : mod, study: name, received: S.clock, aiStatus: 'Not screened', aiProb: null, priority: stat ? 'Priority' : 'Routine', model: 'placeholder · no model', radStatus: 'Unread', final: '—', accession: 'RAD' + int(710000, 719999), viewed: false, dicom: '—' }; S.radiology.unshift(r); const svc = S.services.find(x => x.category === 'Radiology' && x.name.toUpperCase().startsWith(mod === 'X-RAY' ? 'X' : mod)); api.postCharge(pid, name, svc ? svc.price : 3000, 'GST-0', 'Imaging order', o.id); notify('Imaging order', p.name + ' · ' + name, stat ? 'High' : 'Low', 'Radiology', { page: 'radiology', id: r.id }); }
    audit(role, 'Order created · ' + o.test + (stat ? ' STAT' : ''), pid, '—', 'Ordered', o.kind, '—', 'EMR', o.id); emit(); return o; };

  // ---------- F-006 charge capture ----------
  api.runningBill = pid => { const p = api.patient(pid); let b = S.bills.find(x => x.patientId === pid && !/Paid|Settled|Voided|Closed/.test(x.status)); if (!b) { const ad = S.admissions.find(a => a.patientId === pid && a.status === 'Admitted'); b = { id: nid('BILL'), patientId: pid, patient: p.name, admissionId: ad ? ad.id : '—', estimate: ad ? ad.estimate : 0, actual: 0, insurance: 0, patientShare: 0, paid: 0, status: 'Provisional', lines: [], dispute: null, ts: S.clock }; S.bills.unshift(b); } return b; };
  api.postCharge = (pid, item, amount, taxCode, source, ref) => { const b = api.runningBill(pid); if (!b.lines) b.lines = [{ item: 'Charges before charge capture', est: b.estimate, cur: b.actual, taxCode: 'GST-0', source: 'Seed' }]; b.lines.push({ item, est: 0, cur: Math.round(amount), taxCode: taxCode || 'GST-0', source, ref, ts: S.clock }); b.actual = Math.round(b.actual + amount); const pa = S.preauths.find(x => x.patientId === pid && /Approved/.test(x.status)); const cov = pa ? parseInt(String(pa.approved).replace(/[^\d]/g, ''), 10) || 0 : b.insurance || 0; b.insurance = Math.min(cov, b.actual); b.patientShare = Math.max(0, b.actual - b.insurance - (b.discount || 0)); if (b.status === 'Pending') b.status = 'Provisional'; audit('System · charge capture', 'Charge posted · ' + item + ' · ' + inr(amount), pid, '—', inr(b.actual), source, '—', 'Billing', ref || b.id); return b; };
  const origAdvance = api.advanceTime.bind(api); api.advanceTime = min => { const before = Math.floor(S.clock / 1440); origAdvance(min); if (Math.floor(S.clock / 1440) > before) { S.beds.filter(b => b.patientId && /Occupied|Discharge Pending/.test(b.status)).forEach(b => { const r = S.rooms.find(x => x.id === b.roomId); if (r && r.rate) api.postCharge(b.patientId, 'Room · ' + r.type + ' / night', r.rate, 'GST-0', 'Bed-day rule', b.id); }); notify('Bed-day charges posted', 'Midnight rule · room charges added to every occupied inpatient bill', 'Low', 'Billing', { page: 'billing' }); emit(); } };

  // ---------- F-014 tax by master ----------
  S.taxRules = [['Medicine', 'GST-12'], ['Implant', 'GST-12'], ['Consumable', 'GST-12'], ['Reagent', 'GST-12'], ['QC', 'GST-12'], ['Canteen', 'GST-5'], ['Equipment', 'GST-18'], ['Stationery', 'GST-18'], ['General', 'GST-18']].map(([category, code]) => ({ id: 'TR-' + category.toUpperCase(), category, code, active: true }));
  const codeForCategory = cat => (S.taxRules.find(r => r.active && cat && cat.startsWith(r.category)) || {}).code || 'GST-18';
  S.items.forEach(it => it.taxCode = it.taxCode || codeForCategory(it.category));
  S.services.forEach(sv => { if (sv.taxCode === 'exempt') sv.taxCode = 'GST-0'; });
  api.billTax = b => { const p = api.patient(b.patientId); const inter = !!(p && p.state && p.state !== 'Tamil Nadu'); const lines = b.lines && b.lines.length ? b.lines : [{ item: 'Services (pre charge-capture)', cur: b.actual, taxCode: 'GST-0' }]; const rows = lines.map(l => { let code = l.taxCode; if (!code) { const svc = S.services.find(s => s.name === l.item || String(l.item || '').startsWith(s.name)); code = svc ? svc.taxCode : 'GST-0'; } if (code === 'exempt') code = 'GST-0'; const t = S.taxes.find(x => x.code === code) || S.taxes[0]; const taxable = l.cur || 0; const tax = Math.round(taxable * t.rate / 100); const cgst = inter ? 0 : Math.round(tax / 2), sgst = inter ? 0 : tax - Math.round(tax / 2), igst = inter ? tax : 0; return { item: l.item, code: t.code, taxable, cgst, sgst, igst, total: taxable + tax, hsn: t.hsn, source: l.source || '—' }; }); const sum = k => rows.reduce((a, r) => a + r[k], 0); return { rows, taxable: sum('taxable'), cgst: sum('cgst'), sgst: sum('sgst'), igst: sum('igst'), gross: sum('total'), basis: 'Tax code from service / item master (no name matching) · ' + (inter ? 'inter-state → IGST' : 'intra-state → CGST + SGST') }; };
  api.requestPurchase = (itemId, role) => { const it = S.items.find(x => x.id === itemId); if (!it) return; const v = S.vendors.find(x => x.name === it.supplier) || S.vendors[0]; const qty = Math.max(1, it.max - it.qty); const tax = S.taxes.find(t => t.code === it.taxCode) || S.taxes.find(t => t.code === 'GST-18'); const pr = { id: nid('PR-2026'), itemId, item: it.name, qty, unit: it.unit, dept: it.store, requester: who(role), vendorId: v.id, vendor: v.name, amount: qty * it.price, taxCode: tax.code, taxAmt: Math.round(qty * it.price * tax.rate / 100), total: Math.round(qty * it.price * (1 + tax.rate / 100)), status: 'Pending Approval', po: '—', grn: '—', invoice: '—', paid: false, ts: S.clock, history: [{ t: fmt(S.clock), e: 'Requisition raised by ' + who(role) + ' · tax ' + tax.code + ' from item master' }] }; S.purchases.unshift(pr); S.approvals.unshift({ id: nid('AP'), type: 'Purchase approval', patientId: '—', patient: it.name, agentId: '—', agent: 'Procurement (human request)', action: 'Approve PR ' + pr.id + ' · ' + qty + ' ' + it.unit + ' · ' + inr(pr.total) + ' incl. ' + tax.code, risk: pr.total > 100000 ? 'High' : 'Medium', reason: 'Reorder · stock ' + it.qty + ' below min ' + it.min, created: S.clock, sla: 1440, owner: 'Procurement Officer', status: 'Pending', entityId: pr.id }); notify('Purchase requisition raised', pr.id + ' · ' + it.name + ' · awaiting approval', 'Low', 'Procurement', { page: 'procurement', id: pr.id }); audit(role, 'Purchase requisition created', pr.id, '—', 'Pending Approval', it.name, 'Pending', 'Procurement'); emit(); return pr.id; };

  // ---------- F-015 procurement controls: GRN inspection, 3-way match, payment approval ----------
  APPROVER['Vendor payment'] = 'Finance Manager';
  api.advancePurchase = (prId, role, outcome) => { const p = S.purchases.find(x => x.id === prId); if (!p) return; const before = p.status; let next = null;
    if (before === 'Pending Approval') { notify('Approval required', p.id + ' must be approved in the Approval Centre before a PO can be issued', 'Low', 'Procurement', { page: 'approvals' }); emit(); return; }
    if (before === 'Approved') { next = 'Ordered'; p.po = nid('PO-2026'); }
    else if (before === 'Ordered' || before === 'Partially Received') { next = 'Received · QC pending'; p.grn = nid('GRN-2026'); p.grnQty = p.qty; }
    else if (before === 'Received · QC pending') { if (outcome === 'reject') { next = 'QC Rejected · returned to vendor'; p.qcNote = 'Rejected at inspection · debit note to vendor'; notify('GRN rejected at QC', p.id + ' · ' + p.item + ' returned to ' + p.vendor, 'Medium', 'Procurement', { page: 'procurement', id: prId }); } else { next = 'Received'; const it = S.items.find(x => x.id === p.itemId); if (it) { it.qty += p.qty; recompute(it); S.stockTx.unshift({ id: nid('STX'), itemId: it.id, item: it.name, type: 'Receipt', qty: p.qty, from: p.vendor, to: it.store, by: who(role), ts: S.clock, status: 'Posted', reason: p.grn + ' · QC passed by ' + who(role) }); } p.qcNote = 'Inspected and accepted by ' + who(role); } }
    else if (before === 'Received') { p.invoice = 'INV-' + p.vendorId + '-' + int(100, 999); p.invoiceQty = p.qty; p.match = p.qty === p.grnQty && p.grnQty === p.invoiceQty ? 'Matched (PO = GRN = Invoice)' : 'Mismatch'; next = 'Invoice matched'; }
    else if (before === 'Invoice matched') { next = 'Payment pending approval'; S.approvals.unshift({ id: nid('AP'), type: 'Vendor payment', patientId: '—', patient: p.vendor, agentId: '—', agent: 'Accounts payable (human request)', action: 'Pay ' + inr(p.total) + ' to ' + p.vendor + ' · ' + p.invoice + ' · ' + p.match, risk: p.total > 100000 ? 'High' : 'Medium', reason: '3-way match ' + p.match, created: S.clock, sla: 2880, owner: 'Finance Manager', status: 'Pending', entityId: p.id }); notify('Vendor payment awaiting approval', p.vendor + ' · ' + inr(p.total), 'Low', 'Finance', { page: 'approvals' }); }
    if (!next) return; p.status = next; p.history.unshift({ t: fmt(S.clock), e: before + ' → ' + next + ' by ' + who(role) }); audit(role, 'Procurement ' + before + ' → ' + next, p.id, before, next, p.item, '—', 'Procurement', p.po); emit(); };

  // ---------- F-002 drug master + prescriptions ----------
  const DRUGS = [['Aspirin', 'Aspirin 75 mg tab', 'Ecosprin', 'Tablet', '75 mg', 'H', 'PO'], ['Clopidogrel', 'Clopidogrel 75 mg tab', 'Clopilet', 'Tablet', '75 mg', 'H', 'PO'], ['Atorvastatin', 'Atorvastatin 40 mg tab', 'Atorva', 'Tablet', '40 mg', 'H', 'PO'], ['Metoprolol', 'Metoprolol 25 mg tab', 'Metolar', 'Tablet', '25 mg', 'H', 'PO'], ['Pantoprazole', 'Pantoprazole 40 mg', 'Pan', 'Tablet / Injection', '40 mg', 'H', 'PO/IV'], ['Enoxaparin', 'Enoxaparin 40 mg inj', 'Clexane', 'Injection', '40 mg', 'H', 'SC'], ['Paracetamol', 'Paracetamol 650 mg tab', 'Dolo', 'Tablet', '650 mg', 'OTC', 'PO'], ['Ceftriaxone', 'Ceftriaxone 1 g inj', 'Monocef', 'Injection', '1 g', 'H', 'IV'], ['Furosemide', 'Furosemide 40 mg tab', 'Lasix', 'Tablet', '40 mg', 'H', 'PO'], ['Insulin', 'Insulin glargine 100 IU/mL', 'Lantus', 'Injection', '100 IU/mL', 'H', 'SC'], ['Metformin', 'Metformin 500 mg tab', 'Glycomet', 'Tablet', '500 mg', 'H', 'PO'], ['Amlodipine', 'Amlodipine 5 mg tab', 'Amlong', 'Tablet', '5 mg', 'H', 'PO'], ['Ibuprofen', 'Ibuprofen 400 mg tab', 'Brufen', 'Tablet', '400 mg', 'OTC', 'PO'], ['Morphine', 'Morphine 10 mg inj', '—', 'Injection', '10 mg', 'X (narcotic)', 'IV']];
  const INTER = { Aspirin: ['Clopidogrel', 'Enoxaparin', 'Ibuprofen'], Clopidogrel: ['Aspirin'], Enoxaparin: ['Aspirin', 'Ibuprofen'], Ibuprofen: ['Aspirin', 'Enoxaparin'], Metoprolol: ['Amlodipine'], Amlodipine: ['Metoprolol'] };
  const ALLERGY_CLASS = { Ceftriaxone: /penicillin|cephalosporin|beta-lactam/i, Ibuprofen: /nsaid|ibuprofen|aspirin/i, Aspirin: /nsaid|aspirin/i, Morphine: /opioid|morphine/i };
  S.drugs = DRUGS.map(([g, name, brand, form, str, sch, route], i) => { const it = S.items.find(x => x.name.toLowerCase().startsWith(g.toLowerCase())); return { id: 'DRG-' + pad(i + 1), generic: g, name, brand, form, strength: str, schedule: sch, route, itemId: it ? it.id : null, highAlert: /Insulin|Enoxaparin|Morphine/.test(g), controlled: sch.startsWith('X'), interactions: INTER[g] || [], active: true }; });
  const FREQ = ['OD', 'BD', 'TDS', 'QID', 'HS']; const TIMES = { OD: [540], BD: [540, 1260], TDS: [480, 840, 1260], QID: [360, 720, 1080, 1320], HS: [1260] };
  const allergyHit = (p, d) => (p.allergies || []).find(a => (ALLERGY_CLASS[d.generic] && ALLERGY_CLASS[d.generic].test(a)) || a.toLowerCase().includes(d.generic.toLowerCase()));
  const safetyChecks = (p, d) => { const checks = []; const a = allergyHit(p, d); if (a) checks.push('ALLERGY · ' + a); S.prescriptions.filter(r => r.patientId === p.id && /Prescribed|Verified|Dispensed/.test(r.status)).forEach(r => { const o = S.drugs.find(x => x.id === r.drugId); if (o && o.id !== d.id && d.interactions.includes(o.generic) && !checks.some(c => c.includes(o.generic))) checks.push('INTERACTION · ' + d.generic + ' + ' + o.generic); }); if (d.controlled) checks.push('CONTROLLED · Schedule X register entry required'); return checks; };
  S.prescriptions = [];
  S.pharmacy.forEach((t, i) => { const g = t.drug.split(' ')[0]; const d = S.drugs.find(x => x.generic === g) || S.drugs[6]; const p = api.patient(t.patientId); const enc = p ? api.currentEncounter(p.id) : null; const rx = { id: 'RX-' + (50000 + i), patientId: t.patientId, patient: t.patient, encounterId: enc ? enc.id : '—', doctorId: p ? p.doctorId : null, doctor: p ? p.doctor : '—', drugId: d.id, drug: d.name, dose: d.strength, route: d.route.split('/')[0], freq: pick(FREQ), days: int(3, 10), qty: t.qty, ts: t.ts - 30, status: t.status === 'Dispensed' ? 'Dispensed' : t.status === 'Returned' ? 'Returned' : t.status === 'Stock-out' ? 'Stock-out' : 'Verified', verifiedBy: t.status === 'Pending' ? 'Preethi Raj' : 'Preethi Raj', checks: [], txId: t.id, ipd: !!(p && p.type === 'IP'), controlled: d.controlled, batch: t.status === 'Dispensed' ? 'B' + int(1000, 9999) : null }; t.prescriptionId = rx.id; t.drugId = d.id; if (enc) enc.prescriptions.push(rx.id); S.prescriptions.push(rx); });
  // every inpatient gets an active medication list
  S.patients.filter(p => p.type === 'IP').forEach((p, i) => { if (S.prescriptions.some(r => r.patientId === p.id && r.status === 'Dispensed')) return; const set = p.dept === 'Cardiology' || p.dept === 'Cardiac Surgery' ? ['Aspirin', 'Clopidogrel', 'Atorvastatin', 'Metoprolol'] : ['Paracetamol', 'Ceftriaxone', 'Pantoprazole']; const enc = api.currentEncounter(p.id); set.forEach((g, j) => { const d = S.drugs.find(x => x.generic === g); const freq = ['OD', 'BD', 'TDS', 'OD'][j]; const rx = { id: nid('RX'), patientId: p.id, patient: p.name, encounterId: enc ? enc.id : '—', doctorId: p.doctorId, doctor: p.doctor, drugId: d.id, drug: d.name, dose: d.strength, route: d.route.split('/')[0], freq, days: 5, qty: TIMES[freq].length * 5, ts: -1440 + 600 + j, status: 'Dispensed', verifiedBy: 'Preethi Raj', checks: [], ipd: true, controlled: false, batch: 'B' + int(1000, 9999) }; S.prescriptions.push(rx); if (enc) enc.prescriptions.push(rx.id); S.pharmacy.push({ id: nid('PH'), patientId: p.id, patient: p.name, drug: d.name, drugId: d.id, prescriptionId: rx.id, qty: rx.qty, ts: rx.ts + 30, status: 'Dispensed', ward: (p.bed || '').split(' ')[0], batch: rx.batch }); rx.txId = S.pharmacy[S.pharmacy.length - 1].id; }); });
  // rebuild MAR from prescriptions (single source of truth)
  S.mar = []; S.prescriptions.filter(r => r.ipd && /Verified|Dispensed/.test(r.status)).forEach(rx => { const d = S.drugs.find(x => x.id === rx.drugId); const p = api.patient(rx.patientId); if (!p || p.type !== 'IP') return; TIMES[rx.freq].forEach(due => S.mar.push({ id: nid('MAR'), patientId: rx.patientId, patient: rx.patient, bed: (p.bed || '').split(' ')[0], drug: d.name + ' ' + rx.route, prescriptionId: rx.id, due, status: rx.status !== 'Dispensed' ? 'Awaiting pharmacy' : due + 30 < S.clock ? (rnd() < 0.9 ? 'Given' : 'Overdue') : due < S.clock ? 'Due' : 'Scheduled', highAlert: d.highAlert || /IV/.test(rx.route), verified: !!rx.verifiedBy, givenBy: rx.status === 'Dispensed' && due + 30 < S.clock ? 'Anitha Kumar' : '—', prescriber: rx.doctor })); });
  api.prescribe = (pid, text, role) => { const p = api.patient(pid); if (!p) return { error: 'Patient not found' }; const w = (text || '').trim().split(/\s+/).filter(Boolean); if (!w.length) return { error: 'Enter a drug' }; const d = S.drugs.find(x => x.active && (x.generic.toLowerCase() === w[0].toLowerCase() || x.brand.toLowerCase() === w[0].toLowerCase())); if (!d) return { error: 'Unknown drug "' + w[0] + '". Drug master: ' + S.drugs.map(x => x.generic).join(', ') }; const freq = (w.find(x => FREQ.includes(x.toUpperCase())) || 'OD').toUpperCase(); const days = +(w.filter(x => /^\d+$/.test(x)).pop() || 5); const enc = api.currentEncounter(pid); const checks = safetyChecks(p, d); const rx = { id: nid('RX'), patientId: pid, patient: p.name, encounterId: enc ? enc.id : '—', doctorId: S.session.doctorId || p.doctorId, doctor: S.session.role === 'Doctor' ? S.session.name : p.doctor, drugId: d.id, drug: d.name, dose: d.strength, route: d.route.split('/')[0], freq, days, qty: TIMES[freq].length * days, ts: S.clock, status: 'Prescribed', verifiedBy: null, checks, ipd: p.type === 'IP', controlled: d.controlled }; S.prescriptions.unshift(rx); if (enc) enc.prescriptions.push(rx.id); if (checks.some(c => /ALLERGY|INTERACTION/.test(c))) notify('Prescription safety alert', p.name + ' · ' + d.generic + ' · ' + checks.join(' · '), 'High', 'Pharmacy', { page: 'prescriptions', id: rx.id }); else notify('New prescription', p.name + ' · ' + d.name + ' ' + freq + ' × ' + days + ' d · awaiting pharmacist verification', 'Low', 'Pharmacy', { page: 'prescriptions', id: rx.id }); audit(role, 'Prescription created · ' + d.name + ' ' + freq, pid, '—', 'Prescribed', checks.join('; ') || 'No safety flags', '—', 'EMR', rx.id); emit(); return rx; };
  api.overridePrescriptionCheck = (rxId, reason, role) => { const rx = S.prescriptions.find(x => x.id === rxId); if (!rx) return; rx.override = { by: who(role), reason, ts: S.clock }; audit(role, 'Safety check overridden · ' + rx.drug, rx.patientId, rx.checks.join('; '), 'Overridden', reason, '—', 'EMR', rxId); notify('Safety check overridden by prescriber', rx.patient + ' · ' + rx.drug + ' · ' + reason, 'Medium', 'Pharmacy', { page: 'prescriptions', id: rxId }); emit(); };
  api.verifyPrescription = (rxId, role) => { const rx = S.prescriptions.find(x => x.id === rxId); if (!rx || rx.status !== 'Prescribed') return; if (rx.checks.some(c => /ALLERGY/.test(c)) && !rx.override) { notify('Verification blocked', rx.patient + ' · ' + rx.drug + ' · allergy conflict · prescriber must override with reason', 'High', 'Pharmacy', { page: 'prescriptions', id: rxId }); audit(role, 'Verification BLOCKED · allergy', rx.patientId, 'Prescribed', 'Prescribed', rx.checks.join('; '), '—', 'Pharmacy', rxId); emit(); return; } rx.status = 'Verified'; rx.verifiedBy = who(role); rx.verifiedAt = S.clock; if (rx.ipd) { const d = S.drugs.find(x => x.id === rx.drugId); const p = api.patient(rx.patientId); TIMES[rx.freq].forEach(due => S.mar.push({ id: nid('MAR'), patientId: rx.patientId, patient: rx.patient, bed: (p.bed || '').split(' ')[0], drug: d.name + ' ' + rx.route, prescriptionId: rx.id, due, status: 'Awaiting pharmacy', highAlert: d.highAlert || /IV/.test(rx.route), verified: true, givenBy: '—', prescriber: rx.doctor })); } audit(role, 'Prescription verified · ' + rx.drug, rx.patientId, 'Prescribed', 'Verified', rx.checks.join('; ') || 'No safety flags', '—', 'Pharmacy', rxId); emit(); };
  api.dispensePrescription = (rxId, role) => { const rx = S.prescriptions.find(x => x.id === rxId); if (!rx || !/Verified|Stock-out/.test(rx.status)) return; const d = S.drugs.find(x => x.id === rx.drugId); const p = api.patient(rx.patientId); const it = d.itemId ? S.items.find(x => x.id === d.itemId) : null; if (!it) { notify('No inventory mapping', d.name + ' is not linked to a stock item · map it in the drug master', 'Medium', 'Pharmacy', { page: 'drugs', id: d.id }); emit(); return; } if (it.expiryState === 'Expired') { notify('Dispense blocked · expired batch', it.name + ' batch ' + it.batch + ' expired · quarantine', 'High', 'Pharmacy', { page: 'inventory', id: it.id }); audit(role, 'Dispense BLOCKED · expired batch', rx.patientId, rx.status, rx.status, it.batch, '—', 'Pharmacy', rxId); emit(); return; } if (it.qty < rx.qty) { rx.status = 'Stock-out'; notify('Stock-out', d.name + ' · need ' + rx.qty + ', have ' + it.qty + ' · reorder or substitute', 'High', 'Pharmacy', { page: 'inventory', id: it.id }); audit(role, 'Dispense BLOCKED · stock-out', rx.patientId, 'Verified', 'Stock-out', it.qty + ' ' + it.unit, '—', 'Pharmacy', rxId); emit(); return; } it.qty -= rx.qty; recompute(it); S.stockTx.unshift({ id: nid('STX'), itemId: it.id, item: it.name, type: 'Issue', qty: -rx.qty, from: it.store, to: rx.ipd ? (p.bed || '').split(' ')[0] : 'OP pharmacy', by: who(role), ts: S.clock, status: 'Posted', reason: rx.id + ' · batch ' + it.batch }); const tx = { id: nid('PH'), patientId: rx.patientId, patient: rx.patient, drug: d.name, drugId: d.id, prescriptionId: rx.id, qty: rx.qty, batch: it.batch, expiry: it.expiry, ts: S.clock, status: 'Dispensed', ward: rx.ipd ? (p.bed || '').split(' ')[0] : 'OP' }; S.pharmacy.unshift(tx); rx.status = 'Dispensed'; rx.txId = tx.id; rx.batch = it.batch; rx.dispensedBy = who(role); api.postCharge(rx.patientId, 'Pharmacy · ' + d.name + ' × ' + rx.qty, it.price * rx.qty, it.taxCode, 'Pharmacy dispense', rx.id); S.mar.filter(m => m.prescriptionId === rx.id).forEach(m => { m.status = m.due < S.clock ? 'Due' : 'Scheduled'; }); if (it.status !== 'In stock') notify(it.status, it.name + ' · ' + it.qty + ' ' + it.unit + ' left', 'Medium', 'Inventory', { page: 'inventory', id: it.id }); audit(role, 'Dispensed · ' + d.name + ' × ' + rx.qty + ' · batch ' + it.batch, rx.patientId, 'Verified', 'Dispensed', inr(it.price * rx.qty) + ' charged', '—', 'Pharmacy', rxId); emit(); };
  const origDispense = api.dispense.bind(api); api.dispense = (txId, role) => { const t = S.pharmacy.find(x => x.id === txId); const rx = t && S.prescriptions.find(r => r.id === t.prescriptionId); if (rx && rx.status === 'Prescribed') { notify('Pharmacist verification required', rx.drug + ' · verify the prescription before dispensing', 'Medium', 'Pharmacy', { page: 'prescriptions', id: rx.id }); emit(); return; } origDispense(txId, role); if (rx && t.status === 'Dispensed') { rx.status = 'Dispensed'; rx.batch = t.batch || rx.batch; } };
  const origGive = api.giveMedication.bind(api); api.giveMedication = (marId, role) => { const m = S.mar.find(x => x.id === marId); if (m && m.status === 'Awaiting pharmacy') { notify('Not dispensed', m.drug + ' · pharmacy has not dispensed this prescription · administration blocked', 'High', 'MAR', { page: 'prescriptions', id: m.prescriptionId }); audit(role, 'Administration BLOCKED · not dispensed', m.patientId, m.status, m.status, m.drug, '—', 'MAR', marId); emit(); return; } origGive(marId, role); };

  // ---------- F-003 single lab lifecycle ----------
  api.advanceLab = (labId, role) => { const l = S.labs.find(x => x.id === labId); if (!l) return; const s = S.samples.find(x => x.id === l.sampleId) || S.samples.find(x => x.labId === labId); if (!s) { notify('No sample', l.id + ' has no specimen record', 'Medium', 'LIS', { page: 'lab-queue' }); emit(); return; } api.advanceSample(s.id, role); };

  // ---------- F-011 bed board actions ----------
  api.transferBed = (patientId, toBedId, role) => { const p = api.patient(patientId); if (!p) return { error: 'Patient not found' }; const from = S.beds.find(b => b.patientId === patientId); const to = S.beds.find(b => b.id.toUpperCase() === String(toBedId || '').trim().toUpperCase()); if (!to) return { error: 'Bed "' + toBedId + '" not found. Use an ID like C-05 or S-03.' }; if (to.status !== 'Free') return { error: to.id + ' is ' + to.status }; if (from) { from.status = 'Cleaning'; from.patientId = null; } to.status = 'Occupied'; to.patientId = patientId; p.bed = to.id + ' · ' + to.ward; p.bedId = to.id; const ad = S.admissions.find(a => a.patientId === patientId && a.status === 'Admitted'); if (ad) { ad.bed = p.bed; (ad.transfers = ad.transfers || []).push({ ts: S.clock, from: from ? from.id : '—', to: to.id, by: who(role) }); } const outlier = to.dept !== p.dept; S.mar.filter(m => m.patientId === patientId).forEach(m => m.bed = to.id); notify('Bed transfer' + (outlier ? ' · outlier ward' : ''), p.name + ' · ' + (from ? from.id : '—') + ' → ' + to.id + ' (' + to.cls + ')', outlier ? 'Medium' : 'Low', 'Facilities', { page: 'bedboard', id: to.id }); audit(role, 'Bed transfer ' + (from ? from.id : '—') + ' → ' + to.id + (outlier ? ' · OUTLIER' : ''), patientId, from ? from.id : '—', to.id, to.cls, '—', 'HMS', ad ? ad.id : '—'); emit(); return { ok: true }; };
  api.setBedStatus = (bedId, status, role) => { const b = S.beds.find(x => x.id === bedId); if (!b) return; const before = b.status; if (status === 'Free' && b.patientId) return; b.status = status; if (status === 'Free') b.patientId = null; audit(role, 'Bed ' + before + ' → ' + status, bedId, before, status, '—', '—', 'Facilities'); emit(); };

  // ---------- F-012 triage · F-013 outcomes · death register ----------
  S.emergency.filter(e => e.status !== 'Closed').slice(0, 2).forEach(e => { e.acuity = 'Not triaged'; e.status = 'Awaiting triage'; e.esi = null; });
  S.emergency.forEach(e => { const m = String(e.acuity || '').match(/ESI-(\d)/); e.esi = m ? +m[1] : null; e.mlc = !!e.mlc; });
  api.triage = (erId, text, role) => { const e = S.emergency.find(x => x.id === erId); if (!e) return { error: 'ER case not found' }; const m = String(text || '').match(/ESI[- ]?([1-5])/i); if (!m) return { error: 'Start with the ESI level, e.g. "ESI-2 chest pain, MLC"' }; const before = e.acuity; e.esi = +m[1]; e.acuity = 'ESI-' + m[1] + ' (' + who(role) + ')'; e.mlc = /mlc|medico|assault|accident|poison/i.test(text); e.triageNote = text.trim(); e.triagedAt = S.clock; if (e.status === 'Awaiting triage') e.status = e.esi <= 2 ? 'Resuscitation / urgent' : 'Waiting for doctor'; if (e.mlc) notify('MLC registered', e.patient + ' · medico-legal case · police intimation and MLC register entry required', 'High', 'Emergency', { page: 'emergency', id: erId }); if (e.esi === 1) notify('ESI-1 · resuscitation', e.patient + ' · team alerted', 'Critical', 'Emergency', { page: 'emergency', id: erId }); audit(role, 'Triage recorded · ' + e.acuity + (e.mlc ? ' · MLC' : ''), e.patientId, before, e.acuity, text, '—', 'Emergency', erId); emit(); return { ok: true }; };
  S.deaths = S.deaths || [];
  api.recordOutcome = (patientId, text, role) => { const p = api.patient(patientId); if (!p) return { error: 'Patient not found' }; const kind = /death|expired|died|deceased/i.test(text) ? 'Death' : /lama|dama|against/i.test(text) ? 'LAMA' : /transfer|referr/i.test(text) ? 'Transfer out' : /abscond/i.test(text) ? 'Absconded' : null; if (!kind) return { error: 'Start with Death, LAMA, Transfer or Absconded, then the reason' }; const ad = S.admissions.find(a => a.patientId === patientId && a.status === 'Admitted'); const bed = S.beds.find(b => b.patientId === patientId); if (ad) { ad.status = kind === 'Death' ? 'Expired' : kind; ad.outcome = kind; ad.outcomeAt = S.clock; ad.outcomeNote = text; } if (bed) { bed.status = 'Cleaning'; bed.patientId = null; } p.status = kind === 'Death' ? 'Expired' : kind; p.type = 'Discharged'; p.bed = null; const d = S.discharges.find(x => x.patientId === patientId && !x.completed); if (d) { d.completed = true; d.dischargedAt = S.clock; d.outcome = kind; } const enc = api.currentEncounter(patientId); if (enc) { enc.status = 'Closed'; enc.outcome = kind; enc.notes.push({ ts: S.clock, by: who(role), kind: 'Outcome', text, status: 'Signed' }); } S.emergency.filter(e => e.patientId === patientId && !/Closed/.test(e.status)).forEach(e => e.status = 'Closed · ' + kind); S.mar.filter(m => m.patientId === patientId && m.status !== 'Given').forEach(m => m.status = 'Cancelled'); S.prescriptions.filter(r => r.patientId === patientId && /Prescribed|Verified/.test(r.status)).forEach(r => r.status = 'Cancelled'); const bill = S.bills.find(x => x.patientId === patientId && !/Paid|Settled|Voided|Closed/.test(x.status));
    if (kind === 'Death') { const rec = { id: nid('DTH-2026'), patientId, patient: p.name, age: p.age, sex: p.sex, dept: p.dept, ts: S.clock, certifiedBy: S.session.role === 'Doctor' ? S.session.name : p.doctor, cause: text.replace(/^\S+\s*/, '') || 'Not stated', mlc: /mlc|medico|accident|assault|poison|suicide/i.test(text), certificate: 'Pending · Form 4 (MCCD)', mortuary: 'Body in mortuary · release after certificate', billStatus: bill ? 'Hold · compassionate review' : '—', bed: bed ? bed.id : '—', history: [{ t: fmt(S.clock), e: 'Death recorded by ' + who(role) }] }; S.deaths.unshift(rec); if (bill) { bill.status = 'Hold'; bill.hold = 'Death · compassionate review'; } notify('Patient expired', p.name + ' · death certificate, mortuary and bill-hold workflow started' + (rec.mlc ? ' · MLC' : ''), 'High', 'Clinical', { page: 'deaths', id: rec.id }); }
    else notify(kind, p.name + ' · ' + text + ' · bed released to housekeeping', 'Medium', 'Admissions', { page: 'admissions' });
    audit(role, 'Encounter outcome · ' + kind, patientId, 'Admitted', kind, text, '—', 'EMR', ad ? ad.id : '—'); emit(); return { ok: true, kind }; };
  api.deathStep = (id, step, role) => { const r = S.deaths.find(x => x.id === id); if (!r) return; if (step === 'certificate') { r.certificate = 'Issued · Form 4 · ' + fmt(S.clock) + ' · ' + who(role); r.certifiedBy = who(role); } if (step === 'release') { if (!/Issued/.test(r.certificate)) { notify('Release blocked', 'Death certificate must be issued before the body is released', 'Medium', 'Clinical', { page: 'deaths', id }); emit(); return; } if (r.mlc && !r.policeCleared) { notify('Release blocked · MLC', 'Police clearance / post-mortem decision required before release', 'High', 'Clinical', { page: 'deaths', id }); emit(); return; } r.mortuary = 'Released to family · ' + fmt(S.clock); } if (step === 'police') { r.policeCleared = true; r.mlcNote = 'Police intimation acknowledged · ' + fmt(S.clock); } if (step === 'bill') { const b = S.bills.find(x => x.patientId === r.patientId && x.status === 'Hold'); if (b) { b.status = 'Closed · compassionate'; b.hold = null; } r.billStatus = 'Closed · compassionate review complete'; } r.history.unshift({ t: fmt(S.clock), e: step + ' by ' + who(role) }); audit(role, 'Death register · ' + step, r.patientId, '—', step, r.id, '—', 'Medical records', id); emit(); };

  // ---------- F-016 claim outcomes ----------
  api.advanceClaim = (paId, role, outcome) => { const p = S.preauths.find(x => x.id === paId); if (!p) return; const b = p.claimStatus; let next; if (!b) next = 'Claim Ready'; else if (b === 'Claim Ready') next = 'Submitted'; else if (b === 'Submitted') next = 'Under Review'; else if (b === 'Under Review') next = outcome === 'query' ? 'Query Raised' : outcome === 'reject' ? 'Rejected' : outcome === 'partial' ? 'Partially Approved' : 'Approved'; else if (b === 'Query Raised') next = 'Submitted'; else if (b === 'Rejected') next = outcome === 'appeal' ? 'Submitted' : null; else if (/Approved/.test(b)) next = 'Settled'; if (!next) return; p.claimStatus = next; if (!p.claimNo) p.claimNo = nid('CLM-2026'); if (next === 'Rejected') p.rejectionReason = p.rejectionReason || pick(['Pre-existing disease clause', 'Room rent cap exceeded', 'Non-payable consumables', 'Documentation incomplete']); if (next === 'Partially Approved') { p.deduction = p.deduction || inr(int(5000, 40000)); } if (next === 'Settled') { p.paidAmt = p.approved; const bill = S.bills.find(x => x.patientId === p.patientId && x.admissionId !== '—'); if (bill) bill.status = 'Settled'; } if (next === 'Query Raised') notify('Claim query', p.patient + ' · ' + p.insurer + ' · respond within 7 days', 'Medium', 'Claims', { page: 'claims', id: p.id }); p.history.unshift({ t: fmt(S.clock), e: 'Claim ' + (b || 'created') + ' → ' + next + (outcome ? ' (' + outcome + ')' : '') }); audit(role, 'Claim ' + (b || 'created') + ' → ' + next, p.patientId, b || '—', next, p.claimNo + (p.rejectionReason && next === 'Rejected' ? ' · ' + p.rejectionReason : ''), '—', 'TPA portal (simulated)'); emit(); };

  // ---------- F-025 credential verification ----------
  api.renewCredential = (id, role) => { const c = S.credentials.find(x => x.id === id); if (!c) return; const b = c.status; c.status = 'Renewal submitted'; c.submittedAt = S.clock; notify('Credential renewal submitted', c.holder + ' · ' + c.type + ' · HR must verify the council document', 'Medium', 'HR', { page: 'credentials', id }); audit(role, 'Credential renewal submitted', id, b, 'Renewal submitted', c.holder, 'Pending HR verification', 'HR'); emit(); };
  api.verifyCredential = (id, role) => { const c = S.credentials.find(x => x.id === id); if (!c || c.status !== 'Renewal submitted') return; c.status = 'Valid'; c.expiry = '30 Sep 2027'; c.verifiedBy = who(role); c.verifiedAt = S.clock; notify('Credential verified', c.holder + ' · valid until ' + c.expiry + ' · rostering unblocked', 'Low', 'HR', { page: 'credentials', id }); audit(role, 'Credential verified · document checked', id, 'Renewal submitted', 'Valid', c.expiry, '—', 'HR'); emit(); };

  // ---------- F-026 payment gateway states + duplicate guard ----------
  api.recordPayment = (billId, amount, mode, role) => { const b = S.bills.find(x => x.id === billId); if (!b) return; amount = +amount; if (!(amount > 0)) return { error: 'Amount must be positive' }; if (b.status === 'Hold') { notify('Bill on hold', b.id + ' · ' + b.hold, 'Medium', 'Billing', { page: 'billing', id: billId }); emit(); return { error: 'Hold' }; } const dup = S.payments.find(x => x.billId === billId && x.amount === amount && x.mode === mode && /Success|Initiated/.test(x.status) && S.clock - x.ts < 10); if (dup) { notify('Duplicate payment blocked', b.id + ' · ' + inr(amount) + ' via ' + mode + ' already ' + dup.status.toLowerCase() + ' (' + dup.ref + ')', 'High', 'Billing', { page: 'billing', id: billId }); audit(role, 'Duplicate payment BLOCKED', b.patientId, inr(b.paid || 0), inr(b.paid || 0), dup.id, '—', 'Payment gateway', billId); emit(); return { error: 'Duplicate' }; } const gateway = /UPI|Card|Net/.test(mode); const pay = { id: nid('PAY'), billId, patient: b.patient, amount, mode, ts: S.clock, status: gateway ? 'Initiated' : 'Success', gateway: gateway ? 'Razorpay (simulated)' : 'Counter', ref: gateway ? 'txn_' + int(100000, 999999) : 'RCPT-' + int(10000, 99999), by: who(role) }; S.payments.unshift(pay); const settle = () => { b.paid = (b.paid || 0) + amount; b.status = b.paid >= b.patientShare ? (b.admissionId === '—' ? 'Paid' : 'Settled') : 'Part-paid'; S.comms.push({ id: nid('MSG'), patientId: b.patientId, patient: b.patient, ts: S.clock, channel: 'SMS', type: 'Receipt ' + inr(amount) + ' · ' + mode + ' · ' + pay.ref, lang: 'EN', status: 'Delivered', agent: '—' }); audit(role, 'Payment recorded · ' + mode, b.patientId, inr(b.paid - amount), inr(b.paid), pay.ref, '—', 'Payment gateway', pay.id); };
    if (!gateway) { settle(); emit(); return pay; } audit(role, 'Payment initiated · ' + mode, b.patientId, '—', 'Initiated', pay.ref, '—', 'Payment gateway', pay.id); emit();
    setTimeout(() => { if (rnd() < 0.15) { pay.status = 'Failed'; pay.failure = pick(['Bank timeout', 'Insufficient funds', 'UPI PIN cancelled by payer']); notify('Payment failed', b.patient + ' · ' + inr(amount) + ' · ' + pay.failure + ' · retry or change mode', 'High', 'Billing', { page: 'billing', id: billId }); audit(role, 'Payment FAILED · ' + pay.failure, b.patientId, 'Initiated', 'Failed', pay.ref, '—', 'Payment gateway', pay.id); } else { pay.status = 'Success'; settle(); } emit(); }, 1200); return pay; };

  // ---------- F-020 derived KPIs + RAG-backed ask() ----------
  const origKpis = api.kpis.bind(api); api.kpis = () => { const k = origKpis(); const waiting = S.appointments.filter(a => a.status === 'Checked-in' && a.ts <= S.clock); k.opdWaitingN = waiting.length; k.opdWaiting = waiting.length ? Math.round(waiting.reduce((s, a) => s + (S.clock - a.ts), 0) / waiting.length) : 0; k.triagePending = S.emergency.filter(e => e.status === 'Awaiting triage').length; k.rxPending = S.prescriptions.filter(r => r.status === 'Prescribed').length; k.deaths = S.deaths.length; k.bedsCleaning = S.beds.filter(b => b.status === 'Cleaning').length; return k; };
  api.copilot = () => { const k = api.kpis(); const E = api.erpKpis ? api.erpKpis() : {}; const blocked = S.discharges.filter(d => !d.completed && api.dischargeState(d).k === 'blocked'); const byDep = {}; blocked.forEach(d => { const st = api.dischargeState(d); byDep[st.dep || 'other'] = (byDep[st.dep || 'other'] || 0) + 1; }); const top = Object.entries(byDep).sort((a, b) => b[1] - a[1])[0]; const crit = S.labs.filter(l => l.critical && !l.acked).length; return { text: 'Occupancy ' + k.occPct + '% with ' + k.bedsFree + ' beds free and ' + k.bedsCleaning + ' in cleaning. ' + blocked.length + ' of ' + k.dischargeCandidates + ' discharge candidates are blocked' + (top ? ', mostly by ' + top[0] + ' (' + top[1] + ')' : '') + '. ' + (crit ? crit + ' critical result' + (crit > 1 ? 's' : '') + ' unacknowledged. ' : 'No unacknowledged critical results. ') + (k.triagePending ? k.triagePending + ' ER arrival' + (k.triagePending > 1 ? 's' : '') + ' awaiting triage. ' : '') + (k.rxPending ? k.rxPending + ' prescriptions await pharmacist verification. ' : '') + (E.machinesOffline ? E.machinesOffline + ' analyzer' + (E.machinesOffline > 1 ? 's' : '') + ' not online. ' : '') + 'OPD: ' + k.opdWaitingN + ' checked-in patients waiting, average ' + k.opdWaiting + ' min.', sources: 'Derived live from beds · discharges · labs · emergency · prescriptions · appointments · LIS (no template text)' }; };
  api.ask = (q, role) => { let res; try { res = ragAnswer(q, { role, api, history: [] }); } catch (e) { res = { text: 'The assistant could not answer (' + e.message + ').', sources: [], actions: [], status: 'error', intent: 'error', confidence: 0 }; } const links = (res.actions || []).filter(a => a.page).map(a => ({ page: a.page, id: a.id, label: a.label })); S.aiLog = S.aiLog || []; S.aiLog.unshift({ ts: S.clock, role, q, intent: res.intent, status: res.status, voice: false, sources: (res.sources || []).map(x => x.doc), latency: '0.4 s', feedback: null }); audit(role + ' · Assistant', 'Ask Hospital AI · ' + res.intent + ' · ' + res.status, '—', '—', res.status, q, '—', 'Command bar'); return { q, answer: res.text, links, sources: (res.sources || []).map(s => s.doc + (s.sec ? ' ' + s.sec : '')).concat(res.status === 'answered' && !(res.sources || []).length ? ['Structured data (permission-checked)'] : []), ts: fmt(S.clock), confidence: res.confidence ? Math.round(res.confidence * 100) : 0, status: res.status, heuristic: 'Confidence is a heuristic (retrieval score), not a calibrated model probability' }; };
  // F-021: radiology AI score is a placeholder
  S.radiology.forEach(r => { r.model = 'placeholder · no model (F-021)'; });

  // ---------- F-039 persistence ----------
  api.saveSnapshot = () => { try { localStorage.setItem(SNAPSHOT_KEY, JSON.stringify(S)); return true; } catch (e) { return false; } };
  api.restoreSnapshot = () => { try { const raw = localStorage.getItem(SNAPSHOT_KEY); if (!raw) return false; const o = JSON.parse(raw); if (!o || o._v !== VERSION) { localStorage.removeItem(SNAPSHOT_KEY); return false; } Object.keys(o).forEach(k => { S[k] = o[k]; }); return true; } catch (e) { return false; } };
  api.clearSnapshot = () => { try { localStorage.removeItem(SNAPSHOT_KEY); } catch (e) {} };
  api.reset = () => { api.clearSnapshot(); try { localStorage.removeItem('meridian-proto-v2'); } catch (e) {} window.location.reload(); };
  const origApprove = api.approve.bind(api); api.approve = (id, role, note) => { const a = S.approvals.find(x => x.id === id); if (a && a.status === 'Pending' && a.type === 'Vendor payment') { const p = S.purchases.find(x => x.id === a.entityId); if (p) { p.status = 'Closed'; p.paid = true; p.paidAt = S.clock; p.history.unshift({ t: fmt(S.clock), e: 'Payment approved by ' + who(role) + ' · ' + inr(p.total) + ' released to ' + p.vendor }); const v = S.vendors.find(x => x.id === p.vendorId); if (v) v.spendMtd = (v.spendMtd || 0) + p.total; } } return origApprove(id, role, note); };
  const origCreate = api.createMaster.bind(api); api.createMaster = (coll, data, role) => { const r = origCreate(coll, data, role); if (r && !r.error && coll === 'staff') { const rl = ROLE_OF(data.role || '') || 'Front Office'; S.users.unshift({ id: 'USR-' + pad(S.users.length + 1), empId: (S.employees.find(e => e.name === data.name) || {}).empId || '—', name: data.name, username: uname(data.name), role: rl, dept: data.unit, doctorId: null, branchId: 'BR-01', mfa: false, status: 'Active', lastLogin: '—' }); } if (r && !r.error && coll === 'patients') { const p = S.patients.find(x => x.id === r.id); if (p) { p.allergies = []; p.flags = []; p.branchId = 'BR-01'; } } return r; };
  const origAdvSample = api.advanceSample.bind(api); api.advanceSample = (sid, role) => { origAdvSample(sid, role); const s = S.samples.find(x => x.id === sid); const l = s && S.labs.find(x => x.id === s.labId); if (s && l && l.value == null && s.status === 'Machine Processing') setTimeout(() => { if (l.value != null) return; const lt = S.labTests.find(t => t.panel === l.test); const m = lt && String(lt.ref).match(/([\d.]+)\s*[–-]\s*([\d.]+)/); const lo = m ? +m[1] : 1, hi = m ? +m[2] : 10; const abnormal = rnd() < 0.2; l.value = +(abnormal ? hi * 1.25 : lo + rnd() * (hi - lo)).toFixed(2); l.flag = abnormal ? 'High' : 'Normal'; const ch = lt && lt.critHigh && lt.critHigh !== '—' ? parseFloat(String(lt.critHigh).replace(/[^\d.]/g, '')) : null; if (ch && l.value > ch) { l.flag = 'Critical'; l.critical = true; l.acked = false; l.notified = S.clock; l.clinician = (api.patient(l.patientId) || {}).doctor; notify('Critical value', l.patient + ' · ' + l.analyte + ' ' + l.value + ' ' + l.unit + ' · acknowledge within 15 min', 'Critical', 'LIS', { page: 'laboratory', id: l.id }); } emit(); }, 1700); };
  api.restored = api.restoreSnapshot();
  return api;
}

return { VERSION, SNAPSHOT_KEY, installErp3 };
})();
window.__mm2.views4 = (function(){
// V2.0 pages and details: clinical workspace, bed board, death/MLC register, prescriptions, drug master, users (session); patches for emergency, radiology, preauth/claims, purchase, credential, admission.
const { pill } = window.__mm2.views;
const G = 'oklch(0.4 0.12 150)', A = 'oklch(0.5 0.13 70)', RD = 'oklch(0.45 0.17 25)', V = 'oklch(0.45 0.1 300)';
const c = (t, o = {}) => ({ t: t == null ? '—' : String(t), c: o.c || '', bg: o.bg || '', m: !!o.m, b: !!o.b, pill: !!o.p });
const P = s => ({ ...c(s), ...pill(s), pill: true });
const kv = (k, v, col) => ({ k, v: v == null ? '—' : String(v), c: col || '' });
const PAGES4 = ['bedboard', 'deaths', 'prescriptions', 'drugs'];
const ROLE4 = { bedboard: ['Nurse', 'Front Office', 'Doctor', 'Hospital Management', 'Auditor', 'IT Administrator'], deaths: ['Doctor', 'Nurse', 'Front Office', 'Billing', 'Hospital Management', 'Auditor', 'Governance Officer'], prescriptions: ['Doctor', 'Nurse', 'Pharmacy', 'Hospital Management', 'Auditor'], drugs: ['Pharmacy', 'IT Administrator', 'Hospital Management', 'Doctor', 'Auditor', 'Store Manager'] };
const allowed4 = (role, page) => PAGES4.includes(page) ? ROLE4[page].includes(role) : undefined;
// Sidebar regrouped by hospital domain (review §9); labels come from the existing NAV tables, a few renamed here.
function buildNav(navs) {
  const label = {}; navs.forEach(g => g.items.forEach(([k, l]) => label[k] = l));
  Object.assign(label, { clinical: 'Clinical Workspace', laboratory: 'Results & Critical Values', bedboard: 'Bed Board', deaths: 'Death & MLC Register', prescriptions: 'Prescriptions', drugs: 'Drug Master', approvals: 'Approval Centre', facilities: 'Facilities · Housekeeping' });
  const G4 = [['Front office & patients', ['command', 'patients', 'appointments', 'admissions', 'bedboard', 'emergency', 'contact', 'feedback']], ['Clinical', ['clinical', 'nursing', 'mar', 'ot', 'bloodbank', 'discharge', 'deaths']], ['Diagnostics · LIS & imaging', ['lab-dashboard', 'lab-queue', 'laboratory', 'diagnostics', 'lab-tests', 'lab-analyzers', 'lab-reagents', 'radiology']], ['Pharmacy & supply chain', ['prescriptions', 'drugs', 'pharmacy', 'inventory', 'stores', 'procurement', 'vendors', 'cssd']], ['Revenue cycle', ['billing', 'insurance', 'claims', 'finance', 'tax']], ['People', ['hr-dashboard', 'employees', 'attendance', 'credentials', 'staff', 'hr', 'canteen']], ['Administration', ['users', 'roles', 'permissions', 'identity', 'departments', 'services', 'insurers', 'payment-methods', 'facilities', 'integrations', 'integration-arch', 'notifications', 'config', 'reports']]];
  const used = new Set(G4.flatMap(g => g[1]));
  const out = G4.map(([g, keys]) => ({ g, items: keys.filter(k => label[k]).map(k => [k, label[k]]) }));
  navs.forEach(g => { const rest = g.items.filter(([k]) => !used.has(k)); if (rest.length) out.push({ g: g.g, items: rest.map(([k, l]) => [k, label[k] || l]) }); });
  return out;
}
function buildList4(page, api, ctx, base) {
  const S = api.S, now = api.clock(), { role } = ctx; const fmt = api.fmt, ago = t => api.ago(t, now); const ses = S.session || { role };
  const open = (kind, id) => () => ctx.open(kind, id); const go = (p, i) => () => ctx.go(p, i);
  let L = { title: '', sub: '', cols: [], rows: [], stats: [], banner: null, filters: [] };
  const withF = (vals, key) => { L.filters = ['All'].concat(vals).map(v => ({ label: v, active: (ctx.filter || 'All') === v, on: () => ctx.setFilter(v) })); return r => (ctx.filter || 'All') === 'All' || String(typeof key === 'function' ? key(r) : r[key]).startsWith((ctx.filter || 'All')); };
  const st = arr => arr.map(([k, v, col, on]) => ({ k, v, c: col || '', on: on || (() => {}) }));
  switch (page) {
    case 'clinical': { const mine = api.myPatients(); const scope = ses.role === 'Doctor' && ses.doctorId ? 'patients under ' + ses.name : ses.role === 'Nurse' ? (ses.dept && ses.dept !== '—' ? ses.dept + ' inpatients' : 'all inpatients') : 'inpatient census'; L.title = 'Clinical workspace · ' + scope; L.sub = 'Scope comes from the signed-in user (' + ses.name + ' · ' + ses.role + '), not the role label. Open a row for diagnoses, allergies, notes, orders and prescriptions. AI drafts stay drafts until a clinician signs.'; const pend = S.approvals.filter(a => a.status === 'Pending' && a.owner === (role === 'Nurse' ? 'Nurse' : 'Doctor')); L.stats = st([['My patients', mine.length], ['Pending signatures', pend.length, A, go('approvals')], ['Critical alerts', S.labs.filter(l => l.critical && !l.acked).length, RD, go('laboratory')], ['Rx awaiting pharmacy', S.prescriptions.filter(r => r.status === 'Prescribed' && mine.some(p => p.id === r.patientId)).length, A, go('prescriptions')], ['Discharge candidates', S.discharges.filter(d => !d.completed && mine.some(p => p.id === d.patientId)).length, '', go('discharge')]]); L.cols = [['Bed', 90], ['Patient', 170], ['Age', 45], ['Primary diagnosis', 220], ['Allergies', 130], ['Orders open', 80], ['Rx pending', 80], ['EWS', 60], ['Discharge', 160]];
      L.rows = mine.map(p => { const e = api.currentEncounter(p.id); const dx = e && e.diagnoses[0]; const d = S.discharges.find(x => x.patientId === p.id && !x.completed); const v = S.vitals.filter(x => x.patientId === p.id).sort((a, b) => b.ts - a.ts)[0]; const oo = S.orders.filter(o => o.patientId === p.id && o.status !== 'Resulted' && o.status !== 'Cancelled').length; const rx = S.prescriptions.filter(r => r.patientId === p.id && r.status === 'Prescribed').length; return { id: p.id, cells: [c((p.bed || 'OP').split(' ')[0], { m: 1 }), c(p.name, { b: 1 }), c(p.age), c(dx ? dx.code + ' · ' + dx.desc : '—'), p.allergies.length ? P('⚠ ' + p.allergies.join(', ')) : c('None known'), c(oo || '—'), rx ? P(rx + ' to verify') : c('—'), v ? P(v.ews >= 3 ? 'Escalate ' + v.ews : v.ews === 2 ? 'Watch ' + v.ews : 'Normal ' + v.ews) : c('—'), d ? P(api.dischargeState(d).label) : c('—')], go: e ? open('encounter', e.id) : go('patient', p.id) }; }); if (!mine.length) L.banner = { kind: 'info', text: 'No patients are assigned to ' + ses.name + '. Sign in as a consultant with inpatients (top bar → user) or switch role.' }; break; }
    case 'users': { const f = withF(['Active', 'Disabled'], 'status'); L.title = 'Users'; L.sub = S.users.length + ' user accounts · each account links to an employee record (doctors included) · the signed-in user sets role, department and consultant scope · MFA flag per account'; L.stats = st([['Accounts', S.users.length], ['Active', S.users.filter(u => u.status === 'Active').length, G], ['Without MFA', S.users.filter(u => !u.mfa).length, A], ['Signed in now', ses.name]]); L.cols = [['User', 80], ['Name', 170], ['Username', 150], ['Employee', 90], ['Role', 150], ['Department', 150], ['MFA', 50], ['Last login', 110], ['Status', 80]]; L.rows = S.users.filter(f).map(u => ({ id: u.id, cells: [c(u.id, { m: 1 }), c(u.name, { b: 1 }), c(u.username, { m: 1 }), c(u.empId, { m: 1 }), c(u.role), c(u.dept), u.mfa ? P('On') : P('Off'), c(u.lastLogin, { m: 1 }), P(u.status)], go: open('user', u.id) })); break; }
    case 'emergency': { L = base(); if (!L) return null; const rank = id => { const e = S.emergency.find(x => x.id === id); if (!e) return 99; if (e.status === 'Awaiting triage') return -1; if (/Closed/.test(e.status)) return 50; return e.esi || 9; }; L.rows.sort((a, b) => rank(a.id) - rank(b.id)); const nt = S.emergency.filter(e => e.status === 'Awaiting triage').length; const mlc = S.emergency.filter(e => e.mlc && !/Closed/.test(e.status)).length; L.sub = 'Board is ordered by triage acuity (ESI 1 → 5); untriaged arrivals sit on top. Triage is a human action on the case (Doctor / Nurse). MLC flag drives police intimation.'; L.banner = nt ? { kind: 'error', text: nt + ' arrival' + (nt > 1 ? 's' : '') + ' awaiting triage' + (mlc ? ' · ' + mlc + ' active MLC' : '') + '. Open the case → Triage.' } : L.banner; break; }
    case 'radiology': { L = base(); if (!L) return null; L.sub = 'Worklist · accession, DICOM and radiologist read. The "AI probability" column is a PLACEHOLDER with no model behind it (review F-021) and must not be used for prioritisation decisions.'; L.banner = { kind: 'warn', text: 'AI prioritisation score = placeholder (random, no trained model). Radiologist read is the only clinical output. Kept visible so the gap is honest.' }; break; }
    case 'bedboard': { const f = withF(['Free', 'Occupied', 'Reserved', 'Discharge Pending', 'Cleaning', 'Blocked'], 'status'); L.title = 'Bed board · ' + S.wards.length + ' wards · ' + S.rooms.length + ' rooms · ' + S.beds.length + ' beds'; L.sub = 'Branch → ward → room → bed. Kanban groups by ward. Open a bed to transfer its patient, release it after cleaning or block it for maintenance. Midnight rule posts the room charge to every occupied bed.'; const n = s => S.beds.filter(b => b.status === s).length; L.stats = st([['Free', n('Free'), G], ['Occupied', n('Occupied')], ['Reserved', n('Reserved'), A], ['Discharge pending', n('Discharge Pending'), A], ['Cleaning', n('Cleaning'), RD], ['Blocked', n('Blocked'), RD]]); L.cols = [['Bed', 80], ['Ward', 150], ['Room', 110], ['Class', 80], ['Status', 120], ['Patient', 170], ['Department', 130], ['Rate / night', 90]]; L.rows = S.beds.filter(f).sort((a, b) => a.wardId.localeCompare(b.wardId) || a.id.localeCompare(b.id)).map(b => { const p = b.patientId ? api.patient(b.patientId) : null; const r = S.rooms.find(x => x.id === b.roomId); return { id: b.id, cells: [c(b.id, { m: 1, b: 1 }), c(b.ward), c(b.roomId, { m: 1 }), c(b.cls), P(b.status), c(p ? p.name : '—'), c(b.dept), c(r && r.rate ? api.inr(r.rate) : '—', { m: 1 })], go: open('bed', b.id) }; }); break; }
    case 'deaths': { L.title = 'Death & MLC register'; L.sub = 'Statutory registers. Death → certificate (Form 4 / MCCD) by the certifying doctor → body release by Front Office (police clearance first for MLC) → bill closed under compassionate review by Billing. Nothing here can be deleted.'; const D = S.deaths || []; L.stats = st([['Deaths recorded', D.length], ['Certificate pending', D.filter(x => /Pending/.test(x.certificate)).length, A], ['MLC', D.filter(x => x.mlc).length, RD], ['Body in mortuary', D.filter(x => /mortuary/.test(x.mortuary)).length, A]]); L.cols = [['Register', 110], ['Patient', 160], ['Dept', 120], ['Time', 100], ['Cause (as recorded)', 220], ['MLC', 60], ['Certificate', 150], ['Body', 150], ['Bill', 150]]; L.rows = D.map(x => ({ id: x.id, cells: [c(x.id, { m: 1 }), c(x.patient, { b: 1 }), c(x.dept), c(fmt(x.ts), { m: 1 }), c(x.cause), x.mlc ? P('MLC') : c('No'), P(/Issued/.test(x.certificate) ? 'Issued' : 'Pending'), P(/Released/.test(x.mortuary) ? 'Released' : 'In mortuary'), P(/Closed/.test(x.billStatus) ? 'Closed' : x.billStatus === '—' ? '—' : 'Hold')], go: open('death', x.id) })); if (!D.length) L.banner = { kind: 'info', text: 'No deaths recorded in this session. A Doctor records an outcome from the Clinical workspace or an ER case ("Death <cause>", add MLC if medico-legal).' }; break; }
    case 'prescriptions': { const f = withF(['Prescribed', 'Verified', 'Dispensed', 'Stock-out', 'Returned', 'Cancelled'], 'status'); const R = S.prescriptions; L.title = 'Prescriptions'; L.sub = 'Prescription → pharmacist verification (allergy · interaction · controlled-drug checks) → dispense from a mapped stock item with batch and expiry → charge to bill → MAR rows for inpatients. Drug names are not strings: every line references the drug master and its inventory item.'; L.stats = st([['Awaiting verification', R.filter(r => r.status === 'Prescribed').length, A, () => ctx.setFilter('Prescribed')], ['Verified · to dispense', R.filter(r => r.status === 'Verified').length, '', () => ctx.setFilter('Verified')], ['Stock-out', R.filter(r => r.status === 'Stock-out').length, RD, () => ctx.setFilter('Stock-out')], ['Safety flags open', R.filter(r => r.checks.length && !r.override && /Prescribed|Verified/.test(r.status)).length, RD], ['Dispensed today', R.filter(r => r.status === 'Dispensed' && r.ts >= 0).length, G]]); L.cols = [['Rx', 90], ['Patient', 150], ['Drug', 170], ['Dose · route · freq', 130], ['Days · qty', 70], ['Prescriber', 140], ['Safety', 170], ['Verified by', 110], ['Status', 100]]; L.rows = R.filter(f).sort((a, b) => b.ts - a.ts).map(r => ({ id: r.id, cells: [c(r.id, { m: 1 }), c(r.patient, { b: 1 }), c(r.drug), c(r.dose + ' ' + r.route + ' ' + r.freq, { m: 1 }), c(r.days + ' d · ' + r.qty, { m: 1 }), c(r.doctor), r.checks.length ? P(r.override ? 'Overridden' : r.checks[0].split(' · ')[0]) : c('Clear', { c: G }), c(r.verifiedBy || '—'), P(r.status)], go: open('prescription', r.id) })); break; }
    case 'drugs': { const f = withF(['Tablet', 'Injection', 'Tablet / Injection'], 'form'); L.title = 'Drug master'; L.sub = S.drugs.length + ' formulary entries · generic, brand, form, strength, schedule (H / OTC / X), route, mapped inventory item, high-alert flag, interaction pairs. Prescribing and dispensing resolve against this master, never against free text.'; L.stats = st([['Formulary', S.drugs.length], ['High-alert', S.drugs.filter(d => d.highAlert).length, RD], ['Controlled (Sch X)', S.drugs.filter(d => d.controlled).length, A], ['Unmapped to stock', S.drugs.filter(d => !d.itemId).length, A]]); L.cols = [['ID', 70], ['Generic', 120], ['Brand', 100], ['Form · strength', 150], ['Schedule', 90], ['Route', 60], ['Stock item', 170], ['High alert', 80], ['Interactions', 180], ['Status', 80]]; L.rows = S.drugs.filter(f).map(d => { const it = d.itemId ? S.items.find(x => x.id === d.itemId) : null; return { id: d.id, cells: [c(d.id, { m: 1 }), c(d.generic, { b: 1 }), c(d.brand), c(d.form + ' · ' + d.strength), c(d.schedule), c(d.route, { m: 1 }), c(it ? it.name + ' · ' + it.qty + ' ' + it.unit : 'Not mapped', { c: it ? '' : RD }), d.highAlert ? P('High alert') : c('—'), c(d.interactions.join(', ') || '—'), P(d.active ? 'Active' : 'Inactive')], go: open('drug', d.id) }; }); break; }
    default: return null;
  }
  return L;
}
function buildDetail4(kind, id, api, ctx, base) {
  const S = api.S, now = api.clock(), { role } = ctx; const fmt = api.fmt, inr = api.inr; const ses = S.session || { role };
  const D = { title: '', sub: '', badges: [], facts: [], sections: [], actions: [], related: [], ai: null, image: null, steps: [] };
  const badge = s => ({ t: s, ...pill(s) }); const go = (p, i) => () => ctx.go(p, i);
  const act = (label, on, o = {}) => ({ label, on, primary: !!o.primary, disabled: !!o.disabled, hint: o.hint || '' });
  const need = (ok, who) => ok ? {} : { disabled: true, hint: 'Requires ' + who };
  const tell = (title, r) => { if (r && r.error) api.notify(title + ' · not saved', r.error, 'Medium', 'Validation', {}); };
  const doctor = role === 'Doctor', nurse = role === 'Nurse', mgmt = role === 'Hospital Management';
  switch (kind) {
    case 'encounter': { const e = S.encounters.find(x => x.id === id) || api.currentEncounter(id); if (!e) return null; const p = api.patient(e.patientId); if (!p) return null; const v = S.vitals.filter(x => x.patientId === p.id).sort((a, b) => b.ts - a.ts)[0]; const orders = e.orders.map(oid => S.orders.find(o => o.id === oid)).filter(Boolean).sort((a, b) => b.ts - a.ts); const rxs = e.prescriptions.map(rid => S.prescriptions.find(r => r.id === rid)).filter(Boolean).sort((a, b) => b.ts - a.ts); const d = S.discharges.find(x => x.patientId === p.id && !x.completed); const diet = (S.diets || []).find(x => x.patientId === p.id); const ad = S.admissions.find(a => a.patientId === p.id && a.status === 'Admitted');
      D.title = 'Clinical workspace · ' + p.name; D.sub = e.id + ' · ' + e.type + ' · ' + e.doctor + ' · ' + fmt(e.ts) + (p.bed ? ' · ' + p.bed : '') + ' · ' + p.age + ' ' + p.sex; D.badges = [badge(e.status), p.allergies.length ? badge('ALLERGY · ' + p.allergies.join(', ')) : badge('No known allergies')].concat(d ? [badge(api.dischargeState(d).label)] : []);
      D.facts = [kv('Reason / procedure', p.procedure), kv('Latest vitals', v ? 'HR ' + v.hr + ' · BP ' + v.bp + ' · SpO₂ ' + v.spo2 + '% · T ' + v.temp + ' · RR ' + v.rr + ' · EWS ' + v.ews + ' (' + fmt(v.ts) + ')' : 'Not recorded', v && v.ews >= 3 ? RD : ''), kv('Diet', diet ? diet.diet : '—'), kv('Insurance', p.insurer), kv('Blood group', p.blood), kv('Risk flags', (p.risk || []).join(', ') || 'None')];
      D.sections = [{ title: 'Diagnoses (ICD-10)', items: e.diagnoses.length ? e.diagnoses.map(x => kv(x.code, x.desc + ' · ' + x.type + ' · ' + x.by)) : [kv('—', 'No diagnosis recorded', A)] }, { title: 'Allergies', items: p.allergies.length ? p.allergies.map(a => kv('⚠', a, RD)) : [kv('✓', 'No known allergies', G)] }, { title: 'Progress & nursing notes (latest 6)', items: e.notes.slice().sort((a, b) => b.ts - a.ts).slice(0, 6).map(n => kv(fmt(n.ts) + ' · ' + n.kind, n.by + ': ' + n.text)) }, { title: 'Orders', items: orders.length ? orders.slice(0, 10).map(o => { const l = S.labs.find(x => x.orderId === o.id); return kv(o.test + (o.priority === 'STAT' ? ' · STAT' : ''), o.kind + ' · ' + o.status + (l && l.value != null ? ' · ' + l.analyte + ' ' + l.value + ' ' + l.unit + ' · ' + l.flag : ''), l && l.critical ? RD : ''); }) : [kv('—', 'No orders')] }, { title: 'Prescriptions', items: rxs.length ? rxs.slice(0, 10).map(r => kv(r.drug, r.dose + ' ' + r.route + ' ' + r.freq + ' × ' + r.days + ' d · ' + r.status + (r.checks.length ? ' · ' + r.checks.join('; ') + (r.override ? ' · overridden' : '') : ''), r.checks.length && !r.override ? RD : '')) : [kv('—', 'No prescriptions')] }];
      D.actions = [act('Add diagnosis', () => ctx.prompt('Add diagnosis · ' + p.name, 'ICD-10 code then description, e.g. "I21.4 NSTEMI". Free text is accepted and coded later.', r => api.addDiagnosis(e.id, r, role)), { primary: true, ...need(doctor, 'Doctor') }), act('Order test / imaging', () => ctx.prompt('Order · ' + p.name, 'Lab panel or imaging study, add STAT for urgent: "CBC", "Troponin-I STAT", "CT Abdomen", "ECHO". The charge posts to the running bill.', r => tell('Order', api.createOrder(p.id, r, role))), need(doctor, 'Doctor')), act('Prescribe', () => ctx.prompt('Prescribe · ' + p.name, 'Generic name, frequency (OD/BD/TDS/QID/HS), days — e.g. "Aspirin BD 5". Allergy and interaction checks run against the drug master; the pharmacist verifies before dispensing.', r => tell('Prescription', api.prescribe(p.id, r, role))), need(doctor, 'Doctor')), act('Progress note', () => ctx.prompt((nurse ? 'Nursing note' : 'Progress note') + ' · ' + p.name, 'Signed on save with your name and time.', r => api.addNote(e.id, r, role)), need(doctor || nurse, 'Doctor / Nurse')), act('Record allergy', () => ctx.prompt('Record allergy · ' + p.name, 'Substance and reaction, e.g. "Penicillin — anaphylaxis". Active prescriptions are re-checked.', r => api.addAllergy(p.id, r, role)), need(doctor || nurse, 'Doctor / Nurse')), act('Record vitals', () => api.recordVitals(p.id, role), need(doctor || nurse, 'Doctor / Nurse'))];
      if (p.type === 'IP' && !d) D.actions.push(act('Mark likely discharge', () => api.markLikelyDischarge(p.id, role), need(doctor, 'Doctor')));
      if (p.type === 'IP' || p.type === 'ER') D.actions.push(act('Record outcome (death / LAMA / transfer)', () => ctx.prompt('Record outcome · ' + p.name, 'Start with Death, LAMA, Transfer or Absconded, then the reason. "Death cardiac arrest MLC" opens the death + MLC register; the bed goes to housekeeping and the bill is held.', r => tell('Outcome', api.recordOutcome(p.id, r, role))), need(doctor, 'Doctor')));
      if (p.type !== 'IP' && api.can(role, 'admit')) D.actions.push(act('Admit', () => ctx.admit(p.id)));
      D.related = [{ label: 'Patient 360', on: go('patient', p.id) }, { label: 'Nursing record', on: () => ctx.open('nursingPatient', p.id) }].concat(ad ? [{ label: 'Admission ' + ad.id, on: () => ctx.open('admission', ad.id) }] : []).concat(d ? [{ label: 'Discharge case', on: go('discharge-case', d.id) }] : []); break; }
    case 'prescription': { const r = S.prescriptions.find(x => x.id === id); if (!r) return null; const d = S.drugs.find(x => x.id === r.drugId); const it = d && d.itemId ? S.items.find(x => x.id === d.itemId) : null; const p = api.patient(r.patientId); const mar = S.mar.filter(m => m.prescriptionId === r.id); D.title = r.drug + ' · ' + r.patient; D.sub = r.id + ' · ' + r.dose + ' ' + r.route + ' ' + r.freq + ' × ' + r.days + ' d · qty ' + r.qty + ' · ' + r.doctor + ' · ' + fmt(r.ts); D.badges = [badge(r.status)].concat(d && d.highAlert ? [badge('High alert')] : []).concat(r.controlled ? [badge('Schedule X')] : []).concat(r.checks.length ? [badge(r.override ? 'Check overridden' : 'Safety flag')] : []); D.facts = [kv('Encounter', r.encounterId), kv('Drug master', d ? d.id + ' · ' + d.generic + ' (' + d.brand + ') · ' + d.form : '—'), kv('Stock item', it ? it.id + ' · ' + it.qty + ' ' + it.unit + ' · batch ' + it.batch + ' · exp ' + it.expiry + ' · ' + it.expiryState : 'Not mapped', it && (it.expiryState === 'Expired' || it.qty < r.qty) ? RD : ''), kv('Patient allergies', p && p.allergies.length ? p.allergies.join(', ') : 'None known', p && p.allergies.length ? RD : ''), kv('Safety checks', r.checks.length ? r.checks.join(' · ') : 'Clear', r.checks.length && !r.override ? RD : ''), kv('Override', r.override ? r.override.by + ' · ' + r.override.reason + ' · ' + fmt(r.override.ts) : '—'), kv('Verified by', r.verifiedBy ? r.verifiedBy + (r.verifiedAt ? ' · ' + fmt(r.verifiedAt) : '') : 'Pending'), kv('Dispensed', r.status === 'Dispensed' ? (r.dispensedBy || '—') + ' · batch ' + (r.batch || '—') + ' · ' + (r.txId || '') : '—'), kv('MAR', r.ipd ? mar.length + ' administrations · ' + mar.filter(m => m.status === 'Given').length + ' given · ' + mar.filter(m => m.status === 'Awaiting pharmacy').length + ' awaiting pharmacy' : 'Outpatient · no MAR')]; const pharm = role === 'Pharmacy' || mgmt;
      if (r.status === 'Prescribed') { D.actions.push(act('Verify prescription', () => api.verifyPrescription(r.id, role), { primary: true, ...need(pharm, 'Pharmacy') })); if (r.checks.some(x => /ALLERGY/.test(x)) && !r.override) D.actions.push(act('Override safety check (prescriber)', () => ctx.prompt('Override · ' + r.drug, 'Clinical justification is recorded in the audit trail and shown to the pharmacist.', x => api.overridePrescriptionCheck(r.id, x || 'Prescriber judgement', role)), need(doctor, 'Doctor'))); }
      if (/Verified|Stock-out/.test(r.status)) D.actions.push(act('Dispense from stock', () => api.dispensePrescription(r.id, role), { primary: true, ...need(pharm, 'Pharmacy') }));
      if (/Prescribed|Verified/.test(r.status)) D.actions.push(act('Cancel prescription', () => ctx.prompt('Cancel · ' + r.drug, 'Reason', x => api.mutate(s => { r.status = 'Cancelled'; r.cancelReason = x; S.mar.filter(m => m.prescriptionId === r.id && m.status !== 'Given').forEach(m => m.status = 'Cancelled'); }, role, 'Prescription cancelled · ' + r.drug, r.patientId, 'Cancelled', x)), need(doctor, 'Doctor')));
      D.related = [{ label: 'Patient 360', on: go('patient', r.patientId) }, { label: 'Clinical workspace', on: () => { const e = api.currentEncounter(r.patientId); if (e) ctx.open('encounter', e.id); } }].concat(d ? [{ label: 'Drug master', on: () => ctx.open('drug', d.id) }] : []).concat(it ? [{ label: 'Stock item', on: () => ctx.open('item', it.id) }] : []); break; }
    case 'drug': { const d = S.drugs.find(x => x.id === id); if (!d) return null; const it = d.itemId ? S.items.find(x => x.id === d.itemId) : null; const rx = S.prescriptions.filter(r => r.drugId === d.id); D.title = d.name; D.sub = d.id + ' · ' + d.generic + ' · ' + d.brand + ' · ' + d.form + ' · ' + d.strength; D.badges = [badge(d.active ? 'Active' : 'Inactive'), badge('Schedule ' + d.schedule)].concat(d.highAlert ? [badge('High alert')] : []); D.facts = [kv('Route', d.route), kv('Inventory item', it ? it.id + ' · ' + it.name + ' · ' + it.qty + ' ' + it.unit + ' · ' + it.store : 'Not mapped — cannot be dispensed', it ? '' : RD), kv('Tax code (item master)', it ? it.taxCode : '—'), kv('Interactions', d.interactions.join(', ') || 'None on file'), kv('Prescriptions', rx.length + ' total · ' + rx.filter(r => r.status === 'Prescribed').length + ' awaiting verification')]; D.actions = [act(d.active ? 'Deactivate' : 'Activate', () => api.toggleActive('drugs', d.id, role), need(role === 'Pharmacy' || role === 'IT Administrator' || mgmt, 'Pharmacy / IT Administrator'))]; if (!it) D.actions.push(act('Map to stock item', () => ctx.prompt('Map ' + d.generic + ' to an inventory item', 'Enter the item ID (e.g. ITM-03) or the first word of the item name.', x => api.mutate(s => { const f = S.items.find(i => i.id.toUpperCase() === x.trim().toUpperCase() || i.name.toLowerCase().startsWith(x.trim().toLowerCase())); if (f) d.itemId = f.id; else api.notify('Item not found', x, 'Medium', 'Validation', {}); }, role, 'Drug mapped to inventory', d.id, x)), need(role === 'Pharmacy' || mgmt, 'Pharmacy'))); D.related = it ? [{ label: 'Stock item', on: () => ctx.open('item', it.id) }] : []; break; }
    case 'bed': { const b = S.beds.find(x => x.id === id); if (!b) return null; const r = S.rooms.find(x => x.id === b.roomId); const w = S.wards.find(x => x.id === b.wardId); const p = b.patientId ? api.patient(b.patientId) : null; const ad = p ? S.admissions.find(a => a.patientId === p.id && a.status === 'Admitted') : null; D.title = 'Bed ' + b.id + ' · ' + b.cls; D.sub = (w ? w.name + ' · floor ' + w.floor + ' · ' : '') + (r ? r.id + ' (' + r.beds + ' beds)' : '') + ' · BR-01'; D.badges = [badge(b.status)]; D.facts = [kv('Patient', p ? p.name + ' · ' + p.id + ' · ' + p.dept : '—'), kv('Admission', ad ? ad.id + ' · since ' + ad.admitted : '—'), kv('Room rate / night', r && r.rate ? inr(r.rate) + ' (' + r.serviceId + ')' : '—'), kv('Ward nurse in charge', w ? w.nurseInCharge : '—'), kv('Transfers', ad && ad.transfers ? ad.transfers.map(t => t.from + ' → ' + t.to + ' · ' + fmt(t.ts)).join(' · ') : 'None')]; const bedRole = nurse || role === 'Front Office' || mgmt;
      if (p) { D.actions.push(act('Transfer patient to another bed', () => ctx.prompt('Transfer ' + p.name, 'Target bed ID (must be Free), e.g. "C-05" or "S-03". Outlier-ward transfers are flagged. Free beds: ' + S.beds.filter(x => x.status === 'Free' && x.dept !== 'Emergency').slice(0, 8).map(x => x.id).join(', '), x => tell('Transfer', api.transferBed(p.id, x, role))), { primary: true, ...need(bedRole, 'Nurse / Front Office') })); D.related.push({ label: 'Patient 360', on: go('patient', p.id) }); const e = api.currentEncounter(p.id); if (e) D.related.push({ label: 'Clinical workspace', on: () => ctx.open('encounter', e.id) }); }
      if (b.status === 'Cleaning') D.actions.push(act('Cleaning complete → Free', () => api.setBedStatus(b.id, 'Free', role), { primary: true, ...need(bedRole, 'Nurse / Front Office') }));
      if (b.status === 'Free') D.actions.push(act('Block for maintenance', () => api.setBedStatus(b.id, 'Blocked', role), need(bedRole, 'Nurse / Front Office')));
      if (b.status === 'Blocked') D.actions.push(act('Release bed', () => api.setBedStatus(b.id, 'Free', role), { primary: true, ...need(bedRole, 'Nurse / Front Office') })); break; }
    case 'death': { const x = (S.deaths || []).find(y => y.id === id); if (!x) return null; D.title = x.patient + ' · ' + x.id; D.sub = x.age + ' ' + x.sex + ' · ' + x.dept + ' · ' + fmt(x.ts) + ' · certified by ' + x.certifiedBy; D.badges = [badge(/Issued/.test(x.certificate) ? 'Certificate issued' : 'Certificate pending')].concat(x.mlc ? [badge('MLC')] : []); D.facts = [kv('Cause (as recorded)', x.cause), kv('Certificate', x.certificate, /Pending/.test(x.certificate) ? A : ''), kv('Mortuary / body', x.mortuary), kv('MLC', x.mlc ? (x.policeCleared ? 'Police clearance recorded · ' + x.mlcNote : 'Police intimation pending') : 'Not medico-legal', x.mlc && !x.policeCleared ? RD : ''), kv('Bill', x.billStatus), kv('Bed released', x.bed)]; D.steps = x.history.map(h => ({ t: h.t, who: 'Register', what: h.e, k: 'human' }));
      if (/Pending/.test(x.certificate)) D.actions.push(act('Issue death certificate (Form 4)', () => api.deathStep(x.id, 'certificate', role), { primary: true, ...need(doctor, 'Doctor') }));
      if (x.mlc && !x.policeCleared) D.actions.push(act('Record police clearance', () => api.deathStep(x.id, 'police', role), need(role === 'Front Office' || mgmt, 'Front Office')));
      if (!/Released/.test(x.mortuary)) D.actions.push(act('Release body to family', () => api.deathStep(x.id, 'release', role), need(role === 'Front Office' || nurse || mgmt, 'Front Office / Nurse')));
      if (!/Closed/.test(x.billStatus) && x.billStatus !== '—') D.actions.push(act('Close bill · compassionate review', () => api.deathStep(x.id, 'bill', role), need(role === 'Billing' || role === 'Finance Manager' || mgmt, 'Billing')));
      D.related = [{ label: 'Patient 360', on: go('patient', x.patientId) }]; break; }
    case 'user': { const u = S.users.find(x => x.id === id); if (!u) return null; const e = S.employees.find(x => x.empId === u.empId); const acts = S.audit.filter(a => a.user === u.name || (a.actor || '').startsWith(u.name)).slice(0, 6); D.title = u.name; D.sub = u.id + ' · ' + u.username + ' · ' + u.role + ' · ' + u.dept; D.badges = [badge(u.status), badge(u.mfa ? 'MFA on' : 'MFA off')].concat(ses.userId === u.id ? [badge('Signed in')] : []); D.facts = [kv('Employee', e ? e.empId + ' · ' + (e.designation || e.role) + ' · ' + e.employment : u.empId), kv('Consultant scope', u.doctorId ? u.doctorId + ' · ' + S.patients.filter(p => p.doctorId === u.doctorId && p.type === 'IP').length + ' inpatients' : '—'), kv('Branch', u.branchId), kv('Last login', u.lastLogin)]; D.sections = [{ title: 'Recent actions (audit)', items: acts.length ? acts.map(a => kv(fmt(a.ts), a.action + (a.origin ? ' · ' + a.origin : ''))) : [kv('—', 'No actions in this session')] }]; D.actions = [act('Sign in as this user', () => ctx.login(u.id), { primary: true, disabled: u.status !== 'Active', hint: u.status !== 'Active' ? 'Account disabled' : '' }), act(u.status === 'Active' ? 'Disable account' : 'Enable account', () => api.setUserStatus(u.id, u.status === 'Active' ? 'Disabled' : 'Active', role), need(role === 'IT Administrator' || mgmt, 'IT Administrator'))]; D.related = e ? [{ label: 'Employee record', on: () => ctx.open('employee', e.empId) }] : []; break; }
    // ---- patches on existing details ----
    case 'emergency': { const B = base(); if (!B) return null; const e = S.emergency.find(x => x.id === id); if (!e) return B; if (!/Closed/.test(e.status)) { B.actions.unshift(act(e.esi ? 'Re-triage' : 'Triage', () => ctx.prompt('Triage · ' + e.patient, 'ESI level then findings, add MLC for medico-legal: "ESI-2 chest pain radiating to arm" · "ESI-3 RTA head injury MLC".', x => tell('Triage', api.triage(e.id, x, role))), { primary: !e.esi, ...need(doctor || nurse, 'Doctor / Nurse') })); B.actions.push(act('Record death in ER', () => ctx.prompt('Record death · ' + e.patient, 'Cause; add MLC if medico-legal.', x => tell('Outcome', api.recordOutcome(e.patientId, 'Death ' + x, role))), need(doctor, 'Doctor'))); } B.facts.unshift(kv('Triage', e.acuity + (e.mlc ? ' · MLC' : '') + (e.triageNote ? ' · ' + e.triageNote : ''), e.status === 'Awaiting triage' ? RD : '')); const enc = api.currentEncounter(e.patientId); if (enc) B.related.push({ label: 'Clinical workspace', on: () => ctx.open('encounter', enc.id) }); return B; }
    case 'preauth': { const B = base(); if (!B) return null; const p = S.preauths.find(x => x.id === id); if (!p) return B; const ins = role === 'Insurance' || role === 'Finance Manager' || mgmt; const cs = p.claimStatus; B.facts.push(kv('Claim', cs ? cs + (p.claimNo ? ' · ' + p.claimNo : '') + (p.rejectionReason && cs === 'Rejected' ? ' · ' + p.rejectionReason : '') + (p.deduction && /Partial/.test(cs) ? ' · deduction ' + p.deduction : '') : 'Not started'));
      if (/Approved|Partially|Rejected|Settled/.test(p.status) || cs) { if (!cs) B.actions.push(act('Create claim', () => api.advanceClaim(p.id, role), need(ins, 'Insurance'))); else if (cs === 'Claim Ready' || cs === 'Query Raised') B.actions.push(act(cs === 'Query Raised' ? 'Respond to query · resubmit' : 'Submit claim', () => api.advanceClaim(p.id, role), { primary: true, ...need(ins, 'Insurance') })); else if (cs === 'Submitted') B.actions.push(act('TPA: mark under review', () => api.advanceClaim(p.id, role), need(ins, 'Insurance'))); else if (cs === 'Under Review') ['approve', 'partial', 'query', 'reject'].forEach(o => B.actions.push(act('TPA outcome: ' + o, () => api.advanceClaim(p.id, role, o), { primary: o === 'approve', ...need(ins, 'Insurance') }))); else if (/Approved/.test(cs)) B.actions.push(act('Record settlement', () => api.advanceClaim(p.id, role), { primary: true, ...need(ins, 'Insurance') })); else if (cs === 'Rejected') B.actions.push(act('Appeal · resubmit', () => api.advanceClaim(p.id, role, 'appeal'), need(ins, 'Insurance'))); } return B; }
    case 'purchase': { const B = base(); if (!B) return null; const p = S.purchases.find(x => x.id === id); if (!p) return B; const ok = api.can(role, 'inventory') || api.can(role, 'financial'); B.facts.push(kv('Inspection / match', (p.qcNote || 'GRN inspection pending') + (p.match ? ' · ' + p.match : ''))); if (p.status === 'Received · QC pending') { B.actions.unshift(act('QC reject → return to vendor', () => api.advancePurchase(p.id, role, 'reject'), need(ok, 'Store / Procurement'))); B.actions.unshift(act('QC pass → post to stock', () => api.advancePurchase(p.id, role, 'pass'), { primary: true, ...need(ok, 'Store / Procurement') })); } if (p.status === 'Invoice matched') B.actions.unshift(act('Request vendor payment (Finance approval)', () => api.advancePurchase(p.id, role), { primary: true, ...need(ok, 'Procurement / Finance') })); if (p.status === 'Payment pending approval') B.actions.unshift(act('Approve payment in Approval Centre', go('approvals'), { primary: true })); B.facts = B.facts.map(f => f.k === 'Lifecycle' ? kv('Lifecycle', 'Pending Approval → Approved → Ordered → Received · QC pending → Received (stock) → Invoice matched (3-way) → Payment pending approval → Closed') : f); return B; }
    case 'credential': { const B = base(); if (!B) return null; const x = S.credentials.find(y => y.id === id); if (!x) return B; if (x.status === 'Renewal submitted') { B.actions = B.actions.filter(a => !/Renew/i.test(a.label)); B.actions.unshift(act('Verify renewal document → Valid', () => api.verifyCredential(x.id, role), { primary: true, ...need(role === 'HR Manager' || mgmt, 'HR Manager') })); } if (x.verifiedBy) B.facts.push(kv('Verified', x.verifiedBy + ' · ' + fmt(x.verifiedAt))); return B; }
    case 'admission': { const B = base(); if (!B) return null; const a = S.admissions.find(x => x.id === id); if (!a || a.status !== 'Admitted') return B; const p = api.patient(a.patientId); const bed = S.beds.find(b => b.patientId === a.patientId); if (bed) B.actions.push(act('Transfer bed', () => ctx.open('bed', bed.id))); B.actions.push(act('Record outcome (death / LAMA / transfer)', () => ctx.prompt('Record outcome · ' + a.patient, 'Start with Death, LAMA, Transfer or Absconded, then the reason.', x => tell('Outcome', api.recordOutcome(a.patientId, x, role))), need(doctor, 'Doctor'))); const e = p && api.currentEncounter(p.id); if (e) B.related.push({ label: 'Clinical workspace', on: () => ctx.open('encounter', e.id) }); if (a.transfers) B.facts.push(kv('Transfers', a.transfers.map(t => t.from + ' → ' + t.to + ' · ' + fmt(t.ts)).join(' · '))); return B; }
    default: return null;
  }
  return D;
}

return { PAGES4, allowed4, buildNav, buildList4, buildDetail4 };
})();
window.__mm2.erp4 = (function(){
// ERP extension 4 (V2.0 "not yet" items): consultant schedules + slot booking (F-017), OT room scheduling with conflicts (F-018), single RBAC matrix that drives can() and page access (F-019),
// manual lab result entry + report preview (F-022), parameterised report registers (F-023), nurse roster with credential / leave / double-booking blocks, SBAR handover, master-data edit support.
const { APPROVER } = window.__mm2.store;
function installErp4(api) {
  const S = api.S, { audit, notify, emit, nid, rnd, int, pick, fmt, inr } = api._h; const pad = n => String(n).padStart(2, '0');
  const who = role => (S.session && S.session.userId ? S.session.name : role);
  S.taxes.forEach(t => { if (!t.id) t.id = t.code; });

  // ---------- F-017 consultant schedules ----------
  const DOW = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat']; const todayDow = Math.max(0, DOW.indexOf(S.day.slice(0, 3)));
  api.dowOf = dayOffset => DOW[(todayDow + (dayOffset || 0) + 700) % 7];
  if (!S.schedules) S.schedules = S.doctors.map((d, i) => ({ id: 'SCH-' + d.id, doctorId: d.id, doctor: d.name, dept: d.dept, days: i % 3 === 0 ? ['Mon', 'Wed', 'Fri'] : i % 3 === 1 ? ['Tue', 'Thu', 'Fri', 'Sat'] : ['Mon', 'Tue', 'Wed', 'Thu', 'Fri'], start: [540, 600, 840][i % 3], end: [780, 840, 1080][i % 3], slotMin: [15, 20, 15][i % 3], room: 'OPD-' + (1 + (i % 8)), leave: [], active: true }));
  api.scheduleOf = doctorId => S.schedules.find(s => s.doctorId === doctorId);
  api.slotsFor = (doctorId, dayOffset) => { const sc = api.scheduleOf(doctorId); if (!sc || !sc.active) return []; const dow = api.dowOf(dayOffset); if (!sc.days.includes(dow) || sc.leave.includes(dayOffset)) return []; const out = []; for (let m = sc.start; m < sc.end; m += sc.slotMin) { const ts = dayOffset * 1440 + m; const a = S.appointments.find(x => x.doctorId === doctorId && x.ts === ts && !/Cancelled|Rescheduled|No-show/.test(x.status)); out.push({ ts, m, label: fmt(ts).slice(-5), appt: a || null, past: ts < S.clock }); } return out; };
  const origCreateAppt = api.createAppointment.bind(api);
  api.createAppointment = (f, role) => { const doc = S.doctors.find(d => d.id === f.doctorId); const sc = doc && api.scheduleOf(doc.id); if (sc && !f.walkIn) { const dow = api.dowOf(+f.day); if (!sc.days.includes(dow)) return { error: doc.name + ' does not consult on ' + dow + ' (consults ' + sc.days.join(' · ') + ')' }; if (sc.leave.includes(+f.day)) return { error: doc.name + ' is on leave that day · choose another day or consultant' }; if (f.min < sc.start || f.min >= sc.end) return { error: 'Outside consulting hours ' + fmt(sc.start).slice(-5) + '–' + fmt(sc.end).slice(-5) + ' (' + sc.room + ')' }; if ((f.min - sc.start) % sc.slotMin) return { error: 'Not a valid slot · slots every ' + sc.slotMin + ' min from ' + fmt(sc.start).slice(-5) }; } return origCreateAppt(f, role); };
  api.bookSlot = (doctorId, text, role) => { const m = String(text || '').match(/^(.+?)\s*[,;]\s*(\d{1,2}):(\d{2})\s*(?:[,;]\s*(today|tomorrow|day after|[0-2]))?$/i); if (!m) return { error: 'Format: "<patient name or UHID>, HH:MM, today|tomorrow|day after" — e.g. "Kavitha Raman, 09:30, tomorrow"' }; const q = m[1].trim().toLowerCase(); const p = S.patients.find(x => x.id.toLowerCase() === q) || S.patients.find(x => x.name.toLowerCase().startsWith(q)); if (!p) return { error: 'No patient matching "' + m[1] + '"' }; const day = /tomorrow|^1$/i.test(m[4] || '') ? 1 : /day after|^2$/i.test(m[4] || '') ? 2 : 0; return api.createAppointment({ patientId: p.id, doctorId, day, min: (+m[2]) * 60 + (+m[3]), type: 'New', walkIn: false }, role); };
  api.blockLeave = (doctorId, dayOffset, role) => { const sc = api.scheduleOf(doctorId); if (!sc) return; if (sc.leave.includes(dayOffset)) { sc.leave = sc.leave.filter(d => d !== dayOffset); audit(role, 'Leave block removed · ' + sc.doctor, sc.id, 'Leave', 'Consulting', api.dowOf(dayOffset), '—', 'Scheduling'); emit(); return; } sc.leave.push(dayOffset); const affected = S.appointments.filter(a => a.doctorId === doctorId && Math.floor(a.ts / 1440) === dayOffset && /Scheduled|Confirmed/.test(a.status)); affected.forEach(a => { a.status = 'Reschedule needed'; a.note = 'Consultant leave'; }); if (affected.length) notify('Consultant leave · ' + affected.length + ' appointments need rescheduling', sc.doctor + ' · ' + api.dowOf(dayOffset) + ' · patients will be offered new slots', 'Medium', 'Scheduling', { page: 'appointments' }); audit(role, 'Leave blocked · ' + sc.doctor + ' · ' + api.dowOf(dayOffset), sc.id, 'Consulting', 'Leave', affected.length + ' appointments affected', '—', 'Scheduling'); emit(); };
  api.setScheduleTemplate = (doctorId, text, role) => { const sc = api.scheduleOf(doctorId); if (!sc) return { error: 'No schedule' }; const m = String(text || '').match(/(\d{1,2}):(\d{2})\s*[-–]\s*(\d{1,2}):(\d{2})(?:\s*(?:every|\/)\s*(\d+))?\s*(?:min)?\s*(.*)$/i); if (!m) return { error: 'Format: "09:00-13:00 every 15 min Mon Wed Fri"' }; const days = DOW.filter(d => new RegExp('\\b' + d, 'i').test(m[6] || '')); const before = fmt(sc.start).slice(-5) + '–' + fmt(sc.end).slice(-5) + ' /' + sc.slotMin + ' ' + sc.days.join(' '); sc.start = (+m[1]) * 60 + (+m[2]); sc.end = (+m[3]) * 60 + (+m[4]); if (m[5]) sc.slotMin = +m[5]; if (days.length) sc.days = days; audit(role, 'Schedule template changed · ' + sc.doctor, sc.id, before, fmt(sc.start).slice(-5) + '–' + fmt(sc.end).slice(-5) + ' /' + sc.slotMin + ' ' + sc.days.join(' '), '—', '—', 'Scheduling'); emit(); return { ok: true }; };

  // ---------- F-018 OT scheduling ----------
  api.scheduleSurgery = (id, text, role) => { const s = S.surgeries.find(x => x.id === id); if (!s) return { error: 'Surgery not found' }; const m = String(text || '').match(/(OT-?\s?\d|Cath\s*Lab\s*\d)\s*[,;]?\s*(\d{1,2}):(\d{2})\s*(tomorrow)?/i); if (!m) return { error: 'Format: "<room> HH:MM [tomorrow]" — rooms: ' + S.otRooms.map(r => r.name.split(' ')[0]).join(', ') }; const key = m[1].replace(/\s+/g, '').toUpperCase().replace('CATHLAB', 'CATH LAB '); const room = S.otRooms.find(r => r.name.toUpperCase().replace(/\s+/g, '').startsWith(key.replace(/\s+/g, ''))); if (!room) return { error: 'Unknown room "' + m[1] + '"' }; if (/Maintenance|Closed/.test(room.status)) return { error: room.name + ' is ' + room.status }; const start = (m[4] ? 1440 : 0) + (+m[2]) * 60 + (+m[3]); const end = start + s.durationMin; const live = x => !/Completed|Cancelled/.test(x.stage); const roomClash = S.surgeries.find(x => x.id !== s.id && live(x) && x.room === room.name && x.start < end + 30 && x.start + x.durationMin + 30 > start); if (roomClash) return { error: room.name + ' is booked: ' + roomClash.procedure + ' ' + fmt(roomClash.start).slice(-5) + '–' + fmt(roomClash.start + roomClash.durationMin).slice(-5) + ' (+30 min turnover)' }; const surgeonClash = S.surgeries.find(x => x.id !== s.id && live(x) && x.surgeon === s.surgeon && x.start < end && x.start + x.durationMin > start); if (surgeonClash) return { error: s.surgeon + ' is operating in ' + surgeonClash.room + ' at ' + fmt(surgeonClash.start).slice(-5) }; const before = s.room + ' ' + fmt(s.start); s.room = room.name; s.start = start; if (s.stageIdx < 2) { s.stage = 'Scheduled'; s.stageIdx = 2; } s.history.unshift({ t: fmt(S.clock), e: 'Scheduled ' + room.name + ' ' + fmt(start) + '–' + fmt(end).slice(-5) + ' by ' + who(role) }); notify('OT scheduled', s.patient + ' · ' + s.procedure + ' · ' + room.name + ' ' + fmt(start), 'Low', 'OT', { page: 'ot-schedule', id: s.id }); audit(role, 'Surgery scheduled · ' + room.name + ' ' + fmt(start), s.patientId, before, room.name + ' ' + fmt(start), s.procedure, '—', 'OT', s.id); emit(); return { ok: true }; };
  api.otUtilisation = () => S.otRooms.map(r => { const todays = S.surgeries.filter(s => s.room === r.name && !/Cancelled/.test(s.stage) && s.start >= 0 && s.start < 1440); const mins = todays.reduce((a, s) => a + s.durationMin, 0); return { room: r.name, status: r.status, cases: todays.length, mins, pct: Math.min(100, Math.round(100 * mins / 600)) }; });

  // ---------- F-019 single RBAC matrix ----------
  const ACTIONS = [['clinical', 'View clinical record', ['Doctor', 'Nurse', 'Radiologist', 'Laboratory', 'Pharmacy']], ['acknowledge', 'Acknowledge critical value', ['Doctor']], ['admit', 'Admit patient', ['Doctor', 'Front Office', 'Hospital Management']], ['discharge', 'Discharge patient · release bed', ['Nurse', 'Front Office', 'Hospital Management', 'Doctor']], ['frontOffice', 'Register patients · book appointments', ['Front Office', 'Hospital Management', 'Doctor', 'Nurse']], ['lab', 'Collect / process / validate samples', ['Laboratory', 'Pathologist']], ['pathologist', 'Release lab report', ['Pathologist']], ['radiology', 'Finalise radiology read', ['Radiologist', 'Doctor']], ['pharmacy', 'Dispense / return medication', ['Pharmacy', 'Nurse']], ['payments', 'Record payments', ['Billing', 'Front Office', 'Hospital Management']], ['financial', 'View financial data', ['Billing', 'Finance Manager', 'Insurance', 'Hospital Management', 'Front Office', 'Auditor']], ['resolveDispute', 'Resolve billing dispute', ['Billing', 'Hospital Management']], ['inventory', 'Stock issue · purchase requisition', ['Store Manager', 'Procurement Officer', 'Pharmacy', 'Laboratory', 'Hospital Management']], ['hr', 'HR actions (attendance · leave · payroll)', ['HR Manager', 'Hospital Management']], ['canteen', 'Diet plans · meals', ['Canteen Manager', 'Nurse', 'Hospital Management']], ['admin', 'Administration masters · users', ['IT Administrator', 'Hospital Management']], ['configureAgents', 'Configure / publish AI agents', ['AI Administrator']], ['governance', 'Governance actions', ['Governance Officer', 'AI Administrator', 'Hospital Management']]];
  if (!S.permissions) S.permissions = ACTIONS.map(([key, label, roles]) => ({ id: 'PERM-' + key, key, label, roles: roles.slice(), kind: 'action' }));
  const origCan = api.can.bind(api);
  api.can = (role, what) => { if (what === 'approve') return origCan(role, 'approve'); const p = S.permissions.find(x => x.key === what); return p ? p.roles.includes(role) : origCan(role, what); };
  api.buildPageAccess = (pages, roles, allowedFn) => { if (S.pageAccess && S.pageAccess.length) { pages.forEach(([key, label]) => { if (!S.pageAccess.some(p => p.key === key)) S.pageAccess.push({ id: 'PAGE-' + key, key, label, roles: roles.filter(r => allowedFn(r, key)), kind: 'page' }); }); return; } S.pageAccess = pages.map(([key, label]) => ({ id: 'PAGE-' + key, key, label, roles: roles.filter(r => allowedFn(r, key)), kind: 'page' })); };
  api.pageAllowed = (role, page) => { const p = S.pageAccess && S.pageAccess.find(x => x.key === page); return p ? p.roles.includes(role) : null; };
  api.setPermission = (id, role, grant, actor) => { const p = (S.permissions.find(x => x.id === id) || (S.pageAccess || []).find(x => x.id === id)); if (!p) return; const has = p.roles.includes(role); if (grant && !has) p.roles.push(role); if (!grant && has) p.roles = p.roles.filter(r => r !== role); notify('Permission ' + (grant ? 'granted' : 'revoked'), p.label + ' → ' + role + ' · effective immediately for navigation, page guard and actions', 'Medium', 'Administration', { page: 'permissions', id }); audit(actor, 'Permission ' + (grant ? 'granted' : 'revoked') + ' · ' + p.label + ' → ' + role, id, String(has), String(!!grant), p.kind, '—', 'Administration'); emit(); };
  api.approvalTypesFor = role => Object.entries(APPROVER).filter(([, r]) => r === role).map(([t]) => t);

  // ---------- F-022 manual result entry ----------
  const flagFor = (lt, v) => { if (typeof v !== 'number' || !lt) return { flag: 'Result Entered', critical: false }; const m = String(lt.ref).match(/([\d.]+)\s*[–-]\s*([\d.]+)/); const lt2 = String(lt.ref).match(/^<\s*([\d.]+)/); const lo = m ? +m[1] : null, hi = m ? +m[2] : lt2 ? +lt2[1] : null; const ch = lt.critHigh && lt.critHigh !== '—' ? parseFloat(String(lt.critHigh).replace(/[^\d.]/g, '')) : null; const cl = lt.critLow && lt.critLow !== '—' ? parseFloat(String(lt.critLow).replace(/[^\d.]/g, '')) : null; if ((ch != null && v > ch) || (cl != null && v < cl)) return { flag: 'Critical', critical: true }; if (hi != null && v > hi) return { flag: 'High', critical: false }; if (lo != null && v < lo) return { flag: 'Low', critical: false }; return { flag: 'Normal', critical: false }; };
  api.enterResult = (sampleId, text, role) => { const s = S.samples.find(x => x.id === sampleId); if (!s) return { error: 'Sample not found' }; if (!/Sample Received|Processing|Machine Processing/.test(s.status)) return { error: 'Manual entry is allowed once the sample is received in the lab (current: ' + s.status + ')' }; const l = S.labs.find(x => x.id === s.labId); const lt = S.labTests.find(t => t.panel === s.test); const num = parseFloat(String(text).replace(/[^\d.\-]/g, '')); if (lt && lt.resultType === 'Numeric' && isNaN(num)) return { error: 'Numeric result expected (' + lt.unit + ')' }; const val = isNaN(num) ? String(text).trim() : num; const f = flagFor(lt, val); if (l) { l.value = val; l.flag = f.flag; l.critical = f.critical; l.status = 'Result Entered'; l.enteredBy = who(role); l.method = 'Manual entry'; if (f.critical) { l.acked = false; l.notified = S.clock; l.clinician = (api.patient(l.patientId) || {}).doctor; } } s.status = 'Result Entered'; s.manual = true; s.history.unshift({ t: fmt(S.clock), e: 'Result entered manually by ' + who(role) + ' · ' + val + ' ' + (lt ? lt.unit : '') + ' · ' + f.flag + ' · pending technical validation' }); if (f.critical) notify('Critical value (manual entry)', s.patient + ' · ' + (l ? l.analyte : s.test) + ' ' + val + ' · acknowledge within 15 min', 'Critical', 'LIS', { page: 'laboratory', id: l ? l.id : undefined }); audit(role, 'Manual result entered · ' + s.test + ' = ' + val, s.patientId, s.status, 'Result Entered', f.flag, '—', 'LIS', s.id); emit(); return { ok: true }; };

  // ---------- F-023 report registers ----------
  const inRange = (ts, range) => ts == null ? true : range === 'Today' ? ts >= 0 && ts < 1440 : range === '7 days' ? ts >= -6 * 1440 : range === '30 days' ? ts >= -29 * 1440 : true;
  const insurerOf = pid => (api.patient(pid) || {}).insurer || 'Self-pay';
  const REPORTS = [
    { id: 'RPT-CENSUS', name: 'Daily census by ward', domain: 'Operations', owner: 'Nursing Superintendent', group: 'Ward', ranged: false, rows: () => S.beds.filter(b => b.patientId).map(b => ({ _group: b.ward, Bed: b.id, Patient: (api.patient(b.patientId) || {}).name || '—', Class: b.cls, Status: b.status })) },
    { id: 'RPT-OPD', name: 'OPD register (completed consultations)', domain: 'Operations', owner: 'Front Office', group: 'Department', ranged: true, rows: r => S.appointments.filter(a => a.status === 'Completed' && inRange(a.ts, r)).map(a => ({ _group: a.dept, Time: fmt(a.ts), Patient: a.patient, Doctor: a.doctor, Channel: a.channel, Type: a.type })) },
    { id: 'RPT-IPD', name: 'IPD admissions register', domain: 'Operations', owner: 'Medical Records', group: 'Department', ranged: false, rows: () => S.admissions.map(a => ({ _group: a.dept, Admission: a.id, Patient: a.patient, Doctor: a.doctor, Bed: a.bed, Admitted: a.admitted, Status: a.status, _amount: a.estimate })) },
    { id: 'RPT-DIS', name: 'Discharge register', domain: 'Operations', owner: 'Medical Records', group: 'Outcome', ranged: true, rows: r => S.discharges.filter(d => d.completed && inRange(d.dischargedAt, r)).map(d => ({ _group: d.outcome || 'Discharged home', Case: d.id, Patient: d.patient, Doctor: d.doctor, Bed: d.bed || '—', At: fmt(d.dischargedAt) })) },
    { id: 'RPT-MLC', name: 'Death & MLC register', domain: 'Quality', owner: 'Medical Superintendent', group: 'MLC', ranged: true, rows: r => (S.deaths || []).filter(x => inRange(x.ts, r)).map(x => ({ _group: x.mlc ? 'Medico-legal' : 'Non-MLC', Register: x.id, Patient: x.patient, Dept: x.dept, Time: fmt(x.ts), Cause: x.cause, Certificate: x.certificate, Body: x.mortuary })) },
    { id: 'RPT-LAB', name: 'Laboratory register (samples by section)', domain: 'Diagnostics', owner: 'Chief Pathologist', group: 'Section', ranged: true, rows: r => S.samples.filter(s => inRange(s.collectedAt, r)).map(s => { const t = S.labTests.find(x => x.panel === s.test); const l = S.labs.find(x => x.id === s.labId); return { _group: t ? t.section : 'Other', Accession: s.accession, Patient: s.patient, Test: s.test, Result: l && l.value != null ? l.value + ' ' + l.unit + ' · ' + l.flag : '—', Status: s.status, TAT: (s.releasedAt ? s.releasedAt - s.collectedAt : S.clock - s.collectedAt) + ' m' }; }) },
    { id: 'RPT-PHARMA', name: 'Pharmacy sales & GST', domain: 'Finance', owner: 'Chief Pharmacist', group: 'Tax code', ranged: true, rows: r => S.pharmacy.filter(t => t.status === 'Dispensed' && inRange(t.ts, r)).map(t => { const d = S.drugs.find(x => x.id === t.drugId); const it = d && d.itemId ? S.items.find(x => x.id === d.itemId) : null; const amt = it ? it.price * t.qty : 0; const tax = it ? (S.taxes.find(x => x.code === it.taxCode) || {}).rate || 0 : 0; return { _group: it ? it.taxCode : 'Unmapped', Txn: t.id, Patient: t.patient, Drug: t.drug, Qty: t.qty, Batch: t.batch || '—', Taxable: inr(amt), GST: inr(Math.round(amt * tax / 100)), _amount: Math.round(amt * (1 + tax / 100)) }; }) },
    { id: 'RPT-STOCK', name: 'Stock ledger', domain: 'Supply chain', owner: 'Store Manager', group: 'Movement type', ranged: true, rows: r => S.stockTx.filter(t => inRange(t.ts, r)).map(t => ({ _group: t.type, Txn: t.id, Item: t.item, Qty: t.qty, From: t.from, To: t.to, By: t.by, Status: t.status, Reason: t.reason })) },
    { id: 'RPT-DAYBOOK', name: 'Cash collection / day book', domain: 'Finance', owner: 'Cashier', group: 'Mode', ranged: true, rows: r => S.payments.filter(p => p.status === 'Success' && inRange(p.ts, r)).map(p => ({ _group: p.mode, Receipt: p.id, Patient: p.patient, Bill: p.billId, Ref: p.ref || '—', Time: fmt(p.ts), _amount: p.amount })) },
    { id: 'RPT-AR', name: 'Outstanding by payer', domain: 'Finance', owner: 'Finance Manager', group: 'Payer', ranged: false, rows: () => S.bills.filter(b => !/Paid|Settled|Voided|Closed/.test(b.status)).map(b => ({ _group: b.insurance > 0 ? insurerOf(b.patientId) : 'Self-pay', Bill: b.id, Patient: b.patient, Status: b.status, Billed: inr(b.actual), Insurer: inr(b.insurance), Patient_due: inr(Math.max(0, b.patientShare - (b.paid || 0))), _amount: Math.max(0, b.actual - (b.paid || 0)) })) },
    { id: 'RPT-CLAIMS', name: 'Claim ageing by status', domain: 'Finance', owner: 'Insurance Desk', group: 'Claim status', ranged: false, rows: () => S.preauths.filter(p => p.claimStatus).map(p => ({ _group: p.claimStatus, Claim: p.claimNo || '—', Patient: p.patient, Insurer: p.insurer, Requested: p.requested, Approved: p.approved, Age: api.ago(p.createdTs || 0, S.clock), _amount: parseInt(String(p.approved).replace(/[^\d]/g, ''), 10) || 0 })) },
    { id: 'RPT-ATT', name: 'Attendance by unit (today)', domain: 'People', owner: 'HR Manager', group: 'Unit', ranged: false, rows: () => S.employees.filter(e => e.employment !== 'Resigned').map(e => ({ _group: e.unit, Employee: e.empId, Name: e.name, Designation: e.designation || e.role, Shift: e.shift, Today: e.today })) },
    { id: 'RPT-CRED', name: 'Credential expiry', domain: 'People', owner: 'HR Manager', group: 'Status', ranged: false, rows: () => S.credentials.map(c => ({ _group: c.status, Holder: c.holder, Type: c.type, Number: c.number, Expiry: c.expiry })) },
  ];
  api.reports = REPORTS.map(r => ({ id: r.id, name: r.name, domain: r.domain, owner: r.owner, group: r.group, ranged: r.ranged }));
  api.reportData = (id, range) => { const r = REPORTS.find(x => x.id === id); if (!r) return null; const rows = r.rows(r.ranged ? (range || 'Today') : 'All'); const g = {}; rows.forEach(x => { const k = x._group || '—'; g[k] = g[k] || { k, n: 0, amount: 0 }; g[k].n++; g[k].amount += x._amount || 0; }); const groups = Object.values(g).sort((a, b) => b.n - a.n); return { meta: api.reports.find(x => x.id === id), range: r.ranged ? (range || 'Today') : 'All (point in time)', rows, groups, total: { n: rows.length, amount: rows.reduce((a, x) => a + (x._amount || 0), 0) }, cols: rows.length ? Object.keys(rows[0]).filter(k => !k.startsWith('_')) : [] }; };
  api.downloadCsv = (rows, name) => { const cols = rows.length ? Object.keys(rows[0]).filter(k => !k.startsWith('_')) : []; const esc = s => '"' + String(s == null ? '' : s).replace(/"/g, '""') + '"'; const csv = [['Group'].concat(cols).map(esc).join(',')].concat(rows.map(r => [r._group].concat(cols.map(c => r[c])).map(esc).join(','))).join('\n'); const a = document.createElement('a'); a.href = URL.createObjectURL(new Blob([csv], { type: 'text/csv' })); a.download = name.replace(/[^a-z0-9]+/gi, '-').toLowerCase() + '.csv'; a.click(); };
  api.runReport = (id, range, role) => { const d = api.reportData(id, range); if (!d) return; S.reportRuns = S.reportRuns || []; S.reportRuns.unshift({ id: nid('RUN'), reportId: id, range: d.range, rows: d.total.n, by: who(role), ts: S.clock }); audit(role, 'Report run · ' + d.meta.name + ' · ' + d.range, id, '—', d.total.n + ' rows', '—', '—', 'Reporting'); emit(); return d; };

  // ---------- Roster + nurse credentials ----------
  const isNurse = e => /Nurse/.test(e.designation || e.role);
  const nurses = S.employees.filter(e => isNurse(e) && e.employment !== 'Resigned');
  if (!S.credentials.some(c => /Nurses/.test(c.type))) nurses.slice(0, 8).forEach((n, i) => S.credentials.push({ id: 'CRD-N' + pad(i + 1), holder: n.name, type: 'TN Nurses & Midwives Council registration', number: 'TNNMC ' + (81000 + i * 53), expiry: i === 1 ? '31 Aug 2026' : i === 3 ? '30 Sep 2026' : '31 Mar 2028', status: i === 1 ? 'Expired' : i === 3 ? 'Expiring (17 d)' : 'Valid', training: i % 2 ? 'BLS · valid' : 'BLS · due', empId: n.empId }));
  const SHIFTS = ['Morning', 'Evening', 'Night'];
  if (!S.roster) { S.roster = []; S.wards.forEach((w, wi) => { for (let d = 0; d < 3; d++) SHIFTS.forEach((sh, si) => { const pool = nurses.filter(n => n.unit === w.dept || n.unit === w.name); const src = pool.length >= 2 ? pool : nurses; const off = (wi * 3 + d + si) % Math.max(1, src.length - 1); const picks = src.slice(off, off + (w.dept === 'Emergency' ? 3 : 2)).map(n => n.name); if ((wi + si + d) % 5 === 4) picks.pop(); S.roster.push({ id: 'RS-' + w.id + '-' + d + '-' + si, wardId: w.id, ward: w.name, dept: w.dept, day: d, dayLabel: ['Today', 'Tomorrow', 'Day after'][d], shift: sh, required: w.dept === 'Emergency' ? 3 : 2, nurses: picks, incharge: picks[0] || '—' }); }); }); }
  api.assignRoster = (slotId, text, role) => { const sl = S.roster.find(x => x.id === slotId); if (!sl) return { error: 'Slot not found' }; const q = String(text || '').trim().toLowerCase(); const emp = S.employees.find(e => e.name.toLowerCase() === q) || S.employees.find(e => e.name.toLowerCase().startsWith(q)); if (!emp) return { error: 'No employee matching "' + text + '"' }; if (!isNurse(emp)) return { error: emp.name + ' is ' + (emp.designation || emp.role) + ', not nursing staff' }; if (emp.employment === 'Resigned') return { error: emp.name + ' has left the organisation' }; const cred = S.credentials.find(c => c.holder === emp.name); if (cred && /Expired/.test(cred.status)) return { error: emp.name + ' · ' + cred.type + ' EXPIRED ' + cred.expiry + ' · rostering blocked until HR verifies renewal' }; if (sl.day === 0 && emp.today === 'On leave') return { error: emp.name + ' is on approved leave today' }; if (sl.nurses.includes(emp.name)) return { error: emp.name + ' is already on this shift' }; const dbl = S.roster.find(x => x.id !== sl.id && x.day === sl.day && x.shift === sl.shift && x.nurses.includes(emp.name)); if (dbl) return { error: emp.name + ' is already rostered · ' + dbl.ward + ' · ' + dbl.shift }; const prev = S.roster.find(x => x.wardId !== '' && x.day === sl.day && SHIFTS.indexOf(x.shift) === SHIFTS.indexOf(sl.shift) - 1 && x.nurses.includes(emp.name)); if (prev) notify('Back-to-back shift', emp.name + ' worked ' + prev.shift + ' on ' + prev.ward + ' · fatigue rule flagged', 'Medium', 'Roster', { page: 'roster', id: sl.id }); sl.nurses.push(emp.name); if (sl.incharge === '—') sl.incharge = emp.name; audit(role, 'Roster assignment · ' + emp.name + ' → ' + sl.ward + ' ' + sl.shift + ' ' + sl.dayLabel, sl.id, String(sl.nurses.length - 1), String(sl.nurses.length), cred ? cred.status : 'No credential on file', '—', 'Roster'); emit(); return { ok: true }; };
  api.unassignRoster = (slotId, name, role) => { const sl = S.roster.find(x => x.id === slotId); if (!sl) return; sl.nurses = sl.nurses.filter(n => n !== name); if (sl.incharge === name) sl.incharge = sl.nurses[0] || '—'; audit(role, 'Roster removal · ' + name + ' ← ' + sl.ward + ' ' + sl.shift, sl.id, '—', String(sl.nurses.length), '—', '—', 'Roster'); emit(); };

  // ---------- SBAR handover ----------
  api.addHandover = (patientId, text, role) => { const e = api.currentEncounter(patientId); if (!e) return { error: 'No open encounter' }; const t = String(text || '').trim(); if (!/S\s*:|Situation/i.test(t)) return { error: 'Use SBAR: "S: … B: … A: … R: …"' }; api.addNote(e.id, t, role, 'Handover (SBAR)'); return { ok: true }; };
  api.latestHandover = patientId => { const e = api.currentEncounter(patientId); return e ? e.notes.filter(n => /Handover/.test(n.kind)).sort((a, b) => b.ts - a.ts)[0] || null : null; };
  // seed one handover per inpatient in cardiology so the ward page is not empty
  S.patients.filter(p => p.type === 'IP' && p.dept === 'Cardiology').forEach((p, i) => { const e = api.currentEncounter(p.id); if (e && !e.notes.some(n => /Handover/.test(n.kind))) e.notes.push({ ts: 420 + i, by: 'Sheela J', kind: 'Handover (SBAR)', text: 'S: ' + (p.procedure || 'medical management') + ', day ' + (1 + (i % 3)) + '. B: ' + (e.diagnoses[0] ? e.diagnoses[0].desc : 'as per notes') + '. A: stable, EWS 0–1, pain controlled. R: continue plan, watch access site, review labs at 14:00.', status: 'Signed' }); });
  const origCreate4 = api.createMaster.bind(api); api.createMaster = (coll, data, role) => { const r = origCreate4(coll, data, role); if (r && !r.error && coll === 'drugs') { const d = S.drugs.find(x => x.id === r.id); if (d) { const it = S.items.find(x => x.name.toLowerCase().startsWith(String(d.generic || '').toLowerCase())); Object.assign(d, { itemId: it ? it.id : null, interactions: [], highAlert: /Insulin|Heparin|Morphine|Potassium/i.test(d.generic || ''), controlled: String(d.schedule || '').startsWith('X') }); } } return r; };
  return api;
}

return { installErp4 };
})();
window.__mm2.views5 = (function(){
// V2.0 pages set 2: consultant schedules, OT schedule, report registers, RBAC matrix (roles / permissions), nurse roster, ward handover; patches: surgery scheduling, manual result entry + lab report preview, SBAR handover, master-data edit.
const { pill } = window.__mm2.views;
const { ROLES, APPROVER } = window.__mm2.store;
const G = 'oklch(0.4 0.12 150)', A = 'oklch(0.5 0.13 70)', RD = 'oklch(0.45 0.17 25)', T = 'oklch(0.45 0.1 200)';
const c = (t, o = {}) => ({ t: t == null ? '—' : String(t), c: o.c || '', bg: o.bg || '', m: !!o.m, b: !!o.b, pill: !!o.p });
const P = s => ({ ...c(s), ...pill(s), pill: true });
const kv = (k, v, col) => ({ k, v: v == null ? '—' : String(v), c: col || '' });
const PAGES5 = ['schedules', 'ot-schedule', 'roster', 'handover'];
const OVERRIDE5 = ['reports', 'roles', 'permissions'];
const ROLE5 = { schedules: ['Front Office', 'Doctor', 'Hospital Management', 'IT Administrator', 'Auditor'], 'ot-schedule': ['Doctor', 'Nurse', 'Hospital Management', 'Auditor', 'Store Manager'], roster: ['Nurse', 'HR Manager', 'Hospital Management', 'Auditor'], handover: ['Nurse', 'Doctor', 'Hospital Management'] };
const allowed5 = (role, page) => PAGES5.includes(page) ? ROLE5[page].includes(role) : undefined;
const NAV5 = { schedules: ['Front office & patients', 'Consultant Schedules'], 'ot-schedule': ['Clinical', 'OT Schedule'], roster: ['People', 'Nurse Roster'], handover: ['Clinical', 'Ward Handover (SBAR)'] };
const EDITABLE = { vendor: 'vendors', tax: 'taxes', drug: 'drugs', service: 'services', insurer: 'insurers', department: 'departments' };
function buildList5(page, api, ctx, base) {
  const S = api.S, now = api.clock(), { role } = ctx; const fmt = api.fmt, inr = api.inr; const ses = S.session || { role };
  const open = (kind, id) => () => ctx.open(kind, id); const go = (p, i) => () => ctx.go(p, i);
  let L = { title: '', sub: '', cols: [], rows: [], stats: [], banner: null, filters: [] };
  const withF = (vals, key) => { L.filters = ['All'].concat(vals).map(v => ({ label: v, active: (ctx.filter || 'All') === v, on: () => ctx.setFilter(v) })); return r => (ctx.filter || 'All') === 'All' || String(typeof key === 'function' ? key(r) : r[key]).startsWith(ctx.filter || 'All'); };
  const st = arr => arr.map(([k, v, col, on]) => ({ k, v, c: col || '', on: on || (() => {}) }));
  switch (page) {
    case 'schedules': { const f = withF(S.departments.map(d => d.name).filter(d => d !== 'Radiology'), 'dept'); L.title = 'Consultant schedules'; L.sub = 'Slot templates per consultant (days · hours · slot length · room). Booking validates day, hours, slot alignment and leave; a leave block flags every affected appointment for rescheduling.'; const today = S.schedules.filter(sc => sc.days.includes(api.dowOf(0)) && !sc.leave.includes(0)); L.stats = st([['Consultants', S.schedules.length], ['Consulting today (' + api.dowOf(0) + ')', today.length, G], ['On leave (3 days)', S.schedules.filter(sc => sc.leave.length).length, A], ['Slots today', today.reduce((a, sc) => a + api.slotsFor(sc.doctorId, 0).length, 0)], ['Booked today', today.reduce((a, sc) => a + api.slotsFor(sc.doctorId, 0).filter(s => s.appt).length, 0), T]]); L.cols = [['Consultant', 180], ['Department', 140], ['Days', 150], ['Hours', 110], ['Slot', 50], ['Room', 70], ['Today', 140], ['Tomorrow', 120], ['Status', 100]]; L.rows = S.schedules.filter(f).map(sc => { const t0 = api.slotsFor(sc.doctorId, 0), t1 = api.slotsFor(sc.doctorId, 1); const b0 = t0.filter(s => s.appt).length, b1 = t1.filter(s => s.appt).length; return { id: sc.id, cells: [c(sc.doctor, { b: 1 }), c(sc.dept), c(sc.days.join(' ')), c(fmt(sc.start).slice(-5) + '–' + fmt(sc.end).slice(-5), { m: 1 }), c(sc.slotMin + ' m', { m: 1 }), c(sc.room, { m: 1 }), t0.length ? c(b0 + ' / ' + t0.length + ' booked', { c: b0 === t0.length ? RD : '' }) : c(sc.leave.includes(0) ? 'Leave' : 'Not consulting', { c: A }), t1.length ? c(b1 + ' / ' + t1.length) : c(sc.leave.includes(1) ? 'Leave' : 'Not consulting', { c: A }), P(sc.leave.length ? 'Leave blocked' : sc.active ? 'Active' : 'Inactive')], go: open('schedule', sc.id) }; }); break; }
    case 'ot-schedule': { const f = withF(S.otRooms.map(r => r.name.split(' ')[0]), r => r.room); const live = S.surgeries.filter(s => !/Completed|Cancelled|Requested/.test(s.stage)); L.title = 'OT schedule · ' + S.otRooms.length + ' rooms'; L.sub = 'Room × time board. Scheduling checks room overlap (+30 min turnover), surgeon double-booking and room status; the CSSD sterile-set and blood-reservation gates still apply before "In OT". Kanban groups by room.'; const U = api.otUtilisation(); L.stats = st(U.map(u => [u.room.split(' ')[0] + ' · ' + u.status, u.pct + '% · ' + u.cases + ' cases', u.pct > 85 ? RD : u.status !== 'Available' && u.status !== 'In use' ? A : '', () => ctx.setFilter(u.room.split(' ')[0])])); L.cols = [['Room', 130], ['Start', 110], ['End', 60], ['Procedure', 220], ['Patient', 150], ['Surgeon', 150], ['Anaesthetist', 130], ['Team · set', 160], ['Stage', 130]]; L.rows = live.filter(f).sort((a, b) => a.room.localeCompare(b.room) || a.start - b.start).map(s => ({ id: s.id, cells: [c(s.room, { b: 1 }), c(fmt(s.start), { m: 1 }), c(fmt(s.start + s.durationMin).slice(-5), { m: 1 }), c(s.procedure), c(s.patient), c(s.surgeon), c(s.anaesthetist), c(s.nursing + ' · ' + s.cssdSet), P(s.stage)], go: open('surgery', s.id) })); const req = S.surgeries.filter(s => /Requested|Approved/.test(s.stage)); if (req.length) L.banner = { kind: 'warn', text: req.length + ' approved / requested case' + (req.length > 1 ? 's' : '') + ' without an OT slot: ' + req.map(s => s.patient + ' (' + s.procedure.split(' (')[0] + ')').join(' · ') + '. Open the case → Schedule.', action: 'OT & Surgery', on: go('ot') }; break; }
    case 'roster': { const f = withF(['Today', 'Tomorrow', 'Day after'], 'dayLabel'); const R = S.roster; L.title = 'Nurse roster · 3 shifts × 3 days'; L.sub = 'Assignment is blocked for expired credentials, approved leave and double-booking; a back-to-back shift raises a fatigue flag. Gaps against the required headcount are the staffing exceptions.'; const gaps = R.filter(r => r.nurses.length < r.required); L.stats = st([['Shift slots', R.length], ['Fully staffed', R.filter(r => r.nurses.length >= r.required).length, G], ['Gaps', gaps.length, RD, () => ctx.setFilter('All')], ['Gaps today', gaps.filter(r => r.day === 0).length, RD, () => ctx.setFilter('Today')], ['Nurses with expired credential', S.credentials.filter(cr => /Nurses/.test(cr.type) && /Expired/.test(cr.status)).length, A, go('credentials')]]); L.cols = [['Day', 80], ['Ward', 160], ['Shift', 80], ['Required', 60], ['Assigned', 300], ['In charge', 140], ['Status', 100]]; L.rows = R.filter(f).sort((a, b) => a.day - b.day || a.ward.localeCompare(b.ward) || ['Morning', 'Evening', 'Night'].indexOf(a.shift) - ['Morning', 'Evening', 'Night'].indexOf(b.shift)).map(r => ({ id: r.id, cells: [c(r.dayLabel), c(r.ward, { b: 1 }), c(r.shift), c(r.required, { m: 1 }), c(r.nurses.join(', ') || '—', { c: r.nurses.length ? '' : RD }), c(r.incharge), P(r.nurses.length >= r.required ? 'Staffed' : r.nurses.length ? 'Short ' + (r.required - r.nurses.length) : 'Unstaffed')], go: open('rosterSlot', r.id) })); break; }
    case 'handover': { const mine = api.myPatients(); L.title = 'Ward handover · SBAR · ' + (ses.role === 'Nurse' && ses.dept && ses.dept !== '—' ? ses.dept : 'inpatients'); L.sub = 'Latest SBAR handover per patient with EWS and open work. Nurses record a handover from the patient row; Situation · Background · Assessment · Recommendation.'; const rows = mine.map(p => { const h = api.latestHandover(p.id); const v = S.vitals.filter(x => x.patientId === p.id).sort((a, b) => b.ts - a.ts)[0]; const due = S.mar.filter(m => m.patientId === p.id && /Due|Overdue/.test(m.status)).length; const e = api.currentEncounter(p.id); return { p, h, v, due, e }; }); L.stats = st([['Patients', rows.length], ['Handover recorded this shift', rows.filter(r => r.h && r.h.ts >= now - 480).length, G], ['No handover on file', rows.filter(r => !r.h).length, RD], ['EWS ≥ 3', rows.filter(r => r.v && r.v.ews >= 3).length, RD], ['MAR due / overdue', rows.reduce((a, r) => a + r.due, 0), A, go('mar')]]); L.cols = [['Bed', 80], ['Patient', 160], ['EWS', 70], ['MAR due', 70], ['Last handover', 110], ['SBAR (latest)', 420], ['Status', 100]]; L.rows = rows.map(r => ({ id: r.p.id, cells: [c((r.p.bed || '').split(' ')[0], { m: 1 }), c(r.p.name, { b: 1 }), r.v ? P(r.v.ews >= 3 ? 'Escalate ' + r.v.ews : r.v.ews === 2 ? 'Watch ' + r.v.ews : 'Normal ' + r.v.ews) : c('—'), c(r.due || '—', { c: r.due ? A : '' }), c(r.h ? fmt(r.h.ts) + ' · ' + r.h.by : '—', { m: 1 }), c(r.h ? r.h.text : 'No handover recorded', { c: r.h ? '' : RD }), P(r.h && r.h.ts >= now - 480 ? 'Current' : r.h ? 'Stale' : 'Missing')], go: r.e ? open('encounter', r.e.id) : go('patient', r.p.id) })); if (!rows.length) L.banner = { kind: 'info', text: 'No patients in scope for ' + ses.name + '. Sign in as a ward nurse (e.g. Anitha Kumar · Cardiology).' }; break; }
    case 'reports': { const f = withF(['Today', '7 days', '30 days', 'All'], () => ctx.filter || 'All'); L.title = 'Reports & statutory registers'; L.sub = 'Parameterised registers built from the live store. Pick a range with the chips (applies to time-based registers), open a register for grouping, totals, CSV export and print. Every run is audited.'; const range = ctx.filter && ctx.filter !== 'All' ? ctx.filter : 'All'; const runs = S.reportRuns || []; L.stats = st([['Registers', api.reports.length], ['Range', range], ['Runs this session', runs.length], ['Last run', runs[0] ? runs[0].reportId + ' · ' + fmt(runs[0].ts) : '—']]); L.cols = [['Register', 260], ['Domain', 110], ['Owner', 170], ['Grouping', 120], ['Range', 110], ['Rows', 60], ['Groups', 60]]; L.rows = api.reports.map(r => { const d = api.reportData(r.id, range); return { id: r.id, cells: [c(r.name, { b: 1 }), c(r.domain), c(r.owner), c(r.group), c(r.ranged ? range : 'Point in time', { m: 1 }), c(d.total.n, { m: 1 }), c(d.groups.length, { m: 1 })], go: open('report', r.id) }; }); L.filters = L.filters.filter(x => x.label !== 'All').concat([{ label: 'All time', active: (ctx.filter || 'All') === 'All', on: () => ctx.setFilter('All') }]); break; }
    case 'roles': { L.title = 'Roles'; L.sub = ROLES.length + ' roles · one permission matrix drives navigation, page guards, action guards and approval authority. Edit a role\'s rights on the Permissions page (IT Administrator).'; const pages = S.pageAccess || []; L.stats = st([['Roles', ROLES.length], ['Actions in matrix', S.permissions.length], ['Pages in matrix', pages.length], ['Users', S.users.length, '', go('users')]]); L.cols = [['Role', 170], ['Users', 60], ['Actions allowed', 110], ['Pages allowed', 110], ['Approval authority', 260], ['Home', 120]]; L.rows = ROLES.map(r => ({ id: r, cells: [c(r, { b: 1 }), c(S.users.filter(u => u.role === r).length, { m: 1 }), c(S.permissions.filter(p => p.roles.includes(r)).length + ' / ' + S.permissions.length, { m: 1 }), c(pages.filter(p => p.roles.includes(r)).length + ' / ' + pages.length, { m: 1 }), c(api.approvalTypesFor(r).join(' · ') || '—'), c(r === 'Patient' ? 'portal' : '—', { m: 1 })], go: open('role', r) })); break; }
    case 'permissions': { const f = withF(['Actions', 'Pages'], r => r.kind === 'action' ? 'Actions' : 'Pages'); const all = S.permissions.concat(S.pageAccess || []); L.title = 'Permission matrix'; L.sub = 'This table IS the policy: can(role, action) and page access read from it, so a change here takes effect immediately for the signed-in user. Rows open a drawer to grant / revoke per role (IT Administrator, audited).'; L.stats = st([['Actions', S.permissions.length], ['Pages', (S.pageAccess || []).length], ['Roles', ROLES.length], ['Changes this session', S.audit.filter(a => /^Permission (granted|revoked)/.test(a.action)).length, A, go('audit')]]); L.cols = [['Permission', 210]].concat(ROLES.map(r => [r.replace('Hospital Management', 'Hosp. Mgmt').replace('Procurement Officer', 'Procurement').replace('IT Administrator', 'IT Admin').replace('AI Administrator', 'AI Admin').replace('Governance Officer', 'Governance').replace('Finance Manager', 'Finance').replace('Store Manager', 'Stores').replace('Canteen Manager', 'Canteen').replace('HR Manager', 'HR').replace('Front Office', 'Front Off.'), 62])); L.rows = all.filter(f).map(p => ({ id: p.id, cells: [c((p.kind === 'page' ? 'Page · ' : '') + p.label, { b: 1 })].concat(ROLES.map(r => p.roles.includes(r) ? P('✓') : c('·', { c: '#c9cdd1' }))), go: open('permission', p.id) })); break; }
    default: return null;
  }
  return L;
}
function buildDetail5(kind, id, api, ctx, base) {
  const S = api.S, now = api.clock(), { role } = ctx; const fmt = api.fmt, inr = api.inr;
  const D = { title: '', sub: '', badges: [], facts: [], sections: [], actions: [], related: [], ai: null, image: null, steps: [] };
  const badge = s => ({ t: s, ...pill(s) }); const go = (p, i) => () => ctx.go(p, i);
  const act = (label, on, o = {}) => ({ label, on, primary: !!o.primary, disabled: !!o.disabled, hint: o.hint || '' });
  const need = (ok, who) => ok ? {} : { disabled: true, hint: 'Requires ' + who };
  const tell = (title, r) => { if (r && r.error) api.notify(title + ' · not saved', r.error, 'Medium', 'Validation', {}); };
  const mgmt = role === 'Hospital Management', admin = role === 'IT Administrator' || mgmt, doctor = role === 'Doctor', nurse = role === 'Nurse';
  switch (kind) {
    case 'schedule': { const sc = S.schedules.find(x => x.id === id); if (!sc) return null; D.title = sc.doctor; D.sub = sc.dept + ' · ' + sc.room + ' · ' + sc.days.join(' ') + ' · ' + fmt(sc.start).slice(-5) + '–' + fmt(sc.end).slice(-5) + ' · ' + sc.slotMin + '-min slots'; D.badges = [badge(sc.leave.length ? 'Leave blocked' : 'Active')]; const day = (d, label) => { const sl = api.slotsFor(sc.doctorId, d); return { title: label + ' · ' + api.dowOf(d) + (sl.length ? ' · ' + sl.filter(s => s.appt).length + ' / ' + sl.length + ' booked' : sc.leave.includes(d) ? ' · LEAVE' : ' · not consulting'), items: sl.map(s => kv(s.label, s.appt ? s.appt.patient + ' · ' + s.appt.status : s.past ? 'Passed' : 'Free', s.appt ? '' : s.past ? '#8a9096' : G)) }; }; D.sections = [day(0, 'Today'), day(1, 'Tomorrow'), day(2, 'Day after')]; const fo = api.can(role, 'frontOffice'); D.actions = [act('Book a slot', () => ctx.prompt('Book · ' + sc.doctor, '"<patient name or UHID>, HH:MM, today|tomorrow|day after" — e.g. "Kavitha Raman, 09:30, tomorrow". Slots must match the template; clashes and leave are rejected.', r => tell('Booking', api.bookSlot(sc.doctorId, r, role))), { primary: true, ...need(fo, 'Front Office') }), act(sc.leave.includes(0) ? 'Unblock leave · today' : 'Block leave · today', () => api.blockLeave(sc.doctorId, 0, role), need(fo || doctor, 'Front Office / Doctor')), act(sc.leave.includes(1) ? 'Unblock leave · tomorrow' : 'Block leave · tomorrow', () => api.blockLeave(sc.doctorId, 1, role), need(fo || doctor, 'Front Office / Doctor')), act('Edit template', () => ctx.prompt('Template · ' + sc.doctor, '"09:00-13:00 every 15 min Mon Wed Fri"', r => tell('Template', api.setScheduleTemplate(sc.doctorId, r, role))), need(admin || doctor, 'IT Administrator / Doctor'))]; D.related = [{ label: 'Appointments', on: go('appointments') }]; break; }
    case 'report': { const range = ctx.filter && ctx.filter !== 'All' ? ctx.filter : (ctx.filter === 'All' ? 'All' : 'Today'); const d = api.reportData(id, range); if (!d) return null; D.title = d.meta.name; D.sub = d.meta.domain + ' · owner ' + d.meta.owner + ' · grouped by ' + d.meta.group + ' · range ' + d.range + ' · ' + d.total.n + ' rows' + (d.total.amount ? ' · ' + inr(d.total.amount) : ''); D.badges = [badge(d.meta.ranged ? d.range : 'Point in time')]; D.facts = [kv('Rows', d.total.n), kv('Groups', d.groups.length), kv('Total amount', d.total.amount ? inr(d.total.amount) : '—'), kv('Columns', d.cols.join(' · '))]; D.sections = [{ title: 'By ' + d.meta.group, items: d.groups.length ? d.groups.map(g => kv(g.k, g.n + ' row' + (g.n > 1 ? 's' : '') + (g.amount ? ' · ' + inr(g.amount) : ''))) : [kv('—', 'No rows in this range')] }, { title: 'Rows (first 30)', items: d.rows.slice(0, 30).map(r => kv(String(r[d.cols[0]]), d.cols.slice(1).map(k => r[k]).join(' · '))) }]; D.actions = [act('Run & log', () => api.runReport(id, range, role), { primary: true }), act('Export CSV', () => { api.downloadCsv(d.rows, d.meta.name + '-' + d.range); api.runReport(id, range, role); }), act('Print register', () => { api.runReport(id, range, role); setTimeout(() => window.print(), 100); })]; D.related = [{ label: 'Change range on Reports page', on: go('reports') }]; break; }
    case 'permission': { const p = S.permissions.find(x => x.id === id) || (S.pageAccess || []).find(x => x.id === id); if (!p) return null; D.title = (p.kind === 'page' ? 'Page · ' : 'Action · ') + p.label; D.sub = p.id + ' · ' + p.roles.length + ' of ' + ROLES.length + ' roles' + (p.kind === 'action' ? ' · enforced by can(role, "' + p.key + '")' : ' · enforced by navigation and page guard'); D.badges = [badge(p.kind === 'page' ? 'Page access' : 'Action')]; D.facts = [kv('Allowed roles', p.roles.join(' · ') || 'None'), kv('Denied roles', ROLES.filter(r => !p.roles.includes(r)).join(' · ') || 'None')]; D.actions = ROLES.filter(r => r !== 'Patient' || p.kind === 'page').map(r => { const has = p.roles.includes(r); return act((has ? 'Revoke · ' : 'Grant · ') + r, () => api.setPermission(p.id, r, !has, role), { primary: false, ...need(admin, 'IT Administrator') }); }); D.related = [{ label: 'Roles', on: go('roles') }, { label: 'Audit trail', on: go('audit') }]; break; }
    case 'role': { const r = id; if (!ROLES.includes(r)) return base(); const pages = (S.pageAccess || []).filter(p => p.roles.includes(r)); const acts = S.permissions.filter(p => p.roles.includes(r)); const users = S.users.filter(u => u.role === r); D.title = r; D.sub = users.length + ' users · ' + acts.length + ' actions · ' + pages.length + ' pages · approves: ' + (api.approvalTypesFor(r).join(', ') || 'nothing'); D.badges = [badge('Active')]; D.sections = [{ title: 'Actions allowed', items: acts.map(p => kv('✓', p.label, G)).concat(S.permissions.filter(p => !p.roles.includes(r)).map(p => kv('✕', p.label, RD))) }, { title: 'Pages allowed (' + pages.length + ')', items: pages.map(p => kv('✓', p.label, G)) }, { title: 'Approval authority', items: api.approvalTypesFor(r).length ? api.approvalTypesFor(r).map(t => kv('✓', t, G)) : [kv('—', 'No approval authority')] }, { title: 'Users', items: users.slice(0, 12).map(u => kv(u.id, u.name + ' · ' + u.dept + ' · ' + u.status)) }]; D.actions = [act('Edit rights on Permissions page', go('permissions'), { primary: true })]; D.related = [{ label: 'Users', on: go('users') }]; break; }
    case 'rosterSlot': { const sl = S.roster.find(x => x.id === id); if (!sl) return null; D.title = sl.ward + ' · ' + sl.shift + ' · ' + sl.dayLabel; D.sub = sl.id + ' · required ' + sl.required + ' · assigned ' + sl.nurses.length + ' · in charge ' + sl.incharge; D.badges = [badge(sl.nurses.length >= sl.required ? 'Staffed' : 'Short ' + (sl.required - sl.nurses.length))]; D.sections = [{ title: 'Assigned nurses', items: sl.nurses.length ? sl.nurses.map(n => { const cr = S.credentials.find(x => x.holder === n); const e = S.employees.find(x => x.name === n); return kv(n, (e ? (e.designation || e.role) + ' · ' + e.today : '') + (cr ? ' · ' + cr.type.split(' ')[0] + ' ' + cr.status : ''), cr && /Expir/.test(cr.status) ? A : ''); }) : [kv('—', 'Nobody assigned', RD)] }, { title: 'Eligible nurses on this unit', items: S.employees.filter(e => /Nurse/.test(e.designation || e.role) && (e.unit === sl.dept || e.unit === sl.ward) && !sl.nurses.includes(e.name)).slice(0, 8).map(e => { const cr = S.credentials.find(x => x.holder === e.name); return kv(e.name, e.today + (cr ? ' · ' + cr.status : ''), cr && /Expired/.test(cr.status) ? RD : ''); }) }]; const hr = nurse || api.can(role, 'hr'); D.actions = [act('Assign nurse', () => ctx.prompt('Assign · ' + sl.ward + ' ' + sl.shift + ' ' + sl.dayLabel, 'Nurse name (or first name). Blocked if credential expired, on leave today, or already rostered this shift.', r => tell('Roster', api.assignRoster(sl.id, r, role))), { primary: true, ...need(hr, 'Nurse in charge / HR') })].concat(sl.nurses.map(n => act('Remove · ' + n, () => api.unassignRoster(sl.id, n, role), need(hr, 'Nurse in charge / HR')))); D.related = [{ label: 'Credentials', on: go('credentials') }, { label: 'Attendance & leave', on: go('attendance') }]; break; }
    case 'labReport': { const s = S.samples.find(x => x.id === id); if (!s) return null; const l = S.labs.find(x => x.id === s.labId); const t = S.labTests.find(x => x.panel === s.test); const p = api.patient(s.patientId); D.title = 'Laboratory report · ' + s.test; D.sub = 'Hospital · Kolathur, Chennai · NABL (demo) · accession ' + s.accession; D.badges = [badge(s.status === 'Released' ? 'Final' : 'PRELIMINARY · not released')].concat(l && l.critical ? [badge('Critical')] : []); D.facts = [kv('Patient', p ? p.name + ' · ' + p.id + ' · ' + p.age + ' ' + p.sex : s.patient), kv('Referring clinician', (S.orders.find(o => o.id === s.orderId) || {}).doctor || (p ? p.doctor : '—')), kv('Specimen', s.specimen + ' · ' + s.container + ' · collected ' + fmt(s.collectedAt) + (s.receivedAt ? ' · received ' + fmt(s.receivedAt) : '')), kv('Method', s.manual ? 'Manual entry' : s.analyzer + (t ? ' · ' + t.method : '')), kv('Released', s.releasedAt ? fmt(s.releasedAt) + ' · pathologist' : 'Pending')]; D.sections = [{ title: 'Results', items: [kv(l ? l.analyte : s.test, l && l.value != null ? l.value + ' ' + l.unit + ' · ' + l.flag + ' · ref ' + (t ? t.ref : '—') + (t && t.critHigh !== '—' ? ' · critical ' + t.critLow + ' / ' + t.critHigh : '') : 'No result yet', l && l.critical ? RD : l && l.flag === 'High' ? A : '')].concat(l && l.history ? l.history.map(h => kv('Amended ' + h.t, 'previous ' + h.v + ' · ' + h.by + ' · ' + h.reason)) : []) }, { title: 'Signatures', items: [kv('Technical validation', s.history.find(h => /Technical validation/.test(h.e)) ? s.history.find(h => /Technical validation/.test(h.e)).e : 'Pending'), kv('Pathologist', s.history.find(h => /Pathologist review|Released by/.test(h.e)) ? s.history.find(h => /Pathologist review|Released by/.test(h.e)).e : 'Pending'), kv('Disclaimer', 'Synthetic data · demo report layout · not for clinical use')] }]; D.actions = [act('Print report', () => setTimeout(() => window.print(), 100), { primary: true, disabled: s.status !== 'Released', hint: s.status !== 'Released' ? 'Available after pathologist release' : '' })]; D.related = [{ label: 'Sample', on: () => ctx.open('sample', s.id) }, { label: 'Patient 360', on: go('patient', s.patientId) }]; break; }
    // ---- patches ----
    case 'surgery': { const B = base(); if (!B) return null; const s = S.surgeries.find(x => x.id === id); if (!s || /Completed|Cancelled/.test(s.stage)) return B; B.facts.unshift(kv('OT slot', s.room + ' · ' + fmt(s.start) + '–' + fmt(s.start + s.durationMin).slice(-5) + ' · ' + s.durationMin + ' min', s.stageIdx < 2 ? A : '')); B.actions.unshift(act(s.stageIdx < 2 ? 'Schedule OT slot' : 'Reschedule OT slot', () => ctx.prompt('Schedule · ' + s.procedure, '"<room> HH:MM [tomorrow]" — rooms: ' + S.otRooms.map(r => r.name).join(' · ') + '. Room overlap (+30 min turnover) and surgeon double-booking are rejected.', r => tell('OT schedule', api.scheduleSurgery(s.id, r, role))), { primary: s.stageIdx < 2, ...need(doctor || nurse || mgmt, 'Surgeon / OT nurse') })); B.related.push({ label: 'OT schedule board', on: go('ot-schedule') }); return B; }
    case 'sample': { const B = base(); if (!B) return null; const s = S.samples.find(x => x.id === id); if (!s) return B; if (/Sample Received|Processing|Machine Processing/.test(s.status)) B.actions.push(act('Enter result manually', () => ctx.prompt('Manual result · ' + s.test, 'Value in ' + ((S.labTests.find(x => x.panel === s.test) || {}).unit || 'reporting units') + '. Flags and critical alerts are computed from the test master; technical and pathologist validation still follow.', r => tell('Result entry', api.enterResult(s.id, r, role))), need(api.can(role, 'lab'), 'Laboratory'))); B.actions.push(act(s.status === 'Released' ? 'Report preview / print' : 'Report preview (preliminary)', () => ctx.open('labReport', s.id))); return B; }
    case 'nursingPatient': { const B = base(); if (!B) return null; const h = api.latestHandover(id); B.sections.unshift({ title: 'Latest SBAR handover', items: h ? [kv(fmt(h.ts) + ' · ' + h.by, h.text)] : [kv('—', 'No handover recorded', RD)] }); B.actions.unshift(act('Record handover (SBAR)', () => ctx.prompt('Handover · ' + (api.patient(id) || {}).name, '"S: situation. B: background. A: assessment. R: recommendation." Signed with your name and time; visible on the Ward Handover page.', r => tell('Handover', api.addHandover(id, r, role))), { primary: true, ...need(nurse || doctor, 'Nurse') })); B.related.push({ label: 'Ward handover', on: go('handover') }); return B; }
    case 'vendor': case 'tax': case 'drug': case 'service': case 'insurer': case 'department': { const B = base(); if (!B) return null; const coll = EDITABLE[kind]; const rid = kind === 'tax' ? id : id; if (ctx.edit) B.actions.push(act('Edit record', () => ctx.edit(coll, rid), need(api.can(role, 'admin') || (kind === 'drug' && role === 'Pharmacy') || (kind === 'vendor' && api.can(role, 'inventory')), 'IT Administrator'))); return B; }
    case 'master': { const B = base(); if (!B) return null; const [coll, rid] = String(id).split(':'); if (ctx.edit && ['departments', 'services', 'insurers', 'vendors', 'taxes', 'paymentMethods'].includes(coll)) B.actions.push(act('Edit record', () => ctx.edit(coll, rid), need(api.can(role, 'admin'), 'IT Administrator'))); return B; }
    default: return null;
  }
  return D;
}

return { PAGES5, OVERRIDE5, allowed5, NAV5, buildList5, buildDetail5 };
})();
window.__mm2.erp5 = (function(){
// ERP extension 5 (V2.1): authentication layer (sign-in · MFA/OTP · error · locked · expired · sign-out) on top of the existing session/user model,
// SOAP clinical documentation inside the encounter (voice transcript → AI draft → doctor review → signed record), Tamil/English dictation, centralised token map.
const TOKENS = { primary: '#0F766E', secondary: '#2563EB', success: '#16A34A', warning: '#D97706', critical: '#DC2626', error: '#B91C1C', info: '#0284C7', ai: '#7C3AED', bg: '#F8FAFC', surface: '#FFFFFF', text: '#0F172A', text2: '#475569', muted: '#64748B', border: '#E2E8F0', disabled: '#94A3B8', sidebar: '#0F172A' };
// Existing Hospital palette (kept for visual continuity) → semantic token it plays. New UI reuses the same literals so the product stays one product.
const PALETTE = { primary: 'oklch(0.5 0.1 200)', primaryText: 'oklch(0.4 0.1 200)', primaryTint: 'oklch(0.95 0.03 200)', success: 'oklch(0.4 0.12 150)', successTint: 'oklch(0.95 0.04 150)', warning: 'oklch(0.5 0.13 70)', warningTint: 'oklch(0.96 0.05 80)', critical: 'oklch(0.45 0.17 25)', criticalTint: 'oklch(0.96 0.03 25)', ai: 'oklch(0.5 0.1 300)', aiText: 'oklch(0.45 0.1 300)', aiBorder: 'oklch(0.85 0.05 300)', aiTint: 'oklch(0.97 0.02 300)', text: '#15181b', text2: '#52585e', muted: '#8a9096', border: '#e3e6e8', surface: '#fff', bg: '#fbfbfc', sidebar: '#15181b' };
const DEMO_PASSWORD = 'Hospital@2026';
const DEMO_OTP = '246810';
function installErp5(api) {
  const S = api.S, { audit, notify, emit, int, fmt } = api._h;
  api.TOKENS = TOKENS; api.PALETTE = PALETTE;
  const who = role => (S.session && S.session.userId ? S.session.name : role);

  // ---------- Authentication (sits on the existing S.users / S.session / api.login) ----------
  if (!S.auth) S.auth = { state: 'signed-out', userId: null, error: '', attempts: {}, otp: null, lastActive: S.clock, expiredAt: null };
  S.users.forEach(u => { if (!u.phone) u.phone = '+91 ••••• ' + String(4000 + (parseInt(u.id.slice(-2), 10) * 37) % 6000); if (u.locked == null) u.locked = false; });
  const findUser = q => { const s = String(q || '').trim().toLowerCase(); if (!s) return null; return S.users.find(u => u.username === s || u.id.toLowerCase() === s || String(u.empId).toLowerCase() === s || u.name.toLowerCase() === s) || null; };
  api.findUser = findUser;
  api.demoAccounts = () => ['Doctor', 'Nurse', 'Laboratory', 'Radiologist', 'Pharmacy', 'Billing', 'Insurance', 'Front Office', 'Hospital Management', 'IT Administrator'].map(r => S.users.find(u => u.role === r && u.status === 'Active')).filter(Boolean);
  api.signIn = (username, password) => {
    const A = S.auth; A.error = ''; A.errorKind = ''; const u = findUser(username);
    if (!u) { A.error = 'No account matches "' + username + '". Use your Employee ID or username.'; A.errorKind = 'error'; audit('anonymous', 'Sign-in failed · unknown account', username || '—', '—', 'Denied', 'Unknown account', '—', 'Auth'); emit(); return false; }
    if (u.status !== 'Active') { A.error = 'Account ' + u.username + ' is disabled. Contact the IT Administrator.'; A.errorKind = 'unauthorized'; audit(u.name, 'Sign-in refused · account disabled', u.id, u.status, 'Denied', '—', '—', 'Auth'); emit(); return false; }
    if (u.locked) { A.error = 'Account locked after 5 failed attempts. IT Administrator has been notified.'; A.errorKind = 'unauthorized'; audit(u.name, 'Sign-in refused · account locked', u.id, 'Locked', 'Denied', '—', '—', 'Auth'); emit(); return false; }
    if (password !== DEMO_PASSWORD) { A.attempts[u.id] = (A.attempts[u.id] || 0) + 1; const left = 5 - A.attempts[u.id]; if (left <= 0) { u.locked = true; A.error = 'Account locked after 5 failed attempts. IT Administrator has been notified.'; A.errorKind = 'unauthorized'; notify('Account locked', u.name + ' (' + u.username + ') · 5 failed sign-in attempts', 'High', 'Security', { page: 'users', id: u.id }); } else { A.error = 'Incorrect password · ' + left + ' attempt' + (left === 1 ? '' : 's') + ' left before the account locks.'; A.errorKind = 'error'; } audit(u.name, 'Sign-in failed · wrong password', u.id, '—', 'Denied', 'Attempt ' + A.attempts[u.id], '—', 'Auth'); emit(); return false; }
    A.attempts[u.id] = 0;
    if (u.mfa) { A.state = 'otp'; A.userId = u.id; A.otp = { code: DEMO_OTP, to: u.phone, sentAt: S.clock, tries: 0 }; audit(u.name, 'Password accepted · OTP sent', u.id, '—', 'OTP pending', 'SMS to ' + u.phone, '—', 'Auth'); emit(); return 'otp'; }
    activate(u, 'Password only (MFA not enrolled)'); return true;
  };
  const activate = (u, how) => { const A = S.auth; A.state = 'active'; A.userId = u.id; A.otp = null; A.error = ''; A.errorKind = ''; A.lastActive = S.clock; A.expiredAt = null; A.signedInAt = S.clock; api.login(u.id, u.role); audit(u.name, 'Signed in · ' + how, u.id, '—', 'Active', '—', '—', 'Auth'); emit(); };
  api.verifyOtp = code => { const A = S.auth; if (A.state !== 'otp' || !A.otp) return false; const u = S.users.find(x => x.id === A.userId); if (String(code).trim() !== A.otp.code) { A.otp.tries++; if (A.otp.tries >= 3) { A.state = 'signed-out'; A.otp = null; A.error = 'OTP failed 3 times. Sign in again.'; A.errorKind = 'error'; audit(u.name, 'OTP failed · returned to sign-in', u.id, 'OTP pending', 'Denied', '—', '—', 'Auth'); } else { A.error = 'Incorrect code · ' + (3 - A.otp.tries) + ' tr' + (3 - A.otp.tries === 1 ? 'y' : 'ies') + ' left.'; A.errorKind = 'error'; } emit(); return false; } activate(u, 'Password + OTP'); return true; };
  api.resendOtp = () => { const A = S.auth; if (A.state !== 'otp' || !A.otp) return; A.otp.sentAt = S.clock; A.otp.tries = 0; A.error = ''; const u = S.users.find(x => x.id === A.userId); audit(u.name, 'OTP re-sent', u.id, '—', 'OTP pending', 'SMS to ' + u.phone, '—', 'Auth'); emit(); };
  api.cancelOtp = () => { S.auth.state = 'signed-out'; S.auth.otp = null; S.auth.userId = null; S.auth.error = ''; emit(); };
  api.forgotPassword = username => { const u = findUser(username); const A = S.auth; A.error = ''; if (!u) { A.info = 'If an account exists for "' + username + '", a reset link has been sent to its registered email.'; } else { A.info = 'Reset link sent to ' + u.username + '@meridian.example · valid 15 minutes · IT Administrator notified.'; notify('Password reset requested', u.name + ' (' + u.username + ')', 'Low', 'Security', { page: 'users', id: u.id }); audit(u.name, 'Password reset requested', u.id, '—', 'Link sent', '—', '—', 'Auth'); } emit(); return A.info; };
  api.clearAuthInfo = () => { S.auth.info = ''; S.auth.error = ''; S.auth.errorKind = ''; emit(); };
  api.signOut = reason => { const A = S.auth; const u = S.users.find(x => x.id === A.userId); audit(u ? u.name : 'demo', 'Signed out · ' + (reason || 'user action'), u ? u.id : '—', 'Active', 'Signed out', '—', '—', 'Auth'); S.auth = { state: 'signed-out', userId: null, error: '', errorKind: '', attempts: A.attempts || {}, otp: null, lastActive: S.clock, expiredAt: null, info: reason === 'unauthorized' ? '' : (reason ? 'Signed out · ' + reason : '') }; S.session = { userId: null, name: 'Hospital Management (demo)', role: 'Hospital Management', dept: null, doctorId: null, branchId: 'BR-01' }; emit(); };
  api.expireSession = () => { const A = S.auth; if (A.state !== 'active') return; const u = S.users.find(x => x.id === A.userId); audit(u ? u.name : 'demo', 'Session expired · inactivity', u ? u.id : '—', 'Active', 'Expired', '—', '—', 'Auth'); A.state = 'expired'; A.expiredAt = S.clock; A.error = ''; A.errorKind = ''; emit(); };
  api.touch = () => { if (S.auth.state === 'active') S.auth.lastActive = S.clock; };
  const SESSION_MIN = 240; const origAdvance = api.advanceTime.bind(api); api.advanceTime = m => { origAdvance(m); if (S.auth.state === 'active' && S.clock - S.auth.lastActive >= SESSION_MIN) api.expireSession(); };
  api.authUser = () => S.users.find(x => x.id === S.auth.userId) || null;

  // ---------- SOAP documentation (lives inside the encounter clinical record) ----------
  if (!S.soap) S.soap = [];
  const nsid = () => 'SOAP-' + String(1000 + S.soap.length + 1);
  api.soapFor = encId => S.soap.filter(n => n.encounterId === encId).sort((a, b) => b.ts - a.ts);
  api.openSoap = encId => api.soapFor(encId).find(n => n.status !== 'Signed' && n.status !== 'Discarded') || null;
  api.startSoap = (encId, role, lang) => { const e = S.encounters.find(x => x.id === encId); if (!e) return null; const cur = api.openSoap(encId); if (cur) return cur; const n = { id: nsid(), encounterId: encId, patientId: e.patientId, status: 'Draft', lang: lang || 'EN', s: '', o: '', a: '', p: '', transcript: null, aiDraft: null, createdBy: who(role), ts: S.clock, history: [{ ts: S.clock, by: who(role), what: 'SOAP note started' }] }; S.soap.unshift(n); audit(role, 'SOAP note started', e.patientId, '—', 'Draft', '—', '—', 'EMR', encId); emit(); return n; };
  api.setSoapLang = (id, lang, role) => { const n = S.soap.find(x => x.id === id); if (!n) return; n.lang = lang; n.history.push({ ts: S.clock, by: who(role), what: 'Dictation language set to ' + (lang === 'TA' ? 'Tamil' : 'English') }); emit(); };
  const KAV_EN = 'Patient reports no chest pain overnight and slept well. Mild giddiness on standing this morning, settled within a minute. On examination pulse 78, blood pressure 128 over 80, right radial access site clean with no haematoma, chest clear. Assessment: day two post PTCA to LAD, haemodynamically stable, no recurrent ischaemia. Plan: continue dual antiplatelet therapy and high-intensity statin, repeat troponin tomorrow morning, mobilise in the ward with physiotherapy, likely discharge tomorrow if the troponin trend keeps falling.';
  const KAV_TA = 'நோயாளி இரவில் நெஞ்சு வலி இல்லை என்கிறார், நன்றாகத் தூங்கினார். காலை எழுந்து நின்றபோது சிறிது மயக்கம் இருந்தது, ஒரு நிமிடத்தில் சரியானது. பரிசோதனையில் நாடித்துடிப்பு எழுபத்தெட்டு, இரத்த அழுத்தம் நூற்று இருபத்தெட்டுக்கு எண்பது, வலது கை ரேடியல் அணுகல் இடம் சுத்தமாக உள்ளது, ரத்தக்கட்டி இல்லை, நெஞ்சு தெளிவாக உள்ளது. மதிப்பீடு: LAD-க்கு PTCA செய்த இரண்டாம் நாள், நிலையானது, மீண்டும் இஸ்கீமியா இல்லை. திட்டம்: இரட்டை ஆன்டிபிளேட்லெட் மற்றும் ஸ்டேட்டின் தொடரவும், நாளை காலை ட்ரோபோனின் மறுபரிசோதனை, பிசியோதெரபியுடன் வார்டில் நடக்க வைக்கவும், ட்ரோபோனின் குறைந்து வந்தால் நாளை வீட்டிற்கு அனுப்பலாம்.';
  const GEN_EN = p => 'Patient feels better today, pain has reduced and is tolerating oral diet. No new complaints from the family. On examination vital signs are stable, no fever, wound and IV site are clean. Assessment: ' + (p.procedure || 'current admission') + ', progressing as expected. Plan: continue current medications, mobilise as tolerated, review investigations and re-assess on the morning round tomorrow.';
  const GEN_TA = p => 'நோயாளி இன்று நன்றாக உணர்கிறார், வலி குறைந்துள்ளது, உணவு எடுத்துக் கொள்கிறார். குடும்பத்தாரிடம் புதிய புகார்கள் இல்லை. பரிசோதனையில் உயிர்நிலை அளவுகள் நிலையாக உள்ளன, காய்ச்சல் இல்லை, காயம் மற்றும் IV இடம் சுத்தமாக உள்ளது. மதிப்பீடு: ' + (p.procedure || 'தற்போதைய சிகிச்சை') + ' - எதிர்பார்த்தபடி முன்னேற்றம். திட்டம்: தற்போதைய மருந்துகளைத் தொடரவும், முடிந்தவரை நடக்க வைக்கவும், பரிசோதனை முடிவுகளை மறுஆய்வு செய்து நாளை காலை சுற்றுப்பயணத்தில் மறுமதிப்பீடு.';
  api.soapSimPhrase = (id) => { const n = S.soap.find(x => x.id === id); const p = n && api.patient(n.patientId); const kav = p && p.id === 'MER-2026-008421'; const en = kav ? KAV_EN : GEN_EN(p || {}); const ta = kav ? KAV_TA : GEN_TA(p || {}); return n && n.lang === 'TA' ? { original: ta, translation: en } : { original: en, translation: null }; };
  api.setSoapTranscript = (id, t, role) => { const n = S.soap.find(x => x.id === id); if (!n) return; n.transcript = { lang: n.lang, original: t.original, translation: t.translation || null, durationSec: t.durationSec || 0, confidence: t.confidence || 0.94, source: t.source || 'Simulated microphone', ts: S.clock, translationNote: n.lang === 'TA' ? (t.translation ? 'Machine translation for drafting only · the Tamil original is preserved as source of truth' : 'Live Tamil capture: translation is not available in this prototype · original preserved') : null }; n.status = 'Transcript ready'; n.aiDraft = null; n.history.push({ ts: S.clock, by: who(role), what: 'Dictation transcribed · ' + (t.durationSec || 0) + ' s · ' + (n.lang === 'TA' ? 'Tamil' : 'English') }); audit(role, 'Dictation transcribed · ' + (t.durationSec || 0) + ' s · ' + (n.lang === 'TA' ? 'Tamil/English' : 'English'), n.patientId, '—', 'Transcript ready', 'Confidence ' + Math.round((t.confidence || 0.94) * 100) + '%', '—', 'Voice', n.encounterId); emit(); };
  const splitEn = txt => { const t = String(txt || ''); const iO = t.search(/On examination/i), iA = t.search(/Assessment:/i), iP = t.search(/Plan:/i); const cut = (a, b) => t.slice(a < 0 ? 0 : a, b < 0 ? undefined : b).replace(/^(On examination|Assessment:|Plan:)\s*/i, '').trim(); return { s: iO > 0 ? t.slice(0, iO).trim() : (iA > 0 ? t.slice(0, iA).trim() : t), o: iO >= 0 ? cut(iO, iA >= 0 ? iA : iP) : '', a: iA >= 0 ? cut(iA, iP) : '', p: iP >= 0 ? cut(iP, -1) : '' }; };
  api.aiDraftSoap = (id, role) => { const n = S.soap.find(x => x.id === id); if (!n || !n.transcript) return { error: 'Capture or type a transcript first' }; const p = api.patient(n.patientId); const e = S.encounters.find(x => x.id === n.encounterId); const src = n.transcript.translation || n.transcript.original; const parts = splitEn(src); const v = S.vitals.filter(x => x.patientId === p.id).sort((a, b) => b.ts - a.ts)[0]; const labs = S.labs.filter(l => l.patientId === p.id && l.value != null).sort((a, b) => b.ts - a.ts).slice(0, 3); const rx = (e.prescriptions || []).map(rid => S.prescriptions.find(r => r.id === rid)).filter(r => r && /Prescribed|Verified|Dispensed/.test(r.status));
    const o = [parts.o, v ? 'Recorded vitals (' + fmt(v.ts) + '): HR ' + v.hr + ' · BP ' + v.bp + ' · SpO₂ ' + v.spo2 + '% · T ' + v.temp + ' · RR ' + v.rr + ' · EWS ' + v.ews : 'No vitals recorded in the last 24 h', labs.length ? 'Results: ' + labs.map(l => l.analyte + ' ' + l.value + ' ' + l.unit + ' (' + l.flag + ')').join('; ') : ''].filter(Boolean).join('\n');
    const a = [parts.a, e.diagnoses.length ? 'Working diagnoses on record: ' + e.diagnoses.map(d => d.code + ' ' + d.desc).join('; ') : 'No coded diagnosis on record — add ICD-10 before signing', p.allergies.length ? 'Allergies: ' + p.allergies.join(', ') : ''].filter(Boolean).join('\n');
    const pl = [parts.p, rx.length ? 'Active prescriptions: ' + rx.map(r => r.drug + ' ' + r.freq).join('; ') : ''].filter(Boolean).join('\n');
    n.aiDraft = { s: parts.s, o, a, p: pl, model: 'Clinical Documentation Agent v1.2 (AG-17 Voice)', confidence: n.lang === 'TA' ? 0.88 : 0.93, ts: S.clock, grounded: ['Voice transcript', v ? 'Vitals ' + fmt(v.ts) : null, labs.length ? 'LIS results' : null, 'Encounter diagnoses', rx.length ? 'Prescriptions' : null].filter(Boolean) }; n.status = 'AI draft ready';
    n.history.push({ ts: S.clock, by: 'AG-17 Voice', what: 'AI SOAP draft generated · awaiting doctor review' }); audit('AG-17 Voice · Clinical Documentation Agent', 'AI SOAP draft generated · pending doctor review', p.id, 'Transcript ready', 'AI draft ready', 'Draft only · not part of the clinical record', 'Doctor', 'Voice', n.encounterId); emit(); return n; };
  api.acceptSoapDraft = (id, role) => { const n = S.soap.find(x => x.id === id); if (!n || !n.aiDraft) return; ['s', 'o', 'a', 'p'].forEach(k => n[k] = n.aiDraft[k]); n.status = 'Doctor review'; n.history.push({ ts: S.clock, by: who(role), what: 'AI draft loaded into the record for editing' }); audit(role, 'AI SOAP draft opened for review', n.patientId, 'AI draft ready', 'Doctor review', '—', '—', 'EMR', n.encounterId); emit(); };
  api.saveSoap = (id, fields, role) => { const n = S.soap.find(x => x.id === id); if (!n) return; ['s', 'o', 'a', 'p'].forEach(k => { if (fields[k] != null) n[k] = fields[k]; }); if (n.status === 'Draft' || n.status === 'Transcript ready' || n.status === 'AI draft ready') { if ([n.s, n.o, n.a, n.p].some(Boolean)) n.status = 'Doctor review'; } n.history.push({ ts: S.clock, by: who(role), what: 'Draft saved' }); audit(role, 'SOAP draft saved', n.patientId, '—', n.status, '—', '—', 'EMR', n.encounterId); emit(); };
  api.signSoap = (id, role) => { const n = S.soap.find(x => x.id === id); if (!n) return { error: 'Note not found' }; if (role !== 'Doctor') return { error: 'Only the treating doctor can sign a SOAP note' }; const missing = [['s', 'Subjective'], ['o', 'Objective'], ['a', 'Assessment'], ['p', 'Plan']].filter(([k]) => !String(n[k] || '').trim()).map(x => x[1]); if (missing.length) return { error: 'Complete ' + missing.join(', ') + ' before signing' }; const e = S.encounters.find(x => x.id === n.encounterId); const before = n.status; n.status = 'Signed'; n.signedBy = who(role); n.signedAt = S.clock; n.history.push({ ts: S.clock, by: n.signedBy, what: 'Signed · official clinical record' }); e.notes.push({ ts: S.clock, by: n.signedBy, kind: 'SOAP note', text: 'S: ' + n.s.split('\n')[0] + ' · O: ' + n.o.split('\n')[0] + ' · A: ' + n.a.split('\n')[0] + ' · P: ' + n.p.split('\n')[0], status: 'Signed', soapId: n.id }); notify('SOAP note signed', (api.patient(n.patientId) || {}).name + ' · ' + n.signedBy + (n.aiDraft ? ' · AI draft reviewed and edited' : ''), 'Low', 'Clinical', { page: 'soap', id: n.encounterId }); audit(role, 'SOAP note signed · official clinical record', n.patientId, before, 'Signed', n.aiDraft ? 'AI draft reviewed by doctor · ' + (n.transcript ? (n.lang === 'TA' ? 'Tamil dictation' : 'English dictation') : 'typed') : 'Typed by doctor', '—', 'EMR', n.encounterId); emit(); return n; };
  api.discardSoap = (id, reason, role) => { const n = S.soap.find(x => x.id === id); if (!n || n.status === 'Signed') return; n.status = 'Discarded'; n.discardReason = reason || 'Not specified'; n.history.push({ ts: S.clock, by: who(role), what: 'Discarded · ' + n.discardReason }); audit(role, 'SOAP draft discarded', n.patientId, '—', 'Discarded', n.discardReason, '—', 'EMR', n.encounterId); emit(); };
  // Seed: one signed SOAP note for the flagship patient so the "Final clinical record" state is visible before anyone dictates.
  const kav = S.patients.find(p => p.id === 'MER-2026-008421'); const ke = kav && api.currentEncounter(kav.id);
  if (ke && !S.soap.some(n => n.encounterId === ke.id)) { const parts = splitEn(KAV_EN); const ts = S.clock - 1440; S.soap.push({ id: nsid(), encounterId: ke.id, patientId: kav.id, status: 'Signed', lang: 'TA', s: parts.s, o: parts.o + '\nRecorded vitals: HR 82 · BP 132/84 · SpO₂ 97% · T 36.8 · RR 16 · EWS 1', a: parts.a + '\nWorking diagnoses on record: I21.4 Non-ST elevation myocardial infarction', p: parts.p, transcript: { lang: 'TA', original: KAV_TA, translation: KAV_EN, durationSec: 38, confidence: 0.94, source: 'Simulated microphone', ts, translationNote: 'Machine translation for drafting only · the Tamil original is preserved as source of truth' }, aiDraft: { s: parts.s, o: parts.o, a: parts.a, p: parts.p, model: 'Clinical Documentation Agent v1.2 (AG-17 Voice)', confidence: 0.88, ts, grounded: ['Voice transcript', 'Vitals', 'Encounter diagnoses'] }, createdBy: 'Dr. Arjun Menon', signedBy: 'Dr. Arjun Menon', signedAt: ts + 6, ts, history: [{ ts, by: 'Dr. Arjun Menon', what: 'SOAP note started' }, { ts: ts + 1, by: 'Dr. Arjun Menon', what: 'Dictation transcribed · 38 s · Tamil' }, { ts: ts + 2, by: 'AG-17 Voice', what: 'AI SOAP draft generated · awaiting doctor review' }, { ts: ts + 6, by: 'Dr. Arjun Menon', what: 'Signed · official clinical record' }] }); }
  return api;
}

return { TOKENS, PALETTE, DEMO_PASSWORD, DEMO_OTP, installErp5 };
})();
window.__mm2.views6 = (function(){
// V2.1 view patches: SOAP entry points in the existing encounter drawer, page access for the SOAP page, drawer for a signed SOAP note.
const G = 'oklch(0.4 0.12 150)', A = 'oklch(0.5 0.13 70)', V = 'oklch(0.45 0.1 300)';
const kv = (k, v, col) => ({ k, v: v == null ? '—' : String(v), c: col || '' });
const PAGES6 = ['soap'];
const ROLE6 = { soap: ['Doctor', 'Nurse', 'Hospital Management', 'Auditor'] };
const allowed6 = (role, page) => PAGES6.includes(page) ? ROLE6[page].includes(role) : undefined;
const LABELS6 = { soap: 'SOAP note' };
function patchDetail6(kind, id, D, api, ctx) {
  const S = api.S, role = ctx.role, fmt = api.fmt;
  if (kind === 'encounter' && D) {
    const e = S.encounters.find(x => x.id === id) || api.currentEncounter(id); if (!e) return D;
    const notes = api.soapFor(e.id); const open = api.openSoap(e.id);
    D.sections.splice(2, 0, { title: 'SOAP notes (' + notes.filter(n => n.status === 'Signed').length + ' signed' + (open ? ' · 1 in progress' : '') + ')', items: notes.length ? notes.slice(0, 4).map(n => kv(fmt(n.ts) + ' · ' + (n.lang === 'TA' ? 'Tamil' : 'English') + (n.transcript ? ' · voice' : ' · typed'), n.status === 'Signed' ? '✓ Signed · ' + n.signedBy + (n.aiDraft ? ' · AI draft reviewed' : '') : n.status === 'Discarded' ? 'Discarded' : '● ' + n.status + ' · doctor validation required', n.status === 'Signed' ? G : n.status === 'Discarded' ? '' : A)) : [kv('—', 'No SOAP note yet for this encounter')] });
    const canDoc = role === 'Doctor' || role === 'Nurse' || role === 'Hospital Management' || role === 'Auditor';
    D.actions.splice(1, 0, { label: open ? 'Continue SOAP note (' + open.status + ')' : 'SOAP note · voice or typed', on: () => ctx.go('soap', e.id), primary: role === 'Doctor', disabled: !canDoc, hint: canDoc ? (role === 'Doctor' ? 'Dictate in Tamil or English → transcript → AI draft → your review → sign' : 'View only · signing requires the treating doctor') : 'Requires Doctor' });
  }
  return D;
}
const SOAP_STAGES = ['Start', 'Recording', 'Transcript', 'AI draft', 'Doctor review', 'Signed'];
const stageIndex = (n, sv) => { if (!n) return 0; if (n.status === 'Signed') return 5; if (sv && /recording|paused|processing/.test(sv.state)) return 1; if (n.status === 'Doctor review') return 4; if (n.status === 'AI draft ready') return 3; if (n.status === 'Transcript ready') return 2; return 0; };

return { PAGES6, allowed6, LABELS6, patchDetail6, SOAP_STAGES, stageIndex };
})();
